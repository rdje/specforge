{
 "authority": "ADR 0025 decision 2 / DOCTRINE_ENFORCEMENT.md \u00a73 \u2014 a census that reports is not a check. `check_production_genericity_flow.sh` already derives all eighteen counts on every commit; this is the declaration it compares them against, so a drift is caught at commit cadence instead of at push cadence by a cargo test.",
 "boundary": {
  "pinned": {
   "boundary_rows": 144,
   "canonical_seams": 12,
   "grammar_declassifiers": 64,
   "non_authoritative_regions": 9,
   "proof_gates": 25,
   "protected_calls": 28,
   "protected_constructions": 19,
   "protected_types": 15,
   "rule_roots": 120,
   "source_fields": 13,
   "source_parameters": 3,
   "source_returns": 2,
   "source_types": 22,
   "trusted_regions": 11
  },
  "rationale": "These describe the genericity boundary itself \u2014 which types are sources, which roots are rules, which regions are trusted. Ordinary feature work does not move them: measured unchanged across the six consecutive slices that each moved the volume counts below. A change here is a real change to the product's authority structure and must fail closed. Moved once, deliberately, by EXTRACTION-QUALITY-GAUGE.3k.6: two non_authoritative_root rows (diagnostic.constraint_replay, diagnostic.constraint_replay_gates) register the read-only constraint replay, which reads raw evidence to report what the current producer still mints and writes nothing. Registering it as diagnostics_only rather than as a grammar root is the point \u2014 it has no authority over any artifact, and the boundary should say so. Moved a SECOND time, for the same reason and by the same shape, by SIGNAL-DECLARATION-ROW-DROP.4e: one non_authoritative_root row (diagnostic.declaration_replay) registers the read-only declaration replay, which reads raw evidence to report what the current SemanticIR declaration reader does with a persisted artifact's own statements and writes nothing. That the boundary had to be edited at all is the control working: a function that reads raw evidence and reaches semantic control cannot land silently, and declaring it diagnostics_only is the statement that it has authority over no artifact."
 },
 "contract_id": "production-genericity-flow-census",
 "owner_leaf": "PRODUCTION-GRAPH-CENSUS-PIN.1",
 "schema_version": 1,
 "volume": {
  "aggregate_change": {
   "authority_id": "EXTRACTION-QUALITY-GAUGE.3j.1.a-CARRY-THE-MINTING-CLAUSE",
   "baseline": {
    "analyzed_functions": 2461,
    "decision_sites": 13200,
    "helper_edges": 15437,
    "semantic_macros": 1502
   },
   "delta": {
    "analyzed_functions": 1,
    "decision_sites": 3,
    "helper_edges": 4,
    "semantic_macros": 0
   },
   "owner": "LIVE-DOCUMENT-PRESSURE-HEADROOM.4d.ii",
   "rationale": "EXTRACTION-QUALITY-GAUGE.3j.1.a adds one pure predicate, clause_is_quoted_from, and one guard that calls it in ground_constraint_typed. The model is now asked which obligation inside its span it read, and the answer must be a literal quote from that span or the proposal is dropped whole \u2014 .3j measured that a positional gate judging these records refuses 7 of 149 and is WRONG about 4, every one because the gate reads the FIRST modal clause while the record came from a later one. One new analyzed function, three decision sites (the guard's three conjuncts) and four helper edges; the boundary counts do not move, because a predicate over a proposal and its own span is not new authority over any artifact."
  },
  "rationale": "These are sizes, not boundaries: they move whenever production code is added, which is every ordinary slice. Six consecutive slices moved them and each had to edit a literal to land. Declaring them as baseline + delta with an owner converts that edit from re-pinning a number nothing compares into attributing a census change to the leaf that caused it \u2014 the same `aggregate_change` idiom `doctrine/live_document_size/surfaces.jsonl` already uses for the book's byte total."
 }
}
