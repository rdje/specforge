{
  "schema_version": 1,
  "contract_id": "production-genericity-flow-census",
  "owner_leaf": "PRODUCTION-GRAPH-CENSUS-PIN.1",
  "authority": "ADR 0025 decision 2 / DOCTRINE_ENFORCEMENT.md §3 — a census that reports is not a check. `check_production_genericity_flow.sh` already derives all eighteen counts on every commit; this is the declaration it compares them against, so a drift is caught at commit cadence instead of at push cadence by a cargo test.",
  "boundary": {
    "rationale": "These describe the genericity boundary itself — which types are sources, which roots are rules, which regions are trusted. Ordinary feature work does not move them: measured unchanged across the six consecutive slices that each moved the volume counts below. A change here is a real change to the product's authority structure and must fail closed.",
    "pinned": {
      "boundary_rows": 141,
      "canonical_seams": 12,
      "grammar_declassifiers": 64,
      "non_authoritative_regions": 6,
      "proof_gates": 25,
      "protected_calls": 28,
      "protected_constructions": 19,
      "protected_types": 15,
      "rule_roots": 120,
      "source_fields": 13,
      "source_parameters": 3,
      "source_returns": 2,
      "source_types": 22,
      "trusted_regions": 11
    }
  },
  "volume": {
    "rationale": "These are sizes, not boundaries: they move whenever production code is added, which is every ordinary slice. Six consecutive slices moved them and each had to edit a literal to land. Declaring them as baseline + delta with an owner converts that edit from re-pinning a number nothing compares into attributing a census change to the leaf that caused it — the same `aggregate_change` idiom `doctrine/live_document_size/surfaces.jsonl` already uses for the book's byte total.",
    "aggregate_change": {
      "authority_id": "EXTRACTION-QUALITY-GAUGE.3k.1-REFERENCE-MAGNITUDE-REFUSAL",
      "owner": "EXTRACTION-QUALITY-GAUGE.3k.1",
      "rationale": "One pure predicate, is_reference_magnitude_constraint, plus the disjunct that calls it in each of the two deterministic constraint extractors. It refuses a comparative magnitude whose right operand is a reference, the class AMBA DTI published as \"OAS must not be stable\". The counts move by exactly what a new phrase-matching predicate and two call sites cost.",
      "baseline": {
        "analyzed_functions": 2409,
        "decision_sites": 12984,
        "helper_edges": 14988,
        "semantic_macros": 1471
      },
      "delta": {
        "analyzed_functions": 1,
        "decision_sites": 5,
        "helper_edges": 2,
        "semantic_macros": 0
      }
    }
  }
}
