# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`c2bfa2a6` = `CORPUS-PATTERN-REUSE.3b.3a` (first opt-in extractor = measured NO-GO, read-only/docs-only)**; **now committing `CORPUS-PATTERN-REUSE.3b.3a2` (activate-only consume = no current consumer → measured-STANDING, read-only/docs-only)**; ~129→130 ahead of `origin/main` (session `2026-06-15`, fresh session, PNT active). **THIS-SLICE (`.3b.3a2`, read-only/docs-only):** the `.3b.3a` no-go GENERALIZES — a read-only `Extractor`-framework survey shows ZERO production `applies_to` overrides (only 2 test-only `Toy` fixtures) + `ExtractionContext` carries only per-document `statements`, so all 8 surfaces self-gate LOCALLY / default-on → a structurally-safe extractor never needs the cluster mechanism, and the only lexically-ambiguous lever (`.9.10`) has non-clustering docs. The activate-only niche (cross-doc-predictable ∧ not-locally-testable ∧ clustered safe docs) is EMPTY here → `.3b.3b` stays DEFERRED (YAGNI + gate-risk) with a precise re-open trigger; **consume side `.3b.3` = measured-STANDING**. Predecessor `.3b.3a`: serial-prose bus-line lever not cluster-scopable (lexical ⟂ structural; SMBus/I2S benefit on singletons, the only bus-ish cluster holds the measured-regression doc I2C gold 0.600). KM cards `corpus-reuse-serial-prose-lever-not-cluster-scopable` + `corpus-reuse-activate-only-no-current-consumer`.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **3 OWNER-DIRECTED FORWARD LEVERS:** (1) **`NLP-SHALLOW-PARSE` — BUILD frontier MEASURED-EXHAUSTED `2026-06-15` (STANDING, build-exhausted):** both `.2h` (direction) and `.2f` (coordination) resolved measured-NO-GO (already in the production hand grammar); `.3` assembler = consolidation-only → `deferred`; re-open only on a future measured prose gap or an explicit owner consolidation directive. The bigger "digest any PDF" levers stay TABLES + VLM (per the spike). (2) **`CANONICAL-PROMOTION-SWEEP`** (NEW tree) — **next = `.1`** pilot on a few NON-wire docs (CHI/NVMe/CCIX class) to lock the RAM-safe per-doc protocol; RAM-HEAVY 14B → run with the watchdog, `ollama stop` before any ingest, ≥85%-used kill; canonical mutation review-gated; wire docs only under the full gold battery. (3) **`CORPUS-PATTERN-REUSE`** — **`.3b.3a` + `.3b.3a2` DONE `2026-06-15` (read-only, measured NO-GO for ANY first opt-in extractor) → consume side `.3b.3` = measured-STANDING (built-deferred).** Learn side complete; structurally-safe extractors self-gate/default-on (the activate-only niche is empty here), so `.3b.3b` deferred with a precise re-open trigger; only `.4` (offline miner, RAM-heavy, gated) remains buildable. Like NLP, this lever is now standing. ALSO open/standing: **`EXTRACTION-QUALITY-GAUGE`** (`.3` COMPLETE `.3a`–`.3e`; FIELD-cell gate #2 = confirmed NO-GO, `.3e` already closed the class, residual needs a forbidden denylist); **`MEMORY-BOUNDED-INGEST`** (Mostly Done/standing, measured-exhausted); **`PDF-VARIANT-DIGESTION`** (`.13` complete; `.9.10` parked → now the CORPUS-PATTERN-REUSE opt-in candidate). Recently-closed: `LLM-PRIMARY-PROMOTION`, `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE`.
- next_action (PNT active): **TWO of the three forward levers are now measured-STANDING (NLP build-exhausted; CORPUS-PATTERN-REUSE consume built-deferred).** The ONLY remaining buildable forward lever is **`CANONICAL-PROMOTION-SWEEP.1`** — RAM-HEAVY 14B pilot on non-wire docs (CHI/NVMe/CCIX class) to lock the RAM-safe per-doc protocol; review-gated canonical mutation; wire docs only under the full WIRE-BASED-100 gold battery. **Run ONLY with the watchdog: `ollama stop` before any ingest, autonomous kill ≥85% used, never the 90→93% reboot zone** (`feedback_ram_ceiling_monitor`); serialize the 14B vs Docling. Best taken in a session that can give it full watchdog attention (it loads the 14B live + runs sequential converge). **`cargo build --release` before any live measurement; `scripts/run_ci.sh` before declaring a CODE slice green** (fmt-check/rustdoc/KM-diff NOT in per-slice focused checks). Fresh `target/release/specforge` is post-`.3e`; `.3b.3a`/`.3b.3a2` are read-only/docs-only (no rebuild).
- in_flight_uncommitted: the `CORPUS-PATTERN-REUSE.3b.3a2` measured-STANDING slice staged-to-commit (CORPUS-PATTERN-REUSE tree + new KM card `docs/knowledge/corpus-reuse-activate-only-no-current-consumer.md` + regenerated `KNOWLEDGE_MAP.md` + TASK_TREE index + CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/MEMORY). Read-only/docs-only, NO code. Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`. blockers: **none**.
