# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (memory system), `DOCTRINE_ENFORCEMENT.md` (the 4th
  architecture — doctrines are mechanically gated), and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Before committing run `scripts/check_doctrines.sh` (the registry/driver: memory-arch +
  knowledge-map + task-acceptance); hooks + CI run it too. Retrieval: `KNOWLEDGE_MAP.md`.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`6ee6fd1f`** `CORPUS-COVERAGE.2 — re-ingest #25 CoreSight SoC-600 0100 TRM …`. **43 ahead** of `origin/main` (push at ~200 → HOLD). This `CORPUS-COVERAGE.2 #26` commit → **44 ahead**. Untracked (stays): `.claude/settings.json`, `.cache/` (gitignored symlink).
- **MODE: PNT loop** (fresh session `2026-06-23`, separate from the #22–#25 session earlier today). Active tree: **`CORPUS-COVERAGE.2`** (host-local re-ingest of the 57 normalized-missing docs; chipdoc reachable via the git-ignored `.cache/local-references/chipdoc` symlink → `/Users/.../livework/chipdoc`, 169 PDFs). This session bootstrap-read README/AGENTS/MEMORY/CORPUS-COVERAGE/COMMIT, survey-verified ROADMAP (active-frontier pointer current — no real drift) + mdBook (in-sync; only the in-memory-structure count needed a measured refresh), then PNT-picked the re-ingest frontier. **Done this session: #26 Intel VT-d (committing now).**
- **🚧 `CORPUS-COVERAGE.2` re-ingest #26 DONE (committing now) — Intel VT-d 5.0** (`5_0_2024_08_intel_…directed_io`, 354pp / 810 visual; first distinct-vendor tail re-ingest). Docling CPU (0 residuals; RAM 80–83% free; ~4 min) → deterministic cascade. **GENUINE refresh — CORRECTS the #22 "already current" survey:** register_records 103 held (exact), **`message_field_records` (absent)→15 / 4 containers** (`.10c` structure family fires on `Root-Entry Format`/context tables — actualizes the book's `.10c` claim; corpus in-memory-structure total measured 1,220/11→**1,235/12 docs**), **transactions 0→2** (recognition-only), actors 14→13 (consolidation), signal_constraints 1→4; 0 relations (memory-mapped — honest). `.isf` renderable (`agent.isf`, 510 signals / 103 storage / 4 enums / 44 rules); **`fsmgen --strict` success / 0 diagnostics**; `validate` no stage-staleness (0-vs-0), 8/100 INCOMPLETE. Residuals: bit-fields + field-resets unlocated; 7/17 unexplained tables (Lever-D). **Coverage 26 of 57. 24/26 strict-clean** (2 FAIL = DTI Lever A, LPI Lever C).
- **PHASE FINDING (refined #26):** the one-pass register-count survey UNDER-COUNTS refresh value — VT-d was flagged "already current" off `register_records=103` but its stale evidence predated `message_field_records` + the transaction recognizer, so it gained 15 message-fields + 2 transactions. A doc's stale evidence sits at whatever binary last rebuilt it; register-count parity ≠ full-surface parity. The tail still MIXES pure confirmation (#21/#22) and consolidation/recognition refreshes (#17/#23/#24/#25/#26) + bundle restoration; marquee table-family jumps now the exception, but even register-current docs can carry a real refresh.
- next_action (PNT): **continue `CORPUS-COVERAGE.2`** — next re-ingest slice (31 remain): JEDEC eMMC/HBM (`jesd84_b50`/`jesd235`/`jesd235a`), RISC-V AIA (`1_0_2025_03_12_…advanced_interrupt`), USB4/USB3.2, then the thin OpenCAPI×13 PHY/mechanical/TL tail (skip `readme` — non-spec, yields zero protocol surfaces). Check RAM first; one doc per slice; commit per COMMIT.md. **Alternatively** the most substantive non-gated CODE lever is **Lever D** (non-AMBA `Signal|Direction|Width` table-shape + Wishbone `*_O`/`*_I` signal-recall + RISC-V/VT-d structure-table recall, surfaced #18/#19/#21/#26) — needs probe-first design + its own owned `PDF-VARIANT-DIGESTION` leaf (mind owner no-denylist steer). `KG-ISF-COMPLETENESS`/`KG-ISF-TRANSACTIONS` FSMGen-parked.
- in_flight_uncommitted: the **`CORPUS-COVERAGE.2 #26`** staged set (docs/tasks/CORPUS-COVERAGE.md, CHANGES.md, DEVELOPMENT_NOTES.md, LIVE_ACHIEVEMENT_STATUS.md, MEMORY.md, docs/book/src/document-categories.md). blockers: none. **Repo HANDOFF-READY** after commit. (generated/ is git-ignored — the `.2` log table is the durable trace.)
- **OPERATIONAL REALITY:** host RAM monitor before any Docling/14B work — kill ≥85% used ([[feedback_ram_ceiling_monitor]]). This session ran ONE CPU Docling re-ingest (#26; RAM steady 80–83% free, no abort); no Rust build (binary current at HEAD). NOTE: fsmgen is the Perl `subs/fsmgen/bin/fsmgen` (NOT cargo — a stray `cargo build` errors with "no bin target fsmgen"). Artifact-cleanup directive standing (≥24h) — re-ingest restored VT-d's `normalized/` bundle (intended); fsmgen has no build artifacts.
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication; a missing ISF abstraction → verified FSMGen FR, never an emitter hack ([[feedback_isf_no_hacks]] / [[feedback_verify_fsmgen_before_fr]]). `scripts/check_doctrines.sh` green before committing; `scripts/run_ci.sh` green before committing CODE. Push ~every 200 (HOLD).
