# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`db49c4e8`** `KG-ISF-TRANSACTIONS.2m — deterministic AXI-family channel-membership lever (CODE)`. **A `.4` docs-only commit is being created now.** **184 ahead** of `origin/main` (→ 185 after). Tree clean except untracked `.claude/settings.json` (+ the transient `git_message_brief.txt`).
- **MODE: PNT loop** (session `2026-06-17`; open-ended). **Owner: SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT through SUBSTANTIVE north-star gaps:** #1 relation-incompleteness (DONE `KG-ISF-COMPLETENESS.3`), #2 corpus coverage (DONE `CORPUS-COVERAGE.0`+`.1`; re-ingest of the 57 normalized-missing docs BLOCKED — their source PDFs are NOT in the repo, owner re-provides; the 22 repo-tracked PDFs are all already ingested), #3 transactions (`.2a`–`.2m` DONE; VLM-tier frontier exhausted).
- **`KG-ISF-COMPLETENESS.4` DONE `2026-06-17` (this session — measurement-first, read-only, docs-only): bar #5/#6 behavior/temporal lowering-completeness re-assessed on the broader 78-doc corpus → HOLDS.** `.2` measured fidelity over 36 docs; `CORPUS-COVERAGE.0` doubled the corpus to 78 (register/coherency/profile-heavy), so candidate (b) re-checked bar #5 (no silent behavior/temporal drop) + bar #6 at the new scale. Faithfully replicated the three `from_intent_ir` `(rule)` filters over all 78 `intent_ir.json`: `conditional_rules` 2237/516 lower/1670 no-consequent/51 undeclared-named; `signal_constraints` 369/287/0/82; `temporal_invariants` 29343/286/29030 empty/27; `temporal_rules`(320)+`actor_contracts`(211) residualize (bar-#5-safe). The 30 700 empty/no-consequent drops are correctly silent; the **160 undeclared-NAMED-subject drops** are (item-inspected) field mnemonics + DTI message-field paths + prose/hex noise + garbled VLM fragments (`"RREADY is RBR"`), **NOT wire intent** — 0 on all 4 wire docs. **Bar #5/#6 HOLDS; no buildable lowering-residual lever** (the adapter's silent skip is correct; residualizing = the noise `.2`/`.2b` rejected); CONFIRMS+extends `.2`. ADR-0006. Report `docs/research/behavior-temporal-lowering-completeness-78doc.md`; KM `[[behavior-temporal-lowering-broader-corpus]]` (KNOWLEDGE_MAP 104 facts). No Rust/CLI/`.isf` change.
- next_action (**PNT continues; RAM-SAFE default — no VLM**): the most actionable spun-out lever from `.4` is the **DTI `ihi0088` message-field-recognition gap** — **ROOT CAUSE LOCATED `2026-06-17` (scoping over DTI `source_ir.json`):** DTI defines its per-message field bit-layout in **prose `list_item`s `"<FieldName>, bit [N]"` / `"<Field> bits [hi:lo]"`** (e.g. `MMUV, bit [69]`, `MPAM PARTID[8]`) + a bare `Field bits | Field name` table (Table 3-7), NOT in the caption-anchored `| bits | name | description |` tables `build_message_field_records` (`ir/evidence.rs`) keys off → recognizer correctly sees nothing → DTI has **no `message_field_records`** → 30 field obligations leak into `signal_constraints` (`EXTRACTION-QUALITY-GAUGE.FIELD.4` only routes *catalog-declared* fields). **Fix = a NEW prose-message-field extractor** (parse `"<Field>, bit [N]"` list-items scoped to a message section → `message_field_records`, then FIELD.4 routes the obligations to `message_field_constraints`). **GENERALITY MEASURED `2026-06-17` (read-only over all 79 source_ir) → GENERAL/GO:** the `"<Name>, bit [N]"`/`"<Name> bits [hi:lo]"` form appears in **13/79 docs** (1499 matches), concentrated in register/coherency specs — SMMU 421, ARM-Debug-v6 365, GIC 328, DTI 173, CoreSight 170, ACC 30 (then a 1–5-match noise tail) → corpus-wide lever (~6 high-value docs), NOT DTI-only. **PRECISION is the crux** (the scan also hits descriptive prose `IMPLEMENTATION DEFINED, bits [31:0]` / `virtual SEI, bits[24:0]` → MUST gate structurally: field scoped to a register/message section, bit ranges tile, never fire on free prose — `.1a` gate philosophy applied to field defs; ADR-0006). **DESIGN-HEAVY, fresh-session-appropriate** — recommended home: a new `PDF-VARIANT-DIGESTION` leaf (sibling of the `.10a`–`.10e` table recognizers); **measurement-first on the precision gate** + `run_ci`/`kg-bench`/WIRE-BASED-100 re-verification. Secondary spun-out: signal-inventory prose noise (`AMBA`/`ARM`/`APCI` minted as DTI signals); ATP VLM-fragment quality. Do NOT treat "bar re-confirmed" as "complete" (`[[feedback_not_complete_attack_substantive_gaps]]`). **OWN a leaf before any code.** Report `docs/research/behavior-temporal-lowering-completeness-78doc.md` §spun-out.
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** is critical-path.
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B/7B models (`ollama stop` before/after). `.4` was fully read-only/RAM-safe (Python over persisted artifacts, no VLM/Docling). `[[feedback_ram_ceiling_monitor]]`. Push ~every 200 commits or on ask (**184 ahead → HOLD**, < 200; → 185 after this commit).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path); for non-destructive measurement use `<stage> --dry-run` (prints JSON after a `command:`/`mode:`/`artifact_root:` preamble — slice from the first `{`) or build into a TEMP evidence-root. **STALE-PERSISTED GOTCHA:** `generated/` artifacts can be stale (built by an older binary) — rebuild with the current binary before measuring; isolate a change's effect with `git stash`. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **only this `.4` docs-only commit** (no Rust/CLI/`.isf` change) — new report `docs/research/behavior-temporal-lowering-completeness-78doc.md`, new KM card `docs/knowledge/behavior-temporal-lowering-broader-corpus.md` + regenerated `KNOWLEDGE_MAP.md` (104 facts), task tree `KG-ISF-COMPLETENESS.md` (`.4` leaf + Children + Changelog) + `docs/TASK_TREE.md` frontier, CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS, this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY.**
