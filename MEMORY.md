# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 30 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **this commit = `PDF-VARIANT-DIGESTION.5a` (structural doc-class routing); prior baseline `2c5457f4`/`717218bb`; ~21 local commits ahead of `origin/main`** (session `2026-06-08`). Push ~every 200 commits or on ask (`feedback_push_cadence`). CI green = **1410 tests** (`cargo test -p specforge --lib`; was 1400, +10 from `.5a` doc-class tests); kg-bench 151/151. KM 43 facts — **systematically write a KM card per durable fact** (`feedback_systematic_km_cards`). `subs/fsmgen` pinned (`c0b7eaa7`/`d31b0b91`). Local models: `qwen2.5:14b-instruct` (text/NLI) + `qwen2.5vl:7b` (vision); `ollama serve` may be needed. Disk reclaimed ~31.8 GiB `2026-06-07`. **DOCTRINE (owner re-iterated `2026-06-08`, non-negotiable): SpecForge RUNTIME code is PDF-AGNOSTIC (ADR-0006) — concrete chip names live ONLY in gold answer-keys (`test_data/llm_eval/seed_*.json`) + `#[cfg(test)]` fixtures, NEVER in runtime logic (`feedback_no_hardcoded_chip_spec_names`); every decision must preserve this.**
- corpus: **8 git-tracked source PDFs in `corpus/`** (5 wire-based APB/AHB/AXI/AXI-Stream/SWD + NVMe + I2C(nxp) + RISC-V Debug) + `corpus/SOURCE_PDF_REGISTRY.md` (registry lists the 5 wire-based; NVMe/I2C/RISC-V rows pending). Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`). Re-ingest needs `DOCLING_DEVICE=cpu` (`project_docling_mps_cpu`).
- prior_work (DONE, all pushed): **WIRE-BASED-100** = APB/AHB/AXI 100% on all 3 aspects (constraints/relations/temporal, per-fact source-tolerant, no faking) + **SWD-SERIAL-EXTRACTION** 100% (frame/operations/FSM). Detail lives in `docs/tasks/WIRE-BASED-100.md`/`SWD-SERIAL-EXTRACTION.md` + git, not here. GOTCHA: the eval scores PERSISTED evidence — re-ingest before measuring (`eval-scores-persisted-evidence`). `clean --scope source-normalized` breaks evidence rebuilds → re-ingest.
- active_work: **`PDF-VARIANT-DIGESTION`** (HIGH-PRIORITY owner program — digest ANY chip-spec PDF; `project_pdf_variant_digestion`). Lever A (table-kind classify: deterministic register-field `.2`/`.2c`/`.2d` + VLM classify/grid-repair `.2b`/`.2b'`) + Lever B (prose entity capture: signals `.3a` → I2C 0→10, actors `.3b`) both LANDED. Whole-corpus sweep: 75/82 ingest OK, aggregate 1,908 signals / 2,953 registers / 10,632 fields / 3,077 relations — BREADTH proven, but only 4/74 (APB/AHB/AXI/SWD) have VERIFIED precision. Frontier = make breadth TRUSTWORTHY (`.4`–`.8`). **`.5a` DONE (`2026-06-08`)**: structural doc-class routing in `validate` (`classify_document` → protocol/register/interface/guide; `document_class` metric + `evidence_document_class` Info finding; honest guide reporting). Real-data correction: `conditional_rules` is over-produced (fires on guides) → EXCLUDED from the decision. Live over 74 docs: protocol 25 / register 11 / interface 22 / guide 16. KM `document-class-from-structure`. Guardrails: genericity (`feedback_genericity_guardrail`), multi-strategy best-wins (`feedback_multi_strategy_best_wins`), ADR 0006 (no hardcoded chip names), scoring rigor (`feedback_scoring_rigor`). ISF lowering DEFERRED.
- active_tree pivot (`2026-06-08`): owner chose to FIX the quantified gaps → new tree **`EXTRACTION-GAP-FIX`** owns the 4 `.4`-measured gaps. **`.1` DONE** (I2C prose precision 0.600→1.000), **`.2` DONE** (NVMe mnemonic-from-description; field-name 0/29→28/29), **`.3` DONE** (RISC-V register-name from defining heading; 0/60→59/60, 0 false names), **`.4a` DONE** (tiling-gated register-diagram bit recovery BUILT — pure `ir/register_bits.rs` cumulative-LSB-tiling core + two anti-fabrication gates (widths tile std width 8/16/32/64/128 AND names match the field table) + new `recover-register-bits <evidence-ir>` command reusing the shared VLM image transport + `SPECFORGE_VLM_HELPER` hook, `--vlm-provider skip` default no-op; hermetic on real dmcontrol 14/14 + dmstatus residual + name-mismatch residual; lib 1383→1400; no wire-based regression; ADR 0006 clean). **`.4b` DONE (live measurement, honest): metric UNCHANGED (bit-extent 0/179), ZERO fabrication** — 60/60 registers `residual_no_diagram`; direct VLM probes show local `qwen2.5vl:7b` reads names+order right but mis-sizes cells (dmcontrol +spurious reserved → sum 37; dmstatus off-by-one → 33) → standard-width gate rejects → honest residual; + 2 plumbing gaps (diagrams `unknown`-classified not `RegisterBitfield`; field tables fragmented across Docling tables). **`.4` = HONEST BOUNDARY** (machinery built + measured + guardrail validated; metric improvement = follow-up `.4c`: classify register bitfield diagrams + de-fragment field tables + VLM-read robustness / stronger VLM). **All 4 originally-quantified EXTRACTION-GAP-FIX gaps now WORKED.** KM `register-diagram-bit-recovery-via-tiling`. **`PDF-VARIANT-DIGESTION.4` (precision ①) CLOSED**; that tree stays active (frontier `.5a`).
- **HONESTY GUARDRAIL (owner `2026-06-08`, non-negotiable): you CANNOT extract what is not there.** Read the modality where the fact actually lives (NVMe mnemonic→description; RISC-V name→heading; RISC-V bits→layout GRAPHIC), or report an honest completeness RESIDUAL — NEVER fabricate a width/bit-range/field-name/register-name the doc doesn't state. A leaf that can't find the fact closes as verified-absent/residual, not a faked win.
- next_action: **own + implement `PDF-VARIANT-DIGESTION.5c`** (owner-suggested `2026-06-08`, two messages): read the document's OWN front-matter — title / table-of-contents / preface / "early pages of the first chapter" — for a self-declared doc-TYPE signal (generic grammar: guide/overview/manual/specification/architecture/protocol/datasheet words; NO chip/vendor names, ADR 0006). Use it to (a) corroborate the `.5a` structural class and (b) crucially separate a TRUE guide from a SPECIFICATION we under-extracted (structurally-empty doc whose title self-declares a spec = image/table-heavy GAP → VLM frontier `.6`, NOT a real guide). `.5c` is owned `pending` in the tree; take it `in_progress`, implement, surface in `validate`, re-verify over the persisted docs. Then `.5b` (per-doc completeness gauge). Keep APB/AHB/AXI/SWD at 100%, ADR 0006, scoring rigor. (Alternatives if owner redirects: `EXTRACTION-GAP-FIX.4c` bit-graphic plumbing; `EXTRACTION-QUALITY-GAUGE.8/.FIELD`.)
- paused_work: none. in_flight_uncommitted: **none — clean** (`.5a` committed; only untracked file is `.claude/settings.json`, a local harness config that must stay untracked). blockers: none. RESUME (fresh session): read `README.md` + `MEMORY_ARCHITECTURE.md`, then this pointer → **own + implement `PDF-VARIANT-DIGESTION.5c`** (front-matter/ToC doc-type signal — see next_action). Ollama may need `ollama serve` (`qwen2.5:14b-instruct` + `qwen2.5vl:7b`). Re-ingest needs `DOCLING_DEVICE=cpu`.
