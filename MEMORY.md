# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`214a33a9` = `LLM-PRIMARY-PROMOTION.5` FLIP executed (tree CLOSED)**; **now committing `PDF-VARIANT-DIGESTION.13c`** (CHI rebuild + gauge, docs-only); ~119→120 ahead of `origin/main` (session `2026-06-14`, PNT loop owner-authorized unbounded). **THIS-SESSION #2 (`PDF-VARIANT-DIGESTION.13c`, docs-only — `.13` sweep COMPLETE):** rebuilt CHI evidence from the intact `.2` source bundle (no re-ingest; RAM ≥78% free; the `.13b.1` UTF-8 fix holds on 585p), `message_field_records` 0→**106** on canonical (4 containers; `message_fields.container_field_table` fired), downstream rebuilt (semantic 87 / intent 2388), gauge re-measured **9/13 (69.2%)** persisted — UNBLOCKS `EXTRACTION-QUALITY-GAUGE.3c` with concrete error classes (field-obligation-mis-attribution + descriptive-as-invariant). No Rust change (measurement + canonical refresh). **THIS-SESSION #1 (`LLM-PRIMARY-PROMOTION.5`, CODE — tree CLOSED):** executed the owner-authorized default flip the `.4` packet handed to a fresh session. `converge` now PROMOTES the LLM-primary constraint surface BY DEFAULT for live-NLP runs — new `should_promote_constraints(args)` gate (`nlp_provider != skip && !no_promote_constraints_llm`); new `--no-promote-constraints-llm` opt-out (`conflicts_with` the old on-flag); the retained `--promote-constraints-llm` is redundant-but-accepted + still errors with `--nlp-provider skip`; **provider-free runs never promote by construction** (CI/kg-bench/provider-free converge byte-identical). +3 unit tests; lib 1611→**1614**; full `run_ci.sh` GREEN. Gold gates RE-VERIFIED GREEN on CANONICAL artifacts: wire constraints **1.000 ×3** + doc recall **16/16**; WIRE-BASED-100 relations + temporal **3/3+4/4+3/3**=1.000; serial no-regression (SWD/SWD-deriv/I2C) 1.000; **kg-bench 156/156**; provider-free converge byte-stable (no `constraint_promotion:` line). LIVE proof: flagless `converge` APB w/ live qwen2.5:14b fired promotion automatically (26→22 kept) and the freshly default-promoted canonical APB re-gated 1.000 (constraints + temporal). RAM safe (≥42% free at the 14B peak). R1–R4 recall levers stay future candidates.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **`EXTRACTION-QUALITY-GAUGE`** (`.3c` relational-vs-value + descriptive frames — NOW UNBLOCKED by `.13c`'s CHI gauge; concrete error classes measured; `.FIELD` residuals); **`MEMORY-BOUNDED-INGEST`** (RAM `.1`/`.2` + DISK `.3` + RAM guard `.4a` + disk pre-flight `.4b` + adaptive batch `.4c` DONE; frontier `.5` summary streaming — only if warranted, `.4d`/`.3b` deferred/YAGNI); **`PDF-VARIANT-DIGESTION`** (`.13` sweep COMPLETE incl. `.13c` CHI; serial-class structural levers exhausted `.9.12`; `.9.10` prose bus-line PARKED behind NLP-SHALLOW-PARSE); **`NLP-SHALLOW-PARSE`** (`.2a`–`.3` all `proposed` — OWNER-GATED on build scope per the tree's Open Question; spike verdict lukewarm = consolidation/robustness not recall-revolution); **`CORPUS-PATTERN-REUSE`** (`.3b.3` activate-only consume — needs a FIRST opt-in extractor, design open; wire-based must stay 100%). CLOSED this session: `LLM-PRIMARY-PROMOTION` (`.5` flip). Other recently-closed: `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE` (NO-GO).
- next_action (PNT): **`PDF-VARIANT-DIGESTION.13c` DONE — commit it, then PNT-pick the next leaf.** Candidate frontier (owner ungated `2026-06-14` "do each in turn"): (1) `EXTRACTION-QUALITY-GAUGE.3c` (NOW UNBLOCKED — relational-vs-value + descriptive-frame error analysis against the measured CHI gauge; the bigger lever may be that the now-default flip already routes/cleans the field-obligation class — verify before building); (2) `NLP-SHALLOW-PARSE` build scope is OWNER-GATED (the tree's Open Question — do NOT presume the full-tier build; surface to owner if picked); (3) `MEMORY-BOUNDED-INGEST.5` summary streaming (verify warranted first — not yet at measured page counts); (4) ROADMAP lane-hierarchy reconcile (docs-only; the `BOOK-COMMAND-COVERAGE.1` spun-out follow-up — verify the drift is real first, ROADMAP is high-level by-design). HEAVY-14B option if pursued: a flip-consistent canonical-CHI promotion (`ollama stop` first, RAM-serialize). Fresh `target/release/specforge` built `2026-06-14 22:29`.
- in_flight_uncommitted: `PDF-VARIANT-DIGESTION.13c` docs staged-to-commit (PDF-VARIANT-DIGESTION + EXTRACTION-QUALITY-GAUGE trees, TASK_TREE index, CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/MEMORY). Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`. blockers: **none**.
