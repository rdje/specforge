# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`2a18c32e`** `ISF-REGISTER-RESET-EMIT.2 — emit register reset values as ISF (storage (var … (reset V)))`. **An `ISF-REGISTER-RESET-EMIT.3` CODE commit is being created now** (var-width reconciliation; CLOSES the tree; all gates green). **170 ahead** of `origin/main` (→ 171 after). Tree clean except untracked `.claude/settings.json` + cleared `git_message_brief.txt`.
- **`.2i` DONE `2026-06-16` (CODE, option (a) — FSMGen-confirmed via `FSMGEN-REFRESH-INTEGRATE-3`):** grounded per-phase membership grouping shipped as IntentIR **metadata** (NOT ordered ISF steps; FSMGen `2026-06-16`: don't fabricate value/order, a value-less drive is rejected, bodies are source-ordered). `SemanticIr.TransactionPhaseRecord.signal_set` (`ir/semantic.rs`) + `IntentIr.TransactionIntent.phase_membership` (`ir/intent.rs`, `recognize_named_transactions`/`mint_named_transaction`) + `validate <intent-ir>` surface. Measured live: AHB faithful (`basic_transfer`→address:{HREADY},data:{HRDATA,HREADY,HREADYOUT,HWDATA}; HCLK/HWRITE honestly unphased; HREADY spans both), APB minimal (access:{PCLK}), AXI & SWD honest-empty. **`.isf` BYTE-IDENTICAL ×4 wire docs**; cross-phase ORDER + per-signal VALUE stay honest residuals (`.2h`); future ISF phase-group metadata surface (FSMGen-owned) = cross-`.isf` carry path; table-column phase cue = `.2j`.
- **MODE: PNT loop.** `KG-ISF-TRANSACTIONS` measured-EXHAUSTED on the wire docs (`.2a→.2j` done; body-emission PARKED pending FSMGen + owner steer — the metadata-only per-phase grouping is the safe fallback, shipped `.2i`). `KG-ISF-COMPLETENESS` frontier deferred-with-trigger (`.1b.ii`/`.1b.iv`) + broad `.2+`. **→ PNT ran the new tree `ISF-REGISTER-RESET-EMIT` to CLOSE this session (`.0`→`.3` all done).** It lowers the extracted register-field `reset_value` into the ISF `(storage (var … (reset V)))` surface (FSMGen `(reset V)` already `shipped`, `13k:42`/`13m`; no FR) — the gap: `reset_value` is extracted (`ir/source.rs:432`) + carried to IntentIR (`ir/intent.rs:193`) but DROPPED at emit (`ir/isf_ir.rs:730/375`, `IsfStorageVar` has no reset field). Serves the `KG-ISF-COMPLETENESS` north-star bar #6. **TREE CLOSED this commit (`.0`→`.3` all done):** `.0` own (`718495e8`); `.1` measure=GO (`cacdb158`); `.2` emit `(reset V)` (`2a18c32e`); **`.3` var-width reconciliation (CODE, this commit) CLOSES it** — `register_var_width` (`size_bits ⊔ max(bits_high)+1`, never truncating) replaces max-field-extent in the storage build (`ir/isf_ir.rs`), so the over-width composable resets emit at the true register width. Live: AXI `.isf` byte-identical to pre-`.2` baseline (1045/2108 vars change width, 0 in wire docs), CoreSight SoC-600 120→183 resets at corrected widths, FSMGen `--strict` success/0/0 (NVMe/HBM2 keep PRE-EXISTING rule/enum diags — 0 new); WIRE-BASED-100 1.000; kg-bench 156/156; `run_ci.sh` green (lib 1651, +2). Book close-rule subsection added. Push ~200/on ask (`[[feedback_push_cadence]]`); 170 ahead → HOLD push.
- **NORTH STAR (owner, `2026-06-16`, multi-message — `[[project_kg_isf_completeness]]`):** the KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* **REVERSES the prior "defer ISF lowering" steer.** "ISF ready" ≠ adapter renders: ALL 36 built docs already render `.isf` 0-blockers (measured) — fidelity is the IntentIR's. New tree `KG-ISF-COMPLETENESS` owns it; checkable 6-point bar (agents/relations/signals/constraints/behaviors-temporal/ISF-round-trip); WIRE-BASED-100 a hard gate. **Measured 2-defect agent baseline (both coexist → naive fix disproven):** PRECISION (prose-fragment non-agents `for`/`note`/`then_it`/`class_x` minted as actors — they carry ports, so "drop orphans" FAILS) + COMPLETENESS (real agents `producer`/`consumer`/`receiver`/`transmitter` carry 0 ports/relations vs `manager`/`subordinate` 169/168).
- **TRANSACTION DIRECTIVE — sharpened + ELEVATED (owner, `2026-06-16`, multi-message THIS session — `[[project_kg_isf_transactions]]`):** transaction capture is **of utmost importance** and a **MINIMUM / critical-path prerequisite** — *"without this we can't move forward in our PDF→ISF tool."* 4 reqs: (1) **fast + universal recognition** ("very quickly identify transactions in ANY chip-spec PDF" — cheap/deterministic/structural, no LLM, no name list; runtime-capability, NOT rush-code); (2) **boundary/membership precision** ("what's part of transaction X and what's not" — COMPLETE + EXCLUSIVE); (3) **step-by-step** thorough description; (4) **ALL** transactions, protocol or platform. Captured in the sharpened **7-point bar** in `docs/tasks/KG-ISF-TRANSACTIONS.md`. `KG-ISF-TRANSACTIONS` is now the near-term **TOP-PRIORITY** active tree.
- **`.2a` DONE (G2 de-hardcode):** removed the 3 hardcoded `recognize_digital_patterns` blocks → structural universal recognizer (Cue A `SemanticIr.transaction_anchors` threaded EvidenceIR→SemanticIR→IntentIR + Cue B `symbol_definitions` enums; `mint_named_transaction`, `ir/intent.rs`); 212 named txns / 42 docs; `readme` 0. (Detail in CHANGES/task-tree.)
- **`.2b` DONE (G1 — `ir/intent.rs`):** REMOVED the per-actor `{actor}_behavior` synthesis + its 2 orphaned helpers (census-§3.6 phantoms built from `temporal_rules` — already lowered validly; their multi-word `when` tripped FSMGen `when body clauses must be list forms`) → clears that strict-error class corpus-wide, no semantic loss; `mint_named_transaction` composes a grounded `(drive signal value)` body for the Cue-B subset (`idle_transfer`⟺`HTRANS = IDLE`) → renders to `.isf`. Measured re-scoping: section-named txns & per-channel handshakes have NO structural name bridge → faithful handshake→named-txn composition needs the membership `.2c` owns (deferred, not fabricated). `*_behavior` blobs 0 corpus-wide; APB strict-FAIL→PASS. (Detail in CHANGES/task-tree.)
- **`.2c` DONE (G3 grounded signal-set membership — measured):** `TransactionAnchorRecord` gains `signal_set` (`ir/semantic.rs`); `build_transaction_anchors` = union of the section statements' signal-shaped tokens (`StatementContext.signals`) **∩ the declared-signal inventory** (`declared_signal_names`). **KEY FINDING:** the raw token extractor OVER-CAPTURES enum VALUES (`IDLE`/`INCR4`/`NONSEQ`) + abbreviations (`MPMC`/`AHB5`) — the ∩-with-declared is ESSENTIAL to stay faithful (claiming `IDLE` is a signal would breach bar #3). `recognize_named_transactions` builds a relation-grounded direction map (Drives→output/Reads→input); `mint_named_transaction` attaches each member as a port; confidence keyed on explicit Cue-B flag. Boundary-precise: member iff section references it AND doc declares it (shared `HREADY` correctly spans transfers). Measured AHB: `basic_transfer`→{HCLK,HRDATA,HREADY,HREADYOUT,HWDATA,HWRITE}, `burst_operation`→{HADDR,HBURST,HSIZE}, `idle_transfer`→{HTRANS,HREADY}. **No ISF/strict change** (membership is `ports` metadata; emitter lowers `steps`) — strict byte-identical to `.2b`. Gates: ADR-0006 ✓, **WIRE-BASED-100 constraint+temporal F1 = 1.000** ✓, ISF 0 new diagnostics ✓, `kg-bench` 156/156 ✓, `run_ci.sh` green ✓ (lib 1641; anchor+mint tests extended). **DEFERRED beyond batch:** ordered multi-phase BODY (phase sequencing over membership) + finer addr/data/ctrl/resp role sub-typing.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask. (This slice was RAM-safe: deterministic, no 14B.)
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, never runtime; NO deny/filter LISTS (prefer structural gates); goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals; honest RESIDUAL over fabrication. **`scripts/run_ci.sh` before declaring any CODE slice green**; `cargo test` does NOT rebuild the bin — `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (`evidence --dry-run` strips a 4-line header before the JSON) and eval there. `subs/fsmgen` pinned **`030f8c273`** (refreshed `FSMGEN-REFRESH-INTEGRATE-3.1`, `2026-06-16`, `8c39827f → 030f8c273` +9 — carries FSMGen's phase-membership answer; emitted `.isf` strict-valid, 6 canaries + `run_ci.sh` 1645/0 green).
- **EQG `.3` precision program now spans `.3a`–`.3g`; the clean+narrow+universal gate frontier on the persisted corpus is MEASURED-EXHAUSTED.** Remaining NVMe residuals are NOT clean micro-gates: (a) prose-subject catalog class (`NVM`/`LBA`/`FFFF` admitted as "signals" because a register doc has no real wire catalog) and (b) tangled spurious-digit-value class (`HMDLLA 1` vs the GENUINE `BADD 0`/`RECFMT 0h`/`ELEN 0h` — no clean discriminator). Both need deeper non-narrow work (catalog-quality / `.FIELD` re-routing) with real gate risk — pure-hex/lowhigh-level shortcuts were each MEASURED-REJECTED (would flag real `CBA`/`BADD` / kill genuine HBM2/I2C). Don't grind more 1-record micro-gates (genericity guardrail).
- next_action: **CONTINUE PNT — `ISF-REGISTER-RESET-EMIT` CLOSED; pick the next tree.** The two north-star trees remain parked/deferred (KG-ISF-TRANSACTIONS body-emission FSMGen-parked; KG-ISF-COMPLETENESS `.1b.ii`/`.1b.iv` deferred-with-trigger). The cleanest eligible buildable work is **`KG-ISF-COMPLETENESS.2+`** — the remaining north-star bar dimensions per doc (relations/signals/constraints/behaviors-temporal/ISF-round-trip), which needs a **measurement-first split** into a concrete leaf before any code (start read-only: gauge which bar dimension has the largest faithful-lowering gap on the wire docs + broader protocol corpus, WIRE-BASED-100 a hard gate). Alternatively re-scan FSMGEN-REFRESH-INTEGRATE-2.2's conditional ISF candidates (ID-based outstanding-txn correlation / phase-group typing — only if SpecForge already extracts the intent + an empirical probe supports it). Deferred: EQG `.3` exhausted; RAM-heavy `CORPUS-PATTERN-REUSE.4` needs owner go; 24 promoted canonical docs await `R7-VALIDATION`. Optional housekeeping: regenerate the remaining register-heavy docs' canonical `.isf` (only AXI + CoreSight-0100 + the 6 strict-checked docs were regenerated this session; the rest still carry pre-`.2` `.isf` in gitignored `generated/`).
- in_flight_uncommitted: **an `ISF-REGISTER-RESET-EMIT.3` CODE commit is being created now (CLOSES the tree)** — `crates/specforge/src/ir/isf_ir.rs` (register_var_width + 2 tests) + `docs/book/src/pipeline/isf-adapter.md` (Closed-task-trees subsection) + `docs/tasks/ISF-REGISTER-RESET-EMIT.md` (`.3`→done, tree CLOSED) + `docs/TASK_TREE.md` (row→done) + `CHANGES.md` + `DEVELOPMENT_NOTES.md` + `LIVE_ACHIEVEMENT_STATUS.md` + `RUST_CODEBASE_ANALYSIS.md` + this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt` (cleared post-commit). blockers: **none**.
