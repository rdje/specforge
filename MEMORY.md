# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 30 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`547ea1d1` = `PDF-VARIANT-DIGESTION.9.11` (header-trapped timing-table recovery, CODE) (`4e7d3434` = `.9.10` probe, `926b8a97` = `.9.6`, `a95e59d3` = `.9.5` before it); `PDF-VARIANT-DIGESTION.9.12` (serial table-gap triage, CLOSED no-build, DOCS-ONLY) committing now; ~56 ahead of `origin/main`** (session `2026-06-09`). Push ~every 200 commits or on ask (`feedback_push_cadence`). Last verified full gate: **`scripts/run_ci.sh` GREEN for `.9.11`** (fmt + clippy `-D warnings` + lib **1475** + rustdoc + mdBook) + kg-bench 151/151 (`.9.12` docs-only → no code → gate holds). **GOTCHA: per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc NOR `cargo fmt --check` — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`).** **GOTCHA: per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc NOR `cargo fmt --check` — run `scripts/run_ci.sh` before declaring a CODE slice green / before handoff; bare `<X>`/`<NAME>` in doc comments → rustdoc warning, wrap in backticks.** `subs/fsmgen` pinned (`c0b7eaa7`/`d31b0b91`). **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006) — concrete chip names ONLY in gold keys (`test_data/llm_eval/seed_*.json`) + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); every decision preserves this.**
- this_session (`2026-06-09`): PNT → `.9.5` SMBus + `.9.6` I2S baselines (first-ever ingests), `.9.10` prose-signal PROBE (locked Form A; Form C rejected), `.9.11` header-trapped timing-table recovery (CODE, I2S 0→5). **STRATEGIC: owner confirmed goal = #1 (structured+multimodal+bounded-LLM intent mining w/ honest residuals), NOT #2 unrestricted-English (`project_goal_structured_not_unrestricted_english`); the spike "failures" = the intent lives in STRUCTURE. Owner doctrine: NO deny/filter LISTS ("a list to handle 100s of PDFs is a sign of weakness") + case is SOFT (`feedback_avoid_denylists_prefer_structural`, `feedback_case_is_soft_not_critical`) → identify signals by PARTICIPATION, not case/lists.** SMBus/I2S/SWP/CAN/SWD/ADI bundles intact; **APB/AXI normalized bundles reclaimed** → re-ingest (`DOCLING_DEVICE=cpu`) before measuring them (`project_docling_mps_cpu`).
- corpus: **12 git-tracked source PDFs in `corpus/`** — 8 prior (APB/AHB/AXI/AXI-Stream/SWD-ADI/RISC-V Debug/NVMe/I2C) + **4 NEW serial specs (`.9.1`, `2026-06-08`): CAN/SWP/SMBus/I2S** (`bosch/can`, `etsi/swp`, `smbus`, `nxp/i2s`) + `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- prior_work (DONE, pushed): **WIRE-BASED-100** (APB/AHB/AXI 100% on constraints/relations/temporal) + **SWD-SERIAL-EXTRACTION** 100% (frame/operations/FSM). GOTCHA: eval scores PERSISTED evidence — re-ingest before measuring (`eval-scores-persisted-evidence`); `clean --scope source-normalized` breaks evidence rebuilds → re-ingest.
- active_work: **`PDF-VARIANT-DIGESTION.9`** (serial-protocol class — owner-directed `2026-06-08`; CAN/SWP/SMBus/I2S git-tracked). DONE: `.9.1` import (`c265062b`) · `.9.2`/`3d940f1f` CAN baseline · `.9.3a`/`3d85543c` CAN quoted-mode FSM · `.9.4`/`9942e652` SWP baseline · `.9.7` single-word `<NAME> state` FSM (SWP protocol_states 0→4; +18 corpus docs; SWD 13 states + CAN 3 intact) · **`.9.8` SWP prose signals (this slice, above)**. Pattern across `.9`: each new under-extraction is a NEW agnostic grammar, **always probed over all persisted evidence docs BEFORE coding** (ADR 0006). Frontier = `.9.3b`/`.9.9`/`.9.5`/`.9.6`/`.9.8b` (lever backlog, owner may steer). `.6`/`.7` (older VLM/USB-3.2 leaves) blocked on host-local PDFs.
- **HONESTY GUARDRAIL (owner, non-negotiable): you CANNOT extract what is not there** — read the modality where the fact lives or report an honest RESIDUAL; NEVER fabricate a width/bit-range/field/name. APB/AHB/AXI/SWD stay 100%; genericity (`feedback_genericity_guardrail`), multi-strategy (`feedback_multi_strategy_best_wins`), scoring rigor (`feedback_scoring_rigor`). ISF lowering DEFERRED.
- **NEW active tree `EXTRACTOR-ARCHITECTURE`** (owner devil's-advocate review `2026-06-09`, scoped to the **extractor path**): make the EvidenceIR producer layer a coherent whole, not erratic growth. `.1` audit DONE (typed IR target coherent; producers = ~60 free fns in one ~500-line `EvidenceIr::build()` with hand-rolled per-surface dedup + heterogeneous gates; FSM cluster = 4 siblings + 4 inline dedup loops = the failure mode; provenance 2/10 surfaces; no manifest). owner confirmed (`2026-06-09`) "consolidate, unify, as much as possible" → maximal shape (trait+registry+driver). **`.2` DONE (this slice)** — `ir/extractor.rs`: generic `Extractor<R>` trait (`name`/`tier`/`applies_to`/`run`) + borrowed `ExtractionContext` (carries `statements`; grows one field per migrated cluster) + single `run_surface` driver (gate → run → first-wins dedup by per-surface key → inspectable `SurfaceRun`/`SurfaceManifest`/`ExtractionManifest`); LLM/VLM = same frame; 4 hermetic driver tests; ZERO extractors migrated (no behavior change, additive pub API); `run_ci.sh` green (lib 1452). Refactor not rewrite.
- **`.3`–`.6` DONE — 3 clusters migrated, all byte-identical.** `.3` FSM + `.4` semantic-hints onto `run_surface` (key-dedup merge). `.5` KEY FINDING: signal-declaration cluster is the statement-ASSEMBLY phase (seed statements, build-wide counter, side-output, `<8` gate, no dedup), NOT a merge-surface → extracted `synthesize_signal_declaration_seed` + documented the **two-phase** model (don't force it through the driver). `.6` registers = a CONCAT surface → added a 2nd driver mode **`run_surface_concat`** (ordered, no-dedup) + `register_record_surface` helper (2 `Extractor<RegisterRecord>` units + 3 post-passes); RISC-V 44 / NVMe 42 regs byte-identical. THREE categories now explicit: key-merge (`run_surface`) / concat (`run_surface_concat`) / stateful-assembly (own orchestrators). `.7` registered the **actors** surface (single-strategy; I2C byte-identical). **`.8` DONE — wired the run manifest** into `EvidenceIr.extraction_manifest` (serde-default field; the 4 migrated surfaces `record()` their `SurfaceRun`, idempotent per surface name) + surfaced in `validate` (metrics `extraction_manifest_surfaces`/`extraction_extractors_fired` + Info finding). RISC-V manifest correct + deterministic; non-manifest output unchanged; +2 tests; book note. **This IS the per-doc behavioral fingerprint → UNBLOCKS `CORPUS-PATTERN-REUSE.2`.** kg-bench 151/151; lib 1457.
- **NEW active tree `CORPUS-PATTERN-REUSE`** (owner directive `2026-06-09`): reuse extraction patterns across PDFs, clustered by a DERIVED vendor/layout fingerprint (ADR 0006 — no hardcoded vendor names), advisory (adjusts attention not truth), extending the proven `CorpusMemory` plane. `.1` design DONE. KEY INSIGHT: the `EXTRACTOR-ARCHITECTURE` `ExtractionManifest` IS the per-doc behavioral fingerprint to cluster on → the extractor framework is the FOUNDATION. `.1` design + **`.2` DONE** — pure `ir/corpus_cluster` (`document_fingerprint` = structural count-buckets + `.8` manifest fired-set; Jaccard similarity; deterministic greedy `cluster_documents`); 4 tests; **live over 76 docs → 29 clusters, emergent families track reality NO vendor list** (3 CoreSight versions, CCIX r1.0↔r1.1, 7 AMBA protocols together). CAVEAT: `fired:` mostly absent (only ~6 docs rebuilt since `.8`) → structural-shape driven today; corpus re-ingest sweep = enrichment follow-up. `.3` = `corpus-cluster` CLI command + advisory `ExtractionProfile` via `Extractor::applies_to`. KM `corpus-cluster-fingerprint`. LLM/VLM = offline corpus pattern miner (`.4`, `VERB-COVERAGE-CORPUS` shape).
- **NEW finding/tree `EVIDENCE-DETERMINISM`** (surfaced by `.5` verification, `2026-06-09`): the EvidenceIR build is **non-deterministic at the CONTENT level** — two runs of identical code produce set-different `actor_signal_relations` + `extracted_statements` on SWD/ADI (relation-heavy prose doc). Pre-existing (NOT a refactor regression). Undermines reproducibility, eval-score stability (`feedback_scoring_rigor`), and the byte-identical verification methodology. **`.1`+`.2` DONE (tree CLOSED) — owner picked (A).** Two `HashSet`-iteration leaks fixed in `ir/evidence.rs`: (1) `extract_actor_signal_relations` iterated `known_signals: &HashSet` → sort once before the loop (relation key-set invariant, ids/order now stable); (2) `derive_encoding_enum_name` sorted by `Reverse(len)` only → total order (len, then name). **ALL 6 intact-bundle docs now double-run BYTE-IDENTICAL** (was: SWD content-different); SWD eval 1.000; kg-bench 151/151; +1 regression test (HashSet built twice → identical); lib 1455; multiset-preserving. Every future byte-identical migration proof is now sound. ANTI-PATTERN (KM `evidence-build-nondeterminism`): never let HashSet/HashMap iteration order reach output — sort / total-order / BTreeSet.
- **`.9.8` DONE (this slice).** SWP prose-signal capture via a 3rd additive form `definitional_signal_names` (`ir/evidence.rs`): copula `<NAME> is a/an signal` + glossary colon `<NAME>: signal`, candidate must pass `is_hardware_signal_token` on the ORIGINAL (un-cased) token (all-uppercase identifier) → lowercase subjects can't leak, NO denylist. **Grammar locked by probing ALL persisted evidence docs BEFORE coding** (the `.9.7` method): rejected descriptor-apposition (corpus-toxic, floods SWD) + abbreviation-expansion (`MMIO`/`DMA`/`IOVA` garbage); definitional → corpus-wide EXACTLY S1/S2. **SWP signals 0→S1/S2** (SWIO = the shared C6 contact, NOT a 3rd signal → honest residual `.9.8b`, never fabricated). **No regression — before/after `git stash` diff: SWD/ADI declared set BYTE-IDENTICAL**; table-rich APB/AHB/AXI never run prose. 7 tests; `run_ci.sh` green (lib 1468) + kg-bench 151/151; KM `definitional-signal-capture` + book `pipeline/evidenceir.md`.
- **NEW active tree `NLP-SHALLOW-PARSE`** (owner-directed `2026-06-09`: build SpecForge's deterministic in-Rust **NLP arm** — a shallow subject-verb-object parser feeding the EXISTING grounding gates; SpecForge = typed-IR orchestrator over NLP+VLM). Owner asked to (a) own each of the ~9 pipeline components as leaves [DONE: `.2a` tokenizer · `.2b` segmenter · `.2c` POS · `.2d` lemmatizer · `.2e` chunker · `.2f` clause/coord · `.2g` negation/modality · `.2h` passive/verb-sense · `.2i` grounding gate · `.3` integrate] and (b) download tools (careful w/ size). **`.1` spike DONE** — measured general SVO (hand-roll + a real nltk statistical tagger, in a 54MB throwaway `.venv-nlp`, since DELETED) vs the hand grammars on real APB/AXI/SWD/SWP: **generic SVO recovers only 3–8% of the hand grammars' recall**, hand-roll→nltk jump ~0 (POS quality NOT the bottleneck; recall lives in TABLES + domain constructions); grounding gate neutralized the tagger-choke risk. **Honest verdict: shallow-parse = a CONSOLIDATION + unseen-phrasing-robustness play, NOT a recall revolution**; bigger "digest any PDF" lever stays the table-kind classifier + VLM arm. Tooling = grounding-first/model-last: hand-rolled Rust + ≤2 tiny crates (`rust-stemmers`/`unicode-segmentation`); REJECT spaCy/BERT/nlprule (size+determinism). `project_nlp_shallow_parse_direction`.
- PNT (owner said "pick the next tree and roll"): **all 4 serial-class baselines DONE (CAN/SWP/SMBus/I2S — each an ⚠ UNDER-EXTRACTED spec that under-extracts DIFFERENTLY, the `.5a`/`.5c` machinery flags each correctly).** This session also: `.9.10` PROBE (prose bus-line signals — Form A "the `<NAME>` line" locked, but it captures power rails on the MEASURED I2C doc; owner forbade a denylist → PARKED behind participation-identity / the shallow-parser) + **`.9.11` DONE (CODE): header-trapped timing-table recovery — I2S timing_constraints 0→5, SMBus held 84, structural (no list/no case), nested tables stay honest residuals; lib 1475, kg-bench 151/151.** Method for every lever: probe over all persisted docs BEFORE coding (`.9.7`/`.9.8`/`.9.11`), structure-not-lists, residual over fabrication. wire-based APB/AHB/AXI/SWD 100% preserved; ADR 0006 + the no-list/case-soft doctrine.
- next_action (PNT continues, all on-#1 STRUCTURE): **`.9.12` closed the serial structural table sweep — the SMBus/I2S flagged tables are honest residuals (no build), serial-class structural levers EXHAUSTED.** Remaining serial gaps are prose-signal (`.9.10`/`.9.8b`, PARKED behind participation-identity). So the next PNT pick is a NEW direction: (a) **broader-corpus structural table-shape probe** (beyond serial — probe the ~78 persisted `evidence_ir` docs for the next buildable structural gap, `.9.11`-style), or (b) **`CORPUS-PATTERN-REUSE.3`** (corpus-cluster CLI command + advisory `ExtractionProfile` via `Extractor::applies_to` — clean, contained, on-#1, the clustering engine `.2` already works over 76 docs). **OPEN owner go/no-go: advance `NLP-SHALLOW-PARSE` as the participation-based signal-identity foundation (the strategic cure for list-weakness that unblocks `.9.10`; spike-tempered → grounded/structural build, not a generic parser).**
- paused_work: **`.9.10` (prose bus-line signals) PARKED behind `NLP-SHALLOW-PARSE`** (owner: no denylist; needs participation-based identity — probe + design fully recorded in the `.9.10` node, crash-safe). in_flight_uncommitted: **`PDF-VARIANT-DIGESTION.9.12` staged** (DOCS-ONLY: `.9.12` investigate-only node + verification + changelog + frontier + this MEMORY; no code) — committing now. Only untracked: `.claude/settings.json` (stays). blockers: none. **REPO HANDOFF-READY** (clean tree, `run_ci.sh` green lib 1475 + kg-bench 151/151). RESUME: read `README.md`+`MEMORY_ARCHITECTURE.md` then this pointer → PNT into (a) the broader-corpus structural probe or (b) `CORPUS-PATTERN-REUSE.3` (or take the owner's go/no-go on the shallow-parser foundation).
