# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`46faebb0`** `KG-ISF-COMPLETENESS.1b.iv — drop Class-C PURE-INFERRED phantom actors`. **A `KG-ISF-COMPLETENESS.1b.ii` CODE commit is being created now** (named-interface consolidation). **175 ahead** of `origin/main` (→ 176 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (fresh session `2026-06-17`; no BWFSC set → open-ended). `ISF-REGISTER-RESET-EMIT` CLOSED. PNT slices this session: `.2` (`f1a507dd`) → `.2a`/`.2a.i` width (`3d7dba01`) → `.2b` (`0fb81e5c`) → `.1b.iv` phantom drop (`46faebb0`) → **`.1b.ii` interface consolidation (THIS commit)**.
- **`.1b.ii` DONE `2026-06-17` (CODE — THIS commit):** named-interface consolidation — fold an `"X interface"` relation subject onto the bare agent `"X"` (`Subordinate interface`→`Subordinate`, `Transmitter interface`→`Transmitter`) ONLY when `X` is independently a connected relation subject in this doc, so a distinct named block (GIC `CPU interface` = the GICC) is preserved. New `strip_interface_suffix` + `consolidate_interface_actor_relations` post-pass in `actor_signal_relation_surface` (`ir/evidence.rs`), AFTER the `.1b.iii` split + BEFORE dedup (where the connected-agent context the pure-string seam lacks IS available); per-doc gate, no name list (ADR 0006 — same `AXI interface` token SAFE in one CoreSight doc / RISK in two others); +3 tests (incl. the `CPU interface` conflation guard). Census: 6 docs carry an `"* interface"` actor (3 safe / 5 risk); NONE of the 4 WIRE-BASED-100 gold docs do → gold relation surface structurally untouched. Live (fresh post-`.1a`/`.1b` rebuild): AXI-Stream `Transmitter interface`→`Transmitter` (23 rels), AXI+ACE `Subordinate interface`→`Subordinate` (49 rels), 4 wire docs unchanged. **WIRE-BASED-100 HELD 1.000**; `kg-bench` 156/156; `run_ci.sh` GREEN (lib **1657**, +3). Book `pipeline/intentir.md`; KM `[[agent-interface-block-consolidation]]`; report §9. **`.1b` umbrella COMPLETE** (`.1b.i`/`.1b.ii`/`.1b.iii`/`.1b.iv`); with `.1a`, **`KG-ISF-COMPLETENESS.1` agent-surface fidelity is FULLY BUILT**.
- next_action: **CONTINUE PNT.** Agent-surface fidelity (`.1`) is EXHAUSTED as a buildable lever — precision (`.1a`) + completeness (`.1b.i`/`.1b.ii`/`.1b.iii`/`.1b.iv`) all landed. Remaining `KG-ISF-COMPLETENESS` levers: `.2a` signal DIRECTION (FSMGen-NEUTRAL → owner-philosophy only, deferred — needs explicit owner steer), `.2b` (measured-marginal). Next PNT pick: survey the 6-point bar's other dimensions for a buildable gap (bar #2 relation completeness / #4 constraint gauge `EXTRACTION-QUALITY-GAUGE` / #5 behavior-temporal carry — measure-first) OR another active tree (`KG-ISF-TRANSACTIONS` body-emission FSMGen-parked; `MEMORY-BOUNDED-INGEST` standing/build-exhausted; `CORPUS-PATTERN-REUSE.4` offline miner gated; `NLP-SHALLOW-PARSE` standing). Measurement-first; if no clean buildable gap remains, this is a natural fresh-session handoff boundary.
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** elevated to critical-path; `KG-ISF-TRANSACTIONS.2a→.2j` done (body-emission FSMGen-parked).
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes (`[[feedback_ram_ceiling_monitor]]` / `[[project_big_pdf_memory_bounded_ingest]]`). This slice was RAM-safe (Rust-only rebuilds, no 14B/Docling; RAM stayed 71–77% free). Push ~every 200 commits or on ask (175 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (symlink canonical `generated/source_ir` in + run the release binary from a temp CWD so the RELATIVE output root redirects). `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **a `KG-ISF-COMPLETENESS.1b.ii` CODE commit is being created now** — `crates/specforge/src/ir/evidence.rs` (the post-pass + helper + 3 tests) + `docs/tasks/KG-ISF-COMPLETENESS.md` (`.1b.ii` node→done, changelog, `.1b` umbrella) + `docs/TASK_TREE.md` (frontier) + `docs/research/agent-surface-fidelity-measurement.md` (§9) + `docs/knowledge/agent-interface-block-consolidation.md` (new KM card) + `KNOWLEDGE_MAP.md` (regen) + `docs/book/src/pipeline/intentir.md` + `README.md` + `CHANGES.md` + `DEVELOPMENT_NOTES.md` + `LIVE_ACHIEVEMENT_STATUS.md` + this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt` (cleared post-commit). blockers: **none**. **Repo is HANDOFF-READY** — clean checkpoint; agent-surface fidelity `.1` fully built; continue PNT (survey the bar's other dimensions or another tree, measure-first) or hand off.
