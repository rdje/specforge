# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 30 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: `VERB-COVERAGE-CORPUS` close commit pending → **~67 ahead** of pushed `d92a73e3` (NOT pushed — owner said "not time to push", threshold 200). CI green baseline = **1258 tests**. KM 22 facts. `subs/fsmgen` pinned `92d7036b`. **STANDING USER REMINDER (2026-06-05): regularly sync/align the mdBook with the codebase — the book is the SOLE user-facing surface; every tree's close MUST refresh its book subsection (BOOK-METHOD-DOC).**
- active_work_unit: none — **`VERB-COVERAGE-CORPUS` CLOSED `2026-06-05`**: `.1` seedless LLM mine (qwen2.5:14b, OFFLINE/build-time) of the owner's real 82-PDF corpus (`/Users/richarddje/Documents/livework/chipdoc`) → 640 grounded verbs + Claude cross-model curation → `docs/research/verb-coverage-corpus.md`. `.2` integrated the verbs as **actor→signal KG edges** (owner reframed: a signal never acts alone — an actor does X to a signal; `actor —verb→ signal` = the KG; each verb an edge label; the verb list IS the engine). Added the mined verbs to `extract_actor_signal_relations`, **centralized** all 4 relation-verb lists into `ir/normative_vocab.rs` (single place to add an edge verb), closed the passive-form gap, documented the model in the book. Value-pinning (`set`→1) deferred fine-tune-later. CI green 1258. **`PDF-AGNOSTIC-EXTRACTION` value-path `.2` also done earlier this session (positional value extraction; no hardcoded value list); `.3`/`.4` (family-name denylists via `declared_signal_names`, CI guard, sweep) remain.** — Prior: **`CONSTRAINT-CONDITION-SUBJECT` CLOSED `2026-06-05`**: fixed the general current-code cause of one over-extraction class — `extract_signal_constraints`' empty-subject full-text fallback grabbed condition-clause signals ("the following signals must be valid when PSEL asserted" → bogus "PSEL must be VALID"); fallback now excludes condition-clause tokens. **KEY HONEST FINDING: the generated EvidenceIR artifacts are STALE** — existing `CONSTRAINT-SUBJECT-PRECISION` tests already fix width-params/cross-sentence/until-if, so most of the "33/42 over-generation" the NLI run showed is ALREADY FIXED; re-extraction needed to re-measure current extraction quality. +1 test; CI green 1256. — Prior: **`NLI-CLAIM-CONDITION` CLOSED** + REAL-DATA VALIDATION: ran `nli-verify` on the real AMBA APB EvidenceIR (42 constraints, qwen2.5:14b, ~42s) → 36 flagged. The gate WORKS — catches genuine mis-extractions (states ACCESS/SETUP, width params USER_*_WIDTH, clock PCLK, condition signals "PSEL must be VALID" from "when PSEL asserted"). **FINDING: SpecForge's constraint extractor OVER-GENERATES** (33/42 spurious/mis-attributed after the fix) — a real extraction-quality issue worth a future tree (KM `nli-gate-real-apb-validation`). Fix: `constraint_claim_text` now carries `condition_text` → APB re-run 36→33. — Prior: **`NLI-GATE-METRIC` CLOSED** (user "→ surface an nli_* count"): `ir/nli_verify.rs` `nli_demoted_count(&[ResidualDecisionPacket])` + `NLI_RESIDUAL_PREFIX` const; `validate_intent_ir` emits `metric("nli_demoted_contracts", …)` (read-only, no LLM). `validate <intent_ir.json>` surfaces the gate's demotions. +1 test; CI green 1254. — Prior: **`NLI-INTENT-GATE` CLOSED `2026-06-05`** (`.1`–`.2`, user pick "→ (b)"): the NLI verifier made an *active* IntentIR gate (post-build pass — residuals live at IntentIR, not EvidenceIR). `ir/nli_verify.rs`: `obligation_claim_text(&ActorContract)→Option` (phrasable-only: Drive/Stable/Eventually/HandshakeBarrier/Mutex; else None=not gated), `nli_gate_contracts(contracts, verify)` (premise=`provenance.source_text`; NotEntailed→demoted to `ResidualDecisionPacket` `nli_unentailed_<id>`; Entailed/Unknown/un-phrasable kept), `apply_nli_gate(&mut IntentIr)`. **Demote-not-delete**; verifier injected→3 hermetic tests. Opt-in **`intent --nli-verify`** (`--vlm-provider`/`--model`). Book + KM `nli-intent-gate`. CI green 1253. — Earlier: `NLI-ENTAILMENT-VERIFIER` CLOSED (`.1`–`.3`): the verifier module + claim-set gate + live `nli-verify` command. Both NLI trees done. KM `nli-entailment-verifier`, `local-llm-for-text-reasoning`, `eval-gold-interannotator-kappa`.
- κ-study extended `2026-06-05`: **`actor_signal_relation` κ = 1.00** (11/11, 2nd blind agent); **Ollama IS reachable** (started `ollama serve`; `which ollama`=/opt/homebrew/bin/ollama; models pulled = `qwen3-vl:8b`, `qwen2.5vl:7b` ONLY). Cross-model run: **qwen3-vl:8b** correct on single items but thinking NOT disable-able (`think:false`/`/no_think` ignored, ~3400 tok/signal) → too slow (partial 5/6); **qwen2.5vl:7b** fast (21 s) but **κ = 0.285** — it mislabels condition/trigger signals as obligations + invents labels (a model-competence issue, NOT a gold flaw; capable Claude reviewer κ = 0.90 is the signal). Side-finding: qwen2.5vl:7b = SpecForge's DEFAULT extraction VLM → raw extraction likely over-constrains "when X …" conditions.
- LOCAL MODELS NOW (2026-06-05): removed `qwen3-vl:8b` (too-slow thinking); pulled **`qwen2.5:14b-instruct`** (text-only, 9 GB) per user. Local set = `qwen2.5:14b-instruct` (text) + `qwen2.5vl:7b` (vision). Ollama daemon started in-session (`ollama serve`). Results: qwen2.5:14b → **relation κ=1.00**, **constraint κ=0.498** (still trips on condition-vs-obligation in the LABELING framing), **NLI entailment 5/6** (got the subtle "PSEL must be asserted"→NOT-entailed RIGHT). KEY: **NLI framing > free-form labeling** for this nuance; **qwen2.5:14b-instruct is VIABLE for an NLI verifier** (the "NLI gated: needs a model" item is now UNBLOCKED). KM card `local-llm-for-text-reasoning`.
- **`PDF-AGNOSTIC-EXTRACTION` CLOSED `2026-06-05`** (owner SIGNOFF CRITERION, ADR 0006): zero hardcoded chip-spec vocabulary in production. `.2` positional value path (hardcoded AMBA value list → token-after-the-verb). `.3` removed protocol family/vendor names + ARM/AMBA pub-IDs + HTRANS/HBURST encoding values from `signal_stop_words` (semantic.rs) + `collect_subject_signal_tokens` + the conditional denylist (evidence.rs) — SAFE because mis-discovered tokens are filtered downstream by `declared_signal_names` (semantic.rs:213-234, w/ test). `.1`/`.4` CI guard `signal_stop_words_holds_no_chip_spec_vocabulary` (concatenated tokens, can't self-trip) + clean production audit. CI green 1259. **OWNER-GATED CARVE-OUT (not a gap): logic-level kinds `MustBeHigh`/`MustBeLow` (`"must be high"`) — recommended as universal digital semantics (the "how"), NOT a spec name; collapsing = fundamental 9-file IR redesign + `semantic.rs:9744` already normalizes 1/high/true. A separate architectural decision if the owner wants logic levels derived too.** Generic English/digital/doc/role/standards-body tokens retained (document-independent). Test/eval fixtures keep real names (data).
- grammar boundary CONFIRMED by owner (2026-06-05): keep **grammar + derive all domain vocabulary** for NOW (prove SpecForge works very well on the grammar approach first). A **pure-NLP-model** extractor is a deferred future option → tree `PURE-NLP-INTENT-EXTRACTION` created **PARKED** (do not activate until SpecForge complete on grammar + `PDF-AGNOSTIC-EXTRACTION` closed; owner must explicitly activate). A future model still honors ADR 0006 (generalize the *how*, never memorize names).
- PDF-AGNOSTIC-EXTRACTION.2 INCREMENT DONE (2026-06-05): `extract_protocol_state_value` rewritten to POSITIONAL value extraction (was a hardcoded AMBA value list idle/nonseq/incr4/okay…); + positional subject value-exclusion; + dropped value names (HIGH/LOW/IDLE/NONSEQ/…) from `collect_subject_signal_tokens` denylist. +2 tests; CI green 1257; NOT pushed. Remaining .2: `evidence.rs:5833` conditional-rule denylist still lists value names (same positional fix). Owner-flag (deeper, not done): MustBeHigh/Low KINDS are logic-level semantics across 9 files + `semantic.rs:9744` already normalizes 1/high/true → logic levels read as universal "how" not spec-names; collapsing = fundamental IR redesign — recommend retaining.
- **`VERB-COVERAGE-CORPUS`** (owner request) — mine a comprehensive normative-verb vocabulary from `/Users/richarddje/Documents/livework/chipdoc` (82 PDFs/564MB, cross-vendor; PDFs in vendor SUBDIRS not flat; tooling pdftotext+docling+pymupdf; ollama qwen2.5:14b-instruct up). Since runtime extraction is pattern-matching, the verb list IS the engine; unlisted verb = dropped constraint. Verbs=grammar=the "how" (ADR 0006). **METHOD = LLM-mining (qwen2.5:14b), OFFLINE/build-time only** (runtime stays deterministic; parked pure-NLP model in a bounded list-building role) — owner's insight: use the model to overcome the seed-dependence since the runtime NLP model isn't built yet. **Sample head-to-head VALIDATED (2026-06-05, 5 specs/532 sents/146s):** LLM found 23 behavioral verbs the modal-seed regex structurally MISSED (driven/drives/asserts/sampled/holds/pulls/released/remains/detects/…, the active-voice/descriptive forms w/o must/shall) vs 8 overlap — seedless LLM clearly out-recalls. `.1` pipeline: pdftotext → broad signal-presence sentence pre-filter → batched LLM verb extraction → ground each verb in corpus → dedup+provenance → OWNER REVIEWS before wiring. `.2` curate+integrate+centralize grammar. Seed-regex full-82 results in /tmp/verb_mine/phrases.tsv. **Owner: sample first (done) → next = full-82 LLM run.**
- next_action: **"DO ALL 5" COMPLETE `2026-06-05`** (owner directive; order chosen by me): (1) `EVAL-RELATION-GRANULARITY` per-relation-kind P/R/F1 + MUC near-miss; (2) `LOGIC-LEVEL-BOUNDARY` — HIGH/LOW resolved as universal binary-logic "how" (convention-agnostic spelling set centralized in `normative_vocab`; full collapse REJECTED as lossy+9-file+ill-posed), ADR 0006 refined; (3) `REEXTRACTION-REMEASURE` — re-ingested+re-extracted real APB → **constraints 42→16** (session's fixes VALIDATED), 69 relations, 3 remaining bug classes → `CONSTRAINT-EXTRACTION-V2` follow-up; (4) `TABLE-GRITS-CONFORMAL` — GriTS-content + split-conformal as tested capability; (5) `PURE-NLP-INTENT-EXTRACTION` UN-PARKED + first increment (`ir/nlp_relation_extract.rs` bounded-LLM relation extractor: model proposes→declared signals decide, ADR 0006). CI green **1265**, ~78 ahead, NOT pushed. **`CONSTRAINT-EXTRACTION-V2` CLOSED `2026-06-05`** (`.1`–`.4`): `.1` logic-level word never a subject; `.2` multi-bullet cells bounded; `.3` "which means" antecedent excluded; +3 tests, CI 1268. A real-APB re-extract LOOKED bad (21 constraints, LOW/bullet/5-duplicate-IDs) so I re-opened — then `.4` root-caused it: the **`evidence` command MERGES with the stale on-disk artifact** (`merge_signal_constraints` evidence.rs:838), so re-running over the pre-V2 file accumulated old garbled constraints. On a **FRESH rebuild** (delete evidence_ir first): **13 constraints, 0 LOW, 0 bullet-bleed, 0 duplicate IDs** — fixes fully effective; NLI not-entailed 11→8 (the 8 = claim-phrasing "condition appended without `when`" + soft "recommended-not-required" constraints, NOT the bug classes). **TWIN LESSONS: (1) re-measure on real data before declaring an extraction tree done; (2) rebuild FRESH before re-measuring — the materialize-merge is for incremental builds.** Minor follow-ups: an `evidence` idempotency guard; `constraint_claim_text` should join the condition with its connector. pure-NLP next (wire into signal-resolve opt-in; A/B vs patterns; bounded constraint extractor); **owner to confirm the LOGIC-LEVEL-BOUNDARY ADR refinement** (it reverses the earlier "HIGH/LOW forbidden" draft). **68 commits ahead, NOT pushed (owner: not time to push; threshold 200).** — Superseded: RESUME → `PDF-AGNOSTIC-EXTRACTION.2 cont` (evidence.rs:5833 conditional denylist positional fix) → `.3` (family/encoding names via `declared_signal_names` registry) → `.1` (CI guard) → `.4` (sweep+guard-to-zero). Confirm with owner the logic-level-kinds recommendation (retain as universal "how"). Owner (2026-06-05): ".2. It is not time to push." → .2 is THE next focused work, to be done **FRESH** (deliberately NOT started this session — it's real extractor surgery, regression-prone). **DO NOT PUSH** (54 ahead, threshold 200; owner said not time to push). When resuming .2: rewrite the constraint value path in `evidence.rs extract_signal_constraints` (~5606+) from hardcoded `"must be high"→MustBeHigh` classification to POSITIONAL value extraction (value = token after the normative verb), drop hardcoded value words (HIGH/LOW/IDLE/encoding) from the two denylists; verify with the extraction suite (`CONSTRAINT-SUBJECT-PRECISION`) + a re-extraction (no precision regression). `.1` CI guard can ride alongside; then `.3` (document-derived signal validation via `declared_signal_names`, drop family/encoding names) + `.4` (sweep + guard-to-zero). My CONSTRAINT-CONDITION-SUBJECT fix is itself agnostic (position-based, no names; test names are fixtures). (33/42 APB constraints spurious/mis-attributed: states/params/clock get "must be X"; condition signals get the obligation meant for others). A real extraction-quality tree — could tighten the EvidenceIR `signal_constraints` extractor (skip non-signals/states/params; don't attribute the obligation to the "when X" condition signal). Use `nli-verify` as the diagnostic. NLI thread itself DONE (verifier + intent gate + metric + condition fix, all closed). Other backlog: gated (TEDS/GriTS, conformal) or marginal (MUC; per-relation P/R/F1; min>1 monitor). Follow-ups: run `intent --nli-verify` on a real corpus doc to see actual demotions (needs a SemanticIR + `ollama serve` w/ `qwen2.5:14b-instruct`); OR the standing backlog: gated (TEDS/GriTS [table gold], conformal [held-out set]) or marginal (MUC near-miss; per-relation P/R/F1; unguarded-`min>1` monitor). Local models: `qwen2.5:14b-instruct` (text/NLI) + `qwen2.5vl:7b` (vision); ollama may need `ollama serve`. Reminder: grep `KNOWLEDGE_MAP.md`; **regularly sync mdBook (sole user surface)**; **~49 ahead — no push until ~200 or ask.**
- paused_work: none.
- in_flight_uncommitted: none.
- blockers: none — NLI verifier now has a viable local model (`qwen2.5:14b-instruct`).
