# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`0fb81e5c`** `KG-ISF-COMPLETENESS.2b — lowering-coverage gauge MEASURED-MARGINAL → deferred`. **A `KG-ISF-COMPLETENESS.1b.iv` CODE commit is being created now** (Class-C PURE-INFERRED phantom drop). **174 ahead** of `origin/main` (→ 175 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (fresh session `2026-06-17`; no BWFSC set → open-ended). `ISF-REGISTER-RESET-EMIT` CLOSED. PNT slices this session: `.2` measurement (`f1a507dd`) → `.2a`/`.2a.i` width (`3d7dba01`) → `.2b` measured-marginal (`0fb81e5c`) → **`.1b.iv` phantom drop (THIS commit)**.
- **`.1b.iv` DONE `2026-06-17` (CODE — THIS commit):** Class-C PURE-INFERRED phantom-actor drop. The SemanticIR Phase-2 role-term scan (`build_actors`, semantic.rs:3177) mints a generic-role-term actor (`agent`/`controller`/`producer`/…) the instant a statement MENTIONS the word — 0 ports/0 rels, and when nothing else grounds it, responsibilities reduce to the single term-scan marker ``"semantic role inferred around `X` evidence"``. New `is_pure_inferred_phantom_role` + a guard in `build_intent_actors` (`ir/intent.rs`) `continue`-skips exactly such an actor (marker-SHAPE match, ADR-0006, no name list; +2 tests incl. a producer-template drift guard). Measured on a FRESH post-`.1a`/`.1b` rebuild of the 4 wire docs into a temp evidence-root (WRITE-PATH GOTCHA): drops exactly the **21 corpus-wide phantoms / 16 docs** (wire: APB `controller`, AHB `agent`; AXI/SWD 0) with ZERO connected or grounded actors touched; AXI `transmitter`/SWD `host`/GIC `arbiter` (grounded 0/0) deliberately KEPT (completeness over pruning). Stage-diff: evidence+semantic byte-identical, intent differs ONLY by the dropped phantoms; `actor_signal_relations`/`signal_constraints`/`temporal_rules` byte-identical; `.isf` byte-identical (emitter never reads `actors[]`). **WIRE-BASED-100 HELD 1.000** (constraints+relations+temporal APB/AHB/AXI, fresh `eval-extraction --provider skip`); `kg-bench` 156/156; `run_ci.sh` GREEN (lib **1654**, +2). Book `pipeline/intentir.md` "How the actor surface stays faithful"; KM `[[agent-pure-inferred-phantom-drop]]`; report §8.
- next_action: **CONTINUE PNT.** The agent-surface A/B/C taxonomy is now BUILT for the unambiguous subset (`.1a` Class-A precision, `.1b.i`/`.1b.iii` Class-B consolidation, `.1b.iv` Class-C phantom drop). Remaining `KG-ISF-COMPLETENESS` levers are deferred-with-trigger: **`.1b.ii`** (`"X interface"→"X"` strip — needs actor-set context wired into the relation-subject seam `normalize_relation_actor_name`, a pure string fn today; GIC `CPU interface` ≠ `CPU` conflation risk — measurement-first), **`.2a`** signal direction (FSMGen-NEUTRAL → owner-philosophy only). Next PNT pick: assess `.1b.ii` (design the actor-set-context wiring) as the next agent-surface lever, else survey other active trees (`KG-ISF-TRANSACTIONS` body-emission FSMGen-parked; `MEMORY-BOUNDED-INGEST` standing/build-exhausted; `CORPUS-PATTERN-REUSE.4` offline miner gated; `NLP-SHALLOW-PARSE` standing).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** elevated to critical-path; `KG-ISF-TRANSACTIONS.2a→.2j` done (body-emission FSMGen-parked).
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes (`[[feedback_ram_ceiling_monitor]]` / `[[project_big_pdf_memory_bounded_ingest]]`). This slice was RAM-safe (read-only python rebuilds, no 14B/Docling; RAM stayed 71–77% free). Push ~every 200 commits or on ask (174 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (symlink canonical `generated/source_ir` in + run the release binary from a temp CWD so the RELATIVE output root redirects). `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **a `KG-ISF-COMPLETENESS.1b.iv` CODE commit is being created now** — `crates/specforge/src/ir/intent.rs` (the guard + helper + 2 tests) + `docs/tasks/KG-ISF-COMPLETENESS.md` (`.1b.iv`→done, changelog, `.1b` umbrella) + `docs/TASK_TREE.md` (frontier) + `docs/research/agent-surface-fidelity-measurement.md` (§8) + `docs/knowledge/agent-pure-inferred-phantom-drop.md` (new KM card) + `docs/book/src/pipeline/intentir.md` (new section) + `README.md` + `CHANGES.md` + `DEVELOPMENT_NOTES.md` + `LIVE_ACHIEVEMENT_STATUS.md` + this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt` (cleared post-commit). blockers: **none**. **Repo is HANDOFF-READY** — clean checkpoint; continue PNT (assess `.1b.ii` next).
