# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 30 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`.5b` committed `17066076`; this commit = `PDF-VARIANT-DIGESTION.8` (broaden prose-actor capture — robust structural agent grammar); ~24 local commits ahead of `origin/main`** (session `2026-06-08`). Push ~every 200 commits or on ask (`feedback_push_cadence`). CI green = **1427 tests** (`cargo test -p specforge --lib`; was 1421, +6 from `.8`); kg-bench 151/151. KM 43 facts — **systematically write a KM card per durable fact** (`feedback_systematic_km_cards`). `subs/fsmgen` pinned (`c0b7eaa7`/`d31b0b91`). Local models: `qwen2.5:14b-instruct` (text/NLI) + `qwen2.5vl:7b` (vision); `ollama serve` may be needed. Disk reclaimed ~31.8 GiB `2026-06-07`. **DOCTRINE (owner re-iterated `2026-06-08`, non-negotiable): SpecForge RUNTIME code is PDF-AGNOSTIC (ADR-0006) — concrete chip names live ONLY in gold answer-keys (`test_data/llm_eval/seed_*.json`) + `#[cfg(test)]` fixtures, NEVER in runtime logic (`feedback_no_hardcoded_chip_spec_names`); every decision must preserve this.**
- corpus: **8 git-tracked source PDFs in `corpus/`** (5 wire-based APB/AHB/AXI/AXI-Stream/SWD + NVMe + I2C(nxp) + RISC-V Debug) + `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`). Re-ingest needs `DOCLING_DEVICE=cpu` (`project_docling_mps_cpu`).
- prior_work (DONE, all pushed): **WIRE-BASED-100** = APB/AHB/AXI 100% on all 3 aspects (constraints/relations/temporal, per-fact source-tolerant, no faking) + **SWD-SERIAL-EXTRACTION** 100% (frame/operations/FSM). Detail in `docs/tasks/WIRE-BASED-100.md`/`SWD-SERIAL-EXTRACTION.md` + git. GOTCHA: the eval scores PERSISTED evidence — re-ingest before measuring (`eval-scores-persisted-evidence`). `clean --scope source-normalized` breaks evidence rebuilds → re-ingest.
- active_work: **`PDF-VARIANT-DIGESTION`** (HIGH-PRIORITY owner program — digest ANY chip-spec PDF; `project_pdf_variant_digestion`). Lever A (table-kind classify `.2`/`.2c`/`.2d`/`.2b`/`.2b'`) + Lever B (prose entity capture `.3a`/`.3b`) LANDED. Whole-corpus sweep: 75/82 ingest OK, 1,908 signals / 2,953 registers / 10,632 fields / 3,077 relations — BREADTH proven; only 4/74 (APB/AHB/AXI/SWD) have VERIFIED precision. **Item ① `.4` (precision verification) CLOSED.** **Item ② `.5` now COMPLETE (`2026-06-08`): `.5a`+`.5c`+`.5b` all done.** `validate <evidence>` reports (a) `document_class` protocol/register/interface/guide (`classify_document`, conditional_rules excluded as over-produced; honest guide reporting), (b) `document_type_declared` front-matter signal (true guide vs under-extracted spec — `evidence_document_underextracted_spec` Warning), and (c) **`.5b` class-aware per-doc COMPLETENESS gauge** (`document_completeness_gauge`): a guide is `applicable=false`/never penalized; protocol/register/interface gauge each dimension only when its denominator > 0 → registers_without_fields / registers_unresolved_width / signals_without_direction / unexplained_intent_bearing_tables; `document_completeness_gaps` metric + Info `evidence_document_completeness` finding. Live: APB 1 gap; RISC-V Debug regs_unresolved_width 60/60 (XLEN-parametric); GIC guide n/a. KM `document-class-from-structure`. Guardrails: genericity (`feedback_genericity_guardrail`), multi-strategy (`feedback_multi_strategy_best_wins`), ADR 0006, scoring rigor (`feedback_scoring_rigor`). ISF lowering DEFERRED.
- sibling tree **`EXTRACTION-GAP-FIX`** (active): `.1`–`.4b` all DONE (I2C precision 0.600→1.000; NVMe mnemonic field-name 0/29→28/29; RISC-V register-name 0/60→59/60; tiling-gated `recover-register-bits` BUILT + live-measured — metric honestly UNCHANGED 0/179, ZERO fabrication, 60/60 residual; local `qwen2.5vl:7b` mis-sizes dense diagram cells → standard-width gate rejects → honest residual; +2 plumbing gaps). All 4 originally-quantified gaps WORKED — HONEST BOUNDARY; metric improvement = proposed follow-up `.4c`. KM `register-diagram-bit-recovery-via-tiling`.
- **HONESTY GUARDRAIL (owner `2026-06-08`, non-negotiable): you CANNOT extract what is not there.** Read the modality where the fact actually lives, or report an honest completeness RESIDUAL — NEVER fabricate a width/bit-range/field-name/register-name. A leaf that can't find the fact closes as verified-absent/residual, not a faked win.
- **`.8` (broaden prose-actor capture) DONE (`2026-06-08`)**: robust structural agent-definition grammar (`agent_definitions` over a conservative agent-class ALLOWLIST; parenthetical-strip + current-sentence + no-preposition guards — NOT a fragile noun denylist, after expert review). Projected 14→20 docs with actors (+6), all genuine; NVMe canonically 0→1 ("controller"); wire-based 0/clean; additive (eval unaffected). lib 1427; +6 tests. **GOTCHA (re-confirmed): evidence rebuild needs `normalized/<doc>.md`; `clean --scope source-normalized` reclaimed it for 72/75 docs → only NVMe/I2C/RISC-V Debug are canonically rebuildable without re-ingest** (`eval-scores-persisted-evidence`).
- next_action: **PNT — pick the next `PDF-VARIANT-DIGESTION` frontier leaf** (item ② `.5` CLOSED, `.8` DONE): `.6` (VLM levers `.2b`/`.2b'` on the addressable zero-yield image-table-heavy docs — incl. the 5 `.5c`-flagged under-extracted specs; measured uplift, 0 garbage via the verification gate; needs `ollama serve` + on-disk table images → re-ingest a target first) / `.7` (investigate+fix the USB 3.2 evidence-build FAIL + adopt "measure from typed evidence_ir"). Alternatives: sibling active trees `EXTRACTION-QUALITY-GAUGE.8/.FIELD`, `EXTRACTION-GAP-FIX.4c` bit-graphic plumbing. Keep APB/AHB/AXI/SWD at 100%, ADR 0006, scoring rigor.
- paused_work: none. in_flight_uncommitted: **`.8` implemented + all live docs/book/KM updated, staged for commit** (then clean). Only untracked file is `.claude/settings.json`, a local harness config that must stay untracked. blockers: none. RESUME (fresh session): read `README.md` + `MEMORY_ARCHITECTURE.md`, then this pointer → **PNT the next `PDF-VARIANT-DIGESTION` leaf** (`.6`/`.7` — see next_action). Ollama may need `ollama serve` (`qwen2.5:14b-instruct` + `qwen2.5vl:7b`). Re-ingest needs `DOCLING_DEVICE=cpu`.
