# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`b9af710a`** `KG-ISF-TRANSACTIONS.2k — handoff: fresh-session resume pointer to the VLM-tier lever`. **A `.2l` measurement commit is being created now** (docs-only). **182 ahead** of `origin/main` (→ 183 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (session `2026-06-17`; open-ended). **Owner: SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT through SUBSTANTIVE north-star gaps:** #1 relation-incompleteness (DONE `KG-ISF-COMPLETENESS.3`), #2 corpus coverage (DONE `CORPUS-COVERAGE.0`+`.1`), #3 transactions (`.2a`–`.2k` done; **`.2l` measurement DONE this session**).
- **`KG-ISF-TRANSACTIONS.2l` DONE `2026-06-17` (measurement-first, read-only + bounded VLM probe, docs-only):** resolved the last `.2j`-recorded candidate ("AXI/SWD timing-diagram phase columns, VLM-tier"). **Q1 AXI → GO but DETERMINISTIC:** AXI phases = channels; the doc's own caption-named `B1.x … channel signals` tables declared the per-channel signal sets and `table_signal_declaration_provenance` (411 entries) already maps signal→channel (B1.1=26 `AW*` address / B1.2=15 `W*` data / B1.3=14 `B*` response / B1.4=26 `AR*` / B1.5=18 `R*` / B1.6/B1.7 snoop) → channel→phase clean → **VLM superseded**. **Q2 SWD → honest degenerate absence:** 2-wire serial, packet phases are bit-field TIME-segments on shared `SWDIO` (Fig B4-1); recognized phases `signal_set=[]`, transactions `ports=[]` → nothing to recover, any lever. **Q3 VLM → evidence-based NO-GO:** crops carry no per-signal phase-column cue; bounded `qwen2.5vl:7b` probe was contradictory/redundant/hallucinated (`CRDT`/`VALID`), and loaded as **13 GB → host 87% used** (across the 85% kill line; `ollama stop`, recovered). KM card `[[transaction-phase-membership-vlm-vs-channel]]`; book unchanged (no behavior change).
- next_action (**PNT continues; RAM-SAFE — no VLM**): **`KG-ISF-TRANSACTIONS.2m` — DETERMINISTIC AXI-family channel-membership lever.** Group a recognized transaction's `.2c`/`.2k` signal membership by its document-grounded CHANNEL, recovered from the universal `<role> channel signals` table-caption cue (ADR-0006: the document's own vocabulary, no name list); surface channel/phase grouping where the document declares channels — this fills the `.2i` AXI-empty phase grouping deterministically. **MEASUREMENT-FIRST** (scope the universal caption grammar + the channel→phase mapping; weigh "channel membership as its own dimension" vs forcing the address/data/response mapping; genericity guardrail — channel vocabulary is AXI/ACE/CHI-family, the MECHANISM is universal). **Own a leaf first.** Hard gates: WIRE-BASED-100 (the 4 wire docs), `.isf` byte-identical expected (membership is metadata, emitter lowers `steps`), `kg-bench` 156/156, `run_ci.sh` GREEN, ADR-0006. Standing alternate: `CORPUS-COVERAGE` re-ingest the 57 normalized-missing docs (RAM-gated, host-local PDFs — owner re-provides on request).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** is critical-path.
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B/7B models (`ollama stop` before/after). The `.2l` VLM probe momentarily hit 87% used (7B→13 GB) and was stopped cleanly — `.2m` is deterministic/RAM-safe. `[[feedback_ram_ceiling_monitor]]`. Push ~every 200 commits or on ask (182 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path); for non-destructive measurement build into a TEMP evidence-root. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **only this `.2l` measurement commit** (task tree `.2l` done + Frontier, KM card + regenerated `KNOWLEDGE_MAP.md`, CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS, this MEMORY). No code touched. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY.**
