# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`9297364b`** `KG-ISF-TRANSACTIONS.2l — AXI/SWD phase membership: VLM-tier candidate resolved measurement-first`. **A `.2m` CODE commit is being created now.** **183 ahead** of `origin/main` (→ 184 after). Tree clean except untracked `.claude/settings.json` + `git_message_brief.txt`.
- **MODE: PNT loop** (session `2026-06-17`; open-ended). **Owner: SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT through SUBSTANTIVE north-star gaps:** #1 relation-incompleteness (DONE `KG-ISF-COMPLETENESS.3`), #2 corpus coverage (DONE `CORPUS-COVERAGE.0`+`.1`), #3 transactions (`.2a`–`.2l` done; **`.2m` CODE DONE this session**).
- **`KG-ISF-TRANSACTIONS.2m` DONE `2026-06-17` (CODE, measurement-first):** the deterministic AXI-family channel-membership lever — a recognized transaction's `.2c`/`.2k` signal-set membership is now grouped by the document-declared CHANNEL (universal `<role> channel signals` caption cue), filling the `.2i` AXI-empty `phase_membership`. New `EvidenceIr.signal_channel_memberships` (`build_signal_channel_memberships` — `derive_channel_role` caption grammar handling `B1.1` colon + `A2-2` dash forms, continuation-number chaining, **ambiguity gate** for boundary precision; ADR-0006/no name list), carried on SemanticIR, grouped in `mint_named_transaction` → `IntentIr.TransactionIntent.channel_membership`, + `validate` channel inventory. Channel role kept VERBATIM (NOT mapped to address/data/response — non-universal/fabrication-risky; complementary to `.2g`/`.2i` prose phases). Measured: 2025 AXI 154 signals → 8 roles 0 ambiguous; live `validate` `with_channel_membership: 5 (10 groups)`, `atomic_transaction` {write request×12/write data×3/read data×4/write response×3} multi-channel; 2021 AXI+ACE 31/105 ambiguous dropped → honest `unmapped`; AXI-Stream/APB/AHB/SWD empty (no fabrication). **Gates ALL GREEN:** ADR-0006 ✓; **WIRE-BASED-100 PROVABLY ORTHOGONAL** (`git stash` baseline-vs-`.2m` AXI evidence diff: ONLY `signal_channel_memberships`(154) changed, wire-gold surfaces byte-identical; emitted `.isf` byte-identical — emitter lowers `tx.steps` only) ✓; `kg-bench` 156/156 ✓; `run_ci.sh` GREEN (lib **1664**, +2; one `collapsible_if` let-chain fixed) ✓. Book `pipeline/intentir.md`; KM `[[transaction-channel-membership]]`.
- next_action (**PNT continues; RAM-SAFE default — no VLM**): the transaction tree's DETERMINISTIC levers are now built out (recognition `.2a` → bodies `.2b` → membership `.2c`/`.2k` → quick-surface `.2d` → phases `.2g`–`.2j` → channels `.2m`); the VLM-tier transaction frontier is exhausted. **Pick the next SUBSTANTIVE north-star lever** (`[[feedback_not_complete_attack_substantive_gaps]]` — do NOT treat "transaction levers built out" as "complete"). Standing candidates: (a) `CORPUS-COVERAGE` re-ingest the 57 normalized-missing docs (RAM-gated, host-local PDFs — owner re-provides on request; Docling needs `DOCLING_DEVICE=cpu`); (b) re-assess bar #2 relation-completeness / bar #5 behaviors on the now-broader corpus; (c) a new transaction refinement only if a measured gap exists. **MEASUREMENT-FIRST; own a leaf before any code.**
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** is critical-path.
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B/7B models (`ollama stop` before/after). `.2m` was fully deterministic/RAM-safe (no VLM). `[[feedback_ram_ceiling_monitor]]`. Push ~every 200 commits or on ask (**183 ahead → HOLD**, < 200).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path); for non-destructive measurement use `<stage> --dry-run` (prints JSON after a `command:`/`mode:`/`artifact_root:` preamble — slice from the first `{`) or build into a TEMP evidence-root. **STALE-PERSISTED GOTCHA:** `generated/` artifacts can be stale (built by an older binary) — rebuild with the current binary before measuring; isolate a change's effect with `git stash`. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **only this `.2m` commit** — Rust (`ir/evidence.rs` + `ir/semantic.rs` + `ir/intent.rs` + `commands/validate.rs`), task tree `.2m` done + Frontier + changelog (incl. the previously-missing `.2l` changelog entry) + `docs/TASK_TREE.md`, KM card + regenerated `KNOWLEDGE_MAP.md`, book `pipeline/intentir.md`, CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/README/RUST_CODEBASE_ANALYSIS, this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY.**
