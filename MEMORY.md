# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`a958d7db`** `KG-ISF-COMPLETENESS.4 — prose field-def gap generality MEASURED → GENERAL/GO (scoping, docs-only)`. **A `.10f` CODE commit is being created now.** **187 ahead** of `origin/main` (→ 188 after). Tree clean except untracked `.claude/settings.json` (+ the transient `git_message_brief.txt`).
- **MODE: PNT loop** (session `2026-06-17`; open-ended). **Owner: SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT through SUBSTANTIVE north-star gaps:** #1 relation-incompleteness (DONE `KG-ISF-COMPLETENESS.3`), #2 corpus coverage (DONE `CORPUS-COVERAGE.0`+`.1`; re-ingest of the 57 normalized-missing docs BLOCKED — source PDFs host-local, owner re-provides), #3 transactions (`.2a`–`.2m` DONE), #4 message-field recognition (`PDF-VARIANT-DIGESTION.10f` DONE; `.10g` next).
- **`PDF-VARIANT-DIGESTION.10f` DONE `2026-06-17` (this session — CODE): section-HEADING prose message-field recognizer (DTI-class message protocols).** The substantive lever spun out of `KG-ISF-COMPLETENESS.4`. **Scoping CORRECTED measurement-first:** the `.4` note guessed prose `list_item`s `"<Field>, bit [N]"`; the persisted `source_ir.json` shows DTI defines each field as its own **section HEADING** (`STAGES, bits [27:26]` / `M_MSG_TYPE, bits [3:0]`) under a dotted-numbered message container (`3.1.1 DTI_TBU_CONDIS_REQ`) with a `Field descriptions` anchor — NOT list_items (those ~0; body_text matches are descriptive-prose noise the section-heading restriction excludes). Additive `extract_section_header_message_fields` (`ir/evidence.rs`), 4th strategy `message_fields.section_header_field` reading `source_ir.document_sections`. **Container-decides routing** (the `.10b`/`.10c`/`.10e` register-iff-`Attributes`/`Accessing`/caption-`register` rule) keeps the SAME-shape register fields of GIC 612/SMMU 434/CoreSight 42/ACC 5/ARM-Debug-v6 138 OUT of the message surface → deferred sibling `.10g` (~1230 fields). Cleanups: spacing-artifact `<ident> [slice]`→`<ident>[slice]`, unit word `Bits, bit [5:4]` dropped (residual); M/S overlaps kept. **DTI 0 → 159 fields / 17 containers; ONLY DTI fires (1/79).** Gates: old-vs-new `evidence --dry-run` over 7 intact docs byte-identical except the new manifest entry (NVMe 216 / AMD 217 unchanged; ARM-Debug-v6 138 headings→0) → WIRE-BASED-100 provably orthogonal; `kg-bench` 156/156; `run_ci.sh` GREEN (lib 1664→1672, +7). FIELD.4 re-routes the DTI leak on the next live-NLP DTI rebuild (Pattern surface doesn't read the catalog; bundle host-local-blocked). ADR-0006. KM `[[section-header-message-field-extraction]]`.
- next_action (**PNT continues; RAM-SAFE default — no VLM**): **`PDF-VARIANT-DIGESTION.10g`** — the REGISTER-routed twin of `.10f`: the same `<NAME>, bits [hi:lo]` section-heading field form, but for containers that ARE registers (caption-`register` / `Attributes` / `Accessing` sub-heading) → emit to the **register surface** (`RegisterRecord`/`register_records`), not message fields. **Measured candidate (read-only `2026-06-17`):** ~1230 register fields across GIC `ihi0069` 612 / SMMU `ihi0070` 434 / CoreSight `ihi0029` 42 / ACC `ihi0076` 5 / ARM-Debug-v6 `ihi0074` 138 (all INTACT bundles — `evidence --dry-run` works). **PRECISION crux:** these TRMs already carry register records from `.10c` (`registers.bit_assignment_table`) + summary captions — `.10g` MUST NOT double-count (collision-check the existing register surface per `.10c`), and the register golds (NVMe/CCIX/RISC-V — which carry NO section-heading field form, proven by the `.10f` parity) must stay byte-identical. Mirror `.10f`: a new strategy `registers.section_header_field` via `run_surface_concat` on the register surface, container-name from the dotted heading, access/reset honestly absent. **Measurement-first** + `run_ci`/`kg-bench`/WIRE-BASED-100 + register-gold byte-identity. **OWN a leaf before any code.** Secondary spun-out (still open): DTI signal-inventory prose noise (`AMBA`/`ARM`/`APCI` minted as DTI signals); ATP `ihi0082` VLM-fragment quality. Do NOT treat "lever done" as "complete" (`[[feedback_not_complete_attack_substantive_gaps]]`).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** is critical-path.
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B/7B models (`ollama stop` before/after). `.10f` was RAM-safe (cargo + dry-runs over persisted artifacts, no VLM/Docling). `[[feedback_ram_ceiling_monitor]]`. Push ~every 200 commits or on ask (**187 ahead → HOLD**, < 200; → 188 after this commit).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path); for non-destructive measurement use `<stage> --dry-run` (prints JSON after a `command:`/`mode:`/`artifact_root:` preamble — slice from the first `{`) or build into a TEMP evidence-root. **STALE-PERSISTED GOTCHA:** `generated/` artifacts can be stale (built by an older binary) — rebuild with the current binary before measuring; isolate a change's effect with `git stash`. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **only this `.10f` CODE commit** — `crates/specforge/src/ir/evidence.rs` (the extractor + helpers + 4th strategy registration + 7 tests), task tree `docs/tasks/PDF-VARIANT-DIGESTION.md` (`.10f` leaf done + `.10g` spun + build-sub-leaf list) + `docs/TASK_TREE.md` row, new KM card `docs/knowledge/section-header-message-field-extraction.md` + regenerated `KNOWLEDGE_MAP.md`, README/RUST_CODEBASE_ANALYSIS/CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS + book `docs/book/src/pipeline/evidenceir.md`, this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY.**
