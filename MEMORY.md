# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> The bounded **resume pointer**: it describes *now*, not the journey. Overwrite the "Current state"
> block below; never append session narration. How to resume — bootstrap order, doctrine, gates, and
> retrieval — is `AGENTS.md`; the layer contract is `MEMORY_ARCHITECTURE.md`. Everything else is derived
> on read: revision from `git rev-parse HEAD`, work state from `docs/tasks/`, history from `git log`.

## Current state (OVERWRITE this block each update — do not append)
- Active unit: **`LIVE-DOCUMENT-PRESSURE-HEADROOM`** — seven leaves closed this session, detail in
  `docs/tasks/`. **`.21`** partitioned `CLAIM-VERIFICATION-ADOPTION.md` (278,514 of 278,528 bytes) into
  an 8,157-byte bounded root over 11 parts; lossless proved WITHOUT the writer by re-harvesting marker
  payloads from the part FILES to the committed blob `938f909f88e71f3b…`. The stop RELOCATED to
  `EXTRACTION-QUALITY-GAUGE.md` at **258,302 / 278,528 (92.7%)** — next to need partitioning.
  **`.22`/`.22a`/`.22c`**: a registry header now declares the `milestones` band every surface it governs
  already carried; the raise was UNGOVERNED (the authority protocol is keyed by `surface_id` and never
  reached a header), so the protocol was extended FIRST, then `max_records` **64→96** and `max_bytes`
  **65,536→98,304** under one authority, retired after its banked refusal fired RED. Registry
  **61/96 = 63.5%**; one partitioned tree costs **4** records.
  **`.22b`/`.22e`**: the band is computed ONCE centrally over registries **DISCOVERED** in tracked
  `doctrine/**/*.jsonl` (no shared Perl lib exists), so each other loader changed by ONE line — all
  **ten** registries banded, 108/108. `repin_claim_regions.py` now reads FOUR registries and all THREE
  shapes (`{path,region}`; `control.red_case`, inline with NO `kind`; `red_evidence.source_region`,
  file from the control's `producer`/`inputs[0]`): **565 → 574** regions, self-test 19/19. Control: one
  line into a checker made the OLD version report `unchanged 565`, 0 moved, exit 0 over 3 displaced pins.
- **The continuation path is PROVEN**: `CLAIM-VERIFICATION-ADOPTION.17` was declared through it — append
  to the ONE active part OUTSIDE the markers, `- ID:` in the root registry, a `post_migration` route with
  NO `source_literal`, re-pin the part's `sha256`/metrics, `--write`, `--check`; index routes **2 open of
  47**. Edit that contract with Perl `JSON::PP` pretty+canonical, not Python, or it reformats wholesale.
- Next action: **`.22d`** — `book_quantitative_claims.jsonl` is **468 of 512** with NO lifecycle. Size the
  remedy against the EVENT (one chapter split took it **351→464 in a day**), not the 0.74-per-revision
  mean. Decide whether it and `.18` move to their durable owner `CLAIM-VERIFICATION-ADOPTION`; **a
  cross-tree move needs a declared removal** in `doctrine/task_nodes/removals.jsonl` (TASK-NODE-RETENTION),
  so that is its own transaction. `.17` (census evidence-id convention: suffix = region CONTENT digest
  prefix, **68 of 68**, gated by nothing) is open in the owning tree.
- Also open: `SIGNAL-DECLARATION-ROW-DROP.2i` (trapped-row doc claims an arrow arm it lacks; **0 of 663**
  rows, so fix the COMMENT), `.2f` (122 recoveries, blocked on `Unused`), `.2h.2` (per-row-layout census);
  `LIVE-DOCUMENT-PRESSURE-HEADROOM.1`/`.15`-`.18`/`.20`/`.4d.ii`; `COMMIT-GATE-SINGLE-RUN.0` (per-doctrine
  timing, IDLE machine); `TEXT-LAYER-IDENTIFIER-SPLIT.1`; `EXTRACTION-QUALITY-GAUGE.3j`/`.4c` (behind
  `KG-ISF-COMPLETENESS.2a`).
- In-flight uncommitted: none. No background job outstanding.
- Blockers: none. Push cadence **400** (directive `2026-09-13`, FIXED), none due at 288; directive 16 gates
  it on full CI. **Corpus CURRENT — 27/27 at semantic and intent, retention exactly 24.**
  **Any new production Rust FUNCTION moves `flow_census.json`** — re-derive via `aggregate_change`, never
  edit the literal; boundary counts (144/9) must NOT move. **After editing a governed file OR a checker
  script run `python3 scripts/repin_claim_regions.py --check` then `--apply`**, refresh
  `shipped_behavior`, then the `claims.jsonl` digests LAST — that set IS the `Published-claims:` line.
  Standing hazards in fact cards: **[[claim-verification-task-evidence-migrated]]**,
  **[[live-surface-edit-bookkeeping-chain]]**, **[[a-cheap-structural-rule-overfires-until-you-read-its-selection]]**,
  **[[actor-taxonomy-grows-in-pairs-not-terms]]**, **[[a-single-index-bit-cell-is-a-width-of-one]]**,
  **[[declaration-replay-reads-the-legacy-stratum]]**, **[[prior-phrase-utf8-byte-as-char]]**, **[[a-dropped-declaration-row-is-usually-not-a-signal]]**.
