# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`448b5abd`** `PDF-VARIANT-DIGESTION — resume-pointer frontier triage after .10g (continuity, docs-only)` (`.10g` itself = `32f32e7c`). **A `.2n` docs-only commit is being created now.** **190 ahead** of `origin/main` (→ 191 after). Tree clean except untracked `.claude/settings.json` (+ the transient `git_message_brief.txt`).
- **MODE: PNT loop** (session `2026-06-17`; open-ended). **Owner: SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT through SUBSTANTIVE north-star gaps:** #1 relation-incompleteness (DONE `KG-ISF-COMPLETENESS.3`), #2 corpus coverage (DONE `CORPUS-COVERAGE.0`+`.1`; re-ingest of normalized-missing docs BLOCKED — source PDFs host-local), #3 transactions (`.2a`–`.2n` DONE — recognition+bodies+membership+BODY-completeness probe), #4 message/register section-heading recognition (`PDF-VARIANT-DIGESTION.10f`+`.10g` DONE).
- **`KG-ISF-TRANSACTIONS.2n` DONE `2026-06-17` (this session — measurement-first, read-only, docs-only — NO-GO): the transaction ISF BODY is faithfully complete.** Ran the `2026-06-17`-triage-named "buildable transaction body lever" as a FRESH probe-first cycle on the current FSMGen pin `030f8c273`. **Empirical (`subs/fsmgen/bin/fsmgen --strict --check --json`):** a value-free `(on start (sample S as s)) (complete done)` body PASSES strict (probe A; multiple same-state samples pass, D; `(sample …)` is NOT direction-gated, B); an in-body `(drive NAME 0)` with no top-level named drive FAILS `drive 'NAME' not defined` (C — an in-body drive is a CALL to a top-level named drive, which is exactly why `.2b`'s enum-selector drive renders). **FSMGen-accepted ≠ faithful:** `13b-transactions.md` (`(on …)` Entry/Idle State, L133–163) — a sample inside `(on …)` fires AT THE ENTRY transition ("Cycle N: `port && can_accept` → samples captured") + needs an activation guard, so a membership-derived sample body asserts an un-grounded entry-cycle capture + `start` guard the document never states (and could misrepresent wait-for-ready as a one-shot sample) — the fabrication the honest-residual doctrine + FSMGen's `2026-06-16` answer forbid. **NO-GO:** the only body-lowerable grounded fact (the enum-selector drive) already ships (`.2b`); membership already lives faithfully as IntentIR metadata (`.2c` ports / `.2i` phase / `.2m` channel). Reconfirms `.2i` parking with FRESH `030f8c273` evidence (prior probe `8c39827f`). Gates: read-only/docs-only — WIRE-BASED-100 untouched, ADR-0006. KM `[[transaction-body-emission-faithfully-complete]]`.
- next_action (**PNT; RAM-SAFE default — no VLM**; own a leaf BEFORE any code; measurement-first; ADR-0006; WIRE-BASED-100 + register/wire golds + `kg-bench` hard gates). **The transaction tree's faithful deterministic + body levers are now ALL built/measured (`.2n`).** The triage's other options are still BLOCKED/low-value: (a) `.10g` block-qualified reused-mnemonic = LOW-VALUE/HIGH fabrication-risk; (b) DTI signal-inventory prose noise = host-local-blocked; (d) ATP `ihi0082` = VLM/RAM-heavy. **The next SUBSTANTIVE north-star fidelity lever is OWNER-GATED: interface signal DIRECTION emission (`KG-ISF-COMPLETENESS.2`)** — the `.isf` declares ~98% of signals `(output …)`, ignoring the grounded actor-port graph (the largest measured true infidelity); building it needs the single-flat-module actor-perspective decision (deferred by `R6-ISF-ADAPTER.4` + owner). **→ RAISED TO OWNER (AskUserQuestion); awaiting the perspective decision before the next code slice.** Do NOT treat "lever done" as "complete" (`[[feedback_not_complete_attack_substantive_gaps]]`).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** is critical-path.
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B/7B models (`ollama stop` before/after). `.2n` was RAM-safe (cargo-free; `fsmgen` Perl probes + persisted-artifact reads, no VLM/Docling; RAM steady ~70% free). `[[feedback_ram_ceiling_monitor]]`. Push ~every 200 commits or on ask (**190 ahead → HOLD**, < 200; → 191 after this commit).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path); for non-destructive measurement use `<stage> --dry-run` (prints JSON after a `command:`/`mode:`/`artifact_root:` preamble — slice from the first `{`) or build into a TEMP evidence-root. **STALE-PERSISTED GOTCHA:** `generated/` artifacts can be stale (built by an older binary) — rebuild with the current binary before measuring; isolate a change's effect with `git stash`. **FSMGen BODY GRAMMAR (`.2n`, pin `030f8c273`):** in-body `(drive NAME …)` is a CALL to a top-level named drive (defined elsewhere); `(sample S as s)` is value-free + NOT direction-gated but fires at the `(on …)` entry transition. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **only this `.2n` docs-only commit** — task trees `docs/tasks/KG-ISF-TRANSACTIONS.md` (`.2n` node + Frontier + changelog) + `docs/TASK_TREE.md` row, new KM card `docs/knowledge/transaction-body-emission-faithfully-complete.md` + regenerated `KNOWLEDGE_MAP.md`, `CHANGES.md`/`DEVELOPMENT_NOTES.md`/`LIVE_ACHIEVEMENT_STATUS.md`, this `MEMORY.md`. No code, no README/book change (no behavior/command/user-facing change). Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY.**
