# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`32afd5ac`** `KG-ISF-COMPLETENESS.1b.ii — consolidate "X interface" onto the bare agent`. **A `GRITS-CROSS-TOOL.4` DOCS commit is being created now** (document the `grits-consensus` command in mdBook + README). **176 ahead** of `origin/main` (→ 177 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (fresh session `2026-06-17`; no BWFSC set → open-ended). PNT slices this session: `.2` (`f1a507dd`) → `.2a.i` width (`3d7dba01`) → `.2b` (`0fb81e5c`) → `.1b.iv` phantom drop (`46faebb0`) → `.1b.ii` interface consolidation (`32afd5ac`) → **`GRITS-CROSS-TOOL.4` doc-coverage (THIS commit)**.
- **`GRITS-CROSS-TOOL.4` DONE `2026-06-17` (DOCS — THIS commit):** documented the shipped `grits-consensus` command, closing a book-coverage drift the tree left when it closed `.1`–`.3` (a book close-rule miss). Fresh-session bootstrap audit (README→roadmap→codebase→mdBook re-read, 2 delegated survey agents). The mdBook survey flagged "4 missing"; per the `BOOK-COMMAND-COVERAGE` precedent (delegated "5" was really 2) I re-derived the census — 28 `Commands` enum variants from `cli.rs` grep'd vs `docs/book/src/`+`README.md` — finding EXACTLY ONE undocumented shipped command: `grits-consensus` (book#0/README#0; `audit-extraction`/`eval-extraction`/`nli-verify` already covered). New `## grits-consensus` section in `docs/book/src/commands/quality-and-learning.md` (after `kg-bench`, user-friendly style: cross-tool consensus gold / pdfplumber+qwen2.5vl witnesses / adjudication loop / `--min-agree`+`--adjudicate-out` / read-only offline `.venv-eval` boundary; facts cross-checked vs `commands/grits_consensus.rs`+`cli.rs`) + README CLI-surface entry + state bullet; `overview.md` untouched (curated surface omits the eval-judge family by convention). **Docs-only: NO Rust/CLI change** → `run_ci.sh`/`kg-bench` unaffected by construction; `mdbook build docs/book` exit 0; memory-arch GREEN; post-fix census book#=1/README#=2. `GRITS-CROSS-TOOL` fully closed (`.1`–`.4`).
- next_action: **CONTINUE PNT.** This slice was a doc-drift fix surfaced at the bootstrap audit (not a bar lever). The buildable CODE frontier remains measured-EXHAUSTED: agent-surface `.1` fully built; `.2a` direction FSMGen-neutral/deferred; `.2b` measured-marginal; `KG-ISF-TRANSACTIONS` body-emission FSMGen-parked; `MEMORY-BOUNDED-INGEST`/`NLP-SHALLOW-PARSE` standing/build-exhausted; `CORPUS-PATTERN-REUSE.4` offline-miner gated. Next PNT pick: keep mining the bootstrap drift-audit lever (re-derive other code↔README↔book drifts — e.g. the `overview.md` curated-surface omits 7 real commands; the `BOOK-COMMAND-COVERAGE` spun-out `ROADMAP.md` lane-hierarchy drift) OR a measure-first bar dimension; if none clean, natural fresh-session handoff boundary.
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** elevated to critical-path; `KG-ISF-TRANSACTIONS.2a→.2j` done (body-emission FSMGen-parked).
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes (`[[feedback_ram_ceiling_monitor]]` / `[[project_big_pdf_memory_bounded_ingest]]`). This slice was RAM-safe (docs-only: grep + `mdbook build`, no 14B/Docling; RAM 77% free). Push ~every 200 commits or on ask (176 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (symlink canonical `generated/source_ir` in + run the release binary from a temp CWD so the RELATIVE output root redirects). `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **a `GRITS-CROSS-TOOL.4` DOCS commit is being created now** — `docs/book/src/commands/quality-and-learning.md` (new `grits-consensus` section) + `README.md` (CLI-surface entry + state bullet) + `docs/tasks/GRITS-CROSS-TOOL.md` (`.4` node→done, status, changelog) + `docs/TASK_TREE.md` (row) + `CHANGES.md` + `DEVELOPMENT_NOTES.md` + `LIVE_ACHIEVEMENT_STATUS.md` + this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt` (cleared post-commit). blockers: **none**. **Repo HANDOFF-READY** — clean checkpoint; buildable CODE frontier exhausted; continue PNT (bootstrap drift-audit lever / measure-first bar dim) or hand off.
