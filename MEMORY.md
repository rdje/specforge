# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (memory system), `DOCTRINE_ENFORCEMENT.md` (the 4th
  architecture — doctrines are mechanically gated), and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Before committing run `scripts/check_doctrines.sh` (the registry/driver: memory-arch +
  knowledge-map + task-acceptance); hooks + CI run it too. Retrieval: `KNOWLEDGE_MAP.md`.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`92673eea`** `KG-ISF-COMPLETENESS.5.iii — drop _WIDTH parameter-leak members from synthesized enums`. **72 ahead** of `origin/main` (push at ~200 → HOLD); this `PDF-VARIANT-DIGESTION.10h` CODE commit → **73 ahead**. Untracked (stays): `.claude/settings.json`, `.cache/` (gitignored symlink).
- **MODE: PNT loop** (fresh conversation re-ran the full README bootstrap `2026-06-24`; doctrines GREEN). This session's slice (DONE): **`PDF-VARIANT-DIGESTION.10h`** — block-ambiguous register-mnemonic recovery (the `.10g` residual), measurement-first/CODE/GO.
- **✅ `PDF-VARIANT-DIGESTION.10h` CODE LANDED (`2026-06-24`).** `extract_section_header_registers` (`ir/evidence.rs`) replaces the `.10g` drop-all-duplicates gate with the pure helper `collapse_section_header_register_identity`: a register mnemonic reused across ≥2 register-routed section-heading containers collapses to ONE record IFF every occurrence's field set (by uppercased name) ⊆ one maximal occurrence (identical cross-refs + nested views of a single register), keeping that fullest occurrence's REAL layout; disjoint/partial sets (≥2 genuinely-different registers — MEM-AP `CSW` vs JTAG-AP `CSW`) stay an honest residual (never conflated). Probe-measured: the residual lives in EXACTLY 2 docs (ARM-Debug `ihi0074`, CoreSight `ihi0029`); the PDF backend flattens headings to L1 (no ancestor-block name — only dotted-number + field lists). **Verified:** ARM-Debug section-heading regs 12→15 / 57→69 fields (`AUTHSTATUS`/`DEVARCH`/`IDR`), CoreSight 5→6 / 24→29 (`AUTHSTATUS`); full `evidence` register_records ARM-Debug 37→40, CoreSight 26→27; `git stash` baseline diff over 8 golds (CCIX/NVMe/AXI/AHB/GIC/SMMU/ACC/DTI) BYTE-IDENTICAL, the 2 changed docs ADD with ZERO removals (all baseline records preserved). `kg-bench` 156/156; `run_ci.sh` GREEN (lib **1721**, +3 tests + 1 `#[ignore]` probe). ADR-0006 (field-set containment, no name list). Narrowed residual: genuinely-different `CSW`/`CLAIMSET`.
- next_action (PNT): **continue PNT — pick the next surfaced extraction-precision lever** per `[[feedback_not_complete_attack_substantive_gaps]]`, measurement-first. Top candidate `.10i` (block-qualified recovery of the disjoint genuinely-different registers `CSW`/`CLAIMSET`): the `.10h` probe shows the block name IS available — the PARENT SECTION TITLE via dotted-parent (`C2.6 MEM-AP register descriptions`/`C3.5 JTAG-AP register descriptions` → `MEM-AP`/`JTAG-AP`), so `CSW@MEM-AP`(11f)/`CSW@JTAG-AP`(7f) is a clean GO; subtleties = the multi-block IDENTICAL cluster (`CLAIMSET` pair across C1.4/C2.6/C3.5, no single block) + threading the parent title without perturbing the `.10f`/`.10g` SHARED `scan_section_header_field_containers` walk (add `number`/parent to the container struct, additively). Other candidates: **CORPUS-HARDENING** CHI Class-A (4 protocol matrices mis-typed `timing_parameter`, empty `body_rows`); **EXTRACTION-QUALITY-GAUGE** descriptive-FIELD-cell residual (`.3e`/`.3g` already shipped most — re-measure first). KG-ISF-COMPLETENESS frontier exhausted (`.1c.ii` NLP-gated, `.5` built); KG-ISF-TRANSACTIONS FSMGen-gated.
- in_flight_uncommitted: **none** once committed (code: `ir/evidence.rs` containment resolver + `collapse_section_header_register_identity` + 3 hermetic tests + 1 `#[ignore]` probe; docs: task leaf `.10h` done + acceptance checklist + `.10g` residual note + changelog, README bullet, book `pipeline/evidenceir.md` `.10h`, KM card `section-header-register-identity-collapse` + `KNOWLEDGE_MAP.md` regen (127/938), CHANGES/DEV_NOTES/LIVE_ACHIEVEMENT/MEMORY). blockers: none. **Repo HANDOFF-READY.**
- **OPERATIONAL REALITY:** the debug binary carries `.10h`. Generated: ARM-Debug/CoreSight evidence re-emitted via `.10h` (recall gain); the 8 verified golds byte-identical (`git stash` baseline diff). fsmgen = Perl `subs/fsmgen/bin/fsmgen` (NOT cargo); `--check` needs `--json`. Host RAM headroom OK. Artifact-cleanup standing (≥24h). lib test count **1721** (RUST_CODEBASE_ANALYSIS.md count is AUDIT-DOC-RECONCILE-owned).
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication; a missing ISF abstraction → verified FSMGen FR, never an emitter hack ([[feedback_isf_no_hacks]] / [[feedback_verify_fsmgen_before_fr]]). `scripts/check_doctrines.sh` green before committing; `scripts/run_ci.sh` green before committing CODE. Push ~every 200 (HOLD).
