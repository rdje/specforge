# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (pre-`.2b` baseline): **`0bbdc565`** `KG-ISF-TRANSACTIONS.2a — …(G2 de-hardcode)`. **The `.2b` commit is being created now** (this MEMORY update is part of it); the next checkpoint refreshes latest_commit to the `.2b` hash. **152 ahead** of `origin/main` (push at ~200, NOT during the batch). Fresh session resumed full bootstrap → executed batch slice 2/3 (`.2b`).
- **BATCH (owner-authorized): bounded `.2a→.2b→.2c` (BWFSC=3, transaction slices); commit after EACH; push only after the full batch or at ~200 ahead. Completed: `.2a` (1/3) + `.2b` (2/3). NEXT: `.2c` (3/3, FINAL) — then batch complete (push decision at ~200 or on ask).**
- **NORTH STAR (owner, `2026-06-16`, multi-message — `[[project_kg_isf_completeness]]`):** the KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* **REVERSES the prior "defer ISF lowering" steer.** "ISF ready" ≠ adapter renders: ALL 36 built docs already render `.isf` 0-blockers (measured) — fidelity is the IntentIR's. New tree `KG-ISF-COMPLETENESS` owns it; checkable 6-point bar (agents/relations/signals/constraints/behaviors-temporal/ISF-round-trip); WIRE-BASED-100 a hard gate. **Measured 2-defect agent baseline (both coexist → naive fix disproven):** PRECISION (prose-fragment non-agents `for`/`note`/`then_it`/`class_x` minted as actors — they carry ports, so "drop orphans" FAILS) + COMPLETENESS (real agents `producer`/`consumer`/`receiver`/`transmitter` carry 0 ports/relations vs `manager`/`subordinate` 169/168).
- **TRANSACTION DIRECTIVE — sharpened + ELEVATED (owner, `2026-06-16`, multi-message THIS session — `[[project_kg_isf_transactions]]`):** transaction capture is **of utmost importance** and a **MINIMUM / critical-path prerequisite** — *"without this we can't move forward in our PDF→ISF tool."* 4 reqs: (1) **fast + universal recognition** ("very quickly identify transactions in ANY chip-spec PDF" — cheap/deterministic/structural, no LLM, no name list; runtime-capability, NOT rush-code); (2) **boundary/membership precision** ("what's part of transaction X and what's not" — COMPLETE + EXCLUSIVE); (3) **step-by-step** thorough description; (4) **ALL** transactions, protocol or platform. Captured in the sharpened **7-point bar** in `docs/tasks/KG-ISF-TRANSACTIONS.md`. `KG-ISF-TRANSACTIONS` is now the near-term **TOP-PRIORITY** active tree.
- **`.2a` DONE (G2 de-hardcode):** removed the 3 hardcoded `recognize_digital_patterns` blocks → structural universal recognizer (Cue A `SemanticIr.transaction_anchors` threaded EvidenceIR→SemanticIR→IntentIR + Cue B `symbol_definitions` enums; `mint_named_transaction`, `ir/intent.rs`); 212 named txns / 42 docs; `readme` 0. (Detail in CHANGES/task-tree.)
- **`.2b` DONE (G1 composed bodies + `*_behavior` re-levelling — measured, `ir/intent.rs`):** (1) REMOVED the per-actor `{actor}_behavior` synthesis from `synthesize_transactions` + its 2 orphaned helpers (`render_temporal_predicate`/`temporal_consequent_to_step`) — census-§3.6 phantoms built from `temporal_rules` (already lowered validly via the temporal path), redundantly + `.isf`-INVALIDLY re-rendered (their multi-word `when` like `HREADY == HIGH @PreTick` tripped FSMGen `when body clauses must be list forms`); clears that strict-error class corpus-wide, no semantic loss. (2) `mint_named_transaction` composes a grounded `(drive signal value)` body for the Cue-B-corroborated subset (`idle_transfer` ⟺ `HTRANS = IDLE`) → RENDERS to `.isf`. **MEASURED RE-SCOPING:** section-named txns and per-channel handshakes have NO structural name bridge → faithful handshake→named-txn composition needs the grounded signal-set membership `.2c` owns (deferred there, NOT fabricated — bar #3 boundary precision). Measured: `*_behavior` blobs 0 corpus-wide; **APB strict-FAIL→PASS**; AHB `idle_transfer` renders + strict-valid; remaining wire-doc strict fails are PRE-EXISTING non-transaction rule/enum issues `.2b` only unmasked (out-of-scope residuals: AHB HAUSER rule-write conflict; AXI/ace/lti `constraint_*` assignment grammar; axi-stream/gfb rule-write conflicts; trace-bus ATID width; hbm2 enum emission). Gates: ADR-0006 ✓, **WIRE-BASED-100 constraint+temporal F1 = 1.000** ✓ (orthogonal), ISF round-trip 0 new txn diagnostics ✓, `kg-bench` 156/156 ✓, `run_ci.sh` green ✓ (lib 1641; 1 existing test updated).
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask. (This slice was RAM-safe: deterministic, no 14B.)
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, never runtime; NO deny/filter LISTS (prefer structural gates); goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals; honest RESIDUAL over fabrication. **`scripts/run_ci.sh` before declaring any CODE slice green**; `cargo test` does NOT rebuild the bin — `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (`evidence --dry-run` strips a 4-line header before the JSON) and eval there. `subs/fsmgen` pinned `c0b7eaa7`.
- **EQG `.3` precision program now spans `.3a`–`.3g`; the clean+narrow+universal gate frontier on the persisted corpus is MEASURED-EXHAUSTED.** Remaining NVMe residuals are NOT clean micro-gates: (a) prose-subject catalog class (`NVM`/`LBA`/`FFFF` admitted as "signals" because a register doc has no real wire catalog) and (b) tangled spurious-digit-value class (`HMDLLA 1` vs the GENUINE `BADD 0`/`RECFMT 0h`/`ELEN 0h` — no clean discriminator). Both need deeper non-narrow work (catalog-quality / `.FIELD` re-routing) with real gate risk — pure-hex/lowhigh-level shortcuts were each MEASURED-REJECTED (would flag real `CBA`/`BADD` / kill genuine HBM2/I2C). Don't grind more 1-record micro-gates (genericity guardrail).
- next_action: **`KG-ISF-TRANSACTIONS.2c` — G3 signal-set membership + boundary precision (batch slice 3/3, FINAL), measurement-first.** Enumerate each transaction's full signal set with roles (address/data/control/response), COMPLETE + EXCLUSIVE ("what is part of X and what is NOT" — bar #3/#4), grounded in the document's own channel/phase grouping (e.g. the transaction-defining section's own signal references, or a grounded channel→transaction link). **`.2b` established this membership grounding is the PREREQUISITE for the broad composed bodies** — once a named txn's signal set is grounded, its multi-phase body (handshakes/phases among those signals) composes faithfully (the part `.2b` deferred rather than fabricate). Start: MEASURE (read-only) what threaded data grounds per-transaction signal membership (section spans → signal mentions? channel-prefix grouping?) BEFORE coding; do NOT guess a channel→txn mapping (ADR-0006/fabrication). **Gates:** ADR-0006; WIRE-BASED-100 constraint+temporal F1 = 1.000; ISF round-trip 0 new txn diagnostics; `kg-bench` 156/156; `run_ci.sh` green. `cargo build --release` + `dangerouslyDisableSandbox`; `generated/` is gitignored so regenerating canonical wire docs (intent→adapt→`subs/fsmgen/bin/fsmgen --strict --check --json`) is safe for measurement. After `.2c`: batch (3/3) COMPLETE → report + push decision (~200-ahead or owner ask). Then `.2d?` (quick-surface validate inventory). Deferred elsewhere: pre-existing rule/enum ISF-strict issues (candidate slice); `KG-ISF-COMPLETENESS.1b.ii`/`.1b.iv`; EQG `.3` exhausted; RAM-heavy `CORPUS-PATTERN-REUSE.4` needs owner go; 24 promoted canonical docs await `R7-VALIDATION`.
- in_flight_uncommitted: **the `.2b` commit is being created now** — 1 src file (`ir/intent.rs`: −`{actor}_behavior` synthesis + −2 orphaned helpers; `mint_named_transaction` composes grounded body; 1 test updated) + docs (README, CHANGES, DEVELOPMENT_NOTES, LIVE_ACHIEVEMENT_STATUS, RUST_CODEBASE_ANALYSIS, task tree, TASK_TREE row, census §4.2, KM card, book `pipeline/intentir.md`, this MEMORY) + regen KNOWLEDGE_MAP. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt` (cleared post-commit). blockers: **none**.
