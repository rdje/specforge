# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`0b788434` = `ROADMAP-TASKTREE-COVERAGE.6` (post-`.5` ROADMAP↔tree alignment refresh, docs-only)**; **now committing `MEMORY-BOUNDED-INGEST.5`** (summary/`source_ir.json` streaming — measured DEFER, docs-only); ~124→125 ahead of `origin/main` (session `2026-06-15`, fresh session, PNT loop owner-authorized unbounded). **THIS-SESSION:** full README→roadmap→codebase→mdBook read done; 1 slice so far: **`MEMORY-BOUNDED-INGEST.5`** — probe-first measurement (read-only, RAM-safe, no Docling/model) of `source_ir.json` size vs page count over all 78 persisted artifacts: **O(pages) at ~9.3 KB/page** (least-squares slope 9,312 B/page, n=78; worst 17,755), library max 930p→7.0 MB / 842p→9.9 MB, 2,000p→~18 MB ⇒ trivially bounded at every realistic size ⇒ **measured DEFER** (streaming is YAGNI, would only risk the `.1`/`.3`/`.4c` byte-identical guarantees; ingest assembly already backstopped by `.4a`, disk by `.3`). Corrected binding constraint = the **downstream full-serde load** `SourceIr::load_from_path` (`ir/source.rs:573`, `serde_json::from_str(&read_to_string)`, no RAM guard on evidence/semantic/intent) → spun `.5a` (deferred-until-triggered, mirroring `.3b`; re-open trigger = a real doc whose `source_ir.json` would exceed a RAM-safe bound ≈ >20,000 pages). KM card `source-ir-size-scaling` + book "How large is the `source_ir.json` itself?" note (`pipeline/sourceir.md`). Docs-only; no Rust change → memory-arch + KM derive-and-diff + `mdbook build` are the gates (NOT run_ci). MEMORY-BOUNDED-INGEST now **Mostly Done / standing** with a measured-exhausted buildable frontier.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **`EXTRACTION-QUALITY-GAUGE`** (`.3` constraint-precision program COMPLETE `.3a`–`.3e`; `.FIELD` residuals; candidate future leaf = a descriptive-FIELD-cell gate for NVMe `must_be_stable` "This field indicates…" rows — see next_action caveat); **`MEMORY-BOUNDED-INGEST`** (**Mostly Done / standing** — RAM `.1`/`.2` + DISK `.3` + RAM guard `.4a` + disk pre-flight `.4b` + adaptive batch `.4c` DONE; `.5` summary streaming MEASURED-DEFERRED `2026-06-15`; `.5a`/`.3b`/`.4d` deferred-until-triggered; **buildable frontier measured-exhausted**); **`PDF-VARIANT-DIGESTION`** (`.13` sweep COMPLETE incl. `.13c` CHI; serial-class structural levers exhausted `.9.12`; `.9.10` prose bus-line PARKED behind NLP-SHALLOW-PARSE); **`NLP-SHALLOW-PARSE`** (`.2a`–`.3` all `proposed` — OWNER-GATED on build scope per the tree's Open Question; spike verdict lukewarm = consolidation/robustness not recall-revolution); **`CORPUS-PATTERN-REUSE`** (`.3b.3` activate-only consume — needs a FIRST opt-in extractor, design open; wire-based must stay 100%). Recently-closed: `LLM-PRIMARY-PROMOTION` (`.5` flip), `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE` (NO-GO).
- next_action (PNT): **`MEMORY-BOUNDED-INGEST.5` DONE (measured DEFER) — commit it, then PNT-pick the next leaf.** The clean owner-ungated frontier is now NEARLY EXHAUSTED. Remaining owner-ungated candidate: a descriptive-FIELD-cell gate for the SECOND `.3e`-residual shape — BUT per-item analysis (recorded last session) showed `SANICAP`/`ELEN` cells carry a REAL `shall`-obligation (a `.2` condition-capture concern, NOT a spurious subject → do NOT drop), and the non-"This field" `DDR`/`NVDIMM` `must_be_stable` rows need a NEW structural signal (NO denylist) → probe→likely-no-clean-gate, low confidence. OWNER-GATED (do NOT presume): `NLP-SHALLOW-PARSE` build scope (tree's Open Question); a flip-consistent corpus-wide canonical promotion sweep (multi-doc canonical mutation = review-gated + heavy 14B). **DECISION POINT: after this commit, if the FIELD-cell probe also yields no clean gate, the owner-ungated frontier is exhausted — SURFACE TO OWNER for direction (NLP-SHALLOW-PARSE scope vs promotion sweep vs new corpus work) rather than manufacturing low-value slices.** Fresh `target/release/specforge` built post-`.3e`; `.5`+this commit are docs-only (no rebuild).
- in_flight_uncommitted: `MEMORY-BOUNDED-INGEST.5` docs-only staged-to-commit (MEMORY-BOUNDED-INGEST tree + TASK_TREE index + `docs/knowledge/source-ir-size-scaling.md` + regenerated `KNOWLEDGE_MAP.md` + `docs/book/src/pipeline/sourceir.md` + CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/MEMORY). Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`. blockers: **none**.
