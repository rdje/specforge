# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> The bounded **resume pointer**: it describes *now*, not the journey. Overwrite the "Current state"
> block below; never append session narration. How to resume — bootstrap order, doctrine, gates, and
> retrieval — is `AGENTS.md`; the layer contract is `MEMORY_ARCHITECTURE.md`. Everything else is derived
> on read: revision from `git rev-parse HEAD`, work state from `docs/tasks/`, history from `git log`.

## Current state (OVERWRITE this block each update — do not append)

- Active unit: **`INVARIANT-SHAPE-ADMISSION.2` CLOSED `2026-09-12`** — a table row is a **matrix row whose subject lives in its header**, and
  serialization discarded the header, so these 769 constraints are **unreadable at the level where they are published**. The codebase already says so
  elsewhere (`is_post_passive_binding_only_subject`: *a table row supplies subject context from its other cells*). 396 of 769 carry a modal. The
  readable increment is **17 rows** (`.3`); the rest is a table-semantics programme (`.4`, explicitly not a slice). Open:
  **`INVARIANT-SHAPE-ADMISSION.3`/`.4`**; `PROSE-NAME-CELL-DECLARATION.3` (unblocked); `PRODUCTION-GRAPH-CENSUS-PIN` `.1`/`.2`;
  `SIGNAL-DECLARATION-ROW-DROP` `.2c`(unblocked)/`.2d`; `RETAINED-BUNDLE-POPULATION-FROZEN` `.1`-`.3`; `KG-ISF-COMPLETENESS` beyond `.5`;
  `LIVE-DOCUMENT-PRESSURE-HEADROOM` `.1`/`.3`/`.4d.ii`/`.6`/`.8`-`.13`/`.14b`/`.14c`/`.15`-`.18`; `SOURCE-IR-REPRODUCIBILITY`
  `.3`/`.4`/`.6`/`.7`/`.9a`/`.10`/`.13`; `CLAIM-VERIFICATION-ADOPTION` `.8`/`.9`/`.12`/`.13`; `SCRATCH-RESIDUE-CONTAINMENT.1`/`.4`;
  `STATUS-LEDGER-ROLLOVER.2`; `PROVIDER-MODEL-STORE-LOCALITY.1`; `TASK-PART-SEAL-REACHABILITY.0`; `CHANGES-LEDGER-ROLLOVER.4`; `WIRE-BASED-100`
  `.10d`/`.10f`/`.2`/`.3`/`.5` (SWD `13/29`).
- Current state: **the table rows stay published on purpose and the reason is recorded** — they state real obligations (unlike the captions `.1`
  removed), removing them would be the silent drop `[[ANCHORLESS-INVARIANT-DROP]]` warns about, and they reach no machine consumer today (the ISF
  adapter reports 27 blocked states, 0 emitted files), so they currently serve audit where a serialized row is legible to a person. The
  published-constraint precision defect is therefore **half fixed and half scoped**, not half ignored. **15 registered doctrines**, 13 at gate tier.
  Census 27 measurable / 51 legacy.
- Next action: `INVARIANT-SHAPE-ADMISSION.3` — a signal-description row whose description cell states an obligation about the signal that row declares
  becomes a `signal_constraint`. **17 rows** across APB (7), AXI (6), AHB (4), against 84 existing signal_constraints. The population is small enough to
  adjudicate **in full rather than sample**, and that is the bar for this leaf: read all 17, show each is about the signal its own row declares, and
  show no existing signal_constraint changes. Work in EvidenceIR from `StructuredTableRecord` where the header is still present — not from the
  serialized statement, which is the whole finding of `.2`.
- In-flight uncommitted: none after this commit.
- Blockers: none. Standing hazards: **a serialized row has lost the header that reads it** — do not try to parse a constraint that is already a string;
  go back to `StructuredTableRecord`. **Place a new refusal relative to the routes that already exist** — the caption test needs no second condition
  only because the modal route runs first. **A symptom that sums two populations invites one rule that is wrong for both.** **Look for an existing idiom
  before inventing a rule.** **A phantom declaration widens every gate that asks 'does this statement mention a declared signal?'** **Disprove your own
  tree's premise before building on it.** **A correction upstream can expose a silent drop downstream**; only an item-by-item diff of a rebuild finds
  it. **Measure what a path is WORTH before measuring what a rule costs.** **Ask which producer wrote a persisted field before treating it as a defect**
  (`[[persisted-table-kind-is-a-classifier-generation-artefact]]`, `[[declared-spelling-is-the-document-spelling]]`). **A number that justifies a rule
  must be counted over the rule's whole population.** **A check that prints a number is not a check** (`PRODUCTION-GRAPH-CENSUS-PIN`). **A mirror of a
  producer must move with the producer.** **Do not read a green gold as evidence about a stage it does not score.** A semantic-stage change rebuilds the
  whole proof-carrying corpus with no bundle; only an evidence-stage change needs `generated/preserved/` (`[[retained-chain-rebuild-order]]`). **The
  doctrine driver runs no cargo gate** (`[[doctrine-driver-runs-no-cargo-gate]]`). **Re-anchor a digest-bound region by its TEXT.** Never run the
  fixture suite with the locality gate (`SCRATCH-RESIDUE-CONTAINMENT.4`). A live-surface edit sets off `[[live-surface-edit-bookkeeping-chain]]`; this
  file's cap is 50 lines.
