# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 30 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`3119dbea` = `EXTRACTOR-ARCHITECTURE.2` (framework scaffolding); `.3` (FSM cluster migrated onto the frame, byte-identical, CODE) committing now; ~37 ahead of `origin/main`** (session `2026-06-09`). Push ~every 200 commits or on ask (`feedback_push_cadence`). Last verified full gate: **`scripts/run_ci.sh` GREEN** (fmt + clippy `-D warnings` + lib **1448** + rustdoc + mdBook) + kg-bench 151/151, for `.9.7`. **GOTCHA: per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc — run `scripts/run_ci.sh` (or `cargo doc` warning-deny) before declaring a CODE slice green / before handoff; bare `<X>`/`<NAME>` in doc comments → rustdoc HTML warning, wrap in backticks.** `subs/fsmgen` pinned (`c0b7eaa7`/`d31b0b91`). **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006) — concrete chip names ONLY in gold keys (`test_data/llm_eval/seed_*.json`) + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); every decision preserves this.**
- this_session (`2026-06-09`): executed `.9.7` (the `.9.4`-predicted highest-leverage lever). `.venv-docling` present; evidence rebuild reads persisted `source_ir` (no Docling re-ingest); SWP/CAN/SWD/ADI normalized bundles intact, **APB/AXI normalized bundles were reclaimed** by an earlier `clean --scope source-normalized` → their evidence can't be rebuilt without a `DOCLING_DEVICE=cpu` re-ingest (`project_docling_mps_cpu`). Ollama reachable (`qwen2.5:14b-instruct` + `qwen2.5vl:7b`; `ollama serve` may be needed).
- corpus: **12 git-tracked source PDFs in `corpus/`** — 8 prior (APB/AHB/AXI/AXI-Stream/SWD-ADI/RISC-V Debug/NVMe/I2C) + **4 NEW serial specs (`.9.1`, `2026-06-08`): CAN/SWP/SMBus/I2S** (`bosch/can`, `etsi/swp`, `smbus`, `nxp/i2s`) + `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- prior_work (DONE, pushed): **WIRE-BASED-100** (APB/AHB/AXI 100% on constraints/relations/temporal) + **SWD-SERIAL-EXTRACTION** 100% (frame/operations/FSM). GOTCHA: eval scores PERSISTED evidence — re-ingest before measuring (`eval-scores-persisted-evidence`); `clean --scope source-normalized` breaks evidence rebuilds → re-ingest.
- active_work: **`PDF-VARIANT-DIGESTION.9`** (NEW serial-protocol class — owner-directed `2026-06-08`). `.9.1` import (`c265062b`) + `.9.2`/`3d940f1f` CAN baseline + `.9.3a`/`3d85543c` CAN quoted-mode FSM + `.9.4`/`9942e652` SWP baseline ALL DONE. **`.9.7` (single-word `<NAME> state` FSM grammar) DONE (this slice)** — new additive `extract_transition_bound_states`+`transition_bound_state_names_in`+`is_bare_state_name` (`ir/evidence.rs`): grammar = ALL-CAPS word + `state` in a transition/locative binding (`<trigger> [the] <NAME> state`, enter/into/leave/exit/to/in/from/move/transition/…) + after-guard drops `<X> state machine` + denylist (logic-levels + `UNKNOWN`/`UNPREDICTABLE`) + `.9.3a` recur-≥2/≥2-distinct self-gate (replaces a keyword doc-gate; SWP never says "state machine"); ADR 0006. **Grammar LOCKED by probing all 80 persisted evidence docs BEFORE coding** (capword-variant rejected = 38 docs garbage; transition-bound allcaps clean). **SWP protocol_states 0 → 4** (`DEACTIVATED/ACTIVATED/SUSPENDED/HALT`); **SWD/ADI 13 states intact (eval P=R=F1=1.000), CAN 3 quoted intact, both +0 `named_state_*`**; 18 docs gain a real FSM surface (APB `SETUP`/`ACCESS`, AXI low-power `RUN/STOP/ACTIVATE/DEACTIVATE`, CHI/CXS/DTI/CCIX/CoreSight/eMMC/USB4); 8 tests; `run_ci.sh` green (lib 1448) + kg-bench 151/151; KM `transition-bound-state-fsm` + book. Frontier = **lever backlog (owner may steer): `.9.8` SWP prose signals (`S1/S2/SWIO`) · `.9.3b` CAN frame fields · `.9.9` constraint-acronym precision · `.9.5`/`.9.6` SMBus/I2S baselines.** `.6`/`.7` (older VLM/USB-3.2 leaves) blocked on host-local PDFs.
- **HONESTY GUARDRAIL (owner, non-negotiable): you CANNOT extract what is not there** — read the modality where the fact lives or report an honest RESIDUAL; NEVER fabricate a width/bit-range/field/name. APB/AHB/AXI/SWD stay 100%; genericity (`feedback_genericity_guardrail`), multi-strategy (`feedback_multi_strategy_best_wins`), scoring rigor (`feedback_scoring_rigor`). ISF lowering DEFERRED.
- **NEW active tree `EXTRACTOR-ARCHITECTURE`** (owner devil's-advocate review `2026-06-09`, scoped to the **extractor path**): make the EvidenceIR producer layer a coherent whole, not erratic growth. `.1` audit DONE (typed IR target coherent; producers = ~60 free fns in one ~500-line `EvidenceIr::build()` with hand-rolled per-surface dedup + heterogeneous gates; FSM cluster = 4 siblings + 4 inline dedup loops = the failure mode; provenance 2/10 surfaces; no manifest). owner confirmed (`2026-06-09`) "consolidate, unify, as much as possible" → maximal shape (trait+registry+driver). **`.2` DONE (this slice)** — `ir/extractor.rs`: generic `Extractor<R>` trait (`name`/`tier`/`applies_to`/`run`) + borrowed `ExtractionContext` (carries `statements`; grows one field per migrated cluster) + single `run_surface` driver (gate → run → first-wins dedup by per-surface key → inspectable `SurfaceRun`/`SurfaceManifest`/`ExtractionManifest`); LLM/VLM = same frame; 4 hermetic driver tests; ZERO extractors migrated (no behavior change, additive pub API); `run_ci.sh` green (lib 1452). Refactor not rewrite.
- **`.3` DONE (this slice)** — FSM cluster migrated: 4 `Extractor<ProtocolStateRecord>` units (`fsm.jtag_tap`/`fsm.swd_line`/`fsm.quoted_mode`/`fsm.transition_bound`) run by `protocol_state_surface` via `run_surface`; the 4 inline call-site dedup loops collapsed to ONE call. Grammar bodies unchanged. BYTE-IDENTICAL proven: SWP 4 / CAN 3 / SWD 13 (same ids); SWD eval P=R=F1=1.000; kg-bench 151/151; lib 1454. 2 new wiring tests.
- next_action: **`EXTRACTOR-ARCHITECTURE.4`** — migrate the NEXT cluster one slice each, behavior-preserving + corpus-verified. Recommended order by payoff: signals (table+prose+columnless+actor strategies → the biggest cluster) OR semantic-hints (already a near-dispatcher — easy win) OR registers. Each: wrap the strategy fns as `Extractor<R>` units + a `SurfacePolicy` key, replace inline merge with `run_surface`, prove byte-identical on the corpus (kg-bench + wire-based eval 100%). NOTE: signal/register synthesizers thread a `&mut statement_counter` → extend `ExtractionContext` to carry it (interior-mut or a counter handle) when migrating those. Alternatively owner-steer back to PVD breadth (`.9.8` SWP prose signals). Eventually retire the `build()` god-orchestrator.
- paused_work: none. in_flight_uncommitted: **`EXTRACTOR-ARCHITECTURE.3` staged** (`ir/evidence.rs`: import + 4 FSM `Extractor` units + `protocol_state_surface` + call-site collapse + 2 tests; EA tree `.3` node + CHANGES + LIVE_ACHIEVEMENT_STATUS + this MEMORY) — committing now. Only untracked: `.claude/settings.json` (local harness config, stays). blockers: none. **REPO HANDOFF-READY at this commit** (clean tree, `run_ci.sh` green, kg-bench 151/151). RESUME: read `README.md`+`MEMORY_ARCHITECTURE.md` then this pointer → execute `.4` (next cluster migration) or owner-steer.
