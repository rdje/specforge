# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (memory system), `DOCTRINE_ENFORCEMENT.md` (the 4th
  architecture — doctrines are mechanically gated), and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Before committing run `scripts/check_doctrines.sh` (the registry/driver: memory-arch +
  knowledge-map + task-acceptance); hooks + CI run it too. Retrieval: `KNOWLEDGE_MAP.md`.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`c90eb0d9`** `KG-ISF-COMPLETENESS.2a.v — tally correction (69/70)` (chain: `46fdc60a` Lever C → `d423b872` MEMORY refresh → `c90eb0d9` correction). **55 ahead** of `origin/main` (push at ~200 → HOLD); this `.2a.vi` commit → **56 ahead**. Untracked (stays): `.claude/settings.json`, `.cache/` (gitignored symlink).
- **MODE: PNT loop** (fresh session `2026-06-23`). Bootstrap-read README/AGENTS/MEMORY_ARCHITECTURE/MEMORY/TASK_TREE/COMMIT/SESSION_BOOTSTRAP + KG-ISF-COMPLETENESS/CORPUS-COVERAGE frontiers; doctrines GREEN. PNT this session: **Lever C (`.2a.v`) → tally correction → `.2a.vi`** (the RAM-safe ISF-emit frontier of the first active tree).
- **✅ `KG-ISF-COMPLETENESS.2a.v`+`.2a.vi` DONE (CODE, this session) — the ISF-emit strict-FAIL frontier is now FULLY CLOSED: every renderable doc emits FSMGen-`--strict`-clean (70/70 current emits).** `.2a.v` (Lever C): `drop_unconditional_overlap_conflicts` — an UNCONDITIONAL rule (empty guard ⇒ overlaps every guard, never proven disjoint) conflicting with a guarded different-value rule is residualized (`isf_unconditional_overlap_*`); the same-guard dedup missed it; 6 docs FAIL→PASS (all 3 wire golds + LPI/LTI/NVMe). `.2a.vi`: `drop_unrenderable_rule_values` — a rule whose drive VALUE is free PROSE (AXI+ACE loopback `(RLOOP the value that was presented on the ARLOOP signal)`, not a `(port expr)`) is residualized (`isf_rule_value_*`); closed the last AXI+ACE `(port expr)` FAIL → 70/70. Both: honest residual over fabrication (the `(priority …)` and `(port ARLOOP)` recoveries were both rejected as ungrounded), ADR-0006 structural, no name list. Also cleaned 37 stale cache-cruft `.isf` (older binaries; `adapt` emits ONE primary actor/doc). `run_ci.sh` GREEN (lib **1709**); `kg-bench` 156/156; doctrines GREEN; WIRE-BASED-100 orthogonal by construction (emitter-only). KM `[[isf-unconditional-rule-overlap-conflict]]` + `[[isf-unrenderable-rule-value-residual]]`.
- next_action (PNT): **pick the next lever — the ISF-emit strict-FAIL frontier is CLOSED (70/70), so the remaining north-star work is UPSTREAM (extraction/KG completeness), most needing Docling (RAM-gated).** Candidates: **(a)** `KG-ISF-COMPLETENESS.3` relation-completeness corpus refresh (recover stale-dropped relations canonically — Docling/cascade, RAM-gated); **(b)** `CORPUS-COVERAGE.2` continue the re-ingest sweep (29 docs remain — Docling, RAM-gated); **(c)** the dense-prose agent-precision `.1c.ii` / NLP-shallow-parse upstream (bigger, owner-aligned); **(d)** RAM-safe doc upkeep: `RUST_CODEBASE_ANALYSIS.md` line-count refresh + mdBook SUMMARY nav gaps (`nli-verify`/`doctor`/`audit-extraction`/`eval-extraction`). Note: the upstream extraction-precision question behind `.2a.vi` (a constraint value captured as prose) is an `extract-constraints-llm` lever, recorded as a residual.
- in_flight_uncommitted: the **`KG-ISF-COMPLETENESS.2a.vi`** staged set (crates/specforge/src/ir/isf_ir.rs [CODE +1 test]; docs/tasks/KG-ISF-COMPLETENESS.md + CORPUS-COVERAGE.md; docs/knowledge/isf-unrenderable-rule-value-residual.md + KNOWLEDGE_MAP.md; README/CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/MEMORY; docs/book/src/pipeline/isf-adapter.md). blockers: none. **Repo HANDOFF-READY** after commit.
- **OPERATIONAL REALITY:** host RAM monitor before any Docling/14B work — kill ≥85% used ([[feedback_ram_ceiling_monitor]]); this session RAM-light (read-only probes + `CARGO_BUILD_JOBS=2` builds + run_ci; RAM steady ~82% free). fsmgen is the Perl `subs/fsmgen/bin/fsmgen` (NOT cargo). generated/adapters/isf cleaned to the 70 current primary emits (git-ignored cache; intent_ir unchanged). Artifact-cleanup standing (≥24h) — no stray build/log artifacts.
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication; a missing ISF abstraction → verified FSMGen FR, never an emitter hack ([[feedback_isf_no_hacks]] / [[feedback_verify_fsmgen_before_fr]]). `scripts/check_doctrines.sh` green before committing; `scripts/run_ci.sh` green before committing CODE. Push ~every 200 (HOLD).
