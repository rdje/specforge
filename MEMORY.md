# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`0f329f35`** `ISF-REGISTER-RESET-EMIT.3 — reconcile storage-var width to the true register width (closes tree)`. **A `KG-ISF-COMPLETENESS.2` docs-only commit is being created now** (ISF lowering-fidelity measurement; no code). **171 ahead** of `origin/main` (→ 172 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (fresh session `2026-06-17`; no BWFSC set → open-ended). `ISF-REGISTER-RESET-EMIT` CLOSED (`.0`→`.3` all done — register reset now reaches `.isf` at its true width, north-star bar #6). PNT rolled onto `KG-ISF-COMPLETENESS.2`.
- **`.2` MEASUREMENT DONE `2026-06-17` (read-only, docs-only — THIS commit):** gauged which ISF-fidelity bar dimension has the largest faithful-lowering gap (read the full `from_intent_ir`/`render` lowering + replicated its per-element filters over all 36 IntentIR docs; 2 cross-check agents). **Verdict:** bar #6 (round-trip) is ALREADY honest for grounded wire intent — 22k `behaviors`/`constraints` are free-text twins already lowered typed; the 17 425 typed-rule "drops" are un-groundable free-text (16 179 empty-subject temporal_invariants = ToC headings) / no-consequent prose / non-wire register-field paths (0 undeclared drops on all 4 wire docs) → mass-residualizing = dishonest noise. **Largest TRUE infidelity = signal direction/width** (~98% defaulted `output`/`width-1`, AXI `.isf` 283 output vs 4 input; ignores the actor-relative graph `actor_ports`) — but DESIGN-GATED (relationship-relative + single flat module + the deliberate `R6-ISF-ADAPTER.4` default + an OWNER decision in tension w/ the north star). Spun `.2a` (direction/width fidelity, `deferred`-with-trigger + owner steer) + `.2b` (lowering-coverage visibility gauge, buildable/additive). Report `docs/research/isf-lowering-fidelity-measurement.md`; KM `[[isf-lowering-fidelity-gauge]]`. WIRE-BASED-100 untouched.
- next_action: **CONTINUE PNT → build `KG-ISF-COMPLETENESS.2b`** (ISF lowering-coverage visibility gauge) measurement-first: confirm the count/category scope (count only would-be-lowerable intent, NOT the ungroundable-noise/register-field categories `.2` showed are honest absences), then ship as adapter metadata + a `validate <intent-ir>` surface mirroring `storage_reset_residual_packet` — purely ADDITIVE (emitted `.isf` byte-identical → WIRE-BASED-100 trivially held), ADR-0006 (universal counts). `.2a` stays deferred (needs an FSMGen-contract check: does `--strict --check` use signal direction? accept symbolic `(width PARAM)`? + a reference-boundary design + owner steer). Alternatives if `.2b` turns out thin: the deferred north-star sub-leaves (`KG-ISF-TRANSACTIONS` body-emission FSMGen-parked; `KG-ISF-COMPLETENESS.1b.ii`/`.1b.iv`).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** elevated to critical-path; `KG-ISF-TRANSACTIONS.2a→.2j` done (body-emission FSMGen-parked).
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes (`[[feedback_ram_ceiling_monitor]]` / `[[project_big_pdf_memory_bounded_ingest]]`). This slice was RAM-safe (read-only python, no 14B/Docling). Push ~every 200 commits or on ask (171 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **a `KG-ISF-COMPLETENESS.2` docs-only commit is being created now** — `docs/research/isf-lowering-fidelity-measurement.md` + `docs/knowledge/isf-lowering-fidelity-gauge.md` + regenerated `KNOWLEDGE_MAP.md` + `docs/tasks/KG-ISF-COMPLETENESS.md` (`.2` done, `.2a`/`.2b` spun) + `docs/TASK_TREE.md` (frontier) + `CHANGES.md` + `DEVELOPMENT_NOTES.md` + `LIVE_ACHIEVEMENT_STATUS.md` + this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt` (cleared post-commit). blockers: **none**.
