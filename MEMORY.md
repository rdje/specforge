# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`a90d24e7` = `CANONICAL-PROMOTION-SWEEP.1` (non-wire HBM2 canonical-promotion pilot; docs-only — canonical mutation is in git-ignored `generated/`)**; **132 ahead of `origin/main`** (push at ~200; session `2026-06-15`, dedicated session — owner confirmed "start the pilot", then chose **"stop here — handoff-ready"** after `.1`). **THIS-SLICE (`.1`, docs-only):** ran the **no-re-ingest equivalent** of converge's post-stability promotion on the intact canonical HBM2 EvidenceIR (converge always re-ingests → forbidden by the frontier): BEFORE `nli-verify` → `extract-constraints-llm` (promote-in-place) → deterministic `semantic`/`intent`/`adapt` rebuild → AFTER `nli-verify`. Gauge **85.7% → 40.0% not-entailed (2E/12N → 12E/8N), records 14 → 20 — reproduces the `LLM-PRIMARY-PROMOTION.4` /tmp datum EXACTLY on canonical** (oracle). RAM **≥42% free** across all three 14B steps (3s watchdog, never ≤15% free / 90→93% zone); `kg-bench` 156/156; `promotion_status = not_promoted_review_required` (`*.prepromote.bak` backups retained). **Repeatable per-doc protocol locked into the tree Decisions + KM card `canonical-promotion-no-reingest-protocol`.** (NOTE: the prior MEMORY pointer was stale at `c2bfa2a6`; real HEAD was `477b4825` — corrected.)
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **3 OWNER-DIRECTED FORWARD LEVERS:** (1) **`NLP-SHALLOW-PARSE` — BUILD frontier MEASURED-EXHAUSTED `2026-06-15` (STANDING, build-exhausted):** both `.2h` (direction) and `.2f` (coordination) resolved measured-NO-GO (already in the production hand grammar); `.3` assembler = consolidation-only → `deferred`; re-open only on a future measured prose gap or an explicit owner consolidation directive. The bigger "digest any PDF" levers stay TABLES + VLM (per the spike). (2) **`CANONICAL-PROMOTION-SWEEP`** (NEW tree) — **`.1` DONE `2026-06-15`** (non-wire HBM2 pilot landed canonical 85.7%→40.0%, RAM-safe, protocol locked); **frontier now `.2`** (wire docs APB/AHB/AXI/SWD under the full `WIRE-BASED-100` gold battery on canonical, revert-on-regress — the `.3` AXI precedent) **and `.3`** (non-wire scale-out, one doc at a time, host-local-source docs skipped). Both `.2`/`.3` are RAM-HEAVY 14B → dedicated-session discipline (watchdog, `ollama stop` before any ingest, ≥85%-used kill); canonical mutation review-gated. (3) **`CORPUS-PATTERN-REUSE`** — **`.3b.3a` + `.3b.3a2` DONE `2026-06-15` (read-only, measured NO-GO for ANY first opt-in extractor) → consume side `.3b.3` = measured-STANDING (built-deferred).** Learn side complete; structurally-safe extractors self-gate/default-on (the activate-only niche is empty here), so `.3b.3b` deferred with a precise re-open trigger; only `.4` (offline miner, RAM-heavy, gated) remains buildable. Like NLP, this lever is now standing. ALSO open/standing: **`EXTRACTION-QUALITY-GAUGE`** (`.3` COMPLETE `.3a`–`.3e`; FIELD-cell gate #2 = confirmed NO-GO, `.3e` already closed the class, residual needs a forbidden denylist); **`MEMORY-BOUNDED-INGEST`** (Mostly Done/standing, measured-exhausted); **`PDF-VARIANT-DIGESTION`** (`.13` complete; `.9.10` parked → now the CORPUS-PATTERN-REUSE opt-in candidate). Recently-closed: `LLM-PRIMARY-PROMOTION`, `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE`.
- next_action (**owner DECISION GATE — was PNT-PAUSED; this dedicated session executed `.1` only**): `.1` is DONE. The remaining `CANONICAL-PROMOTION-SWEEP` work is `.2` (wire docs — HIGHEST risk: needs the full `WIRE-BASED-100` gold battery re-verified 1.000 on the promoted canonical artifact, revert-on-regress) and `.3` (non-wire scale-out across the in-scope corpus). Both are RAM-heavy live-14B + canonical-mutation work. The owner confirmed only "start the pilot" (`.1`) — so **do NOT auto-continue into `.2`/`.3`; ask the owner first** whether to proceed (and whether in this session or a fresh one). **When picked up, follow the LOCKED protocol** (tree Decisions / KM `canonical-promotion-no-reingest-protocol`): `ollama stop` → ≥40% RAM free → `cargo build --release` → backup `*.prepromote.bak` → BEFORE `nli-verify` → `extract-constraints-llm` (promote, no re-ingest) → `semantic`/`intent`/`adapt` rebuild → AFTER `nli-verify` → `kg-bench` + (wire only) full gold battery → 3s RAM watchdog (kill ≤15% free) → `ollama stop`. **`scripts/run_ci.sh` before declaring any CODE slice green.** Fresh `target/release/specforge` is current; `.1` was docs-only (no rebuild).
- in_flight_uncommitted: **none** — `.1` committed (`a90d24e7`), working tree clean. The canonical HBM2 promotion is in git-ignored `generated/` (NOT committed); `*.prepromote.bak` backups retained there for revert (review-gated). Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`/`generated/`. NOT pushed (owner didn't ask; < 200 threshold). blockers: **none**. **Repo is HANDOFF-READY.**
