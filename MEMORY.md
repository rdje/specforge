# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (memory system), `DOCTRINE_ENFORCEMENT.md` (the 4th
  architecture — doctrines are mechanically gated), and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Before committing run `scripts/check_doctrines.sh` (the registry/driver: memory-arch +
  knowledge-map + task-acceptance); hooks + CI run it too. Retrieval: `KNOWLEDGE_MAP.md`.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`1abfb49c`** `PDF-VARIANT-DIGESTION.10i — block-qualify the genuinely-different register class (the .10h residual)`. **✅ PUSHED `2026-07-25`** (owner directive; `2544d3e8..1abfb49c`, 75 commits) → **0 ahead** of `origin/main`; next push at ~200. Untracked (stays): `.claude/settings.json`, `.cache/` (gitignored symlink).
- **MODE: PNT loop, PAUSED at owner request `2026-07-25`** (fresh conversation re-ran the full README bootstrap: README/AGENTS/MEMORY_ARCHITECTURE/DOCTRINE_ENFORCEMENT/ROADMAP/TASK_TREE/COMMIT + book architecture chapters + codebase entrypoints; doctrines GREEN 3/3). This session: ramp-up → artifact sweep → push. Last CODE slice (DONE `2026-06-24`): **`PDF-VARIANT-DIGESTION.10i`**.
- **✅ `PDF-VARIANT-DIGESTION.10i` CODE LANDED (`2026-06-24`).** `extract_section_header_registers` (`ir/evidence.rs`): when `.10h` field-set containment returns `None` (genuinely-different registers sharing a mnemonic), each occurrence is now BLOCK-QUALIFIED via its parent SECTION TITLE — new pure helpers `section_dotted_number`/`dotted_parent`/`derive_register_block_name` + a `by_number` (dotted-num→title) map + an additive `SectionHeaderFieldContainer.dotted` field; a parent of form `<NUM> <BLOCK> register descriptions` yields the block (`C2.6 MEM-AP register descriptions`→`MEM-AP`), and each block-named occurrence emits as `<NAME>@<BLOCK>` (no-block parent → honest residual; ≥2 still-disjoint sharing a block → re-run `.10h` containment, else residual). Probe-measured: genuinely-different class in EXACTLY 1 doc (ARM-Debug `ihi0074`). **Verified:** ARM-Debug section-heading regs 15→20 / 69→93 fields (+`CSW@MEM-AP`{11}/`CSW@JTAG-AP`{7}/`CLAIMSET@AP`/`@MEM-AP`/`@JTAG-AP`{2}); full `evidence` register_records 40→45; CoreSight `ihi0029` 6/29 unchanged (its `AUTHSTATUS` is `.10h`-collapsible). `git stash` baseline diff over 8 `.10h` golds + ihi0029 BYTE-IDENTICAL; only ihi0074 ADDs 5 with ZERO removals (all 40 baseline preserved). The 5 reach `debugger.isf` as distinct sanitized vars (`csw_mem_ap`/`csw_jtag_ap`/`claimset_*`); FSMGen `--strict --check --json` 0 diagnostics/0 errors. `kg-bench` 156/156; `run_ci.sh` GREEN (lib **1724**, +3 tests). ADR-0006 (universal `<NUM> <BLOCK> register descriptions` grammar, no name list). Narrowed residual: the no-block `D4.5` CLAIMSET (no clean qualifier).
- next_action (PNT): **continue PNT — pick the next surfaced extraction-precision lever** per `[[feedback_not_complete_attack_substantive_gaps]]`, measurement-first. Candidates: **CORPUS-HARDENING** CHI Class-A (4 protocol matrices mis-typed `timing_parameter`, empty `body_rows`); **EXTRACTION-QUALITY-GAUGE** descriptive-FIELD-cell residual (`.3e`/`.3g` already shipped most — re-measure first); **CORPUS-COVERAGE.2** RAM-guarded per-doc re-ingest of the genuinely pre-`.10` stale docs (SMMU/GIC/MMU — Docling-heavy, RAM-permitting). The `.10` section-heading register family is now exhausted (`.10f`/`.10g`/`.10h`/`.10i` done; only the no-block `D4.5` CLAIMSET residual remains, no clean qualifier). KG-ISF-COMPLETENESS frontier exhausted (`.1c.ii` NLP-gated, `.5` built); KG-ISF-TRANSACTIONS FSMGen-gated.
- in_flight_uncommitted: **none** once this resume-pointer refresh commits. blockers: none. **Repo HANDOFF-READY** (tree clean; only the untracked `.claude/settings.json` remains, by design).
- **OPERATIONAL REALITY:** ⚠️ **`target/` IS ABSENT** (reclaimed by the `2026-07-05` sweep) — the next `cargo` command does a FULL cold rebuild (~minutes); there is no `.10i` binary on disk, so re-verify by rebuilding before trusting any "live" measurement. Artifact sweep `2026-07-25`: only 4 stale `.DS_Store` were reclaimable (target/, `normalized/` bundles, `*.prepromote.bak` already gone); `generated/` = 699 M of LIVE IR (evidence 279 M / intent 167 M / source 127 M / semantic 115 M) — KEEP; `.venv-docling` 1.3 G KEEP (re-ingest). Generated state: ARM-Debug `ihi0074` evidence→semantic→intent→`debugger.isf` carries `.10i` (40→45 register_records, FSMGen strict-clean). fsmgen = Perl `subs/fsmgen/bin/fsmgen` (NOT cargo); `--check` needs `--json`. lib test count **1724** (RUST_CODEBASE_ANALYSIS.md count is AUDIT-DOC-RECONCILE-owned).
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication; a missing ISF abstraction → verified FSMGen FR, never an emitter hack ([[feedback_isf_no_hacks]] / [[feedback_verify_fsmgen_before_fr]]). `scripts/check_doctrines.sh` green before committing; `scripts/run_ci.sh` green before committing CODE. Push ~every 200 (HOLD).
