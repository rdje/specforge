# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`cd6acdb0`** `PDF-VARIANT-DIGESTION.10f — section-heading prose message-field recognizer (DTI-class message protocols) (CODE)`. **A `.10g` CODE commit is being created now.** **188 ahead** of `origin/main` (→ 189 after). Tree clean except untracked `.claude/settings.json` (+ the transient `git_message_brief.txt`).
- **MODE: PNT loop** (session `2026-06-17`; open-ended). **Owner: SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT through SUBSTANTIVE north-star gaps:** #1 relation-incompleteness (DONE `KG-ISF-COMPLETENESS.3`), #2 corpus coverage (DONE `CORPUS-COVERAGE.0`+`.1`; re-ingest of normalized-missing docs BLOCKED — source PDFs host-local), #3 transactions (`.2a`–`.2m` DONE), #4 message/register section-heading recognition (`PDF-VARIANT-DIGESTION.10f` message + `.10g` register BOTH DONE).
- **`PDF-VARIANT-DIGESTION.10g` DONE `2026-06-17` (this session — CODE): section-HEADING REGISTER-field recognizer — the register-routed twin of `.10f`.** The SAME `<NAME>, bits [hi:lo]` section-heading layout DTI uses for MESSAGE fields is how ARM ARCHITECTURE specs lay out REGISTER fields (GIC `ihi0069`, SMMU `ihi0070`, CoreSight `ihi0029`, ACC `ihi0076`, ARM-Debug-v6 `ihi0074`): each register a dotted-numbered container (`B2.2.1 ABORT, Abort register`, `6.3.1 SMMU_IDR0`) + `Field descriptions` anchor + one heading per field. **No-drift:** factored `.10f`'s walk into ONE shared `scan_section_header_field_containers` + `distinct_section_header_fields`; `.10f` keeps non-register containers (byte-identical, proven), `extract_section_header_registers` keeps register ones → new strategy `registers.section_header_field` 4th in `register_record_surface` (`run_surface_concat`); access/reset/offset honestly absent. **Decisive precision lever — per-document name-uniqueness residual gate:** a short mnemonic reused across access-port blocks (ARM-Debug `AUTHSTATUS`/`CSW`/`IDR`/`CLAIMSET`/`DEVARCH`; CoreSight `AUTHSTATUS`) is structurally ambiguous (occurrences are a MIX of identical cross-refs, subset views, GENUINELY DIFFERENT registers — `CSW` MEM-AP vs JTAG-AP disjoint) → held as an honest residual, never over-counted nor conflated by the all-distinct fragment merge (ADR-0006, no name list). A unique name matching an existing 0-field record (e.g. `DPIDR`) MERGES via `consolidate_register_field_fragments` (no double-count; `.10g` runs last → existing identity kept). **Measured: 180 registers / 934 fields across EXACTLY 5 docs** (GIC 73/468, SMMU 88/381, CoreSight 5/24, ACC 2/4, ARM-Debug 12/57); ARM-Debug `evidence` 29→37 (+8 new, `DPIDR` enriched, zero dup names); DTI 0 registers + 159 `.10f` msg fields unchanged. Gates: `git stash` diff over 8 golds register+message byte-identical except the new manifest entry (produced 0) → WIRE-BASED-100 + register golds orthogonal; `kg-bench` 156/156; `run_ci.sh` GREEN (lib 1672→1677, +5). KM `[[section-header-register-field-extraction]]`.
- next_action (**PNT; RAM-SAFE default — no VLM**; own a leaf BEFORE any code; measurement-first; ADR-0006; WIRE-BASED-100 + register/wire golds + `kg-bench` hard gates). **Frontier triaged `2026-06-17` (read-only):** **(c) the transaction ordered multi-phase BODY** over the `.2c`/`.2m` membership is the substantive buildable critical-path lever (owner TRANSACTION directive) — START HERE with a fresh probe-first cycle on the transaction code (`synthesize_transactions`/`mint_named_transaction`/`build_transaction_anchors` in `ir/semantic.rs`; `.2g`/`.2h`/`.2i` phase surfaces). **BLOCKED/low-value (do NOT spend on these):** (b) DTI signal-inventory prose noise is BLOCKED — DTI's normalized markdown is host-local-absent so `evidence` cannot rebuild it (persisted DTI evidence has 0 declared signals + the 30 leaked field-obligation `signal_constraints` that FIELD.4 re-routes on the next live-NLP DTI rebuild, also host-local-blocked); (a) the `.10g` block-qualified reused-mnemonic recovery (`AUTHSTATUS`/`CSW`/`IDR`) is LOW-VALUE (~16 containers) / HIGH fabrication-risk → leave as the documented honest residual; (d) ATP `ihi0082` VLM-fragment needs VLM (RAM-heavy). Do NOT treat "lever done" as "complete" (`[[feedback_not_complete_attack_substantive_gaps]]`).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** is critical-path.
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B/7B models (`ollama stop` before/after). `.10f` was RAM-safe (cargo + dry-runs over persisted artifacts, no VLM/Docling). `[[feedback_ram_ceiling_monitor]]`. Push ~every 200 commits or on ask (**187 ahead → HOLD**, < 200; → 188 after this commit).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path); for non-destructive measurement use `<stage> --dry-run` (prints JSON after a `command:`/`mode:`/`artifact_root:` preamble — slice from the first `{`) or build into a TEMP evidence-root. **STALE-PERSISTED GOTCHA:** `generated/` artifacts can be stale (built by an older binary) — rebuild with the current binary before measuring; isolate a change's effect with `git stash`. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **only this `.10g` CODE commit** — `crates/specforge/src/ir/evidence.rs` (shared `scan_section_header_field_containers` + `distinct_section_header_fields`, `.10f` refactored to consume them, `extract_section_header_registers` + `SectionHeaderRegisterExtractor` + 4th register-strategy registration + `type SectionHeaderRegisterCandidate` alias + 5 hermetic tests + 1 `#[ignore]` sweep), task tree `docs/tasks/PDF-VARIANT-DIGESTION.md` (`.10g` leaf done + sub-leaf list) + `docs/TASK_TREE.md` row, new KM card `docs/knowledge/section-header-register-field-extraction.md` + regenerated `KNOWLEDGE_MAP.md`, README/RUST_CODEBASE_ANALYSIS/CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS + book `docs/book/src/pipeline/evidenceir.md`, this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY.**
