# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: baseline **`6e9c18fb` = `LLM-PRIMARY-PROMOTION.5` handoff (record owner FLIP authorization)**; **now committing `LLM-PRIMARY-PROMOTION.5` (the FLIP EXECUTION)**; ~118→119 ahead of `origin/main` (session `2026-06-14`, PNT loop owner-authorized unbounded). **THIS-SESSION (`LLM-PRIMARY-PROMOTION.5`, CODE — tree CLOSED):** executed the owner-authorized default flip the `.4` packet handed to a fresh session. `converge` now PROMOTES the LLM-primary constraint surface BY DEFAULT for live-NLP runs — new `should_promote_constraints(args)` gate (`nlp_provider != skip && !no_promote_constraints_llm`); new `--no-promote-constraints-llm` opt-out (`conflicts_with` the old on-flag); the retained `--promote-constraints-llm` is redundant-but-accepted + still errors with `--nlp-provider skip`; **provider-free runs never promote by construction** (CI/kg-bench/provider-free converge byte-identical). +3 unit tests; lib 1611→**1614**; full `run_ci.sh` GREEN. Gold gates RE-VERIFIED GREEN on CANONICAL artifacts: wire constraints **1.000 ×3** + doc recall **16/16**; WIRE-BASED-100 relations + temporal **3/3+4/4+3/3**=1.000; serial no-regression (SWD/SWD-deriv/I2C) 1.000; **kg-bench 156/156**; provider-free converge byte-stable (no `constraint_promotion:` line). LIVE proof: flagless `converge` APB w/ live qwen2.5:14b fired promotion automatically (26→22 kept) and the freshly default-promoted canonical APB re-gated 1.000 (constraints + temporal). RAM safe (≥42% free at the 14B peak). R1–R4 recall levers stay future candidates.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **`MEMORY-BOUNDED-INGEST`** (RAM `.1`/`.2` + DISK `.3` + RAM guard `.4a` + disk pre-flight `.4b` + adaptive batch `.4c` DONE; frontier `.5` summary streaming — only if warranted, `.4d`/`.3b` deferred/YAGNI); **`PDF-VARIANT-DIGESTION`** (`.13c` CHI gauge = the `.13` sweep's last leaf, HEAVY 14B — `ollama stop` first, RAM-serialize, CHI normalized bundle EXISTS so evidence rebuilds but the gauge needs the 14B; serial-class structural levers exhausted `.9.12`; `.9.10` prose bus-line PARKED behind NLP-SHALLOW-PARSE); **`NLP-SHALLOW-PARSE`** (`.2a` tokenizer leaf — go/no-go; spike verdict lukewarm = consolidation/robustness not recall-revolution); **`CORPUS-PATTERN-REUSE`** (`.3b.3` activate-only consume — needs a FIRST opt-in extractor, design open; wire-based must stay 100%). CLOSED this session: `LLM-PRIMARY-PROMOTION` (`.5` flip). Other recently-closed: `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE` (NO-GO).
- next_action (PNT): **`LLM-PRIMARY-PROMOTION.5` DONE — commit it, then PNT-pick the next leaf.** Candidate frontier (owner ungated `2026-06-14` "do each in turn"): (1) `PDF-VARIANT-DIGESTION.13c` CHI gauge (HEAVY 14B — `ollama stop`, RAM-serialize; fresh `target/release/specforge` built `2026-06-14 22:29`; unblocks `EXTRACTION-QUALITY-GAUGE.3c`); (2) `NLP-SHALLOW-PARSE.2a` tokenizer (pure Rust, go/no-go); (3) `MEMORY-BOUNDED-INGEST.5` summary streaming (verify warranted first — not yet at measured page counts); (4) ROADMAP lane-hierarchy reconcile (docs-only; the `BOOK-COMMAND-COVERAGE.1` spun-out follow-up — recent applied trees absent from R-lanes, R15e understated; verify criteria before any status change).
- in_flight_uncommitted: `LLM-PRIMARY-PROMOTION.5` code+docs staged-to-commit (cli.rs, converge.rs, README, book pipeline/quality-and-learning, task tree + index, CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS). Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`. blockers: **none**.
