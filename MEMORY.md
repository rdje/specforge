# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`3d7dba01`** `KG-ISF-COMPLETENESS.2a.i — signal width fidelity`. **A `KG-ISF-COMPLETENESS.2b` docs-only commit is being created now** (`.2b` measured-marginal → deferred; frontier → `.1b.iv`). **173 ahead** of `origin/main` (→ 174 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (fresh session `2026-06-17`; no BWFSC set → open-ended). `ISF-REGISTER-RESET-EMIT` CLOSED. PNT slices this session: `.2` measurement (`f1a507dd`) → `.2a` FSMGen-contract trigger + `.2a.i` width build (`3d7dba01`) → `.2b` measured-marginal (THIS commit).
- **`.2` MEASUREMENT DONE `2026-06-17`:** gauged the largest ISF faithful-lowering gap. Bar #6 (round-trip) is ALREADY honest for grounded wire intent (22k `behaviors`/`constraints` = free-text twins already lowered typed; the 17 425 typed-rule "drops" are un-groundable free-text/ToC-heading invariants/no-consequent prose/non-wire register-field paths, 0 undeclared on wire docs → residualizing = dishonest noise). Report `docs/research/isf-lowering-fidelity-measurement.md`; KM `[[isf-lowering-fidelity-gauge]]`.
- **`.2a` FSMGen-contract TRIGGER DONE + `.2a.i` width build DONE `2026-06-17` (CODE — THIS commit):** empirical binary probe + book contract (KM `[[fsmgen-ignores-signal-direction]]`) → **direction is FSMGen-NEUTRAL** (flipping a driven output→input still passes `--strict`; `(set port expr)` has no direction constraint) → direction stays deferred (owner-philosophy only, NO technical motivation — the alarming 98% output default is FSMGen-irrelevant). **Width** must resolve to a positive integer → **`.2a.i`** emits the grounded concrete width: `actor_port_concrete_widths` (`ir/isf_ir.rs`) recovers each signal's single unambiguous concrete `actor_ports[].width_hint` (69 signals corpus-wide / 0 conflicts) and the signal lowering prefers it over the width-1 default; never overrides a real >1 hint, never guesses; ADR-0006, +1 test. Live: AXI `.isf` 32 + AHB 2 signals gain real widths (`ARSIZE→3`/`HSIZE→3`/…), non-signal lines 0, APB/SWD byte-identical, **0 NEW FSMGen `--strict` diagnostics** (AXI/AHB keep byte-identical PRE-EXISTING rule diags `constraint_33 (port expr)`/`HAUSER` conflict). WIRE-BASED-100 structurally unaffected (emitter-only, downstream of extraction); `run_ci.sh` GREEN (lib **1652**, +1).
- next_action: **CONTINUE PNT → `KG-ISF-COMPLETENESS.1b.iv`** (Class-C zero-evidence actor drop) — the substantive remaining buildable lever after `.2b` measured-MARGINAL (noise-dominated gauge — not building) and `.2a` direction deferred (FSMGen-neutral, owner-philosophy). Measured `2026-06-17`: zero-evidence (0 ports AND 0 relations) actors = **34% of all actors corpus-wide** (331/969; AXI 12/24, AHB 7/25, SWD 8/23 on the pre-`.1a` persisted corpus). `.1b.iv` is measurement-first + GATE-RISKY: design the PURE-INFERRED-vs-PROSE-GROUNDED provenance discriminator on **freshly-rebuilt post-`.1a`/`.1b` wire evidence** (NOT the persisted pre-`.1a` corpus — WRITE-PATH GOTCHA: build into a temp evidence-root), so a blanket 0/0 drop doesn't delete agents the owner's completeness north star wants kept; WIRE-BASED-100 a hard gate. Best done with fresh-session sharpness. Other deferred levers: `.1b.ii` (`"X interface"→"X"`, named-block conflation risk); `KG-ISF-TRANSACTIONS` body-emission (FSMGen-parked).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** elevated to critical-path; `KG-ISF-TRANSACTIONS.2a→.2j` done (body-emission FSMGen-parked).
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes (`[[feedback_ram_ceiling_monitor]]` / `[[project_big_pdf_memory_bounded_ingest]]`). This slice was RAM-safe (read-only python, no 14B/Docling). Push ~every 200 commits or on ask (171 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **a `KG-ISF-COMPLETENESS.2b` docs-only commit is being created now** — `docs/tasks/KG-ISF-COMPLETENESS.md` (`.2b`→deferred-marginal, frontier `.1b.iv`) + `docs/TASK_TREE.md` (frontier) + `CHANGES.md` + `LIVE_ACHIEVEMENT_STATUS.md` + this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt` (cleared post-commit). blockers: **none**. **Repo is HANDOFF-READY** — clean checkpoint; `.1b.iv` (gate-risky agent-surface design) is the next slice, best with fresh-session sharpness.
