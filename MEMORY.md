# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (memory system), `DOCTRINE_ENFORCEMENT.md` (the 4th
  architecture — doctrines are mechanically gated), and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Before committing run `scripts/check_doctrines.sh` (the registry/driver: memory-arch +
  knowledge-map + task-acceptance); hooks + CI run it too. Retrieval: `KNOWLEDGE_MAP.md`.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`833f23bb`** `CORPUS-COVERAGE.2 — re-ingest #26 Intel VT-d 5.0 …`. **44 ahead** of `origin/main` (push at ~200 → HOLD). This `CORPUS-COVERAGE.2 #27` commit → **45 ahead**. Untracked (stays): `.claude/settings.json`, `.cache/` (gitignored symlink).
- **MODE: PNT loop** (fresh session `2026-06-23`, separate from the #22–#25 session earlier today). Active tree: **`CORPUS-COVERAGE.2`** (host-local re-ingest of the 57 normalized-missing docs; chipdoc reachable via the git-ignored `.cache/local-references/chipdoc` symlink → `/Users/.../livework/chipdoc`, 169 PDFs). This session bootstrap-read README/AGENTS/MEMORY/CORPUS-COVERAGE/COMMIT, survey-verified ROADMAP (active-frontier pointer current — no real drift) + mdBook (in-sync; only the in-memory-structure count needed a measured refresh), then PNT-picked the re-ingest frontier. **Done this session: #26 Intel VT-d (`833f23bb`), #27 JEDEC eMMC 5.0 (committing now).**
- **🚧 `CORPUS-COVERAGE.2` re-ingest #27 DONE (committing now) — JEDEC eMMC 5.0** (`jesd84_b50_2013_09_emmc_5_0`, 296pp / 344 visual; first descriptive-prose register/protocol spec). Docling CPU (0 residuals; RAM 80–82% free; ~5 min) → deterministic cascade. **MIXED refresh:** GENUINE wins = **transactions 0→6** [boot/alt-boot/lock-unlock/DDR-mode/background/HW-reset — 5 grounded, all real eMMC ops], register_records 17 held, interfaces 9→38, 0 message-fields (honest), bundle restored; `host.isf` renderable (62 signals / 17 storage / 28 enums / 16 rules); **`fsmgen --strict` success / 0 diagnostics**; `validate` no stage-staleness (349 non-zero), 51/100 ADEQUATE. **REGRESSION (honest): actors 20→153 / relations 23→349** — fresh Docling of descriptive prose mints ~90+ phantom sentence-fragment actors (`host has`/`host to`/`cache in`/`B write`) the AMBA-tuned `.1a`/`.1b` gates don't catch; phantoms never reach `.isf` (emitter lowers only the renderable actor) → KG-fidelity gap (bar #1), NOT a strict-FAIL → **NEW spun-out lever E**. **Coverage 27 of 57. 25/27 strict-clean** (2 FAIL = DTI Lever A, LPI Lever C; #27 clean).
- **PHASE FINDING (refined #27):** the descriptive-prose doc-class behaves OPPOSITE to the structured AMBA/CoreSight class — fresh re-ingest WORSENS the actor surface (more fragment candidates) rather than refining it. So the tail splits THREE ways: pure confirmation (#21/#22), consolidation/recognition refreshes (#17/#23/#24/#25), and now **prose docs (#27) with a genuine transaction win + an actor-precision regression**. #26 VT-d separately showed even a register-current doc can gain message-fields+transactions (register-count parity ≠ full-surface parity). Marquee table-family jumps remain the exception.
- next_action (PNT): **continue `CORPUS-COVERAGE.2`** — next re-ingest slice (30 remain): JEDEC HBM (`jesd235`/`jesd235a` — DRAM register specs, expect structured/cleaner), RISC-V AIA (`1_0_2025_03_12_…advanced_interrupt`), USB4/USB3.2 (prose — expect lever-E phantom recurrence), then the thin OpenCAPI×13 PHY/mechanical/TL tail (skip `readme` — non-spec). Check RAM first; one doc per slice; commit per COMMIT.md. **STRONGLY RECOMMENDED substantive pivot (owner "attack substantive gaps" directive):** **Lever E — agent-identity precision for descriptive-prose / non-AMBA specs** (NEW, surfaced #27 eMMC: 20→153 phantom actors; bar #1 KG-fidelity). Probe-first: measure phantom-actor counts corpus-wide, then design STRUCTURAL gates (extend `.1b.i` trailing-strip to closed-class auxiliaries/prepositions `host has`/`host to`→`host`; participation/port-floor for prose actors) — NOT a fragment denylist ([[feedback_avoid_denylists_prefer_structural]]); needs its own owned `KG-ISF-COMPLETENESS` leaf + full `run_ci.sh`/`kg-bench`/WIRE-BASED-100 gating. Also open: **Lever D** (non-AMBA table/signal recall, #18/#19/#21/#26). `KG-ISF-COMPLETENESS`/`KG-ISF-TRANSACTIONS` ISF-emitter levers FSMGen-parked.
- in_flight_uncommitted: the **`CORPUS-COVERAGE.2 #27`** staged set (docs/tasks/CORPUS-COVERAGE.md, CHANGES.md, DEVELOPMENT_NOTES.md, LIVE_ACHIEVEMENT_STATUS.md, MEMORY.md — NO book change this slice, eMMC adds 0 message-fields). blockers: none. **Repo HANDOFF-READY** after commit. (generated/ is git-ignored — the `.2` log table is the durable trace.)
- **OPERATIONAL REALITY:** host RAM monitor before any Docling/14B work — kill ≥85% used ([[feedback_ram_ceiling_monitor]]). This session ran TWO CPU Docling re-ingests (#26 VT-d, #27 eMMC; RAM steady 80–83% free, no abort); no Rust build (binary current at HEAD). NOTE: fsmgen is the Perl `subs/fsmgen/bin/fsmgen` (NOT cargo — a stray `cargo build` errors with "no bin target fsmgen"). Artifact-cleanup directive standing (≥24h) — re-ingest restored VT-d + eMMC `normalized/` bundles (intended); no stray build artifacts.
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication; a missing ISF abstraction → verified FSMGen FR, never an emitter hack ([[feedback_isf_no_hacks]] / [[feedback_verify_fsmgen_before_fr]]). `scripts/check_doctrines.sh` green before committing; `scripts/run_ci.sh` green before committing CODE. Push ~every 200 (HOLD).
