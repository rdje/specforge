# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`58341725`** `KG-ISF-TRANSACTIONS.2k — descendant-subsection scope for transaction signal-set membership`. **A MEMORY handoff-refresh commit is being created now** (fresh-session pointer to the owner-chosen VLM-tier lever). **181 ahead** of `origin/main` (→ 182 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (session `2026-06-17`; open-ended). **Owner: SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT through SUBSTANTIVE north-star gaps to exhaustion:** #1 relation-incompleteness (DONE `KG-ISF-COMPLETENESS.3`), #2 corpus coverage (DONE `CORPUS-COVERAGE.0`+`.1`), #3 transactions (body-emission FSMGen-answered-as-metadata `.2a`–`.2j`; **membership-breadth `.2k` DONE `58341725`**). Recent slices: `CORPUS-COVERAGE.1` (`c62de501`) → **`KG-ISF-TRANSACTIONS.2k` descendant-subsection membership (`58341725`, CODE)**. Owner chose a FRESH SESSION for the next (VLM-tier) slice — see next_action.
- **`KG-ISF-TRANSACTIONS.2k` DONE `2026-06-17` (CODE, commit `58341725`):** a named transaction's `.2c` signal-set membership now spans its section's DESCENDANT SUBSECTIONS. Section anchors are line-range/non-overlapping → a transaction named from a parent heading (APB `3.1 Write transfers`) saw only its own intro statements ({PCLK}); the signal-rich prose lives under `3.1.1`/`3.1.2`. `build_transaction_anchors` (`ir/semantic.rs`) now unions in every section whose dotted number is a strict descendant of its own (helpers `leading_section_number` + `is_descendant_section_number`; ADR-0006 grammar, no name list), then derives `signal_set` + `supporting_statement_ids` from the subtree. **Boundary-precise (bar #3)** — read (`3.3.x`) / write (`3.1.x`) subtrees disjoint, so `read_transfer` excludes `PWDATA`/`PSTRB`/`PWUSER`. Measured (old-vs-new deterministic rebuilds): APB write 1→10 / read 1→7; AXI off its empty state (`atomic_transaction` 0→22, `prefetch` 0→8, `writezero` 0→6, `writedeferrable` 0→10); corpus 101/260 carry membership, **0 over-broad**. **`.isf` BYTE-IDENTICAL** (4 wire docs) + relations/constraints/temporal/conditional byte-identical old-vs-new → **WIRE-BASED-100 provably orthogonal**; **`run_ci.sh` GREEN (lib 1662, +2); `kg-bench` 156/156**. Book `pipeline/intentir.md`; KM `transaction-membership-subsection-scope`.
- next_action (**OWNER-CHOSEN `2026-06-17`: FRESH SESSION for this design-heavy + RAM-heavy slice**): **`KG-ISF-TRANSACTIONS.2l?` — AXI/SWD timing-diagram phase columns (VLM-tier).** The deterministic transaction frontier is EXHAUSTED (`.2k` membership now complete to what prose grounds; body-emission FSMGen-answered-as-metadata; role-subtyping is subsumed by `.2i` phase membership). The remaining gap-#3 lever is the `.2j`-recorded candidate: AXI/SWD per-signal phase membership lives in TIMING DIAGRAMS, not prose/tables (measured `.2j`: AXI-l 0/4, SWD 0/63 signal↔phase prose co-occurrence). Path = the VLM enrich arm (`enrich`/`converge --vlm-provider ollama`, the production Ollama+Qwen2.5VL `[[project_llm_provider_ollama_qwen]]`) reading phase order/columns off timing-diagram crops → typed phase membership. **MEASUREMENT-FIRST** (is the cue VLM-recoverable on the wire docs? — a `.2f`/`.2j`-style read-only probe before any code), own a leaf first. **RAM doctrine HARD:** serialize the 14B vs Docling (`ollama stop` first), monitor every job + swap, autonomously kill ≥85% used, never the 90→93% reboot zone (`[[feedback_ram_ceiling_monitor]]`). WIRE-BASED-100 a hard gate; ADR-0006. Standing alternates if VLM is unavailable: `CORPUS-COVERAGE` re-ingest the 57 normalized-missing docs (RAM-gated, host-local PDFs — owner re-provides on request).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** is critical-path; `.2a`–`.2k` done (body-emission FSMGen-answered = IntentIR metadata, not a fabricated `.isf` body).
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes (`[[feedback_ram_ceiling_monitor]]` / `[[project_big_pdf_memory_bounded_ingest]]`). This slice was RAM-safe (Rust build + run_ci + kg-bench + deterministic rebuilds, no 14B/Docling). Push ~every 200 commits or on ask (181 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (symlink canonical `generated/source_ir`/`generated/evidence_ir` in + run the release binary from a temp CWD so the RELATIVE output root redirects). For an old-vs-new byte-identity proof, `git stash` the code file → build OLD binary → rebuild deterministically → pop. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **only this MEMORY handoff-refresh commit** (resume pointer → owner-chosen VLM-tier next slice; `.2k` already landed at `58341725`, run_ci/kg-bench green, tree otherwise clean). Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY — safe to `/exit` and start a fresh session;** the fresh session resumes from this pointer's next_action (VLM-tier AXI/SWD timing-diagram phase columns, measurement-first, RAM doctrine HARD).
