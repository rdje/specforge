# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`45258992` = `NLP-SHALLOW-PARSE.2f` (measured NO-GO → NLP BUILD frontier measured-EXHAUSTED, docs-only)**; **now committing `CORPUS-PATTERN-REUSE.3b.3a` (first opt-in extractor = measured NO-GO, read-only/docs-only)**; ~128→129 ahead of `origin/main` (session `2026-06-15`, fresh session, PNT active). **THIS-SLICE (`.3b.3a`, read-only/docs-only):** selected + measured the reuse plane's FIRST opt-in extractor candidate (the parked `PDF-VARIANT-DIGESTION.9.10` prose bus-line lever). Faithful Form-A re-derivation reproduces the probe (fires 5/78, 0 wire-based; benefit only SMBus `SMBCLK`/`SMBDAT`/`SMBSUS#` + I2S `WS`; noise I2C `VDD`/`VSS`/`DLEN`, eMMC `VDD`, OpenCAPI-TL `AFUC2` false-pos). **`corpus-cluster` decisive: all 5 firing docs are structural SINGLETONS (SMBus/I2S/I2C/OpenCAPI-TL) or paired with a register-heavy non-bus doc (eMMC↔GIC); NO 2-wire-bus family at any threshold (0.40–0.60 swept).** Safe/noisy split is LEXICAL (supply rails) ⟂ the STRUCTURAL fingerprint → cluster-scoping inherits the forbidden-denylist blindness; only learnable bus-ish cluster holds the measured-regression doc I2C (gold 0.600). → NO-GO; established the STRUCTURAL-discrimination criterion, excluded serial-prose levers, spun `.3b.3a2`; `.3b.3b` gated. KM card `corpus-reuse-serial-prose-lever-not-cluster-scopable`.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **3 OWNER-DIRECTED FORWARD LEVERS:** (1) **`NLP-SHALLOW-PARSE` — BUILD frontier MEASURED-EXHAUSTED `2026-06-15` (STANDING, build-exhausted):** both `.2h` (direction) and `.2f` (coordination) resolved measured-NO-GO (already in the production hand grammar); `.3` assembler = consolidation-only → `deferred`; re-open only on a future measured prose gap or an explicit owner consolidation directive. The bigger "digest any PDF" levers stay TABLES + VLM (per the spike). (2) **`CANONICAL-PROMOTION-SWEEP`** (NEW tree) — **next = `.1`** pilot on a few NON-wire docs (CHI/NVMe/CCIX class) to lock the RAM-safe per-doc protocol; RAM-HEAVY 14B → run with the watchdog, `ollama stop` before any ingest, ≥85%-used kill; canonical mutation review-gated; wire docs only under the full gold battery. (3) **`CORPUS-PATTERN-REUSE`** — **`.3b.3a` DONE `2026-06-15` (read-only, measured NO-GO for the leading serial-prose candidate; criterion = a valid first opt-in extractor must be STRUCTURALLY-discriminated, not lexically) → next = `.3b.3a2`** continue the read-only selection search for a structurally-discriminated candidate (e.g. CCIX message-field family); `.3b.3b` build gated on it. Serial-prose levers (`.9.10`/`.9.8b`) EXCLUDED (need participation-identity = build-exhausted NLP path, not the reuse plane). ALSO open/standing: **`EXTRACTION-QUALITY-GAUGE`** (`.3` COMPLETE `.3a`–`.3e`; FIELD-cell gate #2 = confirmed NO-GO, `.3e` already closed the class, residual needs a forbidden denylist); **`MEMORY-BOUNDED-INGEST`** (Mostly Done/standing, measured-exhausted); **`PDF-VARIANT-DIGESTION`** (`.13` complete; `.9.10` parked → now the CORPUS-PATTERN-REUSE opt-in candidate). Recently-closed: `LLM-PRIMARY-PROMOTION`, `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE`.
- next_action (PNT active): **two of three forward levers now measured-exhausted/blocked** (NLP build-exhausted; CORPUS-PATTERN-REUSE consume blocked on a STRUCTURALLY-discriminated candidate). Two read-only options: **`CORPUS-PATTERN-REUSE.3b.3a2`** (continue the opt-in-extractor search measurement-first — look at the CCIX message-field family or another structurally-distinctive cluster; LOW-RAM, no LLM) OR pick up **`CANONICAL-PROMOTION-SWEEP.1`** (the main remaining BUILDABLE lever — RAM-heavy 14B pilot on non-wire docs; run only with the watchdog: ≥85%-used kill, `ollama stop` before any ingest, never the 90→93% reboot zone; review-gated canonical mutation). **`cargo build --release` before any live measurement; `scripts/run_ci.sh` before declaring a CODE slice green** (fmt-check/rustdoc/KM-diff are NOT in per-slice focused checks). Fresh `target/release/specforge` is post-`.3e`; `.3b.3a` is read-only/docs-only (no rebuild).
- in_flight_uncommitted: the `CORPUS-PATTERN-REUSE.3b.3a` measured-NO-GO slice staged-to-commit (CORPUS-PATTERN-REUSE tree + new KM card `docs/knowledge/corpus-reuse-serial-prose-lever-not-cluster-scopable.md` + regenerated `KNOWLEDGE_MAP.md` + TASK_TREE index + CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/MEMORY). Read-only/docs-only, NO code. Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`. blockers: **none**.
