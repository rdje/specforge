# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> The bounded **resume pointer**: it describes *now*, not the journey. Overwrite the "Current state"
> block below; never append session narration. How to resume — bootstrap order, doctrine, gates, and
> retrieval — is `AGENTS.md`; the layer contract is `MEMORY_ARCHITECTURE.md`. Everything else is derived
> on read: revision from `git rev-parse HEAD`, work state from `docs/tasks/`, history from `git log`.

## Current state (OVERWRITE this block each update — do not append)
- Active unit: `LIVE-DOCUMENT-PRESSURE-HEADROOM` — `.14a` is done and `SPEC-TO-INTENT-ALIGNMENT.9c` is
  unblocked. Open in this tree: `.1`/`.3`/`.4`/`.6`/`.8`-`.13`/`.14b`/`.14c`/`.15`-`.18`. Also open:
  `SOURCE-IR-REPRODUCIBILITY` `.3`/`.4`/`.6`/`.7`/`.9a`/`.10`/`.13`; `CLAIM-VERIFICATION-ADOPTION`
  `.8`/`.9`/`.12`/`.13`; `SCRATCH-RESIDUE-CONTAINMENT.1`/`.4`; `STATUS-LEDGER-ROLLOVER.2`;
  `PROVIDER-MODEL-STORE-LOCALITY.1`; `TASK-PART-SEAL-REACHABILITY.0`; `CHANGES-LEDGER-ROLLOVER.4`.
- Current state: `.14a` sharded the alignment task-evidence index by lifecycle per ADR 0046 — landing carries
  open leaves, a derived route catalog carries the complete set, both derive-and-diff generated by
  `--write`. Index 115 -> 43 lines. Its cross-check (declared lifecycle vs the primary part's own status)
  found two things on first run: `.8` was `active` while its children were `done`, and `.9a` has no node
  record anywhere. Adoption is staged: pdf/corpus stay `inline` because their parts carry no node blocks.
- Next action: `LIVE-DOCUMENT-PRESSURE-HEADROOM.15` — make "a cited owner is still open" a mechanical gate,
  with a known-bad case observed RED first. Then `.4` (`research_records`, the closest hard stop) and `.9c`.
- In-flight uncommitted: none after this commit.
- Blockers: none, but `research_records` is at **99.8%** of `lines_each` and **98.4%** of files — one line and
  one file from a hard ceiling with no rollover route. Read every figure from
  `bash scripts/check_live_document_size.sh`; the task-evidence contracts have their own driver,
  `bash scripts/check_task_evidence_contracts.sh`. Never infer ownership from a mention: read the owner's own
  `Status`, and check that a leaf you close holds no assignment rows. Editing a book chapter or prepending to
  `CHANGES.md` moves pinned census regions: prove the prepend is pure, shift, then re-verify each
  `sha256(line bytes + "\n")` by CONTENT anchor, never by offset arithmetic. Owned, not fixed:
  `SOURCE-IR-REPRODUCIBILITY.13`; `CLAIM-VERIFICATION-ADOPTION.8`/`.9`/`.12`/`.13`; `CHANGES-LEDGER-ROLLOVER.4`;
  `SCRATCH-RESIDUE-CONTAINMENT.4` — the `generated/` fixture producer is still signal-unsafe. Never run the
  fixture suite concurrently with the locality gate. `durability.stale_check` is never executed by any gate
  (`.18`), so no claim's staleness marker is load-bearing today.
