# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`d302e24b`** `GRITS-CROSS-TOOL.4 — document the grits-consensus command (mdBook + README)`. **A `KG-ISF-COMPLETENESS.3` DOCS commit is being created now** (relation-completeness measurement: staleness vs honest absence). **177 ahead** of `origin/main` (→ 178 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (fresh session `2026-06-17`; open-ended). **Owner pushed back on "buildable frontier exhausted" (`2026-06-17`): SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — now PNT-ing through 3 SUBSTANTIVE north-star gaps to EXHAUSTION, stop only for fresh-session handoff: #1 relation-incompleteness, #2 corpus coverage, #3 transaction body-emission.** Slices this session: `.2`/`.2a.i`/`.2b`/`.1b.iv`/`.1b.ii` (`32afd5ac`) → `GRITS-CROSS-TOOL.4` (`d302e24b`) → **`KG-ISF-COMPLETENESS.3` relation-completeness measurement (THIS commit, = gap #1)**.
- **`KG-ISF-COMPLETENESS.3` DONE `2026-06-17` (measurement, docs-only — THIS commit):** owner's substantive gap #1. Read-only census (78 evidence + 36 intent) + content sampling answers bar #2: the 0-relation docs are NOT a relation-extraction gap. **(A) STALE intent_ir (recoverable):** `tilelink_1_7_1` evidence 39 / intent 0 (intent 05-16, evidence 06-08); tilelink_1_8_0 40→0, i2c 17→0, wbspec 1→0 + a broader "intent older than evidence" set (gic_600/mmu_700/ATS/dti/opencapi×3/usb4). Deterministic `semantic`→`intent` rebuild recovers — **PROVEN live tilelink_1_7_1 0→39, 40/40 actors connected** (no LLM/Docling, RAM 77%). Operational cause (sweep rebuilt evidence w/o cascading downstream; only `converge` rebuilds the whole chain), not a code bug. **(B) HONEST ABSENCE (not a gap):** nvme/iommu/vt-d/ccix declare ~0 wire signals (cues = ToC/register-legend/agent-message prose); intent lives in register/message-field/transaction surfaces → 0 relations correct, forcing = fabrication. Genericity: relation surface is intrinsically wire-protocol-shaped. Report `docs/research/relation-completeness-measurement.md`; KM `relation-completeness-staleness-vs-absence`; tree `.3`.
- next_action: **CONTINUE PNT through the owner's 3 substantive gaps.** #1 (`.3`) measurement DONE. **NEXT = #2 corpus coverage:** rebuild the stale downstream stages (tilelink×2/i2c/wbspec + the "intent older than evidence" set — **NON-wire only**; deterministic, RAM-safe) so recovered relations land canonically; build the ~42 evidence-only docs to intent where the bundle survives; surface which need re-ingest (norm=False ~24 docs — Docling + source PDF, RAM-gated). Likely paired with a CODE slice: a generic stage-staleness `validate` detector (a downstream artifact carrying fewer relations than its upstream = surfaced, not hidden — serves "the KG must be COMPLETE"). THEN #3 transaction body-emission (empirical FSMGen-contract re-check FIRST per `[[feedback_verify_fsmgen_before_fr]]`). Measurement-first each; **WIRE-BASED-100 a hard gate — do NOT disturb APB/AHB/AXI/SWD without re-verifying.**
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** elevated to critical-path; `KG-ISF-TRANSACTIONS.2a→.2j` done (body-emission FSMGen-parked).
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes (`[[feedback_ram_ceiling_monitor]]` / `[[project_big_pdf_memory_bounded_ingest]]`). This slice was RAM-safe (read-only census + 1 deterministic stage rebuild, no 14B/Docling; RAM 77% free). Push ~every 200 commits or on ask (177 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (symlink canonical `generated/source_ir` in + run the release binary from a temp CWD so the RELATIVE output root redirects). `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **a `KG-ISF-COMPLETENESS.3` DOCS commit is being created now** — `docs/research/relation-completeness-measurement.md` (new) + `docs/knowledge/relation-completeness-staleness-vs-absence.md` (new KM card) + `KNOWLEDGE_MAP.md` (regen) + `docs/tasks/KG-ISF-COMPLETENESS.md` (`.3` node + children + changelog) + `docs/TASK_TREE.md` (frontier) + `CHANGES.md` + `DEVELOPMENT_NOTES.md` + `LIVE_ACHIEVEMENT_STATUS.md` + this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY.** NOTE: local `generated/{semantic,intent}_ir/tilelink_1_7_1` were refreshed by the proof-rebuild (git-ignored cache; non-wire; harmless). Continue PNT → gap #2 corpus coverage.
