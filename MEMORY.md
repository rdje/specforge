# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`11307dad` = `CANONICAL-PROMOTION-SWEEP.0` (own + scope all 3 owner-directed forward levers; handoff-ready, docs-only)**; **now committing `NLP-SHALLOW-PARSE.2h` (measured NO-GO as new code, docs-only)**; ~126→127 ahead of `origin/main` (session `2026-06-15`, fresh session, PNT active). **THIS-SESSION (1 slice so far):** PNT picked the active frontier `NLP-SHALLOW-PARSE.2h` and opened it MEASUREMENT-FIRST (Acceptance Criterion #1) → **measured NO-GO, no production code.** Production `extract_actor_signal_relations` (`evidence.rs` ~L2822-3039) ALREADY handles passive+active drive/read DIRECTION via the voice-separated `normative_vocab` lexicon (disjoint inflections) — the spike's `manager Reads ARID` error was a generic-SVO-prototype artifact, NOT a production bug. Only delta = the `to`/recipient frame, measured negative-EV over 78 evidence artifacts (≈0 grounded yield — subjects are messages the signal-subject gate rejects; `is driven to` is value-dominant; recipients are document-specific node names ADR 0006; wire docs AXI=10/AHB=1 → NOT additive-safe vs the 100% gate; and it's recall, not the scoped precision fix). → `.2h` `done` (NO-GO); frontier → `.2f`. KM card `actor-signal-direction-passive-active-handled`. Gaps-first; precedent `MEMORY-BOUNDED-INGEST.5` / `FULL-PAGE-INTENT-CAPTURE.1`.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **3 OWNER-DIRECTED FORWARD LEVERS (each has a concrete next leaf):** (1) **`NLP-SHALLOW-PARSE`** — scope DECIDED GO/BOUNDED/gaps-first; **`.2h` resolved MEASURED NO-GO `2026-06-15` (production already covers drive/read direction), frontier now `.2f`** (clause split + coordination distribution — open it MEASURED-FIRST; existing coordination handling is partial; hand-rolled, deterministic, offline; reuse existing tokenization/`normative_vocab`/grounding; wire-based 100% hard gate) → `.3` assembler; full ladder `deferred`. (2) **`CANONICAL-PROMOTION-SWEEP`** (NEW tree) — **next = `.1`** pilot on a few NON-wire docs (CHI/NVMe/CCIX class) to lock the RAM-safe per-doc protocol; RAM-HEAVY 14B → run with the watchdog, `ollama stop` before any ingest, ≥85%-used kill; canonical mutation review-gated; wire docs only under the full gold battery. (3) **`CORPUS-PATTERN-REUSE`** — **next = `.3b.3a`** select the first opt-in extractor measurement-first (read-only; candidate = parked `PDF-VARIANT-DIGESTION.9.10` cluster-scoped) → `.3b.3b` build. ALSO open/standing: **`EXTRACTION-QUALITY-GAUGE`** (`.3` COMPLETE `.3a`–`.3e`; FIELD-cell gate #2 = confirmed NO-GO, `.3e` already closed the class, residual needs a forbidden denylist); **`MEMORY-BOUNDED-INGEST`** (Mostly Done/standing, measured-exhausted); **`PDF-VARIANT-DIGESTION`** (`.13` complete; `.9.10` parked → now the CORPUS-PATTERN-REUSE opt-in candidate). Recently-closed: `LLM-PRIMARY-PROMOTION`, `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE`.
- next_action (PNT active): continue PNT on a LOW-RAM, low-risk leaf. **Recommended next = `NLP-SHALLOW-PARSE.2f`** (coordination distribution — open MEASURED-FIRST: measure the residual the existing partial coordination handling leaves before any code; pure-Rust/deterministic/offline) OR `CORPUS-PATTERN-REUSE.3b.3a` (read-only measurement). Save `CANONICAL-PROMOTION-SWEEP.1` (RAM-heavy 14B) for a session with RAM headroom + full watchdog attention. **`cargo build --release` before any live measurement; `scripts/run_ci.sh` before declaring a CODE slice green** (fmt-check/rustdoc/KM-diff are NOT in per-slice focused checks). Fresh `target/release/specforge` is post-`.3e`; the `.2h` slice is docs-only (no rebuild).
- in_flight_uncommitted: the `NLP-SHALLOW-PARSE.2h` measured-NO-GO slice staged-to-commit (NLP-SHALLOW-PARSE tree + new KM card `docs/knowledge/actor-signal-direction-passive-active-handled.md` + regenerated `KNOWLEDGE_MAP.md` + TASK_TREE index + CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/MEMORY). Docs-only, NO code. Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`. blockers: **none**.
