# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> The bounded **resume pointer**: it describes *now*, not the journey. Overwrite the "Current state"
> block below; never append session narration. How to resume — bootstrap order, doctrine, gates, and
> retrieval — is `AGENTS.md`; the layer contract is `MEMORY_ARCHITECTURE.md`. Everything else is derived
> on read: revision from `git rev-parse HEAD`, work state from `docs/tasks/`, history from `git log`.

## Current state (OVERWRITE this block each update — do not append)

- Active unit: **`SIGNAL-DECLARATION-ROW-DROP.3` CLOSED `2026-09-11`** — its own premise corrected by census: **716 of 2,085** legacy declarations
  carry a spelling their document never writes (`ARESETN`/`PSELx`/`qactive_cg`), and **0 of 604** current ones do. The uppercase came from a producer
  that no longer exists; `known_signals` resolves *to* the declared spelling rather than folding it. Shipped as a guard with an observed-RED control.
  Open in this tree: `.2d` only. Also open: `[[PROSE-NAME-CELL-DECLARATION]]`.0; `RETAINED-BUNDLE-POPULATION-FROZEN` `.1`-`.3`; `KG-ISF-COMPLETENESS`
  beyond `.5`; `LIVE-DOCUMENT-PRESSURE-HEADROOM` `.1`/`.3`/`.4d.ii`/`.6`/`.8`-`.13`/`.14b`/`.14c`/`.15`-`.18`; `SOURCE-IR-REPRODUCIBILITY`
  `.3`/`.4`/`.6`/`.7`/`.9a`/`.10`/`.13`; `CLAIM-VERIFICATION-ADOPTION` `.8`/`.9`/`.12`/`.13`; `SCRATCH-RESIDUE-CONTAINMENT.1`/`.4`;
  `STATUS-LEDGER-ROLLOVER.2`; `PROVIDER-MODEL-STORE-LOCALITY.1`; `TASK-PART-SEAL-REACHABILITY.0`; `CHANGES-LEDGER-ROLLOVER.4`; `WIRE-BASED-100`
  `.10d`/`.10f`/`.2`/`.3`/`.5` (SWD `13/29`).
- Current state: **seven slices; three of them corrected something that was already published as true.** `.1` had left 4 of 27 proof-carrying
  documents unloadable — every wire-bearing one — so `eval-extraction` refused every gold for three commits (`.1b` repaired it; all wire numbers
  re-derive, chain-currency green, kg-bench 156/156). `.1a` fixed a clippy failure the doctrine driver cannot see; `.1c` fixed the sampled probe that
  hid `.1b` (`PROOF-SEAL-TOTAL`, CI-tier, 108 probes / 19m02s). `.2a`/`.2b` shipped two extraction rules; `.2c` was deferred by its own adjudication;
  `.3`'s premise was false. **15 registered doctrines**, 13 at gate tier. Census 27 measurable / 51 legacy.
- Next action: `[[PROSE-NAME-CELL-DECLARATION]]`.0 — the census, before `SIGNAL-DECLARATION-ROW-DROP.2d`, because it is a live precision defect rather
  than a recall gap: a row whose name cell is a phrase declares its first word as a signal, and eMMC `table_0020` (a bus-mode matrix typed
  `signal_description`) already mints `HS400`. Measure **through the reader's own provenance**, not the 345-row approximation already recorded — that
  number is known to include legitimate multi-word forms (`AWSIZE, ARSIZE`, `AWUNIQUE a`, `response [1:0]`, `waitrequest waitrequest _ n`). State both
  numbers and the difference.
- In-flight uncommitted: none after this commit.
- Blockers: none. Standing hazards: **a green score is evidence only about the facts its gold names**; **a green GATE can be evidence about one
  sampled document** (`[[evidence-rule-field-content-stales-every-proof]]`); **a persisted legacy artifact is evidence about the producer that wrote
  it, never the one running now** (`[[declared-spelling-is-the-document-spelling]]`) — read what a check actually asked, and which stratum a number
  came from, before reading either. **Measure a candidate rule corpus-wide before shipping, and look at what it selects, not how many**
  (`[[base-name-template-table-is-not-a-catalogue]]`): five rules in this one reader have now over-fired, been withdrawn, or had their premise
  falsified on adjudication. **Changing the CONTENT of an already-registered EvidenceIR rule field stales every persisted proof without moving the
  ruleset seal**, and `rebuild_stage_cascade.sh` refuses a deliberate content delta by design — run the stages directly, one validate per artifact,
  upstream-to-downstream, bundles restored from `generated/preserved/` and returned byte-identical (`[[retained-bundle-population-is-frozen]]`). **The
  doctrine driver runs no cargo gate** (`[[doctrine-driver-runs-no-cargo-gate]]`). **Re-anchor a digest-bound region by its TEXT, never its line
  number** — a blanket digest refresh silently re-pointed two frozen census units at different prose while every gate went green. **A relation can
  become a declaration** (`[[alpha-variant-placeholder-is-not-a-wire]]`); **an identifier may bind to another only when the document says so twice**
  (`[[document-stated-identifier-coreference]]`). **The book can carry a false current-behaviour claim for a month**; no gate covers behavioural
  prose. **A SourceIR classification rule may not read its neighbours** (`[[sourceir-classification-is-per-record]]`). **Do not predict a
  re-derivation's verdict.** Never run the fixture suite with the locality gate (`SCRATCH-RESIDUE-CONTAINMENT.4`). A live-surface edit sets off
  `[[live-surface-edit-bookkeeping-chain]]`; this file's cap is 50 lines.
