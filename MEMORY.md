# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`7a9bacd6`** `KG-ISF-COMPLETENESS.2a.ii — initiator-perspective signal DIRECTION in the emitted .isf (CODE, owner-authorized)` (verified independently: APB `.isf` module `requester`, PRDATA/PREADY/PSLVERR→`(input)`, `fsmgen --strict` 0 diagnostics; `kg-bench` 156/156). **A MEMORY continuity-refresh commit is being created now.** **192 ahead** of `origin/main` (→ 193 after). Tree clean except untracked `.claude/settings.json` (+ the transient `git_message_brief.txt`).
- **MODE: PNT loop** (session `2026-06-17`→`18`; open-ended). **Owner: SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT through SUBSTANTIVE north-star gaps:** #1 relation-incompleteness (DONE `KG-ISF-COMPLETENESS.3`), #2 corpus coverage (DONE `CORPUS-COVERAGE.0`+`.1`; re-ingest BLOCKED — host-local PDFs), #3 transactions (`.2a`–`.2n` DONE), #4 message/register section-heading (`PDF-VARIANT-DIGESTION.10f`+`.10g` DONE), **#5 ISF signal DIRECTION fidelity (`KG-ISF-COMPLETENESS.2a.ii` DONE — owner-authorized).**
- **`KG-ISF-COMPLETENESS.2a.ii` DONE `2026-06-18` (this session — CODE, OWNER-AUTHORIZED): initiator-perspective signal DIRECTION emission.** Owner chose (AskUserQuestion `2026-06-17`: "Build it, initiator perspective"), satisfying the `.2a` direction re-open trigger. The emitted `.isf` interface now lowers grounded actor-relative direction from the protocol's INITIATOR's perspective instead of defaulting non-`Input` signals to `(output)`. `select_initiator_actor` (`ir/isf_ir.rs`) = the net-producer actor (out>in) maximizing `(out,in)`, structural/no name list (ADR 0006); `initiator_perspective_directions` maps Drives→`(output)`/Reads→`(input)` (ambiguous/InOut/Unknown omitted → residual); signal loop prefers it, else flat hint, else `(output)`; `derive_isf_actor_name` (`ir/adapters.rs`) names the module after the same initiator (coherent). **Strict-safe:** per-output named-drive block already Output-filtered → inputs auto-suppressed from drives. **Measured (git-stash baseline-vs-after):** initiators AHB `Manager`(6/2)/APB `Requester`(20/12)/AXI `Manager`(116/52)/SWD `debugger`(2/1); flip APB input 2→12, AXI 4→52, SWD 0→1 (faithful), **AHB 0→0 honest residual** (stale Manager grounding = sidebands + clock/reset only); module names AHB `address_decoder`→`manager`, APB `apb_protocol`→`requester`, AXI `agent`→`manager`, SWD `agent`→`debugger`. **Gates ALL GREEN:** FSMGen `--strict` **0 NEW diagnostics** on all 4 wire docs (AHB `HAUSER`/AXI `ASKSTOP` pre-existing rule conflicts unchanged; APB/SWD PASS); `run_ci.sh` GREEN (lib **1677→1679**, +2 tests); `kg-bench` 156/156; WIRE-BASED-100 orthogonal (emitter-only). `.2a` fully resolved (width `.2a.i` + direction `.2a.ii`). KM `[[isf-initiator-perspective-direction]]`; book `pipeline/isf-adapter.md`.
- next_action (**PNT; RAM-SAFE default — no VLM**; own a leaf BEFORE any code; measurement-first; ADR-0006; WIRE-BASED-100 + register/wire golds + `kg-bench` hard gates). **Both named buildable levers now DONE (`.2n` transaction body NO-GO + `.2a.ii` direction CODE).** Remaining frontier is BLOCKED/gated: (a) `.10g` block-qualified reused-mnemonic = LOW-VALUE/HIGH fabrication-risk; (b) DTI signal-inventory prose noise + re-ingest of 57 normalized-missing docs = host-local-blocked (source PDFs absent — owner can re-provide to unblock corpus-coverage gap #2); (c) `CORPUS-PATTERN-REUSE.4` offline miner + ATP `ihi0082` = VLM/RAM-heavy (owner-gated). **Frontier surveyed (read-only) + both named levers built → the deterministic/RAM-safe/non-blocked buildable frontier is EXHAUSTED for now.** **RAISED TO OWNER `2026-06-18`:** the remaining substantive gaps need owner resources/go — the biggest is corpus-coverage gap #2 (57 of ~79 docs cannot reach `.isf` because their source PDFs are host-local-ABSENT; re-provide per `[[feedback_source_pdfs_in_repo]]` to unblock a RAM-gated re-ingest), or owner go on a VLM/RAM-heavy lever (`CORPUS-PATTERN-REUSE.4` miner / ATP `ihi0082` fragment). Awaiting owner steer. Do NOT treat "levers done" as "complete" (`[[feedback_not_complete_attack_substantive_gaps]]`).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** is critical-path.
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B/7B models (`ollama stop` before/after). `.2a.ii` was RAM-safe (cargo builds + `fsmgen` Perl probes + persisted-artifact reads, no VLM/Docling; RAM steady ~74% free). `[[feedback_ram_ceiling_monitor]]`. Push ~every 200 commits or on ask (**192 ahead → HOLD**, < 200; → 193 after this continuity commit).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path); for non-destructive measurement use `<stage> --dry-run` (prints JSON after a `command:`/`mode:`/`artifact_root:` preamble — slice from the first `{`) or build into a TEMP evidence-root. **STALE-PERSISTED GOTCHA:** `generated/` artifacts can be stale (built by an older binary) — rebuild with the current binary before measuring; isolate a change's effect with `git stash`. **FSMGen BODY GRAMMAR (`.2n`, pin `030f8c273`):** in-body `(drive NAME …)` is a CALL to a top-level named drive (defined elsewhere); `(sample S as s)` is value-free + NOT direction-gated but fires at the `(on …)` entry transition. `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **only this `MEMORY.md` continuity refresh** (`.2a.ii` itself fully committed at `7a9bacd6` + independently re-verified). Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none — but the deterministic buildable frontier is exhausted; awaiting owner steer (re-provide source PDFs to unblock corpus-coverage, or authorize a VLM lever).** **Repo HANDOFF-READY.**
