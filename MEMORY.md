# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`KG-ISF-COMPLETENESS.1`** (measurement) = agent-surface fidelity measurement DONE (read-only, docs-only); prior HEAD `82895ccf` `KG-ISF-COMPLETENESS.0`; **~143 ahead** of `origin/main` (push at ~200). PNT active (owner chose "PNT loop now" `2026-06-16` after a full README→roadmap→codebase→mdBook bootstrap).
- **NORTH STAR (owner, `2026-06-16`, multi-message — `[[project_kg_isf_completeness]]`):** the KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* **REVERSES the prior "defer ISF lowering" steer.** "ISF ready" ≠ adapter renders: ALL 36 built docs already render `.isf` 0-blockers (measured) — fidelity is the IntentIR's. New tree `KG-ISF-COMPLETENESS` owns it; checkable 6-point bar (agents/relations/signals/constraints/behaviors-temporal/ISF-round-trip); WIRE-BASED-100 a hard gate. **Measured 2-defect agent baseline (both coexist → naive fix disproven):** PRECISION (prose-fragment non-agents `for`/`note`/`then_it`/`class_x` minted as actors — they carry ports, so "drop orphans" FAILS) + COMPLETENESS (real agents `producer`/`consumer`/`receiver`/`transmitter` carry 0 ports/relations vs `manager`/`subordinate` 169/168).
- **THIS-SESSION (PNT):** `KG-ISF-COMPLETENESS.1` MEASUREMENT DONE (read-only census over the 4 wire docs + AXI-Stream; no code). The agent surface has **3 structural defect classes** — **A Junk** (subject is a clause/function-word: `For components`/`HPROT bit`/`is recommended`(real subj `Manager` in the that-clause)/`section`/`two-cycle response`/`TREADY input`/`is permitted`); **B Fragment-of-real-agent** (`Subordinate extends`→`Subordinate`, `address decoder`/`decoder also`→`decoder` — genuine AHB fact — `Transmitter interface`→`Transmitter`, `Subordinate and decoder`→split); **C Zero-evidence role-term** (0 ports AND 0 rels, SemanticIR Phase-2 scan). **`.0` hypothesis CORRECTED:** disconnected agents (`producer`/`consumer`/`receiver`/`transmitter`) are NOT a recoverable gap — Class-C is a drive/read subject by a known signal ≈0 times → connecting them = FABRICATION; the win is Class-B consolidation + per-doc Class-C drop. **Genericity proven:** AXI `transmitter` 0/0 vs AXI-Stream `Transmitter` 22/23 → per-doc evidence-keyed, NO name list (ADR 0006). Spun `.1a` (precision structural gate) + `.1b` (consolidation + zero-evidence drop). Report `docs/research/agent-surface-fidelity-measurement.md`; KM `agent-surface-defect-taxonomy`.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask. (This slice was RAM-safe: deterministic, no 14B.)
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, never runtime; NO deny/filter LISTS (prefer structural gates); goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals; honest RESIDUAL over fabrication. **`scripts/run_ci.sh` before declaring any CODE slice green**; `cargo test` does NOT rebuild the bin — `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (`evidence --dry-run` strips a 4-line header before the JSON) and eval there. `subs/fsmgen` pinned `c0b7eaa7`.
- **EQG `.3` precision program now spans `.3a`–`.3g`; the clean+narrow+universal gate frontier on the persisted corpus is MEASURED-EXHAUSTED.** Remaining NVMe residuals are NOT clean micro-gates: (a) prose-subject catalog class (`NVM`/`LBA`/`FFFF` admitted as "signals" because a register doc has no real wire catalog) and (b) tangled spurious-digit-value class (`HMDLLA 1` vs the GENUINE `BADD 0`/`RECFMT 0h`/`ELEN 0h` — no clean discriminator). Both need deeper non-narrow work (catalog-quality / `.FIELD` re-routing) with real gate risk — pure-hex/lowhigh-level shortcuts were each MEASURED-REJECTED (would flag real `CBA`/`BADD` / kill genuine HBM2/I2C). Don't grind more 1-record micro-gates (genericity guardrail).
- next_action: **PNT into `KG-ISF-COMPLETENESS.1a` — precision structural agent-identity gate (CODE), measurement-first.** Gate DESIGNED (code-seam study `2026-06-16`): the ONE DRY seam is `normalize_relation_actor_name` (`ir/evidence.rs:2082`) → `is_meaningful_actor_term` (`prior_memory.rs:607`) — both prose paths + the table path funnel through it. **Scoped gate:** reject a candidate whose FIRST content token is a closed-class FUNCTION WORD (for/then/with/next/of/by/is/has/… — universal English, ADR-0006-safe; codebase already uses such lists) OR a LEADING content verb (ensures/exit/extends/…) — catches `For`/`Then it`/`with write`/`is recommended`/`Exit from`/`next`. **verb-LED only, NOT contains-a-verb** (else kills the Class-B `Subordinate extends` real fact). Descriptor-noun class (`HPROT bit`/`section`/`number of`) → LATER leaf. **DO FIRST:** apply the predicate across all 36 IntentIR docs, confirm it rejects ZERO ≥8-port actors (real-agent proxy). WIRE-BASED-100 a HARD gate via the `.3f`/`.3g` fresh-Pattern temp-evidence-root eval; `cargo build --release` before live measure; `run_ci.sh` before green. Then `.1b` (consolidation; its trailing-strip runs BEFORE `.1a`'s reject). EQG `.3` clean-gate frontier exhausted; RAM-heavy `CORPUS-PATTERN-REUSE.4` needs owner go; 24 promoted canonical docs await `R7-VALIDATION`. **NOT pushed** (~144 ahead, < 200).
- in_flight_uncommitted: **none after this commit** — `KG-ISF-COMPLETENESS.1` measurement + the `.1a` gate design (from a code-seam study) are committed docs-only slices; `.1a` is implementation-ready (gate predicate, DRY seam, ordering nuance, and the corpus collateral-check all captured in the tree leaf). Untracked (stays): `.claude/settings.json`. blockers: **none**. **Repo HANDOFF-READY** (fresh session recommended for the `.1a` code edit, to keep signoff sharpness — owner FRESH-SESSION clause).
