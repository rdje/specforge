# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`570c4458`** `DOC-INTENT-TAXONOMY.2 — per-category ISF-lowering completeness gauge (read-only): the maturity column, measured`. **23 ahead** of `origin/main` (push at ~200 → HOLD). Next commit (`.3a`) → 24 ahead. Untracked (stays): `.claude/settings.json`.
- **MODE: PNT loop** (fresh session `2026-06-22`).
- **OWNER DIRECTIVE (`2026-06-22`) — OWNED as tree `DOC-INTENT-TAXONOMY` ([[project_doc_intent_taxonomy]]):** every chip-spec PDF is *about* something — 6 INTENT CATEGORIES; SpecForge must (1) quickly determine category, (2) extract → IntentIR, (3) lower EVERYTHING to ISF (ISF = the way to SYNTHESIZE PDF intent); ALL categories FULLY handled (SOTA, no hurry). Reinforces NORTH STAR [[project_kg_isf_completeness]]. FSMGen adding verification SV/UVM + VHDL path alongside synthesizable HDL; new ISF abstractions (memory banks, single/dual-port memory, …) anticipated → feed ISF-abstraction gaps back (verified FR, not emitter hack; [[feedback_verify_fsmgen_before_fr]]).
- **✅ `DOC-INTENT-TAXONOMY.0` DONE + COMMITTED `8815a8c5`** (docs-only): the 6-category purpose taxonomy, captured identically in mdBook `document-categories.md` + tree + `TASK_TREE.md`. **✅ `.1` DONE + COMMITTED `1b4657dc`** (read-only census): distribution **36/7/15/2/4/14**; surface counts alone can't recover purpose (cat 2↔3 inseparable / ISA no signature / cat 5↔6 near-empty / register-heavy-protocol trap). Report `document-intent-category-census.md`; KM [[document-intent-category-census]].
- **✅ `DOC-INTENT-TAXONOMY.2` DONE + COMMITTED `570c4458`** (read-only ISF-completeness gauge). Per-surface lowering ledger over all 78 docs (76 `adapter.json` + 2 read-only `adapt --dry-run`; zero mutation); reproducer tracked `scripts/measure_isf_completeness.py` (also persists the per-doc category labels `.1` lost). **Maturity column MEASURED:** cat 1 MATURE (WIRE-BASED-100=1.000) / cat 2+3 PARTIAL / cat 4 THIN / cat 5+6 honest non-targets. **Two dominant TRUE GAPS:** (A) register **bit-fields** 12,638 captured → 0 lowered across 32 docs (registers emit opaque width-only storage; cat 3 largest 9,308); (B) message-field **structures** 1,220 at EvidenceIR → 0 carried to IntentIR (`has_msgfld_key=false`) across 11 docs. Both → SAME FSMGen ISF-abstraction (field-structured storage + packet layouts). Report `document-intent-isf-completeness.md`; KM [[document-intent-isf-completeness]] (map → 112 facts).
- **✅ `DOC-INTENT-TAXONOMY.3a` DONE (this slice), recognizer DESIGN pinned → committing now.** Grounded in real `completeness.rs::classify_document` + measured `.1`/`.2` evidence: extend census with `message_field_records`; 6-cat + `Unresolved` output with confidence + explicit residual; decision order = self-declared guide wins over spurious counts (the `.2` over-extraction finding) → wire-shape rescues register-heavy protocols (register count never vetoes cat 1) → honest low-confidence residuals where structure can't decide (cat 2↔3 / 4 / 5↔6). No name lists (ADR-0006). Cues verified in scope at `validate.rs` ~2888. `.3` decomposed → `.3a` done / `.3b` implement / `.3c` fixtures+CI. Full design in the tree's "Recognizer design (`.3a`)" section.
- next_action (PNT): **`DOC-INTENT-TAXONOMY.3b` — IMPLEMENT the recognizer** per the `.3a` design: `classify_document_intent_category` in `crates/specforge/src/ir/completeness.rs` (sibling to `classify_document`, same pure-fn + in-file-test pattern), wired into `crates/specforge/src/commands/validate.rs` (at the `document_class` block ~2888; all cues already in scope on the EvidenceIR `ir`). **Build is RAM-constrained** (`CARGO_BUILD_JOBS≤2`, monitor RAM, kill ≥85% — [[feedback_ram_ceiling_monitor]]); then `.3c` fixtures + `run_ci.sh` green. ⚠️ **FRESH-SESSION RECOMMENDED for `.3b`** — hard name-list-free classifier + multi-build CI; signoff quality benefits from fresh focus (repo is handoff-ready). **Then `.4+`** highest-leverage lever = Gap-A register-field lowering (32 docs / 12,638 fields) → likely an FSMGen field-structured-storage FR (verify submodule first). **Parallel queued:** `CORPUS-COVERAGE.2` re-ingest #22 — pre-`.10`-stale register docs (CoreSight SoC-600 `100806_*` / eMMC `jesd84_b50` / AMD-IOMMU `48882_*`); 36 normalized-missing docs remain.
- in_flight_uncommitted: **none** (about to commit `.3a`, docs-only design slice). `.0`/`.1`/`.2` committed (`8815a8c5`/`1b4657dc`/`570c4458`); `CORPUS-COVERAGE.2` #21 committed (`3ce2b2d2`). All gates green; mdBook builds. Untracked (stays): `.claude/settings.json`. blockers: none. **Repo HANDOFF-READY.**
- **OPERATIONAL REALITY:** host RAM healthy at ~72–78% free (`memory_pressure`), swap 1.0G/2.0G. Builds run `CARGO_BUILD_JOBS≤2`, RAM monitored, kill ≥85% used (`[[feedback_ram_ceiling_monitor]]`). ollama server up but **no model loaded** (idle — serialize vs Docling).
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication. Re-ingest = re-run of existing deterministic extractors → WIRE-BASED-100 + register/wire golds + `kg-bench` **156/156** orthogonal by construction (the 4 gold docs are not re-ingested). `scripts/run_ci.sh` green before committing CODE (not needed for a re-ingest data slice). Push ~every 200 (**18 ahead after this commit → HOLD**).
