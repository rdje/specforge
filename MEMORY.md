# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> The bounded **resume pointer**: it describes *now*, not the journey. Overwrite the "Current state"
> block below; never append session narration. How to resume — bootstrap order, doctrine, gates, and
> retrieval — is `AGENTS.md`; the layer contract is `MEMORY_ARCHITECTURE.md`. Everything else is derived
> on read: revision from `git rev-parse HEAD`, work state from `docs/tasks/`, history from `git log`.

## Current state (OVERWRITE this block each update — do not append)
- Active unit: **`LIVE-DOCUMENT-PRESSURE-HEADROOM.31`** — partition `docs/tasks/EXTRACTION-QUALITY-GAUGE.md` (2,998 of 3,000 lines; it refused a commit at
  3,012). The pressure tree owns the TRANSACTION (`.21`, `.30`) because **a surface can become too full to record its own remedy**; the EQG file cannot hold
  this leaf.
- Next action: write `doctrine/live_document_size/extraction_quality_gauge_task_evidence.json` (`source_locked`/`complete`) + a bounded root template, then
  `--migrate`. **Derived:** routes are **46 legacy** (EXACTLY the ids in `git log --format=%s <boundary> -- <path>`) **+ 10 structural** = 56, and the
  root's `- ID:` literals must be that same set — bijection, checked BOTH ways. Seams by reader concern: `.2*` pivot, `.3e`/`.3g`/`.3h` positional gates,
  `.3k*` dedup/span, `.3j*` LLM path, root CHI + fix backlog. Then register 4 surfaces, add the contract to `scripts/check_task_evidence_contracts.sh`,
  repath `doctrine/ownership_citations/citations.jsonl`.
- **`.32` unblocked it (`2026-09-17`)**: `declared_node_statuses` read a status only on the CONTINUATION lines under `- ID:`, never the INLINE form on the
  id's own line — **466 of 1,362 node declarations, 34%**. EQG is written entirely inline, so it corroborated **0 of 56** routes; now **56 of 56**, 14
  open / 42 closed; the three migrated trees moved by NOTHING. Suite **64 -> 69**. **[[node-status-has-two-declaration-shapes]]**.
- `EXTRACTION-QUALITY-GAUGE.3j` closed **NO**: gates refuse **7/0/0/0** of 149, **4 of the 7 CORRECT**. DECISION rests on the MECHANISM and holds; COUNTS on
  a population with **0 of 7 refreshed**; mirror self-tested 11/11. **[[an-llm-constraint-record-cannot-be-judged-by-a-positional-gate]]**. Then `.3j.1.a`:
  carry the minting clause on the LLM proposal (`source_text` IS the span); `.3j.1.b` re-censuses. Also `.3j.2` (bare noun `signal` passed catalog
  grounding; size with the REAL `ground_constraint`, NOT a proxy) and `.3j.3` (the LLM pass can never see a span the deterministic paths missed — a recall
  ceiling).
- **NEVER run the full driver by hand** (`G+G` vs `G`). Step 8 = `--fast` every slice + the oracle for the risk (Rust:
  `cargo fmt`/`clippy`/`test -p specforge --lib`, which the gate NEVER runs; producer graph: `--only PRODUCTION-GENERICITY`; a surface or a registered
  enforcer: `--only LIVE-DOC-SIZE`). `--fast` REFUSES without the hook and is NOT a green gate. A gate doctrine has **no stable wall clock**. **Run ONE
  doctrine with `--only ID[,ID...]` (`--list` for ids) — never invoke a gate script by hand.**
- Containment SPEAKS before it refuses (`COMMIT-GATE-SINGLE-RUN.4`): the driver forwards warnings from PASSING checks as `PRESSURE (N)`. It was hiding
  **44**, incl. EQG's own `99.9% — 2 below its 3000 ceiling`.
- Pressure-tree frontier is **`.12`** (909-byte `doctrine_instance` row), part `warning-assignment`, `active`. **`.30b`**: a migrated part reports ONE
  percentage for two strata — measure the WRITABLE one. A post-migration node REPEATS its Goal line; anchor a `Status:` edit on what FOLLOWS it.
- Also open: `CLAIM-VERIFICATION-ADOPTION.16`/`.17`; `COMMIT-GATE-SINGLE-RUN.0a` — **runnability is a property of the MOMENT**: ask
  `measure_doctrine_cost.sh`, never assert the machine. `SIGNAL-DECLARATION-ROW-DROP` has no eligible leaf (`.4c` parked); `TEXT-LAYER-IDENTIFIER-SPLIT.1`
  needs a live VLM (none up).
- In-flight uncommitted: none. No background job outstanding. **Registering a live-document SURFACE costs TWO permanent census records** +
  `expected_current_surfaces` +1; a MEMBER costs none. **`.29b`** owns the cap.
- Blockers: none. Push cadence **400** (directive `2026-09-13`, FIXED) — derive ahead/behind from Git, never from a number here; directive 16 gates a push
  on full CI. **Corpus CURRENT — 27/27 at semantic and intent, retention exactly 24.** **Any new production Rust FUNCTION moves `flow_census.json`** —
  re-derive via `aggregate_change`, never edit the literal; boundary counts (144/9) must NOT move. **After editing a governed file OR a checker script run
  `python3 scripts/repin_claim_regions.py --check` then `--apply`**, then refresh the `source` records in ALL FOUR claim registries, then `claims.jsonl`
  durability digests **LAST** (they pin the other three, so any earlier order cascades). That set IS the `Published-claims:` line. More hazards:
  **[[a-doctrine-subset-must-assert-the-leg-that-pays-for-the-rest]]**, **[[chomp-is-a-no-op-under-a-callers-slurp]]**,
  **[[a-gate-doctrine-has-no-stable-wall-clock-on-this-host]]**, **[[toolbox-catalog-is-a-routed-landing]]**, **[[live-surface-edit-bookkeeping-chain]]**,
  **[[claim-verification-task-evidence-migrated]]**, **[[prior-phrase-utf8-byte-as-char]]**,
  **[[a-cheap-structural-rule-overfires-until-you-read-its-selection]]**, **[[actor-taxonomy-grows-in-pairs-not-terms]]**,
  **[[declaration-replay-reads-the-legacy-stratum]]**, **[[a-dropped-declaration-row-is-usually-not-a-signal]]**.
