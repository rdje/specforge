# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (memory system), `DOCTRINE_ENFORCEMENT.md` (the 4th
  architecture — doctrines are mechanically gated), and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Before committing run `scripts/check_doctrines.sh` (the registry/driver: memory-arch +
  knowledge-map + task-acceptance); hooks + CI run it too. Retrieval: `KNOWLEDGE_MAP.md`.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`5d128f46`** `KG-ISF-COMPLETENESS.5.ii — per-member sentence-spine enum-fragment gate (codes recovered from conflation)`. **70 ahead** of `origin/main` (push at ~200 → HOLD); this `KG-ISF-COMPLETENESS.5.iii` MEASUREMENT commit → **71 ahead**. Untracked (stays): `.claude/settings.json`, `.cache/` (gitignored symlink).
- **MODE: PNT loop** (this fresh conversation re-ran the full README bootstrap `2026-06-24`; doctrines GREEN). This session's slice (DONE): **`.5.iii` MEASUREMENT** of the deeper enum member-quality residuals → **GO on the `_WIDTH` parameter-leak gate**.
- **🧭 `KG-ISF-COMPLETENESS.5.iii` MEASUREMENT DONE (`2026-06-24`) — the `_WIDTH` parameter-leak is the one buildable deeper enum residual; GO.** Read-only census (tracked reproducer `scripts/measure_enum_width_leak.py`) of the 5 deferred deeper classes over 78 IntentIR docs. **Most are SUBSUMED by `.5.i`** (47/54 `_WIDTH` members + the bulk of 319 section-caption survivors sit in generic-named enums `.5.i` drops whole → no `.isf` reach). **But the `_WIDTH` leak reaches the AXI WIRE-GOLD `.isf` and is materially damaging:** 7 members in real-signal-named enums in `ihi0022_l` emit `(BRESP (BRESP_WIDTH 0)(OKAY 0)…)` (dup value 0), `(RRESP (RRESP_WIDTH 0))` (real codes REPLACED), `(AXSNOOP (AWSNOOP_WIDTH 0)(ARSNOOP_WIDTH 1))` (pure junk), `(AWCMO (AWCMO_WIDTH 0)…)` (dup) — a width PARAMETER (`Enum BRESP BRESP_WIDTH = 0.`) mis-read as an encoding VALUE. Unscored by WIRE-BASED-100 (enum surface emitter-orthogonal — why it held 1.000 while the `.isf` carried junk). **Clean FP-free discriminator:** drop `<X>_WIDTH` iff `X` is a declared signal OR the enum's own name (all 6 caught prefixes are declared signals; corpus FP set EMPTY — no `FULL_WIDTH`-style value exists, `FULL`/`HALF` never declared signals). Per-member not per-enum. **NO-GO** on section-caption (FP risk `D1`/`L2`), restart-of-clean (no defect), glossary/front-matter (tiny). ADR-0006.
- next_action (PNT): **`KG-ISF-COMPLETENESS.5.iii` CODE slice** — land the `_WIDTH` parameter-leak member gate in `synthesize_encoding_declarations_for_enum` (`ir/evidence.rs`), parallel to the `.5.ii` spine gate: thread the existing `known_signals` set (in scope at the signal-match caller `evidence.rs:4700` `Some(known_signals)`) for the declared-signal arm; the enum-self-name check needs no plumbing (covers the `None` caller). **HIGH-STAKES: byte-changing on the AXI wire gold → before/after WIRE-BASED-100 eval REQUIRED** (rebuild gold evidence with the preserved baseline binary vs the new binary, `eval-extraction --provider skip` over all 10 seeds — scored surface must stay byte-identical; `manager.isf` recovers BRESP/AWCMO + drops the false RRESP/AXSNOOP). `kg-bench` + FSMGen `--strict` (rebuilt AXI `.isf`) + `run_ci.sh` hard gates. After it: deeper classes are honest residuals; PNT advances to the next active tree.
- in_flight_uncommitted: **none** once committed (docs-only measurement: research report §`.5.iii measurement`, tracked reproducer `scripts/measure_enum_width_leak.py`, `KG-ISF-COMPLETENESS.md` `.5.iii` node + acceptance checklist + changelog, `docs/TASK_TREE.md` index frontier, KM card `[[generic-enum-conflation]]` + `KNOWLEDGE_MAP.md` regen (126 facts/932 keys), CHANGES/DEV_NOTES/LIVE_ACHIEVEMENT/MEMORY). No Rust touched → all oracles orthogonal. blockers: none. **Repo HANDOFF-READY.**
- **OPERATIONAL REALITY:** the release binary `target/release/specforge` carries `.5.i`+`.5.ii` (rebuilt `2026-06-24`); the `.5.iii` `_WIDTH` gate is NOT yet in code. Generated working copy: AXI+APB+CCIX intent/`.isf` are post-`.5.ii` (the AXI `manager.isf` still carries the `_WIDTH` leak `.5.iii` will fix); the rest of `generated/` is pre-`.5.i` (git-ignored; durable trace = commits). For the `.5.iii` code eval, preserve a baseline binary BEFORE rebuilding (as `.5.ii` did at `scratchpad/specforge.baseline`). fsmgen = Perl `subs/fsmgen/bin/fsmgen` (NOT cargo); `--check` needs `--json`. Host RAM headroom OK. Artifact-cleanup standing (≥24h). lib test count **1716** (RUST_CODEBASE_ANALYSIS.md count is AUDIT-DOC-RECONCILE-owned).
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication; a missing ISF abstraction → verified FSMGen FR, never an emitter hack ([[feedback_isf_no_hacks]] / [[feedback_verify_fsmgen_before_fr]]). `scripts/check_doctrines.sh` green before committing; `scripts/run_ci.sh` green before committing CODE. Push ~every 200 (HOLD).
