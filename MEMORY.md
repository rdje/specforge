# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (memory system), `DOCTRINE_ENFORCEMENT.md` (the 4th
  architecture — doctrines are mechanically gated), and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Before committing run `scripts/check_doctrines.sh` (the registry/driver: memory-arch +
  knowledge-map + task-acceptance); hooks + CI run it too. Retrieval: `KNOWLEDGE_MAP.md`.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`d645acef`** `CORPUS-COVERAGE.2 — re-ingest #27 JEDEC eMMC 5.0 …`. **45 ahead** of `origin/main` (push at ~200 → HOLD). This `CORPUS-COVERAGE.2 #28` commit → **46 ahead**. Untracked (stays): `.claude/settings.json`, `.cache/` (gitignored symlink).
- **MODE: PNT loop** (fresh session `2026-06-23`, separate from the #22–#25 session earlier today). Active tree: **`CORPUS-COVERAGE.2`** (host-local re-ingest of the 57 normalized-missing docs; chipdoc reachable via the git-ignored `.cache/local-references/chipdoc` symlink → `/Users/.../livework/chipdoc`, 169 PDFs). This session bootstrap-read README/AGENTS/MEMORY/CORPUS-COVERAGE/COMMIT, survey-verified ROADMAP (active-frontier pointer current — no real drift) + mdBook (in-sync), then PNT-picked the re-ingest frontier. **Done this session: #26 Intel VT-d (`833f23bb`), #27 JEDEC eMMC (`d645acef`), #28 JEDEC HBM2 (committing now).**
- **🚧 `CORPUS-COVERAGE.2` re-ingest #28 DONE (committing now) — JEDEC HBM2 DRAM** (`jesd235a_2015_11_hbm2_dram`, 172pp / 224 visual; deliberate diagnostic — DRAM register/timing-table spec, the structured contrast to #27's prose). Docling CPU (0 residuals; RAM 81% free; ~2 min) → deterministic cascade. **MIXED refresh:** **CONFIRMS the #27 phantom explosion is PROSE-SPECIFIC** — HBM2 actors **52→38 CONSOLIDATED DOWN** (like AMBA/CoreSight, OPPOSITE of eMMC), relations 89→55, interfaces 72→31; GENUINE win = **transactions 0→3** [read/write/trr_mode_operation]; register_records 17 held; 0 message-fields (honest); `validate` no stage-staleness, 55/100 ADEQUATE. **BUT `.isf` strict-FAILS → NEW lever F:** the emitter built a generic `(type TABLE (bits 6))` mega-enum conflating ~10 distinct doc tables, emitting REPAIR_LANE BINARY codes (`0,1,10,…,1000,1111`) as bare decimal tokens → FSMGen rejects `TABLE.REPAIR_LANE_8`=`1000` (needs `4'b1000`); also semantically wrong (binary 1000≠decimal 1000). Lever F = ISF enum-value literal format + binary-radix preservation (kin to Lever A) + generic-`TABLE` mega-enum precision. **Coverage 28 of 57. 25/28 strict-clean** (3 FAIL = DTI/A, LPI/C, HBM2/F).
- **PHASE FINDING (refined #28 — IMPORTANT for the next pick):** **two consecutive re-ingests (#27/#28) each surfaced a NEW substantive lever (E prose-actor-precision, F enum-value-literal) rather than a clean refresh** — the re-ingest tail is now reliably a LEVER-SURFACING exercise, not clean-coverage. Diagnostic value is real (E scoped to prose by #28's clean HBM2 consolidation; F found), but per the owner's "attack substantive gaps, not easy incremental" directive the next high-value work is ACTING on a surfaced lever, not grinding more re-ingests. Lever E (prose-actor-precision, bar #1) is the strongest substantive candidate; Lever F/A (ISF value-literal) is a tidy emitter family.
- next_action (PNT): **RECOMMENDED PIVOT — own + probe a surfaced lever** (owner "substantive gaps" directive; two consecutive lever-surfacing slices). **Lever E — agent-identity precision for descriptive-prose / non-AMBA specs** (bar #1; #27 eMMC 20→153 phantoms; #28 HBM2 proved it's prose-specific). Probe-first (read-only, RAM-light): measure phantom-actor counts across the freshly-re-ingested docs (#17–#28), characterize the structural patterns (real-agent+trailing closed-class auxiliary/preposition `host has`/`host to`; bare-noun fragments `following`/`value`), then design STRUCTURAL gates (extend `.1b.i` trailing-strip to closed-class auxiliaries/prepositions; a participation/port-floor for minting a prose relation-subject actor) — NOT a fragment denylist ([[feedback_avoid_denylists_prefer_structural]]); needs its OWN owned `KG-ISF-COMPLETENESS` leaf + full `run_ci.sh`/`kg-bench`/WIRE-BASED-100 gating. Sibling: **Lever F/A** (ISF enum/value literal format — `ISF-VALUE-WIDTH-EMIT` family, compile-gated). **OR continue `CORPUS-COVERAGE.2`** (29 remain: RISC-V AIA, USB4/USB3.2 [prose, expect lever-E recurrence], OpenCAPI×13 thin tail; skip `readme`) — but this keeps surfacing levers without acting. **Lever code work is high-stakes → best started on a FRESH session for full sharpness.**
- in_flight_uncommitted: the **`CORPUS-COVERAGE.2 #28`** staged set (docs/tasks/CORPUS-COVERAGE.md, CHANGES.md, DEVELOPMENT_NOTES.md, LIVE_ACHIEVEMENT_STATUS.md, MEMORY.md — NO book change, HBM2 adds 0 message-fields). blockers: none. **Repo HANDOFF-READY** after commit. (generated/ is git-ignored — the `.2` log table is the durable trace.)
- **OPERATIONAL REALITY:** host RAM monitor before any Docling/14B work — kill ≥85% used ([[feedback_ram_ceiling_monitor]]). This session ran THREE CPU Docling re-ingests (#26 VT-d, #27 eMMC, #28 HBM2; RAM steady 80–83% free, no abort); no Rust build (binary current at HEAD). NOTE: fsmgen is the Perl `subs/fsmgen/bin/fsmgen` (NOT cargo — a stray `cargo build` errors with "no bin target fsmgen"). Artifact-cleanup directive standing (≥24h) — re-ingest restored VT-d/eMMC/HBM2 `normalized/` bundles (intended); no stray build artifacts.
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication; a missing ISF abstraction → verified FSMGen FR, never an emitter hack ([[feedback_isf_no_hacks]] / [[feedback_verify_fsmgen_before_fr]]). `scripts/check_doctrines.sh` green before committing; `scripts/run_ci.sh` green before committing CODE. Push ~every 200 (HOLD).
