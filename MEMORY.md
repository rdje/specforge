# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`87422c03` = `MEMORY-BOUNDED-INGEST.5` (summary/`source_ir.json` streaming — measured DEFER, docs-only)**; **now committing the 3-LEVER OWNERSHIP/SCOPE slice** (owner-directed "do all these" + "make handoff-ready"); ~125→126 ahead of `origin/main` (session `2026-06-15`, fresh session). **THIS-SESSION (2 slices):** #1 `MEMORY-BOUNDED-INGEST.5` (committed `87422c03`) — measured DEFER: `source_ir.json` O(pages) ~9.3 KB/page (n=78), library max 9.9 MB, trivially bounded; streaming YAGNI; real far-off cost = downstream full-serde load `SourceIr::load_from_path` (`ir/source.rs:573`) → spun `.5a`; KM `source-ir-size-scaling`. #2 (THIS COMMIT, docs-only) — **owner picked ALL 3 forward levers; owned + scoped each, made handoff-ready (NO code this session — context-heavy; fresh session executes):** (a) **NLP-SHALLOW-PARSE** scope DECIDED (owner-delegated) = GO/BOUNDED/gaps-first → frontier `.2h` (passive/verb-sense direction) → `.2f` (coordination) → `.3` (assembler); full academic ladder `deferred` (spike: POS not the bottleneck); (b) **CANONICAL-PROMOTION-SWEEP** = NEW tree (land the default LLM-primary promotion on CANONICAL artifacts; per-doc SEQUENTIAL + RAM-safe 14B + review-gated; wire docs only under full gold battery) → frontier `.1` non-wire pilot; (c) **CORPUS-PATTERN-REUSE.3b.3** "design open" blocker RESOLVED → split `.3b.3a` (select first opt-in extractor, measurement-first; candidate = parked `PDF-VARIANT-DIGESTION.9.10` prose bus-line lever cluster-scoped to its safe serial/SMBus family) → `.3b.3b` (build + activate-only consume). Docs-only → memory-arch + KM gates.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **3 OWNER-DIRECTED FORWARD LEVERS, owned + scoped this session, READY FOR FRESH-SESSION EXECUTION (each has a concrete first leaf):** (1) **`NLP-SHALLOW-PARSE`** — scope DECIDED GO/BOUNDED/gaps-first; **next code slice = `.2h`** (passive-voice + verb-sense direction; hand-rolled, deterministic, offline; reuse existing tokenization/`normative_vocab`/grounding; wire-based 100% hard gate) → `.2f` → `.3`; full ladder `deferred`. (2) **`CANONICAL-PROMOTION-SWEEP`** (NEW tree) — **next = `.1`** pilot on a few NON-wire docs (CHI/NVMe/CCIX class) to lock the RAM-safe per-doc protocol; RAM-HEAVY 14B → run with the watchdog, `ollama stop` before any ingest, ≥85%-used kill; canonical mutation review-gated; wire docs only under the full gold battery. (3) **`CORPUS-PATTERN-REUSE`** — **next = `.3b.3a`** select the first opt-in extractor measurement-first (read-only; candidate = parked `PDF-VARIANT-DIGESTION.9.10` cluster-scoped) → `.3b.3b` build. ALSO open/standing: **`EXTRACTION-QUALITY-GAUGE`** (`.3` COMPLETE `.3a`–`.3e`; FIELD-cell gate #2 = confirmed NO-GO, `.3e` already closed the class, residual needs a forbidden denylist); **`MEMORY-BOUNDED-INGEST`** (Mostly Done/standing, measured-exhausted); **`PDF-VARIANT-DIGESTION`** (`.13` complete; `.9.10` parked → now the CORPUS-PATTERN-REUSE opt-in candidate). Recently-closed: `LLM-PRIMARY-PROMOTION`, `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE`.
- next_action (HANDOFF — owner asked to make SPECFORGE handoff-ready after the 3 levers are tracked): **all 3 levers are now task-tree owned + scoped + ready.** A fresh session picks ANY of the 3 first leaves above (`.2h` / `.1` pilot / `.3b.3a`) and rolls — each tree's Frontier + Decisions hold the full plan. **Recommended order:** start with the LOW-RAM, low-risk ones — `NLP-SHALLOW-PARSE.2h` (pure-Rust, deterministic, real precision win) or `CORPUS-PATTERN-REUSE.3b.3a` (read-only measurement) — and save `CANONICAL-PROMOTION-SWEEP.1` (RAM-heavy 14B) for a session with RAM headroom + full attention to the watchdog. **`cargo build --release` before any live measurement; `scripts/run_ci.sh` before declaring a CODE slice green.** Fresh `target/release/specforge` is post-`.3e` (these 2 slices are docs-only, no rebuild).
- in_flight_uncommitted: the 3-lever ownership/scope slice staged-to-commit (NLP-SHALLOW-PARSE tree + NEW CANONICAL-PROMOTION-SWEEP tree + CORPUS-PATTERN-REUSE tree + TASK_TREE index + CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/MEMORY). Docs-only, NO code. Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`. blockers: **none**.
