# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 30 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`98fdcca4` (`WIRE-BASED-100.5e`), 44 ahead of `origin/main`, NOT pushed** (owner hold; push ~200 or on ask). CI green = **1310 tests**. Disk: reclaimed ~31.8 GiB on `2026-06-07` (cargo clean + normalized; `target` 24G→766M after rebuild). KM 23 facts. `subs/fsmgen` pinned. Local models: `qwen2.5:14b-instruct` (text/NLI) + `qwen2.5vl:7b` (vision); `ollama serve` may be needed. **Owner spec PDFs now in-repo: `corpus/` (5 wire-based, git-tracked) + `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger source library is host-local; owner provides its path on request — do NOT log it (`feedback_source_pdfs_in_repo`).**
- active_work: **`WIRE-BASED-100`** (owner NON-NEGOTIABLE bar: 100% on every WIRE-BASED spec APB/AHB/AXI/SWD — literal bus wires, unlike packet CHI). **APB gold-100% ACHIEVED** (`eval-extraction seed_apb.json --provider skip` → the `source-tolerant + filtered` block = constraints + relations P=R=F1=1.000, per-fact, no faking; `.1`/`.1b`/`.6`/`.6c`/`.7`/`.7c`). Harness thesis: LLM judges, Rust grounds, the document decides — `ir/entity_typing` + `ir/condition_extract` + `ir/constraint_extract_llm` (CHI constraint precision 17%→64%) + `ir/extraction_filters`. Conformal/GriTS UNBLOCKED (NLI-oracle; cross-tool consensus + agent-adjudication). CHI = packet/flit: `DBID`/`TxnID` are FIELDS not signals.
- **`.3a`+`.3b` DONE (`.3b` commit pending):** completeness-gauge accuracy. `.3a` (`07e995a1`): APB CATALOG is **already 35/35** (incl. all 14 `*CHK`, from `table_0004`/`0005`/`0014`); `table_0016/0017/0018` are docling-mangled DUPLICATE views → over-count, fixed via `signal_table_covered_by_inventory` (content-based densest-distinct col, rotation-proof, strict) → misses 5→3. `.3b`: a `normative_statement` whose obligation a typed record already cites is NOT a residual (`uncaptured_normative_statement_ids`) → misses 3→**2** (both genuine: `table_0018` docling-garbage + `statement_0370` honest non-wire EDC). **`.2` was a FALSE alarm** (field-name bug `signal_name` vs `subject_signal`): `statement_0223` PSTRB-LOW IS captured (`dyn_sigcon_0015`, dynamic path); gold-100% already includes it. KM card `apb-signal-catalog-fully-extracted`.
- **`.4` DONE (commit pending): APB now 100% on ALL THREE aspects.** temporal `P=R=F1=1.000` (was 0.333). Root: antecedent select `PSEL` dropped because catalog declares `PSELx`(→`PSELX`), prose says `PSEL`. Owner-delegated **signoff = canonicalize** (one IR identity): `resolve_indexed_signal_family` (un-indexed prose → declared indexed family; index-suffix grammar, ADR-0006, purely additive) + `temporal_clause_value` now takes `known_signals` (a signal token is never a value → bare list member stays value-less, shared value distributes) + corrected temporal gold `PSEL`→`PSELX` (canonical identity, fact unchanged). APB gold preserved; kg-bench green; +2 tests. KM card `indexed-signal-family-canonicalization`. APB: constraints 1.0, relations 1.0, temporal 1.0, catalog 35/35, gauge accurate.
- **`.5a`+`.5b` DONE (`.5b` commit pending): AHB constraint extraction correct.** STALE-EVIDENCE TRAP found: `.5a`'s `P=0.364` measured the PERSISTED AHB evidence (built by pre-fix code; normalized reclaimed → `specforge evidence` errors; AHB PDF absent → no re-ingest). `eval-extraction` loads persisted evidence (only re-runs Nlp tier on a temp copy) → it was scoring stale data. Proven hermetically (isolated extractor + instrumented build = 0 records): current code ALREADY suppresses the list-introducer FPs (APB condition-subject fix covers AHB) — that `.5a` finding was STALE, not current. The ONE real defect: `must not change` → `MustNotChange` AND `negated=true` (double-negative). `.5b` fix: `negated = negated && !matches!(kind, MustNotChange|MustBeDeasserted)` in `extract_signal_constraints`. Current code now extracts all 6 AHB gold facts + 0 FPs → AHB constraints 100% on fresh evidence; demonstrated via 3 `wire_based_100_5b` hermetic tests (eval re-confirmation re-ingest-gated). APB gold-100% preserved; kg-bench green. KM `eval-scores-persisted-evidence`.
- **`.5d` DONE (commit pending): owner directive — copied the 5 wire-based source PDFs into the repo (`corpus/`, git-tracked) + `corpus/SOURCE_PDF_REGISTRY.md` (document_key→repo path). UNBLOCKS re-ingest → fresh cross-spec eval. Owner: only the PDFs I intend to use (not the full library); can add more as needed.** Memory: `feedback_source_pdfs_in_repo` (also: do NOT log the owner's library path — re-provided on request).
- **`.5c` DONE: foundation de-risked + cross-spec blocker surfaced.** Rebuilt APB evidence+semantic FRESH → constraints/relations/temporal ALL still 1.000 (APB milestone is current, NOT stale-inflated). **Rebuildability audit: only APB is rebuildable among wire-based specs; AHB/AXI/AXI-Stream/SWD normalized RECLAIMED + PDFs ABSENT → fresh cross-spec eval is RE-INGEST-GATED.** `.4` index-family resolver generality locked (`HSEL→HSELX` hermetic test).
- **`.5e` DONE (commit pending): AHB constraints REAL 100%.** Re-ingested AHB from the in-repo corpus PDF (`DOCLING_DEVICE=cpu`), rebuilt evidence+semantic, re-ran eval → `signal_constraint P=R=F1=1.000` (tp=6 fp=0 fn=0; content-anchored 9/9). The `.5b` negated fix demonstrated on LIVE data (no longer hermetic-only); corpus-PDF → re-ingest → fresh-eval pipeline proven. AHB constraints now match APB.
- **`.5f` DONE (commit pending): AHB relation gold + baseline.** Added 6 relation items to `seed_ahb.json`; `actor_signal_relation` source-tolerant **`P=0.714 R=0.833 F1=0.769`** (tp=5 fp=2 fn=1). Gap = ACTOR RESOLUTION: `(address, drives, HREADYOUT)` (anaphora "it"=Subordinate on stmt_0311) + `(response it, drives, HRESP)` (garbage actor on stmt_0705); 5 clean relations land. AHB constraints stay 1.000.
- next_action: **`WIRE-BASED-100.5g`** — AHB relation actor discrimination → 100%: reject garbage/common-noun/phrase-fragment actors (`address`, `response it`) + resolve pronoun subject to the real actor (`Subordinate`); reuse `.6`/`.6c` entity-typing harness + heuristic (`ir/extraction_filters`, `ir/entity_typing`). General (ADR 0006), keep APB gold-100% + kg-bench green; re-measure on fresh AHB evidence (re-ingest from `corpus/` if normalized was reclaimed — it was, in the disk sweep, but evidence/semantic survive). Then **`.5h`** AHB temporal gold→100%, then **AXI** (`corpus/.../IHI0022_L_*.pdf`) + **SWD/ADI** (`corpus/.../IHI0074_A_*.pdf`): ingest→evidence→semantic→gold→eval→fix. APB=100% all 3 + AHB constraints=100%. CAVEAT: re-ingest/rebuild before trusting any baseline (KM `eval-scores-persisted-evidence`). Owner (NON-NEGOTIABLE): every score objectively measured + demonstrated PER-FACT — no fake/gamed scoring (`feedback_scoring_rigor`).
- paused_work: none. in_flight_uncommitted: `.5f` gold+docs staged-not-committed (run COMMIT.md next). blockers: none.
