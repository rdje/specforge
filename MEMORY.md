# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`5c2eb319` = `EXTRACTION-QUALITY-GAUGE.3e` (descriptive-field-cell spurious-subject gate)**; **now committing `ROADMAP-TASKTREE-COVERAGE.6`** (post-`.5` ROADMAP↔tree alignment refresh, docs-only); ~123→124 ahead of `origin/main` (session `2026-06-15`, PNT loop owner-authorized unbounded). **THIS-SESSION = 2 slices:** #1 **`EXTRACTION-QUALITY-GAUGE.3e` (committed `5c2eb319`)** — pure `is_descriptive_field_cell_spurious_subject(text, subject)` in `ir/evidence.rs`, wired as `subject_signals.retain(…)` in BOTH `extract_signal_constraints` (`sigcon_*` — all 9 errors) AND `extract_dynamic_signal_constraints`; in a `"this field <descriptive-verb>"` cell a subject NOT occurring (identifier-boundary) BEFORE the marker → DROP (CCIX `SRAM`/`DDR`/`CCIX`/…, NVMe `FFFF`), the field's own mnemonic before it → KEEP. Probe-first (15 rec/2 docs → 9 err + 6 keep); +3 tests; lib 1622→**1625**; run_ci GREEN; kg-bench 156/156; NVMe 21→20, CCIX r1.0a 23→15, corpus residual 0; ALL wire gold 1.000 on gated-Pattern rebuilds; `.3` program now `.3a`–`.3e`. #2 (THIS COMMIT) **`ROADMAP-TASKTREE-COVERAGE.6`** — a grep proved 0/8 headline post-`.5`-lock trees referenced in `ROADMAP.md` while R6/R7 lanes list theirs; added a consolidated "Applied task trees since the alignment lock" subsection (headline programs by lane + pointer to the canonical `docs/TASK_TREE.md` index, no row-duplication per the tree Non-Goal) + refreshed the months-stale `Immediate next milestone`; docs-only (root doc, not in mdBook), memory-arch gate green; umbrella re-CLOSED.
- **RAM-SAFETY (owner, non-negotiable): 90→93% used RAM = reboot DANGER ZONE; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes** (`feedback_ram_ceiling_monitor` / `project_big_pdf_memory_bounded_ingest`). Push ~every 200 commits or on ask (`feedback_push_cadence`).
- **GOTCHA:** per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc/`cargo fmt --check`/the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green (long test lines → `cargo fmt --all`; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). **`cargo test` does NOT rebuild the `specforge` BIN — `cargo build --release` before any live measurement.** `subs/fsmgen` pinned (`c0b7eaa7`).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case; goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English; you CANNOT extract what is not there — honest RESIDUAL over fabrication.
- corpus: **22 git-tracked source PDFs in `corpus/`** (8 prior + 4 serial + 10 from the owner's library `.13a`); keys verified per-item; `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- active_trees (open): **`EXTRACTION-QUALITY-GAUGE`** (`.3` constraint-precision program COMPLETE: `.3a`/`.3b`/`.3c`/`.3d` all done; `.FIELD` residuals; candidate future leaf = a descriptive-FIELD-cell gate for NVMe `must_be_stable` "This field indicates…" rows); **`MEMORY-BOUNDED-INGEST`** (RAM `.1`/`.2` + DISK `.3` + RAM guard `.4a` + disk pre-flight `.4b` + adaptive batch `.4c` DONE; frontier `.5` summary streaming — only if warranted, `.4d`/`.3b` deferred/YAGNI); **`PDF-VARIANT-DIGESTION`** (`.13` sweep COMPLETE incl. `.13c` CHI; serial-class structural levers exhausted `.9.12`; `.9.10` prose bus-line PARKED behind NLP-SHALLOW-PARSE); **`NLP-SHALLOW-PARSE`** (`.2a`–`.3` all `proposed` — OWNER-GATED on build scope per the tree's Open Question; spike verdict lukewarm = consolidation/robustness not recall-revolution); **`CORPUS-PATTERN-REUSE`** (`.3b.3` activate-only consume — needs a FIRST opt-in extractor, design open; wire-based must stay 100%). CLOSED this session: `LLM-PRIMARY-PROMOTION` (`.5` flip). Other recently-closed: `BOOK-COMMAND-COVERAGE`, `FULL-PAGE-INTENT-CAPTURE` (NO-GO).
- next_action (PNT): **`ROADMAP-TASKTREE-COVERAGE.6` DONE — commit it, then PNT-pick the next leaf.** Candidate frontier (owner-ungated; route AROUND owner-gated/review-gated items): (1) `MEMORY-BOUNDED-INGEST.5` summary streaming (verify warranted first — likely a measured DEFER, the guard backstops it); (2) a descriptive-FIELD-cell gate for the SECOND `.3e`-residual shape — BUT per-item analysis showed the `SANICAP`/`ELEN` cells carry a REAL `shall`-obligation (a `.2` condition-capture concern, NOT a spurious subject → do NOT drop), and the non-"This field" `DDR`/`NVDIMM` `must_be_stable` table rows need a NEW structural signal (NO denylist) — so this is a probe→likely-no-clean-gate, lower priority. OWNER-GATED (do NOT presume): `NLP-SHALLOW-PARSE` build scope (tree's Open Question); a flip-consistent corpus-wide canonical promotion sweep (multi-doc canonical mutation = review-gated + heavy 14B). **Remaining clean owner-ungated frontiers are thinning (probe→defer or owner-gated) — if no clean slice remains, surface to owner.** Fresh `target/release/specforge` built post-`.3e` (`.6` is docs-only, no rebuild).
- in_flight_uncommitted: `ROADMAP-TASKTREE-COVERAGE.6` docs-only staged-to-commit (`ROADMAP.md` + ROADMAP-TASKTREE-COVERAGE tree + TASK_TREE index + CHANGES/DEVELOPMENT_NOTES/LIVE_ACHIEVEMENT_STATUS/MEMORY). Only untracked: `.claude/settings.json` (stays) + ignored `.venv-*`. blockers: **none**.
