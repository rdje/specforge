# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 30 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: **`531c10cd` = `PDF-VARIANT-DIGESTION.10e` probe checkpoint (DOCS, landed); the `.10e` BUILD slice (CODE) committing now; 93→94 ahead of `origin/main`** (session `2026-06-11`). Last verified full gate: **`scripts/run_ci.sh` GREEN for `.10e`** (lib **1567**) + kg-bench **154/154**. Push ~every 200 commits or on ask (`feedback_push_cadence`). **GOTCHA: per-slice focused checks (fmt+clippy+`cargo test`) do NOT run rustdoc NOR `cargo fmt --check` NOR the KM derive-and-diff — run `scripts/run_ci.sh` before declaring a CODE slice green / before handoff (long test lines → `cargo fmt --all`; clippy deny `type_complexity` on test view-tuples → local `type` alias; editing a `docs/knowledge/*` card → regen `knowledge-map/scripts/gen_knowledge_map.sh`). NEW GOTCHA (caught live this session): `cargo test` does NOT rebuild the `specforge` BIN — copy a binary from a fresh `cargo build` before any live measurement, or you measure the previous build.** `subs/fsmgen` pinned (`c0b7eaa7`/`d31b0b91`). **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006) — concrete chip names ONLY in gold keys + `#[cfg(test)]`, NEVER in runtime (`feedback_no_hardcoded_chip_spec_names`); NO deny/filter LISTS, case is SOFT, identify by PARTICIPATION not lists/case (`feedback_avoid_denylists_prefer_structural`, `feedback_case_is_soft_not_critical`); goal = #1 structured+multimodal+bounded-LLM intent mining w/ honest residuals, NOT #2 unrestricted-English (`project_goal_structured_not_unrestricted_english`); you CANNOT extract what is not there — honest RESIDUAL over fabrication.**
- this_session (`2026-06-11`, fresh session, PNT continuing): **`PDF-VARIANT-DIGESTION.10e` DONE (probe `531c10cd` + BUILD this commit)** — the byte-location placement family extracts. **THE PROBE OVERTURNED THE RECORDED HYPOTHESIS**: the 60 `byte location | size (bytes) | register description` tables (CCIX-ONLY, 15×4 versions) are NOT register-at-offset maps — 0/60 captions say register, all name in-memory error-record STRUCTURES (`CCIX PER Memory Error Type Structure`, `Vendor-Specific Log Info`) → typed home = `message_field_records` via NEW 3rd strategy `message_fields.byte_location_table` (`.10c` caption-decides; the RO/RsvdZ attribute column does NOT outvote the caption). LITERAL capture: `byte_offset` = stated byte location (unambiguous beside `bit_range: None`, doc amended), `bit_width` = size×8 exact arithmetic (symbolic `(indicated by VenLen)` → None). **Family-LOCAL name grammar — NEVER the shared `recover_field_mnemonic` (it would TRUNCATE `FRU ID`→`FRU`, `CCIX Message`→`CCIX`, `ATC Instance ID`→`ATC`)**: head-before-definitional-frame (`This field/bit/value/register/structure`/`This is`/`All subsequent fields`), trailing-paren mnemonic outranks head AND is trusted past wrapped-cell bleed (`(Chan)` — post-probe decision made during live per-item verification, fixture-locked), period-heads = bleed refused (2 rows), bare-short cells = page-wrapped names. Byte-exact chains (`next_off == prev_off + size`, 31/31; symbolic tails close; fresh restart at 0). **Live (stub protocol, canonical untouched, final post-fmt binary): rev2.0 0→45/6, r1.0 +45/6, r1.0a +36/4, rev1.1 +35/5 (≈161 fields), ZERO pre-existing records changed (record-set diff per doc), only `message_field_records`+manifest keys differ; per-item eyeball clean. 12-doc parity: every surface byte-identical, manifest-only delta. lib 1567 / kg-bench 154 / full CI GREEN.** Residuals: lost-caption chains (Port 7-9 ×4 versions; ATC+LogInfo r1.0a; Cache rev1.1 — re-ingest lever), 2 period-bleed rows, 24 Reserved + 8 wrapped-prose skips (full reconciliation in the leaf). KM `byte-location-structure-field-extraction` + `.10a` card corrected; book `.10e` subsection + `.10a` closing corrected; README `.10e` bullet; RUST_CODEBASE_ANALYSIS 3rd-strategy note.
- corpus: **12 git-tracked source PDFs in `corpus/`** — 8 prior (APB/AHB/AXI/AXI-Stream/SWD-ADI/RISC-V Debug/NVMe/I2C) + **4 NEW serial specs (`.9.1`, `2026-06-08`): CAN/SWP/SMBus/I2S** (`bosch/can`, `etsi/swp`, `smbus`, `nxp/i2s`) + `corpus/SOURCE_PDF_REGISTRY.md`. Owner's larger library is host-local — provided on request, **do NOT log its path** (`feedback_source_pdfs_in_repo`).
- prior_work (DONE, pushed): **WIRE-BASED-100** (APB/AHB/AXI 100% on constraints/relations/temporal) + **SWD-SERIAL-EXTRACTION** 100% (frame/operations/FSM). GOTCHA: eval scores PERSISTED evidence — re-ingest before measuring (`eval-scores-persisted-evidence`); `clean --scope source-normalized` breaks evidence rebuilds → re-ingest.
- active_work: **`PDF-VARIANT-DIGESTION.9`** (serial-protocol class — owner-directed `2026-06-08`; CAN/SWP/SMBus/I2S git-tracked). DONE: `.9.1` import (`c265062b`) · `.9.2`/`3d940f1f` CAN baseline · `.9.3a`/`3d85543c` CAN quoted-mode FSM · `.9.4`/`9942e652` SWP baseline · `.9.7` single-word `<NAME> state` FSM (SWP protocol_states 0→4; +18 corpus docs; SWD 13 states + CAN 3 intact) · `.9.8` SWP prose signals (tree + KM `definitional-signal-capture`). Pattern across `.9`: each new under-extraction is a NEW agnostic grammar, **always probed over all persisted evidence docs BEFORE coding** (ADR 0006). Frontier = `.9.3b`/`.9.9`/`.9.5`/`.9.6`/`.9.8b` (lever backlog, owner may steer). `.6`/`.7` (older VLM/USB-3.2 leaves) blocked on host-local PDFs.
- **HONESTY GUARDRAIL (owner, non-negotiable): you CANNOT extract what is not there** — read the modality where the fact lives or report an honest RESIDUAL; NEVER fabricate a width/bit-range/field/name. APB/AHB/AXI/SWD stay 100%; genericity (`feedback_genericity_guardrail`), multi-strategy (`feedback_multi_strategy_best_wins`), scoring rigor (`feedback_scoring_rigor`). ISF lowering DEFERRED.
- **Tree `EXTRACTOR-ARCHITECTURE` — CLOSED `2026-06-10`, all acceptance criteria met** (owner directive `2026-06-09` "consolidate, unify, as much as possible"; full history in `docs/tasks/EXTRACTOR-ARCHITECTURE.md` + KM `extractor-path-architecture`). End state: `ir/extractor.rs` framework (generic `Extractor<R>` trait + `run_surface` key-merge + `run_surface_concat` + `ExtractionManifest` persisted on `EvidenceIr`, surfaced in `validate`, = the CORPUS-PATTERN-REUSE fingerprint); **EIGHT surfaces registered, every migration 12-doc byte-identical-proven** (FSM `.3` / semantic-hints `.4` / registers `.6` / actors `.7` / serial-frame+operations `.9a` / polarity `.9b` / relations `.9c` — the converge-loop surfaces record per pass, `record()` replace keeps the final converged run); **three-category model** documented in the module (`.9d`): key-merge / concat / stateful-assembly (signal seed + constraint family OFF the drivers BY DESIGN — cross-surface id counter); `.10a` extracted the 243-line statement/visual assembly block (`assemble_evidence_statements`) → **`build_with_prior_memory` 449→224 lines, god-orchestrator RETIRED, FULL byte-identity incl. manifest**. Book: `quality/validation.md` tree-level subsection. lib 1499.
- **NEW active tree `CORPUS-PATTERN-REUSE`** (owner directive `2026-06-09`): reuse extraction patterns across PDFs, clustered by a DERIVED vendor/layout fingerprint (ADR 0006 — no hardcoded vendor names), advisory (adjusts attention not truth), extending the proven `CorpusMemory` plane. `.1` design DONE. KEY INSIGHT: the `EXTRACTOR-ARCHITECTURE` `ExtractionManifest` IS the per-doc behavioral fingerprint to cluster on → the extractor framework is the FOUNDATION. **`.2` DONE** (pure `ir/corpus_cluster`: `document_fingerprint` = structural count-buckets + `.8` manifest fired-set; Jaccard; deterministic greedy `cluster_documents`). **`.3a` DONE — the `corpus-cluster` command** (`commands/corpus_cluster.rs`, read-only/additive, no extraction-path change): walks `generated/evidence_ir/*/evidence_ir.json`, fingerprints+clusters, prints emergent families+singletons; `--evidence-root`/`--threshold` (0.6); **live 78 docs → 28 clusters / 14 multi-doc / 0 skipped, NO vendor list** (3 CoreSight SoC-600 versions, 7 AMBA specs, AMD-IOMMU/GIC/eMMC). **`.3b` owner-go'd + split → `.3b.1` DONE + `.3b.2` DONE:** `.3b.1` = typed `ClusterExtractionProfile` + pure `derive_extraction_profiles` surfaced in `corpus-cluster`. **`.3b.2` = the 8th prior family persisted**: `CorpusMemory` schema 5→6 (`#[serde(default)]`, v5 stores loadable), `ExtractionProfilePriorRecord` (signature-keyed, deliberately NOT ProtocolFamily-scoped — ADR-0006 signature IS the scope), strict signature-subset lookup `extraction_profile_priors_for` (empty sigs skipped), `learn-priors` harvest via accepted artifacts' EvidenceIR fingerprints at NEW shared `corpus_cluster::DEFAULT_FINGERPRINT_SIMILARITY_THRESHOLD=0.6` (also the `corpus-cluster --threshold` clap default — no drift), **multi-member clusters ONLY** (singleton = no cross-doc pattern). Live: 10 IntentIR artifacts → 2 profiles (support-5 TileLink×2+I2C+HBM2+GFB; support-2 CXS+Wishbone), `fired_extractors` honestly empty (pre-manifest evidence). **Decision: consume is ACTIVATE-ONLY** (recorded). CAVEAT: `fired:` tokens mostly absent corpus-wide → re-ingest/evidence-rebuild sweep = the data lever. KM `corpus-cluster-fingerprint` (extended). Frontier: `.3b.3` (activate-only consume via `applies_to`; NEEDS a first opt-in extractor — none exists; wire-based APB/AHB/AXI/SWD must stay 100%) → `.4` (offline LLM/VLM cluster miner, gated).
- **NEW finding/tree `EVIDENCE-DETERMINISM`** (surfaced by `.5` verification, `2026-06-09`): the EvidenceIR build is **non-deterministic at the CONTENT level** — two runs of identical code produce set-different `actor_signal_relations` + `extracted_statements` on SWD/ADI (relation-heavy prose doc). Pre-existing (NOT a refactor regression). Undermines reproducibility, eval-score stability (`feedback_scoring_rigor`), and the byte-identical verification methodology. **`.1`+`.2` DONE (tree CLOSED) — owner picked (A).** Two `HashSet`-iteration leaks fixed in `ir/evidence.rs`: (1) `extract_actor_signal_relations` iterated `known_signals: &HashSet` → sort once before the loop (relation key-set invariant, ids/order now stable); (2) `derive_encoding_enum_name` sorted by `Reverse(len)` only → total order (len, then name). **ALL 6 intact-bundle docs now double-run BYTE-IDENTICAL** (was: SWD content-different); SWD eval 1.000; kg-bench 151/151; +1 regression test (HashSet built twice → identical); lib 1455; multiset-preserving. Every future byte-identical migration proof is now sound. ANTI-PATTERN (KM `evidence-build-nondeterminism`): never let HashSet/HashMap iteration order reach output — sort / total-order / BTreeSet.
- **NEW active tree `NLP-SHALLOW-PARSE`** (owner-directed `2026-06-09`: build SpecForge's deterministic in-Rust **NLP arm** — a shallow subject-verb-object parser feeding the EXISTING grounding gates; SpecForge = typed-IR orchestrator over NLP+VLM). Owner asked to (a) own each of the ~9 pipeline components as leaves [DONE: `.2a` tokenizer · `.2b` segmenter · `.2c` POS · `.2d` lemmatizer · `.2e` chunker · `.2f` clause/coord · `.2g` negation/modality · `.2h` passive/verb-sense · `.2i` grounding gate · `.3` integrate] and (b) download tools (careful w/ size). **`.1` spike DONE** — measured general SVO (hand-roll + a real nltk statistical tagger, in a 54MB throwaway `.venv-nlp`, since DELETED) vs the hand grammars on real APB/AXI/SWD/SWP: **generic SVO recovers only 3–8% of the hand grammars' recall**, hand-roll→nltk jump ~0 (POS quality NOT the bottleneck; recall lives in TABLES + domain constructions); grounding gate neutralized the tagger-choke risk. **Honest verdict: shallow-parse = a CONSOLIDATION + unseen-phrasing-robustness play, NOT a recall revolution**; bigger "digest any PDF" lever stays the table-kind classifier + VLM arm. Tooling = grounding-first/model-last: hand-rolled Rust + ≤2 tiny crates (`rust-stemmers`/`unicode-segmentation`); REJECT spaCy/BERT/nlprule (size+determinism). `project_nlp_shallow_parse_direction`.
- PNT (owner said "pick the next tree and roll"): **all 4 serial-class baselines DONE (CAN/SWP/SMBus/I2S — each an ⚠ UNDER-EXTRACTED spec that under-extracts DIFFERENTLY, the `.5a`/`.5c` machinery flags each correctly).** This session also: `.9.10` PROBE (prose bus-line signals — Form A "the `<NAME>` line" locked, but it captures power rails on the MEASURED I2C doc; owner forbade a denylist → PARKED behind participation-identity / the shallow-parser) + **`.9.11` DONE (CODE): header-trapped timing-table recovery — I2S timing_constraints 0→5, SMBus held 84, structural (no list/no case), nested tables stay honest residuals; lib 1475, kg-bench 151/151.** Method for every lever: probe over all persisted docs BEFORE coding (`.9.7`/`.9.8`/`.9.11`), structure-not-lists, residual over fabrication. wire-based APB/AHB/AXI/SWD 100% preserved; ADR 0006 + the no-list/case-soft doctrine.
- next_action (PNT continues): **NEXT PICK = `PDF-VARIANT-DIGESTION.12` (header-trapped SIGNAL tables — the `.9.11` lever's signal sibling; census probe DONE this session, full plan in the leaf).** 21 real tables / ~487 trapped signal tokens across AXI (12 tables/306 — the B2.2/B2.3 presence matrices on the GOLD doc), ACE (5/134), LTI (2/44), APB (1/3 — the exact WIRE-BASED-100.3 `table_0018` residual). Remaining probe per-item BEFORE building: new-vs-declared diff against canonical inventories (resolve the right artifact key — `interface_signals` read 0), gauge-ledger state for the 21 tables, typed-capture decision (duplicates → coverage-only, never minted records; presence-CONDITION intent measured), `Continued` fragment chaining. GOLD-SAFETY BAR: AXI/APB promoted + standing gauges — additive only, golds re-measure at documented states. After `.12`: full survey done this session — everything else is OWNER-gated (LLM-PRIMARY-PROMOTION.5 flip · CORPUS-PATTERN-REUSE.3b.3 · NLP-SHALLOW-PARSE.2a go/no-go) or host-local-blocked (re-ingest sweep; EXTRACTION-QUALITY-GAUGE.3c needs CHI re-provide). PROTOCOL RULE (2× burned): restore the Pattern baseline BEFORE any promotion measurement. Canonical state: APB+AHB+AXI ALL PROMOTED + gates green; 12 swept docs carry standing Pattern gauges (canonical, by design). Other open: LLM-PRIMARY-PROMOTION`.5` flip OWNER-GATED (packet in `docs/tasks/LLM-PRIMARY-PROMOTION.md`, recommendation FLIP) · `CORPUS-PATTERN-REUSE.3b.3` (constraints.llm_primary = first opt-in extractor candidate; intersects owner-gated flip — ask owner) · `NLP-SHALLOW-PARSE.2a` (owner go/no-go). PROTOCOL RULE (2× burned): restore the Pattern baseline BEFORE any promotion measurement. Canonical state: APB+AHB+AXI ALL PROMOTED + gates green; 12 swept docs carry standing Pattern gauges (canonical, by design — also why `.10b` did NOT rebuild canonical NVMe). RESIDUALS: field-PRESENCE kind; CHI + CCIX/CoreSight + AMD re-ingest (host-local, do NOT log paths); validate integration of `message_field_*` deferred; byte-location placement tables (register-at-offset lever, future leaf); NVMe lost-caption dword tables (7, capless residual).
- paused_work: **`PDF-VARIANT-DIGESTION.9.10` (prose bus-line signals) PARKED behind `NLP-SHALLOW-PARSE`** (owner: no denylist; needs participation-based identity); serial-class structural levers EXHAUSTED (`.9.12`). in_flight_uncommitted: **`PDF-VARIANT-DIGESTION.12` census checkpoint staged (DOCS)** — new pending leaf `.12` in the tree (census + remaining-probe plan + gold-safety bar) + this MEMORY — committing now. `.10e` landed `3a8d0eaa`; /tmp verification artifacts + parity worktree cleaned. Only untracked: `.claude/settings.json` (stays) + ignored `.venv-eval`/`.venv-docling`. blockers: none (LLM-PRIMARY-PROMOTION.5 flip + NLP-SHALLOW-PARSE go/no-go need the OWNER). **REPO HANDOFF-READY once this commit lands.** RESUME: read `README.md`+`MEMORY_ARCHITECTURE.md` then this pointer → PNT per next_action; standing lesson: per-item diffs catch what aggregates mask — and never measure with a stale binary.
