# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> The bounded **resume pointer**: it describes *now*, not the journey. Overwrite the "Current state"
> block below; never append session narration. How to resume — bootstrap order, doctrine, gates, and
> retrieval — is `AGENTS.md`; the layer contract is `MEMORY_ARCHITECTURE.md`. Everything else is derived
> on read: revision from `git rev-parse HEAD`, work state from `docs/tasks/`, history from `git log`.

## Current state (OVERWRITE this block each update — do not append)

- Active unit: **`EXTRACTION-QUALITY-GAUGE.3k.2k` CLOSED `2026-09-13` (CODE)** — **a predicate that states a RELATION is not a value.**
  `.3k.2b` gated the value slot on ONE shape (the passive participle) and an adjective wears no participle ending, so `shall be unique
  within the NVM subsystem` published `must_be_value UNIQUE`. The discriminator is positional, not lexical: a state is complete at the
  predicate, a relation names its operand or scope with a preposition immediately after it (`by` excluded — it marks an AGENT). One scan
  returns the value AND that next word, so the two questions cannot read different binder occurrences. `replay-constraints` is
  **identical across the change — 183/263/137/126 over all 77 loadable documents** — so nothing rebuilt.
- **It caught a fabrication one leaf back, and that is the transferable lesson.** `.3k.1`'s control asserted *"The value of PRANGE must be
  greater than 0"* still yields "a value binding". The record it pinned was `must_be_value GREATER` — the comparative in the value slot,
  with the literal `0` nowhere in it. **A control that asserts a record EXISTS pins whatever that record says, fabrication included.**
  Corrected to pin `.3k.1`'s own gate verdict; `.3k.1`'s node carries the amendment.
- Next action: **`EXTRACTION-QUALITY-GAUGE.3k.3`, whose prerequisite is now satisfied and whose whole addition measurement is already in
  its node** — do not re-derive it. One record per obligation clause: **+22 records, −2 (NVMe `sigcon_0005`/`0006`, this leaf's own
  population), 0 other persisted records lost** over the 74 comparable documents. The design took two corrections that the node records:
  the whole-statement subject FALLBACK is a cross-clause leak (AXI minted `ACADDR must_be_asserted` from a clause whose subject is the
  pronoun `it`), and a FRONTED condition empties the subject part (`text_before_condition_marker` cuts at offset 0), which is why the
  fallback fires at all. Both fixes are in the node. Two open questions there too: APB's row/statement duplication, and eMMC's
  `PARTITION\_ACCESS` escaped-underscore fragment.
- In-flight uncommitted: none after this commit.
- Blockers: none. Push cadence is **400** commits per director directive `2026-09-13` and is FIXED there, so no push is due at 232;
  directive 16 still gates it on full CI. `scripts/check_doctrines.sh --all` did not finish in 50 minutes — `CHAIN-CURRENCY` re-executes
  the real pipeline for every persisted artifact across four stages. Budget hours, not minutes, and run it detached. Standing hazards:
  **an evidence-stage change that MOVES a current-schema artifact's content stales its proof and it then refuses to LOAD** — measured this
  session: `.3k.2k` moved nothing and all 77 loaded; the `.3k.3` prototype moved AHB/AXI-L/APB-E and all three dropped out. Rebuild
  (`evidence → validate → semantic → validate → intent → validate → adapt`, each validated exactly once, upstream-first); only those three
  have held-out bundles (`generated/preserved/WIRE-BASED-100.10/`): restore, rebuild, `diff -r`, remove, retention back to 24.
  **Run `replay-constraints` before sizing any extractor change** (`[[persisted-census-measures-published-not-current]]`) and read
  `row_stratum_unjudged_documents` with it (`[[legacy-source-classifications-are-neutralized-on-load]]`). **A census counts the population
  of the FUNCTION being changed** (`[[constraint-record-producer-strata]]`) and **never from a mirror**
  (`[[one-modal-vocabulary-per-constraint-record]]`). **A Rust change moves `flow_census.json` and the re-derivation must ATTRIBUTE the
  delta to the owning leaf**; a new module moves `module_inventory.tsv`; a new command `CLI_SURFACE_REGISTRY`; a new raw-evidence reader
  `information_flow_boundary.tsv` — all fail closed. **Editing a live surface sets off a fixed bookkeeping chain** —
  `[[live-surface-edit-bookkeeping-chain]]`, and it is exact: fact-card catalog `--print-plan` into `planned_outputs` then `--write`, the
  `fact-card-catalog-count` assertion plus its INDEX line-3 region, the `shipped_behavior` aggregate authority, the three
  `surface_registry` pins, then the claim digests — and the claim IDs that move ARE the `Published-claims:` declaration.
  **A book section about an extraction rule goes in its concern's chapter, not the stage chapter**, and a changed rule leaves standing
  book text: `.3k.2k` had to correct a table row asserting the old behaviour. **The doctrine driver runs no cargo gate**
  (`[[doctrine-driver-runs-no-cargo-gate]]`). Never run the fixture suite with the locality gate (`SCRATCH-RESIDUE-CONTAINMENT.4`).
