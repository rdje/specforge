# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (memory system), `DOCTRINE_ENFORCEMENT.md` (the 4th
  architecture — doctrines are mechanically gated), and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Before committing run `scripts/check_doctrines.sh` (the registry/driver: memory-arch +
  knowledge-map + task-acceptance); hooks + CI run it too. Retrieval: `KNOWLEDGE_MAP.md`.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`78274560`** `KG-ISF-COMPLETENESS.5 — measure the generic-TABLE mega-enum conflation`. **67 ahead** of `origin/main` (push at ~200 → HOLD); this `KG-ISF-COMPLETENESS.5.i` commit → **68 ahead**. Untracked (stays): `.claude/settings.json`, `.cache/` (gitignored symlink).
- **MODE: PNT loop** (this conversation re-ran the full README bootstrap `2026-06-24`: read entry docs + `docs/TASK_TREE.md` + KG-ISF-COMPLETENESS tree + the `.5` decision packet; doctrines GREEN). This conversation's slice: **`KG-ISF-COMPLETENESS.5.i`** (the high-stakes enum gate-code flagged for a fresh session — and this was one).
- **✅ `KG-ISF-COMPLETENESS.5.i` DONE (`2026-06-24`, CODE) — the `.isf` no longer emits generic junk-named / cross-table-conflated enums.** Two ADR-0006-structural edits: (1) `derive_encoding_enum_name` fallback (`ir/evidence.rs`) keeps the candidate **only when independently evidenced** (a declared signal OR a column-header reference token of the table) else `None` → kills the generic enums + the `build_symbol_definitions` merge-by-name conflation; (2) emitter (`ir/isf_ir.rs`) gates the `(types)` block by `emitted_enums()` (no orphan `(type)`). **Measured:** corpus census (33 rebuildable docs) generic-named enums **82→8** / total enum records **422→105** (8 survivors = document-evidenced column-header tokens like CCIX "Table of Contents" → `.5.ii` residual); real signal enums byte-identical; every affected `.isf` FSMGen `--strict` 0 diagnostics. WIRE-BASED-100 = **1.000 before==after** (PROVEN via before/after eval); nvme/i2c golds identical; `kg-bench` 156/156; `run_ci.sh` GREEN (lib **1712**, +4 tests). Two `.5` corrections: real enums are signal-match-origin (byte-identical), and the gate also cleans off-gold junk constraints from dropped-enum `discovered_values` (score-orthogonal). Report §`.5.i LANDED`; KM `[[generic-enum-conflation]]` → LANDED.
- next_action (PNT): **`KG-ISF-COMPLETENESS.5.ii`** — the per-table MEMBER-QUALITY gate (calibration-gated; needs its own precision/recall measurement BEFORE code). Residual now precise: the **8 document-evidenced generic survivors** (CCIX `TABLE` still 27 conflated junk members; gic_600/coresight `DATA`; AXI+ACE `CACHE`/`ENCODING`; SMMU `ATTRIBUTES`) + signal-match-origin junk-member enums (real signal name, fragment/restarting members). At `synthesize_encoding_declarations_for_enum` (`evidence.rs`) refuse an enum whose name-column holds sentence fragments / whose value sequence restarts — threshold MUST be measured per-item ([[feedback_scoring_rigor]]). Other open leaves are deferred (`.1c.ii` upstream-NLP-gated, `.2b` measured-marginal).
- in_flight_uncommitted: **none** once committed (code: `ir/evidence.rs` fallback gate + 3 tests, `ir/isf_ir.rs` types-block gate + strengthened test; docs: research report §`.5.i LANDED`, KM card + `KNOWLEDGE_MAP.md` regen, `KG-ISF-COMPLETENESS.md` checklist/nodes/changelog, README/CHANGES/DEV_NOTES/LIVE_ACHIEVEMENT/book `pipeline/isf-adapter.md`/MEMORY). blockers: none. **Repo HANDOFF-READY.**
- **OPERATIONAL REALITY:** **the release binary `target/release/specforge` now carries the `.5.i` gate (rebuilt `2026-06-24`)** — re-emitting any persisted doc's `.isf` will now drop its junk enums (the persisted `generated/` `.isf` are pre-`.5.i`); a corpus `adapt` refresh is available but `generated/` is git-ignored (durable trace = this commit). fsmgen = Perl `subs/fsmgen/bin/fsmgen` (NOT cargo); `--check` needs `--json`. Host RAM headroom OK this session. Artifact-cleanup standing (≥24h). lib test count is now **1712** (was 1709; RUST_CODEBASE_ANALYSIS.md count is AUDIT-DOC-RECONCILE-owned, not refreshed in this slice — non-architectural change).
- **DOCTRINE (non-negotiable):** RUNTIME PDF-AGNOSTIC (ADR-0006), no deny/filter lists, honest RESIDUAL over fabrication; a missing ISF abstraction → verified FSMGen FR, never an emitter hack ([[feedback_isf_no_hacks]] / [[feedback_verify_fsmgen_before_fr]]). `scripts/check_doctrines.sh` green before committing; `scripts/run_ci.sh` green before committing CODE. Push ~every 200 (HOLD).
