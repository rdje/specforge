# MEMORY — resume pointer (layer A of `MEMORY_ARCHITECTURE.md`; overwrite-only, keep small)

> This file is the bounded **resume pointer**. It describes *now*, not the journey.
> History lives in `git log` + `CHANGES.md`; work state lives in the task-trees
> (`docs/tasks/`); durable facts/decisions live in `docs/decisions/`. Do **not** append
> session narration here — overwrite the "Current state" block instead.
> (The prior ever-growing MEMORY.md is preserved in git history before commit `6d668eb2`.)

## How to resume (any AI, any harness)
- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- All work is tracked in task-trees under `docs/tasks/` (index: `docs/TASK_TREE.md`);
  follow `COMMIT.md` after every slice (unit id in the commit subject).
- Non-negotiable doctrine: see `docs/decisions/0003-task-tree-and-commit-doctrine.md`
  (no code change without an owning task-tree first; signoff quality; zero
  ROADMAP↔code↔mdBook drift; push ~every 200 commits; artifact cleanup ≥ every 24h).
- Durable cross-cutting facts: `docs/decisions/` (e.g. Docling CPU device 0001,
  LLM/VLM provider default 0002); retrieval index: `KNOWLEDGE_MAP.md` (grep before re-deriving).

## Current state (OVERWRITE this block each update — do not append)
- latest_commit (baseline): **`3e0cc57c`** `CORPUS-COVERAGE.0 — build every ingested doc to IntentIR/.isf (36->78 intent, 0 failures)`. **A `CORPUS-COVERAGE.1` CODE commit is being created now** (stage-staleness `validate` detector). **179 ahead** of `origin/main` (→ 180 after). Tree clean except untracked `.claude/settings.json`.
- **MODE: PNT loop** (fresh session `2026-06-17`; open-ended). **Owner pushed back on "buildable frontier exhausted" (`2026-06-17`): SpecForge is NOT complete (`[[feedback_not_complete_attack_substantive_gaps]]`) — PNT-ing through 3 SUBSTANTIVE north-star gaps to EXHAUSTION, stop only for fresh-session handoff: #1 relation-incompleteness (DONE), #2 corpus coverage (`.0`+`.1` DONE), #3 transaction body-emission (NEXT).** Slices this session: `GRITS-CROSS-TOOL.4` (`d302e24b`) → `KG-ISF-COMPLETENESS.3` gap#1 (`4abdc924`) → `CORPUS-COVERAGE.0` gap#2 build-out (`3e0cc57c`) → **`CORPUS-COVERAGE.1` stage-staleness detector (THIS commit, CODE)**.
- **`CORPUS-COVERAGE.1` DONE `2026-06-17` (CODE — THIS commit):** the durable north-star win from gaps #1/#2. `validate <intent-ir>`/`<semantic-ir>` now emits a `stage_staleness` Warning (`intent_stale_relations_dropped`/`semantic_stale_relations_dropped`) when the downstream carries **0 `actor_signal_relations` while its upstream (loaded via the carried `semantic_ir_path`/`evidence_ir_path`) carries some** — the silent tilelink 39→0 class. **Zero-versus-some** → false-positive-free (the agent-identity gates re-attribute/merge but never empty a non-empty set; a register/command doc's honest 0 relations has an empty upstream too → silent); upstream I/O paid only when empty (`let`-chain); skipped when off-disk. Pure helper `stage_staleness_relation_finding` + 3 unit tests; **verified live** (release, temp-CWD to dodge WRITE-PATH GOTCHA): positive fires (0-vs-39), nvme (0-vs-0) + healthy silent, canonical untouched. ADR-0006; WIRE-BASED-100 unaffected by construction; **`run_ci.sh` GREEN (lib 1660, +3); `kg-bench` 156/156**. Book `quality/validation.md`; KM `stage-staleness-validate-detector`. (Process lesson logged: `cargo test --lib` does NOT rebuild `target/debug/specforge` — always `cargo build` before a live CLI check.)
- next_action: **CONTINUE PNT → gap #3 (the last named gap): transaction body-emission.** Gaps #1 (`.3`) + #2 (`CORPUS-COVERAGE.0`+`.1`) fully landed. Gap #3 = `KG-ISF-TRANSACTIONS` composed transactions are recognized (`.2a`–`.2j`) but per-phase bodies don't emit ("FSMGen-parked"). **Measurement-first, FSMGen-contract re-check FIRST** (`[[feedback_verify_fsmgen_before_fr]]`: `subs/fsmgen` is already at `030f8c273` = `FSMGEN-REFRESH-INTEGRATE-3`, refreshed to pick up FSMGen's ANSWER to the transaction-phase-membership question — read `subs/fsmgen/docs/book/src/13*` + EMPIRICALLY `--strict --check` whether composed per-phase transaction bodies lower). Read `docs/tasks/KG-ISF-TRANSACTIONS.md` for the exact parked state. **WIRE-BASED-100 a hard gate — do NOT disturb APB/AHB/AXI/SWD without re-verifying.** This is a DESIGN-HEAVY slice — a fresh session may serve it best (see handoff note).
- **NORTH STAR (owner, `2026-06-16`, `[[project_kg_isf_completeness]]`):** KG/IntentIR must be COMPLETE (all real agents, relations, constraints, behaviors) so it lowers FAITHFULLY to ISF — *"extracting for extracting is not the goal."* Checkable 6-point bar; WIRE-BASED-100 a hard gate. **TRANSACTION directive (`[[project_kg_isf_transactions]]`)** elevated to critical-path; `KG-ISF-TRANSACTIONS.2a→.2j` done (body-emission FSMGen-parked).
- **RAM-SAFETY (owner, non-negotiable):** 90→93% used RAM = reboot DANGER; monitor every background job + swap, autonomously kill at ≥85% used; serialize Docling vs the 14B model (`ollama stop` before ingests); QUALITY INVARIANT under restriction — only SPEED flexes (`[[feedback_ram_ceiling_monitor]]` / `[[project_big_pdf_memory_bounded_ingest]]`). This slice was RAM-safe (Rust build + run_ci + kg-bench, no 14B/Docling; RAM 72–77% free). Push ~every 200 commits or on ask (179 ahead → HOLD).
- **DOCTRINE (non-negotiable): RUNTIME code is PDF-AGNOSTIC (ADR-0006)** — concrete chip names ONLY in gold keys + `#[cfg(test)]`; NO deny/filter LISTS (structural gates); honest RESIDUAL over fabrication. `scripts/run_ci.sh` before declaring any CODE slice green; `cargo build --release` before live measurement. **WRITE-PATH GOTCHA:** commands persist by the artifact's `artifact_layout` (canonical path), NOT the input path; for non-destructive measurement build into a TEMP evidence-root (symlink canonical `generated/source_ir` in + run the release binary from a temp CWD so the RELATIVE output root redirects). `subs/fsmgen` pinned **`030f8c273`** (`FSMGEN-REFRESH-INTEGRATE-3`).
- in_flight_uncommitted: **a `CORPUS-COVERAGE.1` CODE commit is being created now** — `crates/specforge/src/commands/validate.rs` (helper + 2 `let`-chain guards + 3 tests) + `docs/tasks/CORPUS-COVERAGE.md` (`.1`→done) + `docs/knowledge/stage-staleness-validate-detector.md` (new KM card) + `KNOWLEDGE_MAP.md` (regen) + `docs/book/src/quality/validation.md` (book note) + `README.md` (state bullet) + `docs/TASK_TREE.md` (row) + `CHANGES.md` + `DEVELOPMENT_NOTES.md` + `LIVE_ACHIEVEMENT_STATUS.md` + this MEMORY. Untracked (stays): `.claude/settings.json`, `git_message_brief.txt`. blockers: **none**. **Repo HANDOFF-READY.** Continue PNT → gap #3 transaction body-emission (design-heavy; FSMGen-contract re-check first; **good fresh-session boundary** if context is long). HANDOFF: gaps #1+#2 fully landed + the durable stage-staleness guard; only gap #3 remains of the owner's three.
