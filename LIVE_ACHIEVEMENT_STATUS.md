# LIVE_ACHIEVEMENT_STATUS
## Current snapshot
- ✅ **`ACTIVE-TASK-EVIDENCE-CONTAINMENT.1.1` (`2026-08-09`) — Done: every active-task information role and consumer is measured.** Thirteen non-overlapping regions close all 2,393 lines / 222,616 target bytes; the nominal frontier alone is 1,392 lines / 124,201 bytes because it stores `.9`–`.13` activity history. The census classifies 32 exact-path and 89 identifier-reference inputs, 18 book citations, 24 fact cards, every generic reader, and the manual writer; no executable target access or fragment-sensitive link exists. Verified commits conflict with stale open headers and formal pending `.6`/`.7` are narratively blocked, so no eligible PDF leaf is inferred. Target unchanged. Frontier → `.1.2` active topology decision.
- ✅ **`ACTIVE-TASK-EVIDENCE-CONTAINMENT.0` (`2026-08-09`) — Done: active containment is owned before the target changes.** The untouched `PDF-VARIANT-DIGESTION` source is pinned at 2,393 lines / 222,616 bytes / max 191, SHA-256 `9284dce4…a19d4`, Git blob `7d89ea4f…b555`, 70 path commits, and only 207 bytes before warning. The legacy frontier names `.9.3` active despite child/later completion evidence and several node headers disagree with their bodies, so `.1.1` owns an explicit current/history and reader/writer census before topology or target edits. No product/target/artifact/threshold/ceiling change. Frontier → `.1.1`.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.10b.ii` (`2026-08-09`) — Done: terminal task evidence is migrated and the program is closed.** The `05d80d29` source is preserved byte-for-byte in a 2,538-line / 242,172-byte capsule at `f8e10e…96b68`. The stable task path is now a 119-line / 7,978-byte closed summary over a 22-line / 812-byte three-link index and exact manifest. Migrated enforcement verifies identity, closed status/frontier, routes, provenance, milestones, ceilings, and immutable retrieval; 15 cases pass. `.10` and the entire adoption program are closed with no product/canonical/shared-input/ceiling change. Next → create active PDF task containment ownership before any PDF append.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.10b.i` (`2026-08-09`) — Done: the exact pre-migration source is mechanically locked.** A neutral `source_locked`/`migrated` checker now runs through `LIVE-DOC-SIZE`; 15 cases reject source/capsule identity drift, unsafe or premature paths, active compact roots, missing/misordered routes, manifest/provenance drift, and independent overflow. The complete source is 2,538 lines / 242,172 bytes / max 746 at `f8e10e…96b68`, with 8,503 bytes of pre-rollover headroom, while every declared archive path is absent. No content moved or product/surface/threshold/ceiling changed. Frontier → `.10b.ii` exact copy and bounded closure.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.10a` (`2026-08-09`) — Done: the terminal task-evidence boundary is designed.** The source is 2,451 lines / 232,649 bytes with 18,026 bytes of pre-rollover headroom; 38 commits, recent 49.7-line / 5,370-byte average growth, four exhaustive semantic regions, and 19 readers select ADR 0018. `.10b.i` commits an exact source/verifier boundary; `.10b.ii` copies it byte-for-byte and leaves a bounded closed root/index/manifest. No topology/executable/threshold/ceiling/product change. The active PDF-variant tree is separately blocked at 222,616 bytes / 207 before warning until new containment ownership. Frontier → `.10b.i`.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.9b` (`2026-08-09`) — Done: the rolling-ledger route is partitioned and fail-closed.** A fixed 34-line / 1,512-byte landing routes four complete indexes totaling 37 lines / 1,378 bytes. Four unchanged-control manifests preserve the former nine data rows byte-for-byte; all four registry plus 12 surface consumers use their ledger authority, immutable members are unchanged, and the old shared manifest is absent. Registry v2 plus 24 focused and 55 lifecycle/overflow cases reject foreign members, broken/duplicate/missing edges, cycles/disconnection, wrong/missing links/order, overflow, and residue. `.9` closed with no threshold/ceiling/product change. Frontier → `.10a` task-evidence containment design.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.9a` (`2026-08-09`) — Done: the bounded archive-route design is accepted.** The shared index is 81 lines / 5,311 bytes with 218 bytes before rollover; its last direct route needed 301 bytes. Exact block, manifest, consumer, live-window, and 28-segment capacity measurements select ADR 0017's stable four-ledger landing plus bounded per-ledger indexes/manifests under unchanged controls. The audit also found that predecessor/successor scalars are not yet enforced as one reciprocal, acyclic, complete chain. No archive, consumer, executable, threshold, or ceiling changed. It separately exposes this task file at 229,064 bytes / 82.2% health and opens `.10a`/`.10b` before rollover. Frontier → `.9b` atomic route and chain/index verification.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.8g` (`2026-08-09`) — Done: the independent resulting-tree audit closes `.8`.** Cold JSONL enumeration proves 14 primaries + three secondaries and 17/17 once-only markers; Cargo/README/book/CI and Git-index/feedback/JSON agree. No live FSMGen hash or route literal remains in executable source. Historical occurrences are revision evidence; Rust `1.95.0` script values are self-contained temporary contracts with no repository-authority coupling. All 47 + 25 + 11 focused cases, six doctrines, 1,779 tests / five ignored, rustdoc, mdBook, and locality pass. No product/artifact/data/donor/submodule/surface/threshold/ceiling change. Frontier → `.9a` bounded archive-route design.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.8f` (`2026-08-09`) — Done: the feedback self-test derives every synthetic current literal from data.** `render_current_root` now expands the contract's complete `current_root.required_literals` array and knows no role, position, hash shape, or live value. A new eleventh case replaces both literals with unrelated fixture strings before rendering and validation; the live FSMGen hash is absent from executable source. Real protocol check/report, 11/11 protocol cases, all 47 + 25 derived-state mutations, six doctrines, 1,779 tests / five ignored, rustdoc, mdBook, and locality pass. Canonical feedback/product/artifact/donor/submodule/threshold/ceiling state is unchanged. Frontier → `.8g` independent closure audit, then `.9`.
- 🔎 **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.8e` (`2026-08-09`) — Audit complete; `.8` closure refused on a stored self-test pin.** Data-only enumeration confirms 14 primaries + three secondaries and 17/17 once-only markers; the neutral checker is project-agnostic and the adapter follows alternate declarations without fallback. All 47 + 25 derived-state cases, feedback-protocol 10/10, six doctrines, 1,779 tests / five ignored, rustdoc, mdBook, and locality pass. A repository-wide value/consumer scan nevertheless finds the live FSMGen hash embedded in `render_current_root`; its synthetic root is validated against the live contract, so this is update-coupled executable state rather than an independent fixture. Frontier → `.8f` derive all required literals from data, `.8g` re-audit, then `.9`; no product/artifact/donor/submodule/threshold/ceiling change.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.8d` (`2026-08-09`) — Done: every adapter secondary copy is data-owned and no fallback path remains.** The existing 14 primary contracts now declare exactly three nested secondaries: mdBook Rust is a governed surface copy; CI Rust and the feedback JSON pin are control copies. Neutral validation owns closed roles/ownership, same-volume regular paths, surface membership, non-Markdown control identity, and exact markers. The adapter contains none of the three paths and alternate declared paths pass while old locations are corrupted. All 47 neutral + 25 adapter cases, six doctrines, 1,779 tests / five ignored, rustdoc, mdBook, and locality pass. Fact index 32,643 bytes / 535 before rollover. Frontier → `.8e` independent audit; `.9` remains the archive-route boundary.
- 🔎 **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.8c` (`2026-08-09`) — Audit complete; `.8` closure refused on a data-plane completeness defect.** All 14 primary contracts enumerate as 1 derive-on-read / 2 authored-intent / 1 immutable-evidence / 10 verified copies across 12 paths/surfaces; every primary marker is unique and present, eight core plus two adapter authorities execute, current Rust/gitlink values agree, 44 focused mutations pass, and the neutral checker has no SpecForge names or heuristic discovery. However, the registry names only the README Rust and feedback Markdown copies; the adapter hardcodes the mdBook/CI Rust paths and feedback JSON pin. Therefore the data-only registry cannot prove its own complete copy membership. The required second status rollover seals 12 exact records in segment 0002 and returns this root to 60; its shared archive route is now only 218 bytes before rollover and is owned by `.9`. Frontier → `.8d`, `.8e`, then `.9`; no product code, canonical artifact, donor file, threshold, or ceiling changed.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.8b` (`2026-08-09`) — Done: exact current-state fields are mechanically classified and authority-bound.** Fourteen bounded contracts classify 1 derive-on-read / 2 authored-intent / 1 immutable-evidence / 10 verified-copy records. Eight existing currentness verifiers execute directly; the local adapter independently proves Cargo `rust-version` against README/book/CI and the stage-zero mode-160000 FSMGen Git-index object against both retained pin copies. `MEMORY.md` derives HEAD on read and forbids a latest-commit shadow. New 28-case neutral plus 16-case adapter suites reject heuristic discovery, stored shadows, invalid/off-surface/duplicate declarations, degraded verifiers, semver drift, and wrong/missing gitlinks. The existing fact index is 32,639 bytes, 539 before rollover. Frontier → `.8c` independent resulting-tree audit, then return to `CORPUS-COVERAGE.2.33d.iii`; no product code, canonical artifact, donor file, threshold, or ceiling changed.
- 🧭 **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.8a` (`2026-08-09`) — Done: the newer donor derived-state delta is measured and locally bounded.** The read-only review pins the 419-line guide (`6f10…4744`), 426-line donor doctrine (`5cd3…7d88`), and 319-line local doctrine (`bb91…a9a`). SpecForge already ships external collection indexes, bounded projection sets, 41 governed surfaces, eight enforced currency contracts, and 15 executable non-budget lifecycle verifiers, but has zero field-level declarations. Three gaps prove the distinction: resume-pointer corpus counts have no fresh-clone authority; Rust `1.95.0` copies are not compared to workspace `rust-version`; and the feedback pin agrees with its JSON copy but not mechanically with the Git-index gitlink. The durable report classifies 15 field families across derive-on-read, verified copy, authored intent, immutable evidence, and removal. The fact index is dispositioned at 32,664 bytes—514 bytes before rollover—so `.8b` updates the same card or partitions first. Frontier → `.8b` bounded local registry/checker and exact Rust/gitlink adapters; `.8c` independent audit. No product code, canonical artifact, donor file, threshold, or ceiling changed.
- ✅ **`CORPUS-COVERAGE.2.33d.ii` (`2026-08-09`) — Done: dense-prose signal authority is grounded at its three upstream seams.** Both declaration catalogs now require canonical `Signal <identifier> is input|output|inout|internal|local|width` grammar; the broad gate preserved valid `inout` despite its zero retained-corpus count. `Bus (ACRONYM)` no longer becomes a one-bit signal, and ordinary port/pin tables require an explicit signal caption, compact identity header, or headerless connector-pin structure. A combined regression proves `AT`/`USB`/`ENHANCED`/`NO` cannot re-enter through relations, directions, or table provenance. Tie selection stays lexicographically last and is now accurately documented/tested. Full library 1,779/1,779, serial/table regressions, WIRE golds plus fresh SWD, KG 156/156, Clippy, full CI, mdBook, doctrines, and locality pass. The triggered engineering-notes rollover seals 27 exact records; its archive index is at a controlled 81.2% line / 81.5% byte warning, below 90% rollover. Frontier → `.2.33d.iii` measured-unnecessary closure, then `.d.iv` real USB proof.
- ✅ **`CORPUS-COVERAGE.2.33d.i` (`2026-08-09`) — Done: the universal signal-authority repair is measured and bounded.** Across 80 EvidenceIR artifacts, 97 non-declaration `signal <word>` matches produce 120 relations and 11 direction promotions; 17 `Bus (ACRONYM)` width-one captures produce 135 relations and 12 promotions; 11 of 32 port/pin-only table classifications pass the current authority gate. Direction synthesis copies existing catalog names rather than inventing them, so `.d.ii` owns three upstream grammar/structure repairs and `.d.iii` is measured unnecessary if direct convergence + real USB proof pass. I2C/I2S/SWD/SWP, real/rotated signal tables, connector pins, WIRE 1.000, KG 156/156, FSMGen, and full CI are explicit gates. Nine of 79 IntentIR docs have maximum producer ties; live behavior chooses the lexicographically last tie. Durable report `dense-prose-signal-authority-measurement`; no product/generated artifact changed. Frontier → `.2.33d.ii`.
- 🚧 **`CORPUS-COVERAGE.2.33c` (`2026-08-09`) — Done: USB 3.2 reaches every stage, but its strict-clean ISF is semantically blocked.** The 548-page current-binary chain validates through EvidenceIR, SemanticIR, IntentIR, and adapter; corpus coverage is now 80 SourceIR / 2 normalized / 80 EvidenceIR / 79 downstream chains, with 33 refreshes complete and 23 real chip-spec docs left. SourceIR 6,584→5,830 is visual-label suppression (all 53 loss pages have visuals; short affected-page fragments 543→3; 548/507/283 pages/visuals/tables hold), not prose loss. Four weak candidates (`AT`/`USB`/`ENHANCED`/`NO`) reinforce through relation-derived directions and make a phantom request actor renderable. FSMGen strict reports zero diagnostics on the four-port/29-rule output, proving syntax but not semantic fidelity. Durable fact `dense-prose-false-signal-loop-reaches-isf`; the universal repair is owned by `.2.33d`. Exact rollback removed after comparison/zero residue; 156/156 KG and docs/path/locality gates pass.
- ✅ **`CORPUS-COVERAGE.2.33b` (`2026-08-09`) — Done: both signal-declaration catalogs are UTF-8 safe and USB EvidenceIR completes.** One shared helper checks the valid match prefix for start-of-text or `. `, eliminating duplicated backward byte slicing without any document/codepoint/name special case. A two-catalog regression proves the multi-byte-marker prose neither panics nor declares `INTEGRITY`, while a real declaration after non-ASCII prose retains `PREADY`. Rebuilt release `1710e5…` constructs and validates the already-promoted USB source as 918 anchors / 8,267 spans / 507 visuals / 22,029 links / 8,412 statements. Focused EvidenceIR passes 252 tests / five ignored; full CI passes 1,776 tests / five ignored plus all doctrines, Clippy, rustdoc, and mdBook. The triggered change-ledger rollover seals 29 aged-out records and leaves the declared 85+2 live view. Frontier after commit → `.2.33c` downstream cascade and honest quality disposition.
- 🚧 **`CORPUS-COVERAGE.2.33a` (`2026-08-09`) — Probe done: USB 3.2 fresh ingest succeeded and exposed a deterministic UTF-8 EvidenceIR panic.** Current release CPU ingest completed at 81–82% free RAM with 548 pages / 507 visual assets / 283 tables / 5,830 elements / zero residuals; the pre-ingest SourceIR is preserved in a byte-verified same-SSD rollback. Evidence writes nothing before panicking on a private-use bullet preceding `signal`: both declaration catalogs subtract two bytes from a valid match offset and slice inside the three-byte character. `.2.33b` owns one shared UTF-8-safe boundary helper and regressions; `.2.33c` then resumes the cascade without re-ingest. The initial repo-relative symlink refusal was an independent, expected portability guard; the resolved PDF is the authorized external input.
- ✅ **`CORPUS-COVERAGE.3` (`2026-08-09`) — Done: corpus refresh progress is reconciled with normalized-cache lifecycle.** The apparent `32 restored` versus `1 retained` conflict was caused by the documented `2026-07-05` cleanup intentionally reclaiming all normalized caches after refresh #32. All five retained stage artifacts remain present for every completed refresh document (160/160), so `.2` stays at 32 completed refreshes with 24 real chip-spec documents unrefreshed; today's separate cache/stage census is 80 SourceIR / 1 normalized / 79 EvidenceIR / 78 SemanticIR/IntentIR/adapters. The task frontier, durable retrieval fact, and mdBook now state that distinction; the stale AIA “not yet re-ingested” claim is corrected. No generated artifact or product code changed.
- ✅ **`SWD-SERIAL-EXTRACTION.7e.ii.b` (`2026-08-09`) — Done: canonical SWD pipeline promoted and the program closed.** The fresh portable tracked-PDF chain is SourceIR 400 pages / 386 assets / 210 tables / 6,784 elements and exact EvidenceIR→SemanticIR→IntentIR 11/4/13/1 protocol parity; all 29 derivation facts score 1.000. The adapter accounts for all 29 records, remains renderable, and passes FSMGen strict with zero diagnostics. Evidence/Intent validators, 156/156 KG, APB/AHB/AXI WIRE 1.000, 1,072-artifact path portability, and locality are green. Full CI passes 1,775 tests / five ignored plus all doctrines, rustdoc, and mdBook. Both rollback snapshots and exact temporaries are removed; `.project-data/tmp` contains only `.gitkeep`. Stale retrieval claims are replaced and `.7e.ii`, `.7e`, `.7`, and `SWD-SERIAL-EXTRACTION` are closed.
- ✅ **`SWD-SERIAL-EXTRACTION.7e.ii.a` (`2026-08-09`) — Done: fresh Docling sidecars cross the portable-storage boundary before promotion.** The newly restored ADI metadata leaf exposed absolute runtime/staging paths that reclaimed caches could not reveal. The staged sidecar now records repository-relative input/final-Markdown paths (or an explicitly labeled external input) before the atomic swap; focused tests and a second fresh 400-page run prove 29/29 SWD fidelity, exact downstream parity, and zero repository-owned absolute values across 1,072 JSON artifacts. Full CI is green with all six doctrines, 1,775 passed / five ignored, rustdoc, mdBook, and final locality. The subsequent `.7e.ii.b` signoff is now complete.
- ✅ **`SWD-SERIAL-EXTRACTION.7e.i` (`2026-08-09`) — Done: convergence snapshots carry and count exact protocol records.** EvidenceIR, SemanticIR, and IntentIR snapshots retain all four ordered typed vectors, their stage fact counts include every record, and the pass-level evidence count includes them too. A real three-stage fixture proves exact five-record deltas, same-cardinality content detection, reorder detection, and protocol-empty compatibility. Full CI is green with all six doctrines, 1,772 passed / five ignored, rustdoc, mdBook, and locality. Frontier → `.7e.ii` fresh tracked-PDF promotion and program closure.
- ✅ **`SWD-SERIAL-EXTRACTION.7d` (`2026-08-09`) — Done: every canonical protocol record has an explicit ISF adapter disposition.** The adapter emits one stable, ordered residual packet per frame field, operation, state, and interface-edge timing, naming provenance and missing executable bindings; the directly lowerable subset remains empty. A five-record regression proves deterministic/round-trip accounting, byte-identical independently licensed ISF, renderability, and real FSMGen strict success with zero diagnostics. Full CI is green with all six doctrines, 1,771 passed / five ignored, rustdoc, mdBook, and locality. The triggered status rollover preserves 12 aged-out records in authenticated segment 0001 and leaves a 60-record live window. Frontier → `.7e.i` convergence accounting.
- ✅ **`SWD-SERIAL-EXTRACTION.7c` (`2026-08-09`) — Done: all four protocol collections now reach canonical IntentIR exactly.** IntentIR reuses the upstream record types, clones without reinterpretation, defaults absent legacy keys to empty, omits empty keys, and reports four validation counts. Three-stage record/order/provenance parity plus focused IntentIR, validation, prior-learning, formatting, and warning-deny Clippy checks pass. Full CI is green with all six doctrines, 1,770 passed / five ignored, rustdoc, mdBook doctests/build, and final locality. Frontier → `.7d` explicit adapter accounting; convergence accounting remains `.7e.i`.
- ✅ **`SWD-SERIAL-EXTRACTION.7b` (`2026-08-09`) — Done: all four SWD protocol collections now reach SemanticIR exactly.** The schema reuses EvidenceIR record types with default-empty/skip-empty compatibility; the builder clones without filtering or reinterpretation; validation exposes four deterministic counts. Full record/order/provenance, legacy-empty, validation, 394 SemanticIR tests, and a live canonical ADI 11/4/13/0 dry-run pass. Full CI is green with all six doctrines, warning-deny formatting/Clippy, 1,768 passed / five ignored, rustdoc, mdBook, KG fixtures, and locality. Cold review also tracked convergence snapshot blindness under `.7e.i`; immediate frontier → `.7c` IntentIR projection.
- ✅ **`SWD-SERIAL-EXTRACTION.7a` (`2026-08-09`) — Done: exact projection plus honest ISF accounting are frozen before schema work.** The four scored EvidenceIR surfaces are inventoried against downstream schemas and the typed ISF model. ADR 0016 requires lossless SemanticIR/IntentIR carry-through and explicit residuals unless a record supplies every behavioral binding; the currently safe direct-lowering subset is empty. The canonical zero-edge artifact is root-caused as the deliberately unpromoted pre-`.4e` cache, not a migration regression; `.7e` owns a fresh tracked-PDF ingest and full-chain promotion. mdBook and all six doctrines pass; implementation frontier → `.7b`.
- ✅ **`ARTIFACT-PATH-PORTABILITY.5` (`2026-08-09`) — Done: independent schema, data, workflow, and residue proofs close the portability program.** The cold read found and repaired dormant `FigureRegion.raw_image_path`, whose direct Serde could have activated an absolute path before any artifact existed to scan. Project-rescan artifact/replay/working-directory strings are now pinned too; the oracle passes 12/12 self-tests and scans 343,237 path values across 667 JSON files with 82 labeled external absolutes and zero repository-owned absolutes. Live loaders consumed 80/79/78/78/78 Source/Evidence/Semantic/Intent/adapter artifacts, exact all-IntentIR learning repeated deterministically, recovery passed, and retired-root generated residue remains zero. Full CI passes 1,766 tests / five ignored, 156/156 KG fixtures, doctrines, rustdoc, mdBook, locality, and residue. The portability tree is closed; frontier → `SWD-SERIAL-EXTRACTION.7` canonical protocol projection.
- ✅ **`ARTIFACT-PATH-PORTABILITY.4` (`2026-08-09`) — Done: the generated corpus is migrated and future absolute-path residue fails the locality doctrine.** A verified same-volume migration changed exactly 392/978 files, made 262,996 retired-root values relative, and added 157 origin labels; the pure 721,679,372-byte migration snapshot preserves membership, contains zero retired-root values, and retains 82 labeled external source-library values. Final CI refreshed only the expected ignored mdBook products. A self-tested oracle pins producer seams and scans all 667 present JSON artifacts. Real Source/Evidence/Semantic/Intent/adapter validation plus learning/recovery exercised the migrated state. The first workflow also exposed and fixed the difference between intentionally reclaimed provenance and existence-required stage inputs; focused reference/cleanup cases pass. Frontier → `.5` independent cold-read closure.
- ✅ **`ARTIFACT-PATH-PORTABILITY.3` (`2026-08-08`) — Done: every canonical stage and consumer now uses the move-safe path boundary.** SemanticIR, IntentIR, adapters, and typed prior memory join SourceIR/EvidenceIR in serializing repository-owned lineage/layout/emitted-target/learned-source paths relative while exposing current-root absolute runtime paths. Project-validation, learning, recovery, KG fixtures, and convergence use the common resolver; wrong-stage inputs are rejected before internal resolution and ambiguous legacy roots fail closed. Complete downstream legacy round-trip tests, focused command checks, 156/156 KG fixtures, and 1,758 passes / five ignored are green; full CI covers all doctrines, rustdoc, mdBook, and locality. The 335-file generated corpus is deliberately unchanged; frontier → `.4` verified migration and residue enforcement.
- ✅ **`MDBOOK-DOCTEST-HYGIENE.2` (`2026-08-08`) — Done: native mdBook doctests now gate every canonical docs/full-CI run, and the bounded tree is closed.** `scripts/run_docs_ci.sh` tests examples before building the 36-page HTML book; `scripts/run_ci.sh` already invokes that entrypoint exactly once. The focused gate and complete CI suite pass from the final tree, so future executable-Rust fence drift can no longer hide behind a green render-only build. No example body or product behavior changed; frontier → `ARTIFACT-PATH-PORTABILITY.3`.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.4c` (`2026-08-08`) — Done: `DEVELOPMENT_NOTES.md` is now the second bounded whole-record live window over an exact archive.** Froze the pinned 1,601-record / 20,921-line / 2,170,230-byte source at `docs/archive/rolling-ledgers/development-notes/source-through-2026-08-08.md` (SHA-256 `76b51a3f450cdb1e764922dc366cf6ff55529cb95f7a6f410bfba1f1f378fedc`), extended the bounded common manifest/index, and mechanically derived the H1 prologue plus newest 60 H2 records. The `.4c` rationale is the first post-capsule prepend. Exact suffix verification exposed the blank before record 61 as a successor-owned separator; the corrected renderer omits that one LF at live EOF while the capsule preserves every source byte. `engineering_notes` leaves transition debt at 1,480 lines / 194,412 bytes; capsule retrieval runs unconditionally. Frontier → `.4d` status migration with its Rust-managed validation trailer preserved.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.4b` (`2026-08-08`) — Done: `CHANGES.md` is now a bounded whole-record live window with an exact retrievable archive.** Froze the pinned 1,798-record / 32,682-line / 2,629,033-byte source at `docs/archive/rolling-ledgers/changes/source-through-2026-08-08.md` (SHA-256 `d89809322857aab3c506dde1cc6caaf57e22d0349655b37ddaab1f7bf22ba994`), registered it in the self-bounded archive manifest/index, then mechanically derived the root from the newest 85 records plus the exact detached `.1`/`.0` compatibility records. The `.4b` entry is the first post-capsule prepend. `change_history` leaves transition debt at 1,368 lines / 199,851 bytes; the archive terminal is immutable and query-first. Retrieval, source identity, live suffix, link, and pressure checks run unconditionally. Frontier → `.4c` `DEVELOPMENT_NOTES.md` migration.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.4a` (`2026-08-08`) — Done: one lossless protocol now governs all four root rolling ledgers before any history moves.** Measured `CHANGES.md`, `DEVELOPMENT_NOTES.md`, `LIVE_ACHIEVEMENT_STATUS.md`, and `RUST_CODEBASE_ANALYSIS.md` at their real whole-record boundaries; pinned source identities, deliberately reviewed live windows, local pressure limits, reader/writer seams, repository-relative archive routes, and the source-capsule overlap contract in a self-bounded JSONL registry. `scripts/check_rolling_ledger_protocol.pl` reconstructs every planned source byte-for-byte and runs unconditionally under `LIVE-DOC-SIZE`; its migrated-state contract requires an immutable exact source capsule, bounded index, manifest identity, retrieval proof, and retained-record suffix. The two `.0`/`.1` containment records accidentally appended below the 2026-03-31 `CHANGES.md` tail are an explicit compatibility exception: the live view will promote them, while the capsule preserves original order and exact bytes. No record moved in `.4a`. Frontier → `.4b` atomic `CHANGES.md` migration.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.5a` (`2026-08-08`) — Done: two mdBook current-state contradictions repaired and locked to code.** Reverified `isf_ir.rs` initiator selection/direction lowering and the live `extract-contracts` CLI+dispatch, then corrected actor-connectivity's pre-`.2a.ii` claim and constrained extraction's pre-delivery “no code” introduction. The older documentation-audit narrative is now explicitly time-layered, and current prose routes to the detailed ISF-direction and command chapters. `scripts/check_book_current_truth.sh` binds both statements to their code seams and runs as the mdBook maintained-reference currency verifier under `LIVE-DOC-SIZE`. Frontier → `.4a` shared lossless rolling-ledger protocol; no historical record moves before that contract is proved.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.3c` (`2026-08-08`) — Done: lifecycle and control-plane proof gate closed.** Added 48 repository-volume positive/fail-closed fixtures covering all seven local lifecycles, membership/query, canonical inputs, freshness/currency, routes, exact coverage, locality, all six size dimensions, archive manifests, frozen/reference/debt controls, and Git-history ceiling/baseline/maintained-reference authority. Closed JSONL schema/version/field/identifier/array/scalar bounds and README route-registry count/byte/record bounds. The suite runs unconditionally through `LIVE-DOC-SIZE`; 48/48 pass. The common `.3` enforcement program is complete; frontier moves to `.5a` mdBook truth-drift repair before collection migrations.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.3b` (`2026-08-08`) — Done: complete live-document containment enforcement activated atomically.** Adopted the SpecForge-owned neutral doctrine + ADR 0007, self-bounded JSONL registry, deterministic Perl/shell checker, and fifth doctrine entry together—no unenforced prose interval and no donor thresholds/dependency. All 553 resulting-tree Markdown files classify exactly once across 24 surfaces. Independent count/line/byte/aggregate/width ceilings, immutable debt baselines plus bounded continuity allowance, index/query closure, executed Knowledge Map freshness, honest currency debt, frozen SHA, README routes, exact mdBook aggregate authority, and ceiling-increase authority are active. The first resulting-tree pass exposed and fixed nested `docs/research/grounding/` coverage plus the missing genericity ADR in `docs/decisions/INDEX.md`; nothing deleted. Neutral body matches the reviewed template after the local note; checker syntax/report/current pass, doctrines 5/5, mdBook, KM 131/954, and diff are green. Frontier → `.3c` exhaustive lifecycle/fail-closed fixtures.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.3a` (`2026-08-08`) — Done: complete live-surface census and local enforcement contract locked.** Decomposed `.3` so doctrine+ADR+registry+checker activate atomically in `.3b` and lifecycle-negative proofs close in `.3c`. Parent Git owned 550 Markdown files across 13 mutually exclusive families at entry (132,211 lines / 12,253,499 bytes); the required coverage-authority fact card makes the committed set 551, and the FSMGen gitlink is correctly outside scope. Largest entry file: `CHANGES.md` 32,626 lines / 2,624,882 bytes; widest line: `docs/TASK_TREE.md` 18,932 bytes. Local JSONL schema, seven lifecycle classes, independent line/byte/count/aggregate/width dimensions, exact zero-growth debt, maintained-reference authority, exactly-once coverage, route composition, and ceiling-change authority are specified; no donor cap copied and no active doctrine changed yet. Frontier → `.3b` atomic implementation.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.2` (`2026-08-08`) — Done: SpecForge README policy adopted and the landing page bounded with closed routes.** Proved the old 602-line/170,891-byte README's status/history, docs-index, file-map, and deep command material duplicated richer task-tree/ledger/mdBook/tree homes; retained a verified 127-line/4,834-byte entrypoint with all landing duties. Local ceilings are 150 lines/5,800 bytes (18.1%/20.0% explicit headroom), enforced independently and unconditionally as doctrine four. The data-only registry closes all 21 reader links plus seven author-overflow routes at owned terminals and declares oversized neighbors as `.5` transition debt; `.3` must pin their measured ceilings before migration. The guard exposed and corrected stale knowledge-map architecture pointers (`knowledge-map/KNOWLEDGE_MAP_ARCHITECTURE.md` is canonical); a fact card prevents archaeology. Checks: fail-closed self-test, doctrines 4/4, mdBook, CLI help, strict doctor, `inspect README.md`, diff hygiene. Frontier → `.3` complete live-surface registry/checker.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.1` (`2026-08-08`) — Done: pointer and commit semantics stabilized.** `COMMIT.md` now uses impact-based documentation routing; no-op co-staging is forbidden. `MEMORY.md` no longer mirrors HEAD and its gate now enforces 50 lines / 4,096 bytes / 160 max-line bytes, required resume fields, and no latest-commit shadow. Current pointer passes and a forced-cap probe fails closed. Frontier: `.2` README policy, landing-page trim, local caps, and unconditional guard.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.0` (`2026-08-08`) — Done: ownership and local measurement.** Full ramp-up and donor-policy review are complete. Local census: README 602 lines/170,891 bytes; CHANGES 2.62 MB; DEVELOPMENT_NOTES 2.17 MB; LIVE_ACHIEVEMENT_STATUS 572 KB; RUST_CODEBASE_ANALYSIS 1.05 MB; generated KNOWLEDGE_MAP 1.02 MB. Default Rust temporary workspaces can escape to boot-volume temp. The new active tree owns SpecForge-local policies, transitive route enforcement, lossless lifecycle migrations, mdBook drift repair, and same-volume storage. No deletion or migration occurred. Doctrines 3/3, mdBook build, and diff check are green. Frontier: `.1` pointer/commit semantics, then `.2` README policy/trim/guard.
- ✅ **`PDF-VARIANT-DIGESTION.10i` (`2026-06-24`) — Done (CODE): the `.10h` genuinely-different register residual is now RECOVERED by block-qualification.** `.10h` collapses identical/nested views of one register but left the GENUINELY-DIFFERENT class (disjoint field sets under one mnemonic — MEM-AP `CSW` 11 fields vs JTAG-AP `CSW` 7 fields) a full residual, because the flattened heading hierarchy carries no ancestor-block heading. The `.10i` measurement (the `.10h` `#[ignore]` block probe, read-only over persisted `source_ir`) refined that: the block name is still printed — as the parent SECTION TITLE, reached via the dotted-parent number — in the universal form `<dotted-num> <BLOCK> register descriptions` (`C2.6 MEM-AP register descriptions` → `MEM-AP`, `C3.5 JTAG-AP register descriptions` → `JTAG-AP`, `C1.4 AP Register Descriptions` → `AP`), while a parent with no block token (`D4.5 Register descriptions`) honestly yields none. `extract_section_header_registers` (`ir/evidence.rs`) now threads each container's dotted number (`SectionHeaderFieldContainer.dotted`, additive — `.10f` ignores it) and, when `.10h` containment returns `None`, BLOCK-QUALIFIES each occurrence via the new pure helpers `section_dotted_number`/`dotted_parent`/`derive_register_block_name` + a `by_number` map: each block-named occurrence emits as `<NAME>@<BLOCK>`, a no-block occurrence stays residual, ≥2 still-disjoint occurrences sharing one block re-run `.10h` containment (else residual — never conflated). **Recall (per-item):** the genuinely-different class is in EXACTLY 1 doc — ARM-Debug `ihi0074` gains `CSW@MEM-AP`{11}, `CSW@JTAG-AP`{7}, `CLAIMSET@AP`/`@MEM-AP`/`@JTAG-AP`{2}; section-heading registers 15→**20** / 69→**93** fields; full `evidence` register_records 40→**45**. The no-block `D4.5` CLAIMSET stays residual; CoreSight `ihi0029` 6/29, GIC 73, SMMU 88, ACC 2 byte-identical (CoreSight's lone reused `AUTHSTATUS` is `.10h`-collapsible → never reaches `.10i`). **`.isf`-safe:** the 5 registers reach the ARM-Debug `debugger.isf` as 5 distinct sanitized storage vars (`csw_mem_ap`/`csw_jtag_ap`/`claimset_*`) and the re-emit passes FSMGen `--strict --check` with **0 diagnostics**. **NO REGRESSION:** a `git stash` baseline-vs-change `evidence` diff over the 8 `.10h` golds + CoreSight `ihi0029` is **byte-identical**; only `ihi0074` changes, ADD-only (5 records, ZERO removals, all 40 baseline records preserved) → pure recall gain. `kg-bench` 156/156; full `run_ci.sh` GREEN (+3 hermetic tests). ADR-0006 (universal `<NUM> <BLOCK> register descriptions` grammar, no name list). Book `pipeline/evidenceir.md` `.10i`; KM `[[section-header-register-block-qualification]]`. SpecForge NOT complete.
- ✅ **`PDF-VARIANT-DIGESTION.10h` (`2026-06-24`) — Done (CODE): the `.10g` block-ambiguous register-mnemonic residual is now PARTLY RECOVERED by field-set containment.** `.10g` dropped every register mnemonic reused across ≥2 register-routed section-heading containers (ARM-Debug `AUTHSTATUS`/`CSW`/`IDR`/`CLAIMSET`/`DEVARCH`, CoreSight `AUTHSTATUS`) because a blind merge would conflate genuinely-different registers or over-count cross-references. A probe-first measurement established the residual lives in EXACTLY 2 docs and that the PDF backend flattens headings to L1 (no ancestor-block heading — only the dotted-number hierarchy + the field lists survive). `collapse_section_header_register_identity` (`ir/evidence.rs`) replaces the `.10g` drop-all gate: a reused mnemonic collapses to ONE record IFF every occurrence's field set is a subset of one MAXIMAL occurrence (identical cross-references + nested views of a single register), keeping that fullest occurrence's REAL layout; disjoint sets (≥2 genuinely-different registers — MEM-AP `CSW` vs JTAG-AP `CSW`) stay an honest residual (never conflated). **Recall (per-item):** ARM-Debug section-heading registers 12→**15** / 57→**69** fields (+`AUTHSTATUS`/`DEVARCH`/`IDR`), CoreSight 5→**6** / 24→**29** (+`AUTHSTATUS`); full `evidence` register_records ARM-Debug 37→**40**, CoreSight 26→**27**. **NO REGRESSION:** a `git stash` baseline-vs-change `evidence` diff over 8 golds (CCIX r1.0, NVMe register golds; AXI, AHB wire golds; GIC, SMMU, ACC section-header no-dup; DTI `.10f` message-field) is **byte-identical**, and the only 2 changed docs ADD records with ZERO removals (every baseline record byte-identically preserved) → pure recall gain, no conflation; `kg-bench` 156/156; `run_ci.sh` GREEN (lib 1718→**1721**, +3 tests + 1 `#[ignore]` probe). ADR-0006 (universal field-set containment, no name list). Narrowed residual: the genuinely-different `CSW`/`CLAIMSET` class (block-qualification needs a clean per-occurrence block name the flattened hierarchy does not provide). Book `pipeline/evidenceir.md` `.10h`; KM `[[section-header-register-identity-collapse]]`. SpecForge NOT complete.
- ✅ **`KG-ISF-COMPLETENESS.5.iii` (`2026-06-24`) — Done (CODE): the enum surface now drops `_WIDTH` parameter leaks, so the AXI gold `.isf` enum surface is faithful.** Measured-first (read-only census, tracked reproducer `scripts/measure_enum_width_leak.py`) then landed: a configuration row (`Enum BRESP BRESP_WIDTH = 0.`) mis-read into a value enum reached the AXI WIRE-GOLD `manager.isf` as `(BRESP (BRESP_WIDTH 0) (OKAY 0) …)` (junk member duplicating value `0`), `(RRESP (RRESP_WIDTH 0))` (real codes replaced), `(AXSNOOP (AWSNOOP_WIDTH 0) (ARSNOOP_WIDTH 1))` (pure junk) — a false bar-#6 `.isf` fact, unscored by WIRE-BASED-100 (enum surface emitter-orthogonal — why it held 1.000 while the `.isf` carried junk). `is_width_parameter_leak_member` + a `continue`-skip in `synthesize_encoding_declarations_for_enum` after the `.5.ii` spine gate (`ir/evidence.rs`) drops a member named `<X>_WIDTH` where `X` is the enum's own name OR a declared signal (`known_signals` threaded from the signal-match caller); a pure-parameter enum empties → honest residual. **Document-grounded, FP-free (ADR 0006):** a genuine `FULL_WIDTH`/`HALF_WIDTH` value is KEPT (`FULL`/`HALF` not signals); corpus false-positive set EMPTY (a unit test pins `FULL_WIDTH` kept). **Verified per-item:** AXI evidence drops EXACTLY the 7 leaks (statements 6414→6407; non-Enum statement set byte-identical); `manager.isf` now `(BRESP (OKAY 0)(EXOKAY 1)(SLVERR 2)(DECERR 3)(DEFER 4)(TRANSFAULT 5)(RESERVED 6)(UNSUPPORTED 7))` + `(AWCMO (CLEAN_AND_INVALIDATE 0)(CLEAN_ONLY 1))`, the false RRESP/RCHUNKNUM/RCHUNKSTRB/AXSNOOP `_WIDTH` enums gone; FSMGen `--strict` success/0. **NO REGRESSION:** WIRE-BASED-100 = **1.000 before==after** (AXI before/after eval identical; APB/AHB/SWD/i2c evidence byte-identical → gate inert); `kg-bench` 156/156; `run_ci.sh` GREEN (lib **1718**, +2). `.5` enum-surface fidelity now has all three member gates (`.5.i` name + `.5.ii` spine + `.5.iii` `_WIDTH`); the self-named `*_WIDTH` pseudo-enums stay an honest residual. **Frontier → KG-ISF-COMPLETENESS has no further immediately-buildable leaf** (`.1c.ii` NLP-gated, `.2b` measured-marginal) → PNT advances to the next active tree. SpecForge NOT complete.
- ✅ **`KG-ISF-COMPLETENESS.5.ii` (`2026-06-24`) — Done (CODE): the enum surface now has a per-MEMBER sentence-fragment gate that cleans junk-conflated enums WITHOUT losing their real codes.** `is_prose_fragment_member_name` + `PROSE_SENTENCE_SPINE_WORDS` (`ir/evidence.rs`) gate the member loop in `synthesize_encoding_declarations_for_enum`: a synthesized member whose `_`-token set carries an English sentence-spine word (copula/aux/modal/article/demonstrative/relativizer/subordinator; collisions `a`/`i`/`its`/`can`/`may`/`am` EXCLUDED per `.1a`) is a captured prose sentence → skipped, so a conflated enum keeps its codes and a pure-prose table empties (honest residual). **Per-member, not per-enum** (a whole-enum drop loses AXI `BRESP`'s codes); **not value-restart** (AHB `HPROT` restarts but all 15 members clean). Per-item: precision 1.000 (0/115 clean anchor), recall 1.000 (269/269 junk anchor). The AXI `manager.isf` now emits `(BRESP (OKAY 0)(EXOKAY 1)(SLVERR 2)(DECERR 3)(DEFER 4)(TRANSFAULT 5)(RESERVED 6)(UNSUPPORTED 7))` (codes recovered). **NO REGRESSION:** WIRE-BASED-100 = **1.000 before==after** (PROVEN, all 10 seeds, scored surface byte-identical); FSMGen `--strict` `success` on rebuilt AXI+APB `.isf`; `kg-bench` 156/156; `run_ci.sh` GREEN (lib **1716**, +4 tests). ADR 0006 (universal grammar). Deeper member-quality classes (glossary/front-matter/section-caption/`_WIDTH`/restart-of-clean) are honest residuals. **`.5` enum-surface fidelity now built** (`.5.i` name-gate + `.5.ii` member-gate). Report §`.5.ii LANDED`; KM `[[generic-enum-conflation]]` → LANDED. SpecForge NOT complete.
- ✅ **`KG-ISF-COMPLETENESS.5.i` (`2026-06-24`) — Done (CODE): the `.isf` no longer emits generic junk-named or cross-table-conflated enums.** Two ADR-0006-structural edits: (1) `derive_encoding_enum_name`'s caption-token fallback (`ir/evidence.rs`) keeps the candidate **only when independently evidenced** — a declared signal OR a column-header reference token of the table — else returns `None` (existing `continue` contract → no enum minted → honest residual), killing the generic enums AND the `build_symbol_definitions` merge-by-name conflation; (2) the emitter (`ir/isf_ir.rs`) gates the `(types …)` block by `emitted_enums()` so a member-dropped enum leaves no orphan `(type …)`. **Two `.5` corrections (cleaner):** the genuinely-named enums come from the signal-match loop ABOVE the fallback → **byte-identical**, and the structural gate is *strictly stronger* than a name-only gate (also drops fallback-origin `COMMAND`/`AMBA`/`READ`/`CACHE`/`RELEASE`); it also cleans off-gold junk value-constraints from dropped-enum `discovered_values` (AXI `ACTIVATEACK A`→ grounded `ACTIVATEACK 1`), score-orthogonal. **Measured:** corpus census (33 rebuildable docs) generic-named enums **82→8** / total enum records **422→105** (8 survivors = document-evidenced column-header tokens, e.g. CCIX "Table of Contents" → `.5.ii` residual); per-doc before→after on HBM2 + 4 wire golds + AXI-Stream + SWP + 6 worst offenders (amd 34→5, gic_600 23→6, coresight 19→5, cortex_a76 16→7, CHI 14→4, CHI-C2C 7→1) — real signal enums byte-identical, every `.isf` FSMGen `--strict` 0 diagnostics. **NO REGRESSION:** WIRE-BASED-100 = **1.000 before==after** (proven via before/after `eval-extraction` on rebuilt gold evidence); nvme-registers + i2c golds identical; `kg-bench` 156/156; `run_ci.sh` GREEN (lib 1712, +4 tests). Genericity proven (`DATA` kept where a real gic_600 signal, dropped where a bare HBM2 caption word). Report `docs/research/generic-enum-conflation-measurement.md` §`.5.i LANDED`; KM `[[generic-enum-conflation]]` → LANDED. Frontier → `.5.ii` (member-quality gate, calibration-gated). SpecForge NOT complete.
- 🧭 **`KG-ISF-COMPLETENESS.5` (`2026-06-24`) — Done (measurement, read-only, docs-only): the generic-`TABLE` mega-enum conflation is localized, measured, and decided.** The `.isf` emits a generic junk-named enum (HBM2 `(type TABLE (bits 6))`) fusing ~7 unrelated value-tables — a bar-#6 fidelity defect deferred by `.2a.iv` (Lever F). **EXTRACTION-born** (not the emitter): `derive_encoding_enum_name`'s fallback (`evidence.rs:4457-4461`) names an unmatched encoding table after its caption keyword (`Table N`→`TABLE`, via `is_hardware_signal_token` `evidence.rs:7106`), and `build_symbol_definitions` (`semantic.rs:2782`) merges every same-named table into one enum. **Measured:** 56/78 docs / 96 generic + 271 real-named-but-junk enums (HBM2 `TABLE` = 7 fused tables, 29 dup values, 30 fragment names) → a name-only gate is insufficient; the load-bearing signal is member quality. **Decision GO, extraction-side, decomposed:** `.5.i` (safe fallback name-gate + emitter orphan-`(type)` fix — pending, fresh-session-preferred since it changes wire-gold `.isf` bytes) / `.5.ii` (member-quality gate, calibration-gated). WIRE-BASED-100 scores orthogonal; `.isf` bytes change (strict improvement). No code → all oracles orthogonal; `check_doctrines.sh` GREEN; KM 125→126. Report `docs/research/generic-enum-conflation-measurement.md`; KM `[[generic-enum-conflation]]`. SpecForge NOT complete.
- ✅ **`CORPUS-COVERAGE.2` re-ingest #32 (`2026-06-24`) — Done: JEDEC HBM-gen1 (`jesd235`) bundle-restoration + corpus-provenance finding.** Current-binary deterministic re-ingest (Docling CPU, RAM 83% free, staged-swap). **Finding:** the library `JESD235_2013-10_HBM_DRAM.pdf` is a **6-page legal-exhibit cover** (`DOCKET`/`Netlist Ex 2021` + 12 cover images, 0 tables), NOT the real HBM gen1 standard → the thin yield (0 registers/relations/constraints; statements 69→68; intent actors 2→2 / interfaces 1→7) is **honest source-driven absence**, not an extraction gap (contrast #28 HBM2 `jesd235a`). `adapt` recovers 16 signals but **BLOCKS honestly** ("no behavioral content"; 1 residual; no `.isf` fabricated — the `CORPUS-COVERAGE.0` intent-no-isf class). normalized bundle RESTORED. After #32: **24 real chip-spec docs still normalized-missing** (tail overwhelmingly thin → next PNT pivots to a surfaced upstream extraction-precision lever). No code → WIRE-BASED-100 / `kg-bench` orthogonal; `check_doctrines.sh` GREEN.
- ✅ **`BOOK-COMMAND-COVERAGE.3` (`2026-06-24`) — Done: completed the Commands-overview "Current surface" list (21→28; mdBook no-drift).** A fresh-session mdBook drift survey (then RE-DERIVED by hand — `cli.rs` has 28 subcommands, `docs/book/src/commands/overview.md`'s "Current surface:" list named 21) found the operational map missing the 7 EXTRACTION-QUALITY-GAUGE / GRITS members (`nli-verify`, `entity-type`, `extract-conditions`, `extract-constraints-llm`, `eval-extraction`, `audit-extraction`, `grits-consensus`) — each already had a dedicated section (`.1`/`.2`'s bar held), only the overview list was stale. Added the 7 after `signal-resolve` (README order) with flag signatures cross-checked against the `cli.rs` clap arg structs, plus a navigation pointer whose CI-safe-no-op wording was CORRECTED after re-deriving the real defaults (extraction/judge commands default to live `ollama`, not `skip`). `mdbook build docs/book` exit 0; list now 28 bullets (all 7 grep `PRESENT`). Both book command surfaces (overview list + per-command sections) locked to the CLI. Owned by re-opening `BOOK-COMMAND-COVERAGE` (`.3`). Docs-only → lib unchanged, oracles orthogonal; `check_doctrines.sh` GREEN.
- ✅ **`AUDIT-DOC-RECONCILE.3` (`2026-06-24`) — Done: refreshed `RUST_CODEBASE_ANALYSIS.md` to live counts.** The `2026-06-08` currency section had drifted four counts; refreshed (each backed by a reproducible command, verified at HEAD): **≈103.2K → 128,742 LoC / 65 files; 25 → 28 subcommands; 25 → 28 IR modules; 1435 → 1709 lib tests** (ran fresh). Added a largest-modules table; older dated sections preserved; no architecture claim reversed. Self-check withdrew one over-specific sub-claim (the old entry's command enumeration undercounted). Owned by re-opening `AUDIT-DOC-RECONCILE` (`.3`). Docs-only → lib unchanged (1709); `check_doctrines.sh` GREEN.
- ✅ **`BOOK-COMMAND-COVERAGE.2` (`2026-06-24`) — Done: dedicated `nli-verify` section in the Commands chapter (mdBook no-drift).** The session-start mdBook survey found `nli-verify` was the only quality command without a dedicated `##` section in `commands/quality-and-learning.md` (14 of 15 had one; its treatment lived only in `architecture-rationale.md`). Added the section (invocation, NLI entailment behavior, CI-safe abstain, persisted `extraction_quality_gauge`, active-gate `intent --nli-verify`), facts cross-checked vs `commands/nli_verify.rs` + `cli.rs`; `mdbook build` exit 0; sections 14→15. Owned by re-opening `BOOK-COMMAND-COVERAGE` (`.2`). Docs-only → lib unchanged, oracles orthogonal; `check_doctrines.sh` GREEN.
- ✅ **`CORPUS-COVERAGE.2` re-ingest #31 (`2026-06-24`) — Done: AMBA CHI C2C 2026 variant (`ihi0098_a_b`) marquee message-field refresh.** Current-binary re-ingest (Docling CPU, 122pp, normalized bundle restored) → **`message_field_records` 0 → 143 / 12 containers** (sibling of #29's 0→149 — confirms the CHI-C2C family is a genuine marquee). transactions 2→3; lone stale relation 1→0 (fragment drop); 0 registers (honest). `agent.isf` renderable (69 ports), FSMGen `--strict --check` **0 diagnostics**. After #31: **25 real chip-spec docs still normalized-missing**. No code change → oracles orthogonal; `check_doctrines.sh` GREEN.
- ✅ **`CORPUS-COVERAGE.2` re-ingest #30 (`2026-06-24`) — Done: RISC-V AIA (`1_0_2025_03_12`) bundle-restoration + confirmation.** Current-binary re-ingest (Docling CPU, 89pp, normalized bundle restored); all deterministic surfaces byte-near-identical (statements 1193 / registers 0 / cond_rules 39 held) — the #21/#22 "already current-binary-equivalent" class. Honest absence: 0 registers (APLIC/IMSIC layouts in 12 `unknown` structured tables → re-confirms the RISC-V Lever-D recall opportunity). `agent.isf` renderable (91 ports), FSMGen `--strict --check` **0 diagnostics**. After #30: **26 real chip-spec docs still normalized-missing**. No code change → oracles orthogonal; `check_doctrines.sh` GREEN.
- ✅ **`CORPUS-COVERAGE.2` re-ingest #29 (`2026-06-24`) — Done: AMBA CHI C2C (`ihi0098_a`) marquee message-field refresh.** Current-binary re-ingest (release first rebuilt to current; Docling CPU, 108pp, normalized bundle restored) → **`message_field_records` 0 → 149 / 13 containers** (the `.10` families fire on CHI packet/flit field tables absent in the stale pre-`.10` evidence — a real KG-completeness gain). 0 registers / 0 relations (coherency/message protocol — honest absence per `KG-ISF-COMPLETENESS.3`); `agent.isf` renderable (69 ports), FSMGen `--strict --check` **0 diagnostics**. Surfaced (not fixed in-slice): generic-`TABLE` mega-enum conflation + signal-acronym prose noise (standing upstream levers). After #29: **27 real chip-spec docs still normalized-missing**. No code change → WIRE-BASED-100 / `kg-bench` orthogonal; `generated/` git-ignored (durable trace = the `.2` log); `check_doctrines.sh` GREEN.
- ✅ **`KG-ISF-COMPLETENESS.3` (`2026-06-24`) — Done (verification-only): north-star bar #2 relation-completeness is resolved.** Re-census over all 78 persisted `intent_ir.json` vs `evidence_ir.json` finds **0 stale docs** (zero with `evidence>0 & intent==0`): the `CORPUS-COVERAGE.2` re-ingest sweep rebuilt the once-stale docs via `converge` (whole-chain cascade), so the recovered relations landed canonically — `tilelink` 33/34, I2C **17**, `gic_600` 101, `mmu_700` 25, ATS 9, DTI 1, opencapi-TL 15, USB4 13 (each `intent`==`evidence`). The stage-staleness detector (sub-step ii) was already shipped + unit-tested as `CORPUS-COVERAGE.1`. The 33 register/PHY/command docs at 0/0 are the correct honest-absence class (relation-completeness N/A; intent lives on register/message-field/transaction surfaces); wire protocols held at WIRE-BASED-100 = 1.000. No fabrication; no code change → all oracles orthogonal; `check_doctrines.sh` GREEN. KM `[[relation-completeness-staleness-vs-absence]]` + `[[stage-staleness-validate-detector]]`.
- ✅ **`KG-ISF-COMPLETENESS.2a.vi` (`2026-06-23`) — Done (CODE): ISF rule-drive-value validity gate → the renderable corpus is now 70/70 FSMGen-strict-clean.** Closed the LAST open ISF-emit strict-FAIL: AXI+ACE `ihi0022_h_c` failed on `rule 'constraint_48' assignment actions require '(port expr)'` because its drive value was free PROSE (`(RLOOP the value that was presented on the ARLOOP signal)` — a loopback obligation). New pass `drop_unrenderable_rule_values` drops a rule whose any drive value fails `is_safe_isf_scalar_value` and records an `isf_rule_value_*` residual (prose value unrecoverable → honest residual, never a fabricated `(port expr)`). Measured: only `ihi0022_h_c` (4 rules) carries a prose-valued drive corpus-wide → byte-identical elsewhere; AXI+ACE FAIL→PASS; full sweep over all 70 current emits **70/70 strict-clean** (was 69/70; 0 PASS→FAIL). The ISF-emit strict-FAIL frontier (Levers A/B/C/F + this gate) is fully CLOSED. `kg-bench` 156/156; `run_ci.sh` GREEN (lib 1709, +1); WIRE-BASED-100 orthogonal by construction. KM `[[isf-unrenderable-rule-value-residual]]`.
- ✅ **`KG-ISF-COMPLETENESS.2a.v` (`2026-06-23`) — Done (CODE): ISF unconditional-rule-overlap conflict residual (Lever C) → 6 docs FSMGen-strict FAIL→PASS, incl. all 3 wire golds.** The `.isf` emitter now resolves the `isf_conflicting_rule_writes` cross-guard conflict the same-guard dedup missed: an unconditional rule (empty guard) overlaps every guard, so a new `drop_unconditional_overlap_conflicts` pass drops each differently-valued rule on a signal that has an unconditional driver and records an `isf_unconditional_overlap_*` residual (the unconditional value kept; never a fabricated `(priority …)`). MEASUREMENT correction: the conflict was NOT LPI-only (the "27/28 clean" tally was stale). Fix takes AXI `ihi0022_l` / AHB `ihi0033_c` / AXI-Stream `ihi0051_b` wire golds + LPI + LTI `ihi0089_d` + NVMe FAIL→PASS; 0 PASS→FAIL. **Honest current-emit tally (after cleaning 37 stale cache-cruft `.isf` — `adapt` emits one primary actor per doc): 69/70 PASS**, the lone FAIL = AXI+ACE `ihi0022_h_c` on the orthogonal `(port expr)` grammar (a spun-out Non-Goal) → the ISF-emit strict-FAIL frontier is effectively closed (Levers A/B/C/F resolved). `kg-bench` 156/156; `run_ci.sh` GREEN (lib 1708, +2); WIRE-BASED-100 orthogonal by construction. KM `[[isf-unconditional-rule-overlap-conflict]]`.
- ✅ **`KG-ISF-COMPLETENESS.2a.iv` (`2026-06-23`) — Done (CODE): ISF enum value-literal emit-gate (Lever F) → JEDEC HBM2 strict-clean; + Lever A drift corrected.** The `.isf` emitter now drops an enum carrying a bare binary-looking token (only `0`/`1` digits, length ≥ 4 — an un-qualified binary literal FSMGen rejects; verified by a value sweep against the real FSMGen: `1000`/`1111` fail, `69152`/`999`/`4'b1000` pass) to an `isf_enum_value_literal_*` residual, instead of emitting a literal FSMGen rejects. HBM2's mega-conflated `TABLE` (REPAIR_LANE binary codes mis-read as decimals) is now residualized → HBM2 FSMGen `--strict --check` **success / 0 diagnostics** (only `TABLE` dropped; `EXTEST_RX`/`DWORD_MISR` kept). **Corpus-wide byte-identical EXCEPT HBM2** (wire golds + GIC-600 `69152`/ARM-Debug `3360`/CoreSight/Avalon enums all carry legit decimals the criterion never flags). The initial "count-derived width overflow" hypothesis was DISPROVEN by a before/after `.isf` diff + the FSMGen sweep (measurement-first). **Lever A drift corrected:** DTI is already strict-clean (`ATST` = `1'd1`); the `CORPUS-COVERAGE.2` #8 strict-FAIL was a stale log entry. **Strict tally now 27/28 renderable docs clean, 1 FAIL (LPI Lever C).** `run_ci.sh` GREEN (lib 1706, +2); `kg-bench` 156/156; WIRE-BASED-100 orthogonal; doctrines GREEN. KM `isf-enum-value-literal-emit-gate`; book `pipeline/isf-adapter.md`.
- ✅ **`KG-ISF-COMPLETENESS.1c.ii` (`2026-06-23`) — Done (MEASURED, read-only): the bulk dense-prose phantom class has no clean within-doc structural gate → bounded honest residual; the fix is upstream.** Measured the multi-word single-`REL-INFERRED` leading-noun subjects (eMMC: 109). A `.1b.ii`-style connectivity fold is DISPROVEN unsafe on AXI+ACE `ihi0022_h_c`: `caching Manager`→`Manager` is correct but `Manager component`→`component` is WRONG (the agent is the modifier, not the noun-phrase head; the agent token's position varies). Within one doc a real descriptive reference (`caching Manager`) and a phantom (`basic bus`) are structurally indistinguishable (same single relation, same provenance, same shape; no `ProtocolActorRecord` grounding surface). A drop is forbidden by the genericity guardrail; a participation threshold by completeness. **The genuine fix is upstream relation-subject extraction precision on descriptive prose** (the owner-directed `NLP-SHALLOW-PARSE` direction), not a downstream rule → recorded as an honest residual (phantoms never reach `.isf`). **Lever E fully scoped: `.1c.i` clean structural win SHIPPED; `.1c.ii` upstream-NLP-gated.** Report `docs/research/agent-identity-prose-class-measurement.md` §8. No code change → all oracles orthogonal; `check_doctrines.sh` GREEN.
- ✅ **`KG-ISF-COMPLETENESS.1c.i` (`2026-06-23`) — Done (CODE): dense-prose trailing preposition/auxiliary strip — the measurement-clean first gate of Lever E.** Same-session continuation of the `.1c` probe. Extended `consolidate_trailing_fragment` (`ir/evidence.rs`) with a new `NON_ACTOR_TRAILING_FUNCTION_WORDS` const (prepositions + auxiliaries/modals, a SUBSET of the leading function-word lexicon, EXCLUDING conjunctions — which stay `.1b.iii`'s job; drift-guarded), so a dense-prose relation subject like `host has`/`host to`/`cache in` folds onto its leading agent (`host`/`cache`) the way `.1b.i` folds `Subordinate extends`→`Subordinate`. **Verified per item:** new-binary `evidence→semantic→intent` cascade on JEDEC eMMC → **actors 153→138** (the 4 aux/prep `host` variants merge onto `host`; 29 phantom names removed; the remaining `host selects`/`host tries` are trailing-*verb* residuals → `.1c.ii`); `host.isf` strict-clean (62 signals, FSMGen success/0 diag). **No regression:** WIRE-BASED-100 = 1.000 on fresh-Pattern new-binary evidence (constraints APB/AHB/AXI 6/6·6/6·3/3, relations APB/AHB/AXI/SWD 5/5·6/6·6/6·1/1, temporal 3/3·4/4·3/3; SWD lone constraint 0/1 documented promotion-only); `kg-bench` 156/156; `run_ci.sh` GREEN (lib 1704, +2 tests). ADR-0006 (closed-class grammar; 0 ≥8-port actors are `X<aux/prep>` across all 78 docs → no real agent renamed; wire golds byte-identical). README/book/KM/live-docs synced; task leaf carries the enforced acceptance checklist. **Frontier → `.1c.ii`** (single-relation noun-phrase precision, deferred-with-trigger).
- ✅ **`KG-ISF-COMPLETENESS.1c` (`2026-06-23`) — Done (measurement-first PROBE, read-only, docs-only): Lever E agent-identity precision for the DENSE-PROSE doc class OWNED + scoped.** Fresh-session PNT pivot off `CORPUS-COVERAGE.2` (owner "attack substantive gaps, not easy incremental"): the re-ingest tail had become reliably lever-surfacing (#27 eMMC → E, #28 HBM2 → F), so the high-value move is ACTING on a surfaced lever, not grinding more re-ingests. Read-only over the 78 persisted IntentIR docs. **Findings:** (1) the eMMC explosion (153 actors / 349 rel) is a **relation-subject extraction-precision** problem — 148/153 actors are CONNECTED phantoms minted from a single `REL-INFERRED` relation whose leading token is a noun (so `.1a` passes, `.1b.iv` can't touch); only 5 pure-unconnected, and those are the grounded-keep role terms `.1b.iv` correctly preserves. (2) A name-SHAPE-only drop is DISPROVEN unsafe — AMBA's REAL agents (`agent`/`controller`/`decoder`) share the same shape classes as eMMC's phantoms → would fail WIRE-BASED-100; the fix must be grammatical normalization or participation/grounding, never shape. (3) The doc class is DENSE-PROSE (AXI+ACE `ihi0022_h_c` 189 + CHI 87 explode too; the terse AXI gold `ihi0022_l` 21 is clean). **Scoped:** `.1c.i` (extend `.1b.i` trailing-strip to prepositions+auxiliaries — corpus-wide safety MEASURED CLEAN, 0 ≥8-port actors are `X<aux/prep>` across all 78 docs, reach 138 names/~17 docs) = landable first gate; `.1c.ii` (single-relation noun-phrase phantom precision, the bulk ≈120) deferred-with-trigger pending its own measurement. Report `docs/research/agent-identity-prose-class-measurement.md`; KM `agent-identity-prose-class-measurement`; book honesty caveat in `pipeline/evidenceir.md`. No code change → WIRE-BASED-100 + `kg-bench` 156/156 orthogonal; `check_doctrines.sh` GREEN. **Frontier → `.1c.i`** (trailing aux/prep strip — best on a fresh session per the high-stakes gate-code rule).
- 🚧 **`CORPUS-COVERAGE.2` (`2026-06-23`) — In Progress (28 of 57 docs re-ingested): #28 = JEDEC HBM2 DRAM (diagnostic structured contrast) CONFIRMS the #27 phantom explosion is PROSE-SPECIFIC — HBM2 actors 52→38 CONSOLIDATED (like AMBA/CoreSight), transactions 0→3, BUT its `.isf` strict-FAILS on a generic-`TABLE` mega-enum emitting binary values as bare decimal tokens (`REPAIR_LANE_8`=`1000`) → NEW spun-out lever F (ISF enum-value literal format, kin to Lever A). #27 = JEDEC eMMC 5.0 (first descriptive-prose register/protocol spec) is a MIXED refresh — genuine transactions 0→6 (all real eMMC ops) BUT a phantom-actor explosion (actors 20→153) the AMBA-tuned agent-identity gates don't catch on prose → NEW spun-out lever E (KG-precision; the `.isf` is unaffected — emitter lowers only the renderable actor). #26 = Intel VT-d 5.0 (first distinct-vendor re-ingest) is a GENUINE refresh that CORRECTS the #22 "already current-binary-equivalent" survey — flagged current off its 103 registers, it gained 15 message-fields + 2 transactions because its stale evidence predated those surfaces. The CoreSight SoC-600 cluster (#23=0701, #24=0200, #25=0100) is COMPLETE — all genuine consolidation+transaction refreshes; #22 = Cortex-A76 TRM byte-identical confirmation.** Fresh-session PNT slices, register/TRM phase, Docling CPU (RAM steady 67–83% free, `.4a` guard armed, Ollama idle), deterministic cascade `evidence`→`semantic`→`intent`→`adapt --target isf`, real `fsmgen --strict --check --json` clean on 25 of 28 (3 strict-FAIL on emitter levers A/C/F). **#26 VT-d:** register_records 103 held, `message_field_records` (absent)→15/4 containers (`.10c` structure family fires on `Root-Entry Format`/context tables — actualizes the book's `.10c` claim; corpus in-memory-structure total 1,220/11→1,235/12 docs), transactions 0→2 (recognition-only), actors 14→13; 0 relations (memory-mapped — honest); 8/100 INCOMPLETE; `agent.isf` strict-clean. **Phase correction: the one-pass register-count survey under-counts refresh value — a register-current doc can still gain message-fields + transactions.** **#27 eMMC:** transactions 0→6 [boot/alt-boot/lock-unlock/DDR-mode/background/HW-reset — 5 grounded, all real ops]; register_records 17 held; interfaces 9→38; 0 message-fields (honest); `host.isf` strict-clean (62 signals); 51/100 ADEQUATE. **BUT actors 20→153 / relations 23→349** — fresh Docling of descriptive prose mints ~90+ phantom sentence-fragment actors (`host has`/`host to`/`cache in`/`B write`) that `.1a`/`.1b` (clean on AMBA docs) don't gate → KG-fidelity gap (bar #1), the `.isf` is unaffected. **Phase finding: the descriptive-prose doc-class behaves OPPOSITE to structured AMBA/CoreSight — fresh re-ingest worsens the actor surface, not refines it (HIGH-VALUE lever E for the USB/HBM/guide tail).** **#23/#24/#25 (CoreSight SoC-600 0701/0200/0100) are GENUINE refreshes (NOT byte-identical):** their stale evidence predated the `.1a`/`.1b`/`.1b.iv` agent-identity consolidation gates + the section-heading transaction recognizer → consolidation (e.g. #23 actors 60→47 / relations 64→41 / interfaces 20→8; #24 46→37/47→35/13→6; #25 44→34/41→31/12→5) + transactions 0→1–2 (NEW typed surface); registers held (597/631/833→828); `dp.isf` strict-clean (#23's 3 `ATB` rule-conflicts RESIDUALIZE cleanly, unlike LPI #7); `validate` no stage-staleness, 52–53/100 ADEQUATE; honest residuals (unlocated bit-fields + field-resets; #24/#25 + 1 `isf_temporal_unrepresentable`). **#22 Cortex-A76 was ALREADY current-binary-equivalent (byte-identical, the #21 class):** value = bundle restoration + confirmation; 24/100 INCOMPLETE. **Phase finding:** "already current-binary-equivalent" is NOT uniform — a doc's stale evidence sits at whatever binary last rebuilt it, so re-ingest still refreshes when that binary predated a later gate; the tail mixes pure confirmation (#21/#22) and consolidation/recognition refreshes (#17/#23/#24/#25) + bundle restoration; marquee table-family jumps now the exception (but #26 shows a register-current doc can still carry a real message-field + transaction refresh, and #27/#28 show re-ingest reliably surfaces NEW substantive levers — E prose-actor-precision, F enum-value-literal — rather than clean refreshes). **25/28 re-ingested docs strict-clean** (3 FAIL = DTI width-align Lever A, LPI rule-conflict Lever C, HBM2 enum-value-literal Lever F; #27 is strict-clean — its phantom regression is a KG-precision gap, not a strict-FAIL). No code change → WIRE-BASED-100 + register/wire golds + `kg-bench` 156/156 orthogonal by construction. **Status snapshot UNCHANGED** (`CORPUS-COVERAGE` stays `active`, `.2` In Progress). SpecForge NOT complete — re-ingest long tail continues.
- ✅ **`DOC-INTENT-TAXONOMY.4d.i` (`2026-06-23`) — Done (measurement + decision packet, read-only, docs-only; tracked reproducer `scripts/measure_cat4_csr_bit_recovery.py`): cat-4 RISC-V CSR bit-position recovery MEASURED non-viable as a deterministic build → honest residual, no code, no FR.** The planned "genuine buildable cat-4 lever" (a deterministic parser for RISC-V CSR bit-layout tables → `bits_high`/`bits_low`, auto-lowering via `.4a.ii`) was tested against the real source + the human-reviewed bit gold **before writing it**, overturning the pre-investigation's optimistic *"deterministically tractable"* verdict (`feedback_scoring_rigor`). **The bits live in the bit-layout IMAGE for 53 of 56** RISC-V Debug register-with-address headings (incl. the cleanest gold register `dmcontrol`, `![Image]` only — no table); **only 7 diagrams were flattened to a text table, and those are garbled or symbolic** — `dmstatus`'s explicit high-bit row is off by ~8 (places `ndmresetpending` at bit 16, gold 24), drops a 7-field middle band, mixes doubled cells + two stacked half-rows; `tdata1`'s positions are symbolic XLEN-relative — so a deterministic parse recovers ~0 correct fields and/or **fabricates** wrong bits. The proven tiling gates (`ir/register_bits.rs`) are **order-blind** (width-sum + name-multiset only), so a row-jumbled table could pass both gates with WRONG bits — strictly more dangerous than the VLM front-end, on the shared register path `.4a.ii` (24 docs / 6,570 fields) relies on. **Verdict: no Rust code, no FSMGen FR** (ISF already expresses register fields via `.4a.ii`; a speculative parser would fabricate — `feedback_verify_fsmgen_before_fr` / `feedback_isf_no_hacks`); the genuine lever is a **sharper VLM read** for the existing gated path (`recover-register-bits`), VLM-accuracy-bound, owned OUTSIDE the `.4` ISF-lowering program; the AIA sub-lever is blocked on RAM/Docling-gated re-ingest (`CORPUS-COVERAGE`) + prose-bound. Mirrors `.4c.i`. **Gates:** no code / no canonical mutation → wire golds / `kg-bench` / emitted `.isf` byte-identical by construction (WIRE-BASED-100 orthogonal); `scripts/check_doctrines.sh` green; `mdbook build` green; knowledge-map derive-and-diff in sync (119→120 facts). Report `docs/research/cat4-csr-bit-position-recovery-measurement.md`; KM `[[cat4-csr-bit-position-recovery-not-deterministic]]`; book `document-categories.md` cat-4 maturity refined. **Status snapshot CHANGED:** `DOC-INTENT-TAXONOMY` `.4d.i` Done; the cat-4 register-recall question is closed (honest residual, VLM-accuracy-bound); the `.4` actionable frontier is now exhausted except the FSMGen-gated `.4b`. SpecForge NOT complete.
- ✅ **`DOC-INTENT-TAXONOMY.4c.i` (`2026-06-23`) — Done (measurement, read-only, docs-only; tracked reproducer `scripts/measure_cat3_topology_recall.py`): cat-3 topology-capture recall is CAPTURE-RECALL-GATED, not abstraction-gated → no FSMGen FR (honest residual).** Measured the full 15-doc cat-3 (platform/system-IP) set vs the 4 cat-1 wire gold docs as a reference baseline. **Cat-3 topology capture is unfaithful: SPARSE** — 380 actors → 135 `signal_connectivity` edges = **0.355 edges/actor**, vs the SAME surface at **4.108 edges/actor** on wire docs (~12× denser there, because the protocol signal graph IS the topology); **three-quarters HALF-CONNECTED** — only **24%** of cat-3 edges carry both a producer AND a consumer (vs **85%** on wire; GIC-600's 66 edges collapse to 4 usable, GIC-400's 8 to 0); and **ROOTLESS** — **0/10** `infrastructure_signals` carry a resolved clock/reset source. Name quality is the *smaller* problem (endpoints **95%** clean; 12 escaped corpus-wide) — refining `.4c`'s "sparse + noisy" to **sparse + half-connected + rootless-infra with minor name noise**; robust to dropping the 4 borderline docs (cat-3 core 0.395 / 20% / 0). **Verdict: no FSMGen FR** (an FR on a 12×-too-sparse / three-quarters-broken capture would be unfalsifiable — `feedback_verify_fsmgen_before_fr`); topology stays an **honest residual**; the lever (if pursued) is **upstream extraction-recall owned OUTSIDE the `.4` ISF-lowering program** (denser/fully-connected connectivity + clock/reset source resolution), recorded as a cross-reference NOT a `.4` gap (mirrors `.4d.i` cat-4 + the cat-2 structure-recall frontier; `feedback_scoring_rigor`). Even with faithful capture, lowering also needs a multi-actor ISF emit + a construct FSMGen's behavioral ATL frontier does not subsume — resolved WITH FSMGen only after capture clears the bar. **Gates:** no code / no canonical mutation → wire golds / `kg-bench` / emitted `.isf` byte-identical by construction (WIRE-BASED-100 orthogonal); `scripts/check_doctrines.sh` green; `mdbook build` green; knowledge-map derive-and-diff in sync (118→119 facts). Report `docs/research/cat3-topology-capture-recall-measurement.md`; KM `[[cat3-topology-capture-recall]]`; book `document-categories.md` cat-3 maturity refined. **Status snapshot CHANGED:** `DOC-INTENT-TAXONOMY` `.4c.i` Done; cat-3 topology question closed (capture-recall-gated, honest residual); frontier → `.4d.i` (cat-4 code) / `.4b` Gap B (gated). SpecForge NOT complete.
- ✅ **`DOC-INTENT-TAXONOMY.4e` (`2026-06-23`) — Done (measurement triage, read-only, docs-only): the `conditional_rules` ISF-lowering shortfall (`.2` Result 3) is HONEST RESIDUAL, not a gap — and the `.2` per-category measurement phase is now COMPLETE.** Classified 603 `conditional_rules` across 9 representative docs (all 4 buildable categories) vs each doc's declared-signal inventory + consequent quality: A no-consequent prose 392 (65%) / B undeclared 14 (2%) / C placeholder 33 (6%) / D declared+action 164 (27%). The D "candidate-lever" bucket is **161/164 bare deontic modals** (`shall`/`must`/`shall not`), only **3/603** carry a concrete value/level cue → lowering would fabricate the obligation. **No buildable ISF lever, no FSMGen FR** — the adapter already lowers the 516 cleanly-grounded conditional obligations corpus-wide; the only upside is upstream EXTRACTION quality (`extract-constraints-llm`), recorded as a cross-reference, NOT minted as a `.4` gap (`feedback_scoring_rigor`). **Gates:** no code / no canonical mutation → wire golds / `kg-bench` / emitted `.isf` byte-identical by construction (WIRE-BASED-100 orthogonal); `scripts/check_doctrines.sh` green; `mdbook build` green; knowledge-map derive-and-diff in sync (117→118 facts). Report `docs/research/conditional-rule-lowering-triage.md`; KM `[[conditional-rule-lowering-triage]]`. **Status snapshot CHANGED:** `DOC-INTENT-TAXONOMY` `.4e` Done; `.2` Result 3 closed; the `.2` measurement phase is complete → remaining `.4` work is CODE (`.4d.i` cat-4 / `.4c.i` cat-3 measurement / `.4b` Gap B gated). SpecForge NOT complete.
- ✅ **`DOC-INTENT-TAXONOMY.4c` (`2026-06-23`) — Done (measurement + decision packet, read-only, docs-only): the category-3 (platform / system-IP topology) ISF-lowering question is decided.** Profiled 3 representative cat-3 docs (of 15) + re-verified FSMGen pin `d327129b7`. **Two halves:** (1) **cat-3's register half already lowers** (register maps + bit-fields via `.4a.ii` — CoreSight SoC-600 ~3,250 fields, GIC-600 33 regs — same road as cat-2; not the gap); (2) **cat-3 DOES carry a typed topology surface** (`signal_connectivity` producer→consumer graph — GIC-600 66 edges / SoC-600 6 — and `infrastructure_signals` clock/reset distribution), refining `.2`'s "hint-level" to **captured-but-sparse-and-unlowered**, but ISF has **no declarative static-topology construct** (composition is transaction-level only; FSMGen's ATL multi-actor frontier is behavioral generated-child wiring, not a declarative netlist; the SpecForge emit is single-initiator-actor so cross-component topology is structurally absent by design). **No FSMGen FR filed yet** — premature because the topology capture is sparse/noisy (6 edges across SoC-600's 60 actors; `None`/escaped actor names) AND ISF is a per-actor format where static topology may deliberately be the integrator's concern above per-module synthesis (resolve WITH FSMGen — `feedback_verify_fsmgen_before_fr`). Topology stays an honest residual; next lever = measurement `.4c.i` (topology-capture recall across all 15 cat-3 docs). **Gates:** no code / no canonical mutation → wire golds / `kg-bench` / emitted `.isf` byte-identical by construction (WIRE-BASED-100 orthogonal); `scripts/check_doctrines.sh` green; `mdbook build` green; knowledge-map derive-and-diff in sync (116→117 facts). Report `docs/research/cat3-topology-isf-lowering-decision.md`; KM `[[cat3-topology-isf-lowering-decision]]`; book `document-categories.md` cat-3 maturity refined. **Status snapshot CHANGED:** `DOC-INTENT-TAXONOMY` `.4c` Done; frontier → `.4c.i` (cat-3 measurement) / `.4d.i` (cat-4 code) / `.4e` (conditional triage) / `.4b` Gap B (gated). SpecForge NOT complete.
- ✅ **`DOC-INTENT-TAXONOMY.4d` (`2026-06-23`) — Done (measurement + decision packet, read-only, docs-only): the category-4 (CPU ISA) ISF-lowering Open Question is RESOLVED.** Measured the corpus's 2 cat-4 docs + re-verified FSMGen pin `d327129b7`. **Cat-4 intent splits into three:** (1) **CSR / register intent REUSES the existing register/storage abstraction** — no new ISF construct, no FSMGen FR (CSRs are structurally registers — RISC-V Debug captures its 44 Debug-Module CSRs as `register_records`; FSMGen titles `(storage (var … (fields …)))` the "register map / CSR" construct, `13a-actor-interface.md:419`/`:468` — the same construct `.4a.ii` lowers cat-2/cat-3 fields into); (2) **the cat-4 register gap is EXTRACTION RECALL, not abstraction** — RISC-V Debug captures 44 regs / 179 fields with name/access/reset/description but **0/179 located** (bit-layout column unparsed), RISC-V AIA captures **0 registers** (IMSIC/APLIC CSR intent in prose `conditional_rules`/`behaviors`; 211 empty prose `interfaces`), so fields reach `.isf` 0 times because they aren't *located*, not because ISF can't express them → spun out **`.4d.i`** (recover RISC-V CSR field bit positions + a RISC-V-shaped register recogniser; once located, auto-lower via `.4a.ii`); (3) **instruction / privilege-mode / exception / memory-ordering → honest NON-TARGET** (software-visible ISA semantics, not synthesizable hardware intent; no ISF construct + FSMGen lists none → no FR per `feedback_verify_fsmgen_before_fr` / `feedback_isf_no_hacks`; conditional-future only if FSMGen's SV/UVM path scopes ISA-model verification). **Gates:** no code / no canonical mutation → wire golds / `kg-bench` / emitted `.isf` byte-identical by construction (WIRE-BASED-100 orthogonal); `scripts/check_doctrines.sh` green (memory-arch + knowledge-map + task-acceptance); `mdbook build` green; knowledge-map derive-and-diff in sync (115→116 facts). Report `docs/research/cat4-isa-csr-lowering-decision.md`; KM `[[cat4-isa-csr-lowering-decision]]`; book `document-categories.md` cat-4 maturity refined. **Status snapshot CHANGED:** `DOC-INTENT-TAXONOMY` `.4d` Done; cat-4 Open Question resolved; frontier → `.4d.i` (cat-4 build lever) / `.4b` Gap B (gated) / `.4c` cat-3 topology / `.4e` conditional triage. SpecForge NOT complete.
- ✅ **`DOC-INTENT-TAXONOMY.4a.ii` (`2026-06-22`) — Done (CODE): the `.isf` emitter now lowers register bit-fields into FSMGen's declarative field-structured storage — 6,570 fields / 2,531 registers / 24 docs now reach `.isf` (was 0).** Gap A closed. `IsfStorageVar` gained `fields: Vec<IsfStorageField>`, fed by the pure `register_storage_fields` + `normalize_field_access` (`crates/specforge/src/ir/isf_ir.rs`) and rendered as `(storage (var NAME (width N) [(reset V)] (fields (field FNAME (bits HI LO) [(access …)] [(reset V)] [(enum (M V)…)]) …)))`. **Admission is structural / fail-closed (ADR 0006, no name list):** located fields only (unlocated → honest gap); a sanitized-name collision (count ≥ 2) drops the whole colliding group (handles repeated `Reserved`/`res0` gaps + flattened mis-extractions → FSMGen duplicate-name fail-closed); residual overlap fails the register's field block closed; access normalized to FSMGen's 10-token set (omit when unmapped); a field `(reset)` only when the parent reset is composed, as that value's own bit slice (matches the parent slice by construction); enum members that fit the field width. The unlowered remainder is the `isf_register_fields_not_lowered` adapter residual (folds in `.4a.i`, now superseded). **Measured live with the real emitter:** 6,570 fields / 2,531 registers / 24 docs (CoreSight SoC-600 1,166/1,118/974, GIC arch 424, CCIX 380, SMMU 332, CHI-C2C 276, …). **Gates:** the 4 WIRE-BASED-100 golds emit 0 fields → emitted `.isf` byte-identical (metadata-only / schedule-safe; orthogonal); RISC-V IOMMU / GIC / CoreSight SoC-600 keep `fsmgen --strict` `success / 0 diagnostics` (0 NEW); `inferred_storage[].fields[]` round-trip asserted on the live binary; `kg-bench 156/156`; full `cargo test 1702 / 0` (warning-deny, +6 tests incl. `register_fields_pass_fsmgen_strict_and_round_trip`); `cargo fmt`/`clippy -D warnings` clean; `run_ci.sh` green. **Status snapshot CHANGED:** `DOC-INTENT-TAXONOMY` `.4a.ii` Done (highest-leverage Gap-A lever); `.4a.i` superseded; frontier → `.4b` Gap B (FSMGen-deferred packet/flit) / `.4c` cat-3 topology / `.4d` cat-4 ISA / `.4e` conditional triage. SpecForge NOT complete.
- 🧭 **`FSMGEN-REFRESH-INTEGRATE-5.1` (`2026-06-22`) — Done (CLOSED): FSMGen SHIPPED declarative storage fields → bumped the pin `5ce0335c5 → d327129b7` + UN-GATED `DOC-INTENT-TAXONOMY.4a.ii`.** FSMGen ran `ISF-FIELD-STRUCTURED-STORAGE-FRONTIER.1`/`.2` and shipped exactly the construct the `.4a` FR requested: `(storage (var … (fields (field NAME (bits HI LO) [(access …)] [(reset V)] [(enum …)]))))` — metadata-only/schedule-safe (scheduled `.fsm` byte-identical with/without `(fields …)`), report key `inferred_storage[].fields[]`. **Verified empirically:** `*_passes_fsmgen_strict_validation` ×6 PASS, `run_ci.sh` green, `kg-bench 156/156`; no Rust code touched → emitted `.isf` byte-identical, WIRE-BASED-100 orthogonal. **Status snapshot CHANGED:** FSMGen pin now `d327129b7`; **`DOC-INTENT-TAXONOMY.4a.ii` (register bit-field emit, 12,638 fields / 32 docs at zero today) is UN-GATED and is the highest-leverage buildable lever**, superseding `.4a.i` (now a fallback); Gap B (`.4b`, packet/flit) stays FSMGen-deferred. FR marked SHIPPED in `docs/FSMGEN_FEEDBACK.md`. SpecForge NOT complete.
- 🧭 **`FSMGEN-REFRESH-INTEGRATE-4.1` (`2026-06-22`) — Done (CLOSED): bumped the FSMGen pin `030f8c273 → 5ce0335c5` + integrated the ACCEPTED field-structured-storage FR answer.** FSMGen answered the `DOC-INTENT-TAXONOMY.4a` FR: **ACCEPTED** as a real ISF gap + valid future direction, accepted the proposed shape (storage var + optional declarative field partition, checked metadata, fail-closed), but **NOT shipped** — gated on FSMGen's own `ISF-FIELD-STRUCTURED-STORAGE-FRONTIER.1` audit; FSMGen confirmed the no-hack stance. **Verified empirically on the new binary:** `*_passes_fsmgen_strict_validation` ×6 PASS (emitted `.isf` still strict-valid), `run_ci.sh` green (1696 tests / 0 failed, rustdoc, mdBook), `kg-bench 156/156`; no Rust code touched → WIRE-BASED-100 orthogonal, no contract break. **Status snapshot CHANGED:** FSMGen pin now `5ce0335c5`; `DOC-INTENT-TAXONOMY.4a.ii` (field-structured emit) STAYS gated on FSMGen shipping the construct; `.4a.i` (adapter honest residual + keep field maps as IntentIR metadata) is the FSMGen-endorsed near-term move. FR RESOLVED in `docs/FSMGEN_FEEDBACK.md`. SpecForge NOT complete.
- 🧭 **`DOC-INTENT-TAXONOMY.4a` (`2026-06-22`) — Done (measurement/design + verified FSMGen FR, docs-only): Gap A — register bit-field ISF lowering localized + decided.** The `.2` highest-leverage gap (register **bit-fields** reach `.isf` 0 times — 12,638 fields / 32 docs — while registers lower 1:1 to opaque `(storage (var … (width N)))`). Two read-only probes: (1) the bit-field intent is FULLY captured (`RegisterFieldRecord`, `source.rs:414`) and carried UNCHANGED into IntentIR (`IntentIr.register_records` clone, `intent.rs:193`), dropped ONLY at the ISF-emit boundary (`IsfStorageVar { name, width, reset }`, `isf_ir.rs:852`; opaque render `:391`) → **no SpecForge carry gap**; (2) an empirical read of the pinned `subs/fsmgen` (`030f8c273`) proved ISF `(storage …)` declares opaque width-only scalars only — **no named-bit-field/packed-record construct** (`set-field`/`extract` are runtime ops; not on the FSMGen backlog). **Decision:** a missing ISF abstraction → filed a **verified FSMGen FR** (`docs/FSMGEN_FEEDBACK.md`, `2026-06-22`) for declarative field-structured storage, NOT an emitter hack (`[[feedback_isf_no_hacks]]` / `[[feedback_verify_fsmgen_before_fr]]`; 3 emitter-only alternatives rejected as fabrication/loss). Gap B (1,220 msg-fields) shares the same missing abstraction + lacks an `Evidence→Intent` carrier. Report `docs/research/register-bit-field-isf-lowering-design.md`; KM `register-bit-field-isf-lowering-gap` (map 114→115 / 824 keys); book honest-residual note in `docs/book/src/pipeline/isf-adapter.md`. **Gates:** `scripts/check_doctrines.sh` green; `mdbook build` green; knowledge-map in sync; **no code/canonical mutation → WIRE-BASED-100 + `kg-bench` + emitted `.isf` orthogonal by construction.** **Status snapshot CHANGED:** `DOC-INTENT-TAXONOMY` `.4` opened + `.4a` Done; decomposed into `.4a.i` (adapter honest-residual, code) / `.4a.ii` (field-structured emit, gated on FSMGen) / `.4b` Gap-B carrier / `.4c` cat-3 topology / `.4d` cat-4 ISA / `.4e` conditional triage; frontier → `.4a.i` (RAM-light, no FSMGen dependency) then `.4b`. SpecForge NOT complete.
## Highest-priority remaining gap
- Current PNT frontier: `ACTIVE-TASK-EVIDENCE-CONTAINMENT.1.2` — accept the bounded active-root, semantic-part,
  provenance, current-state precedence, update/rotation, limit, and migration contract: In Progress
- All 10 task trees closed (R7-VALIDATION now done — .1/.2/.3/.4 complete, .5 deferred). 1018 Rust tests pass, clippy clean. PNT frontier exhausted across all active lanes: Done
- R7-VALIDATION task tree: all actionable leaves done (.1 temporal handshake gap, .2 multi-predicate antecedents, .3 KG-quality benchmarks, .4 adapter validation for .fsm); .5 (tracked approval evidence) deferred — R7 exhausted: Done
- None — all identified `direction_hint` consumers are now behind graph-first resolution: Done
- R15-GRAPH-DIRECTION-MIGRATION task tree closed — 3 leaves resolved: Done
- Earlier ROADMAP frontier exhaustion is superseded by the measured `.2.33d` adapter-trust repair and the
  remaining 23-document corpus refresh queue; R7-VALIDATION.5 remains deferred: In Progress
- Latest unseen-protocol live run:
- `IHI0051_B_2021-04_AMBA_AXI_Stream_Protocol_Specification.pdf` converged in `2` pipeline iterations and currently validates at `90/100 EXCELLENT`; parity-check table semantics now recover both structural KG ownership and carried width hints for the `*CHK` surface, declared graph-direction and width coverage are both `22/22`, `ACLK` / `ARESETN` now surface as infrastructure connectivity with a dedicated system-contract note instead of an ordinary missing-producer warning, six same-cycle temporal rules now carry explicit `0`-cycle windows, there are now no remaining typed temporal conflicts because assertion-vs-level comparison is polarity-aware rather than flattened, and the last carried interface-grouping residual is now gone
- Canonical per-signal polarity surface: Done
- Live projected polarity coverage across AXI/APB/AHB/AXI-Stream is now `1 / 1 / 1 / 1`; carry-through is real, and non-reset control polarity recovery now covers explicit asserted-when-level prose, unambiguous collective active-level prose, and safe mixed clause-local prose without suffix guessing
- The refreshed live AMBA baseline is now AXI `85/100 GOOD`, APB `90/100 EXCELLENT`, AHB `94/100 EXCELLENT`, and AXI-Stream `90/100 EXCELLENT`; the old `94/95/95/90` snapshot was stale relative to the current semantic/intent stack, fresh rebuilds from current tracked `IntentIR` artifacts restored AHB to the excellent lane, the latest AXI semantic-hint hygiene fix removed the last blocked-handshake residual plus the false `AWAKEUP` / `CRVALID` semantic-role conflicts, the infrastructure-row hygiene fix removed the false `ACLK` / `ARESETN` interface-direction conflicts by refusing to treat `External` as a protocol actor, and the latest tie-off hygiene fix removed the bogus `BROADCAST*` missing-consumer warning by stopping appendix `Tie-off` rows from becoming fake producer actors

## Validation Projection

<!-- validation_projection:start -->
- Last reviewed projected validation snapshot:
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `71/100 GOOD` from `generated/intent_ir/ihi0022_l_2025_08_amba_axi_protocol_specification/intent_ir.json`
  - `IHI0024_E_2023-02_AMBA_5_APB_Protocol_Specification.pdf` (`intent_ir`): `74/100 GOOD` from `generated/intent_ir/ihi0024_e_2023_02_amba_5_apb_protocol_specification/intent_ir.json`
  - `IHI0033_C_2021-09_AMBA_5_AHB_Protocol_Specification.pdf` (`intent_ir`): `65/100 ADEQUATE` from `generated/intent_ir/ihi0033_c_2021_09_amba_5_ahb_protocol_specification/intent_ir.json`
  - `IHI0051_B_2021-04_AMBA_AXI_Stream_Protocol_Specification.pdf` (`intent_ir`): `67/100 ADEQUATE` from `generated/intent_ir/ihi0051_b_2021_04_amba_axi_stream_protocol_specification/intent_ir.json`
- Highest-signal projected findings:
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf`: [warning:quality_score] IntentIR quality score is 71/100 (GOOD)
  - `IHI0024_E_2023-02_AMBA_5_APB_Protocol_Specification.pdf`: [warning:interface_signal_conflicts] 5 interface signal conflict(s) detected; conflicting direction/width evidence is still unresolved in the carried interface surface
  - `IHI0033_C_2021-09_AMBA_5_AHB_Protocol_Specification.pdf`: [warning:interface_signal_conflicts] 4 interface signal conflict(s) detected; conflicting direction/width evidence is still unresolved in the carried interface surface
  - `IHI0051_B_2021-04_AMBA_AXI_Stream_Protocol_Specification.pdf`: [warning:interface_signal_conflicts] 1 interface signal conflict(s) detected; conflicting direction/width evidence is still unresolved in the carried interface surface
- Rescan execution summaries: 0
- Targeted rescan queue:
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `intent_ir_canonical_surface_corroboration` for `AXLEN`, `BROADCASTSHAREABLE` (replay `evidence_ir -> semantic_ir`, action: run local NLP enrichment on EvidenceIR, rebuild SemanticIR and IntentIR, and validate whether..., 4 command hint(s), `planned_not_executed`)
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `intent_ir_canonical_surface_corroboration` for `AXPROT` (replay `evidence_ir -> semantic_ir`, action: run local NLP enrichment on EvidenceIR, rebuild SemanticIR and IntentIR, and validate whether..., 4 command hint(s), `planned_not_executed`)
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `intent_ir_canonical_surface_corroboration` for `ACADDRCHK`, `ACCRDTCHK`, `ACPENDINGCHK`, `ACREADYCHK`, `ACTIVATEACKCHK`, `ACTIVATEACKDCHK`, `ACTIVATEREQCHK`, `ACTIVATEREQDCHK` (replay `evidence_ir -> semantic_ir`, action: run local NLP enrichment on EvidenceIR, rebuild SemanticIR and IntentIR, and validate whether..., 4 command hint(s), `planned_not_executed`)
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `intent_ir_canonical_surface_corroboration` for `signal_connectivity_conflict_0001`, `signal_connectivity_conflict_0002`, `signal_connectivity_conflict_0003`, `signal_connectivity_conflict_0004`, `temporal_conflict_0003`, `temporal_conflict_0005` (replay `source_ir -> evidence_ir -> semantic_ir`, action: restart from SourceIR through EvidenceIR, SemanticIR, and IntentIR, then validate whether the..., 4 command hint(s), `planned_not_executed`)
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `intent_ir_canonical_surface_corroboration` for `ACREADY`, `ACVALID`, `ARACT`, `ARACTV`, `ARMMUATST`, `ARMMUSSIDV`, `ARPBHA`, `ARREADY` (replay `evidence_ir -> semantic_ir`, action: run local NLP enrichment on EvidenceIR, rebuild SemanticIR and IntentIR, and validate whether..., 4 command hint(s), `planned_not_executed`)
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `intent_ir_canonical_surface_corroboration` for `signal_connectivity_conflict_0001`, `signal_connectivity_conflict_0002`, `signal_connectivity_conflict_0003`, `signal_connectivity_conflict_0004` (replay `evidence_ir -> semantic_ir`, action: run local NLP enrichment on EvidenceIR, rebuild SemanticIR and IntentIR, and validate whether..., 4 command hint(s), `planned_not_executed`)
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `intent_ir_canonical_surface_corroboration` for `temporal_conflict_0001`, `temporal_conflict_0002`, `temporal_conflict_0003`, `temporal_conflict_0004`, `temporal_conflict_0005` (replay `evidence_ir -> semantic_ir`, action: run local NLP enrichment on EvidenceIR, rebuild SemanticIR and IntentIR, and validate whether..., 4 command hint(s), `planned_not_executed`)
  - `IHI0022_L_2025-08_AMBA_AXI_Protocol_Specification.pdf` (`intent_ir`): `intent_ir_canonical_surface_corroboration` for `temporal_signal_constraint_llm_sigcon_0000`, `temporal_signal_constraint_llm_sigcon_0002`, `temporal_signal_constraint_llm_sigcon_0003`, `temporal_signal_constraint_llm_sigcon_0004`, `temporal_signal_constraint_llm_sigcon_0006`, `temporal_signal_constraint_llm_sigcon_0007`, `temporal_signal_constraint_llm_sigcon_0008`, `temporal_signal_constraint_llm_sigcon_0009` (replay `evidence_ir -> semantic_ir`, action: run local NLP enrichment on EvidenceIR, rebuild SemanticIR and IntentIR, and validate whether..., 4 command hint(s), `planned_not_executed`)
  - ... and 21 more targeted rescan recommendation(s)
<!-- validation_projection:end -->
