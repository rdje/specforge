- ✅ **`CLAIM-VERIFICATION-ADOPTION.7.1` (`2026-08-30`) — Done (new gate-tier doctrine; reproducers `perl scripts/check_published_assertions.pl --self-test` and `--check`): the gate exists and executes.** A record binds a published value in a governed region to a field of a named producer's JSON report; the checker **runs the producer and compares** — the leg ten recorded instances went stale for want of, because a digest proves a region has not changed and never that its numbers still re-derive. Registered as the **twelfth** doctrine through the existing driver, so hook and CI wiring are untouched. Four closed outcomes — `derived`, `gated` (with an exact known-bad region inside the named control), `authored` (a tracked decision), `dated` (a revision that resolves) — and a fifth is **refused**, so a value can no longer be carried on a trajectory. Two surfaces disagreeing on one field fail with **no producer run**; a set is compared as an **enumeration**, never a size; and `excludes_self` is required the moment an enumerator returns the record's own publishing surface. **Self-test 16/16** on a disposable repository-local fixture, and the drift leg additionally observed **RED on real shipped prose** by revert-and-re-apply: `TOOLBOX.md`'s `**5** views` edited to `**6**` produced `is stale: 'views' re-derives to '5', published '6'`, both files restored byte-exact. **And it caught a live drift inside its own commit, unprompted:** adding this leaf's fact card moved `card_count` **249 → 250** while the seeded record still published 249, and all three legs fired — the same-transaction shape no reader and no digest control ever caught. **Two implementation findings recorded:** the coverage grammar first keyed on the `[claim:]` tag's own line and would have been blind to every value the tag governs (authors close a *paragraph* with it), and the first real-content probe chose a producer that is derive-and-diff over the file it was meant to watch. Ships in `inventory` phase; `.7.2` populates and flips to `frozen`. `DOCTRINE_ENFORCEMENT.md` §10 and the mdBook chapter both updated. **Status CHANGED:** `.7.1` Done; 12 registered doctrines. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.7.0` (`2026-08-30`) — Done (design freeze, no code; reproducers `perl scripts/check_live_document_size.pl`, `git show --name-only`): asked a third time, `.11a` does **not** fully hold either — and that settles the design question rather than starting a sixth correction.** `.11a`'s warning-line total was invalidated by its own commit (its `CHANGES.md` prepend crossed another band), and its "exactly three surfaces" was a classification published without its predicate — five files carried the literal, two being dated records §1 exempts. Both **withdrawn, not corrected**. Everything else in `.11a` re-derives. **Five consecutive rounds — `.6`/`.6a`/`.6b`, `.10`/`.10a`, `.11`/`.11a` — each invalidated by its own transaction**, so `.7` is split (`.7.0` freeze, `.7.1` gate, `.7.2` population) and the contract is frozen with every element traced to the instance that defeated the alternative: execute the producer and compare the field; four outcomes with **no** slot for "a trajectory shows it has held"; a population derived at check time and **never stored**, because three commits running moved their own population by describing it; a declared `excludes_self`, because `.11`'s classifier went green for the surfaces it named *because* it named them; cross-surface disagreement with no producer run; membership compared as an enumeration, not a size; and mechanism claims out of scope with an `adjudicated_against` field, since no checker can decide whether two accounts predict the same observation. **Status CHANGED:** `.7` split and `.7.0` Done; `.7.1` is the gate. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.11a` (`2026-08-30`) — Done (docs-only correction; reproducers `git show --name-only` over the attributed commits, `check_live_document_size.pl` vs `check_live_document_size.sh`): asked a second time whether `.11`'s findings held, re-derived them, and **five do not**.** Two mechanism claims read off co-occurrence: only **one** of the three commits said to have republished `identity_gated 7` edited a publisher, and `d23e8bae` withdrew `current_surfaces` from **two** surfaces, not three — the book chapter never carried it, and "all three" was quoted from that commit's own body rather than re-derived. Two set claims with the wrong denominator: **18 lines / 13 surfaces** is one checker's output where the doctrine driver emits **35** lines across **four** producers, and the ownership grep both scored `done` trees as owners (hiding `corpus_task_evidence_parts`) and turned green for the three surfaces it named, because writing the finding is what made `docs/tasks/` mention them. One reported defect **withdrawn entirely**: the `Opening Pressure Boundary (92e59c97)` table is a dated snapshot §1 exempts. **Both corrections are stronger than what they replace** — `.10` edited `TOOLBOX.md` while adopting the rule against carrying the value, and the real unowned set was four, not three. Everything else re-derives unchanged. `.11`'s own prepends also moved the warned surface count **13 → 15**; both new ones are owned. **Status CHANGED:** `.11a` Done; `LIVE-DOCUMENT-PRESSURE-HEADROOM.7` re-scoped to a reviewed assignment over the gate-level population. Self-catch rate for this adoption: **four**, three of them only because the director asked again. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.11` (`2026-08-30`) — Done (docs-only correction; reproducers `check_current_claim_census.pl --report`, `check_book_quantitative_claims.pl --report`): three earlier sweeps named their population from memory; the enumerating command returns **15** at `9fc76685` and **16** here, because writing this record joins the set — not two.** Six stale publications no gate could see; four of them here, two more below. `authority_outcomes.identity_gated` **7 → 8** on `TOOLBOX.md`, the mdBook enforcement chapter, and the census fact card — attributed per revision to `d23e8bae`, which registered one surface, noticed the *other* field that move touched (`current_surfaces` 39 → 40), withdrew that one on all three publishers, and left this one on the same sentence. The book chapter's **78 → 89** incomplete regions. The whole `.3b.3.3` book vector still carried in `mdbook-quantitative-census-freeze.md`, title included (**307/78/221/186** → 321/89/224/189) — the numbers `.6b` repaired on the chapter while quoting them off it. And a set claim short one member: the doctrine-adoption card's registered list, missing `PROOF-SEAL-CURRENCY` (**10/nine** → 11/10). **Withdrawn as one class, six stale plus three carried-but-correct:** `.10`'s derived-or-gated-never-carried rule also takes `authority_outcomes.derived` (unmoved), the `mdbook-quantitative-census-frozen` assertion's own eleven constants, and `control_audit`'s 7/7/6; §3 Leg 3 forbids substituting today's value. **Survivors say why:** `unresolved` **0** is gated, `views` **5** authored. **Recorded not repaired:** nothing checks §10 against the driver's `DOCTRINES` array, both 11 today — `.7`'s. **Gates:** book vector **321/8/89/224 identical** across the edit; **17** regions relocated by recorded SHA-256, counted by diffing the registries rather than summing the passes. **Two more of the same class, repaired:** `MEMORY.md`'s five-surface warning census against a producer that warns about **13** (three named by no tree, including `active_resume` at 100% of rollover — the pointer itself), and `LIVE-DOCUMENT-PRESSURE-HEADROOM`'s frontier still calling `.2a`/`.2b`/`.2c` pending while its tree section had them done. **Status CHANGED:** `.11` Done; `LIVE-DOCUMENT-PRESSURE-HEADROOM.7` opened; `.7` tenth instance, `.9` third. SpecForge NOT complete.
- ✅ **`LIVE-DOCUMENT-PRESSURE-HEADROOM.2c` (`2026-08-29`) — Done (control-plane change; reproducers `perl scripts/check_task_tree_catalog.pl --check`, `perl scripts/test_live_document_size.pl`): the task index is sharded by lifecycle, so the no-cap directive is now true rather than relocated.** `.2a` removed the file-count cap and moved the binding stop to `docs/TASK_TREE.md` at one row per tree; copying the Knowledge Map's quantum split would have relocated it again, because a landing listing every tree grows with project lifetime whatever the quantum. The landing now carries every **open** tree — anything not `done` or `superseded` — and routes the complete catalog to derived parts under `docs/task-catalog/`, so the bound measures **concurrent work in flight**, which ordinary work reduces by finishing trees. Measured: landing **404 -> 295 lines** with **25** open trees, **150** trees across **3** parts. Generation follows the two existing remedies exactly — `--write` renders landing and parts, `--check` derive-and-diffs both and refuses an unplanned part, and no member list is hand-edited. **One checker rule changed, over-strict rather than wrong:** `routed_membership` required its index inside the surface, which holds for `docs/knowledge/INDEX.md` but not for `docs/TASK_TREE.md`; location and one-hop membership are orthogonal, so a routed index may sit outside its collection when it is itself a classified surface. The control asserting the old rule now asserts what protects the reader, and a new control covers the surviving refusal. **Bounds derived, not copied:** a full part is deterministically 67 lines, so its health targets are set to keep a full part below the 80% warning — copying `fact_card_titles`' 80-line target would have warned at 83.8% with no action available. **Found and tracked, not widened:** `external_membership`'s classification refusal tests only that the path is a tracked Markdown file (`.6`). Suite **92 -> 93**, all passing, published count corrected on both surfaces. **It also falsified one of `.6a`'s carried values:** `current_surfaces` **39**, carried on a 29-revision trajectory in which it never moved, became **40** the moment this slice registered a surface. A trajectory shows what has not happened, never what cannot — all three publishers withdraw it to `--report` in the same commit, and `.7` gains an eighth instance. **Status snapshot CHANGED:** `LIVE-DOCUMENT-PRESSURE-HEADROOM` `.2c` Done, closing `.2`; `.6` opened; `CLAIM-VERIFICATION-ADOPTION.10` opened after directive 17's upstream-update check found three rules missing from the local restatement. SpecForge NOT complete.
- ✅ **`LIVE-DOCUMENT-PRESSURE-HEADROOM.2b` (`2026-08-29`) — Done (control-plane retirement; reproducer `bash scripts/check_live_document_size.sh`): the single-use ceiling-increase authority `.2a` consumed is retired, and its refusal was observed RED first.** With the tree clean at `ef3b4bb4`, the generic gate reported `surface 'task_evidence' has unused or banked ceiling-increase authority` and exited 1 — HEAD gate-failing on a clean tree by design, because the authority is single-use, `.2a` could not land without it, and the registry refuses to bank it once the new bands are committed. That RED observation is what separates a required retirement from a cosmetic one. After retirement the gate is green at **889** Markdown files across **55** governed surfaces and the registry holds only its control record. **Status snapshot CHANGED:** `LIVE-DOCUMENT-PRESSURE-HEADROOM` `.2b` Done; `.2c` is the remaining half of the director's no-cap directive, because the stop is currently relocated to `docs/TASK_TREE.md` rather than removed. SpecForge NOT complete.
- ✅ **`LIVE-DOCUMENT-PRESSURE-HEADROOM.2a` (`2026-08-29`) — Done (control-plane change; reproducers `perl scripts/test_live_document_size.pl`, `bash scripts/check_live_document_size.sh`, `perl scripts/check_task_tree_catalog.pl`): there is no longer a cap on the number of task-trees, and the removal is a declared exemption a checker enforces rather than a null.** The director decided there is to be no limit and that a completed tree stays as project history; measurement supported it on every axis — files **151/160 (94.4%)** against `lines_total` **7.5%** and `bytes_total` **6.0%**, the 160 authored in `cb65d5c7` as current-plus-headroom and never derived, and **no declared rollover**, making it a stop compliant work could reach with no remedy. **Delivered as a contract, not a null:** `surfaces.jsonl` gains a `cardinality_exemption` (authority, work unit, route surface, rationale) and `check_live_document_size.pl` gains `validate_cardinality_exemption`, refusing as separate faults a null with no exemption, a null in one band only, an exemption that also unbounds a resource dimension, an exemption routed to itself / to an unregistered surface / to one not covering the declared index / to one unbounded in any dimension, and an untracked authority. **Both enforcers moved together:** `check_task_tree_catalog.pl`'s independent `my $MAX_TASKS = 160;` is deleted — a registry-only change would have read as delivered while a literal in another file still capped the plane at 160. **Controls:** the suite goes **84 -> 92** cases, one positive and seven RED, all passing; the published suite count was corrected on both surfaces carrying it in the same commit. **Stated plainly:** this **relocates** the stop rather than abolishing it — `docs/TASK_TREE.md` now binds at one row per tree, **404 of 512 lines**, so about **108** further trees instead of 9, and that residual stop has no rollover either until `.2c` shards the index. ADR 0045 records the rationale and the relocation. **Status snapshot CHANGED:** `LIVE-DOCUMENT-PRESSURE-HEADROOM` `.2a` Done; `.2b` must retire the single-use ceiling-increase authority or the next commit fails on the unused-or-banked refusal. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.6b` / `CHANGES-LEDGER-ROLLOVER.3` (`2026-08-29`) — Done (docs-only correction plus one ledger transaction; reproducers `check_book_quantitative_claims.pl --report`, `check_current_claim_census.pl --report`): the drift `.6` and `.6a` corrected in `TOOLBOX.md` was live on two more surfaces, and both are now swept.** The mdBook chapter that documents the census published "56 exact evidence units: 11 derived, seven identity-gated, six registered, zero incomplete, and 32 excluded" against a producer reporting **70** and **11/7/4/0/48**, and `regions=307, registered=8, incomplete=78, excluded=221` against **321/8/89/224**; the `current-claim-census-freeze` fact card repeated both, with "56 exact authority units" in its own title. **Attribution measured from a cheaper instrument than `.6a`'s worktree replay:** the mdBook census's frozen contract is a *tracked registry*, so counting outcomes across its **31** revisions gives the exact trajectory with no checkout — 304/8/75/221 through `a4a08cd4`, **307/8/78/221 at `be3b12e6`, which is what the chapter says and was right when written**, then 308, 309/80, 318/89, 319, 320, and 321/8/89/224 from `fdda3c53` on. The chapter was never updated after `be3b12e6`, going stale at the **first** of those moves — `4dac5642` (`2026-08-27 02:14`) — and repaired at `2b9e8899`: **stale for two days and twenty hours**, under a green gate. (Corrected `2026-08-30` on re-verification: this record first published **seven** moves and **twelve days**; the moves are **six** and the twelve-day figure was the writing-to-settling span, not the staleness window.) **Remedy is `.6a`'s, not fresh numbers:** both surfaces state the old vectors as dated boundaries, name the two mechanisms that move them, and route current truth to `--report`; the card is retitled; and `TOOLBOX.md` withdraws the last mdBook count it carried (89), which stepped once in the same 29 measurements over which `registered` stepped twice. **Ordering constraint discharged:** the mdBook incomplete count was re-derived *after* the book edit and is **unchanged at 89** — the edit removed numbers rather than adding candidates; one frozen region moved 540 -> 547. **`.9` gains a sharper demonstration:** at `40acadb2` that 557-line chapter held **five** census regions (31, 49, 88, 306, 540) and **none** was a line carrying a stale count — `units` is outside the closed noun list and a backticked `key=value` matches no clause, so the surface documenting the census is one the census cannot see. **Ledger rollover in the same commit (`CHANGES-LEDGER-ROLLOVER.3`):** the committed root was 1,619 lines (89.9% of the 1,800-line health target), so the protocol's rule that the leaf tripping the signal performs the rollover applied. Sealed 12 records / 504 lines / 45,619 bytes into `segment-0015-2026-08-29.md` under a plan pinning `40acadb2` and its exact opening blob; dry run exact and warning-safe before the applied run; nothing edited, reordered, or re-bounded; root **1,114 lines (61.9%) / 176,223 bytes (69.1%)**, and 1,152 lines after this record. **Gates:** book census `--check` green at 39 files / 21 candidate files / 321 regions; current-claim census green; knowledge map 269 facts; fact-card catalog 248 cards; 4 ledgers satisfy the lossless protocol. **Status snapshot CHANGED:** `CLAIM-VERIFICATION-ADOPTION` `.6b` Done — every surface publishing the census counts is now swept, and `.7` owns the gate; `CHANGES-LEDGER-ROLLOVER` `.3` Done and `.4` opened for the pinned 75-record migration suffix, which occupies 58.6% of the line budget and 66.9% of the byte budget before any current record and is why each rollover buys only about a dozen slices. **Also corrected `.6a` on re-verification** (the director asked whether the finding was trusted): `.6a` published a *mechanism* it had not measured — "its own rollover sealed 18 records while adding 2". Re-derived from the registry blobs, `fdda3c53` and `5fe81128` both hold **59** evidence rows with **5** `CHANGES.md` rows and differ by exactly **one row removed and one added**, so the total held because one retirement cancelled one addition. Every number `.6a` published still re-derives and the withdrawal stands; only the cause was wrong — and `.6` had already written the true mechanism in its own commit body, so its published `60` contradicted itself. Routed to `.7`, whose gate must reach a claimed mechanism and not only a claimed count. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.6a` (`2026-08-29`) — Done (docs-only correction; reproducer: a detached-worktree replay of `check_current_claim_census.pl --report` over the 28 consecutive revisions `e6f5012d` -> `60a81db7` plus `.6`'s own older `50775894` anchor, each run with that revision's OWN checker): the four counts `.6` recorded as "confirmed unchanged" were already false in `.6`'s own commit.** `.6` withdrew two per-commit counters and confirmed eight; `.6a` re-derived those eight and found four had drifted, then refused to withdraw them on an endpoint comparison and measured the trajectory instead. **Measured:** `registered` **6 -> 5 -> 4** and candidate closure **86/51/35 -> 72/50/22 -> 69/49/20**, stepping at exactly two commits — `5fe81128`, whose own `CHANGES.md` rollover carried 14 claim-annotated regions out of the live window *inside the commit that published the old values*, and `1507adbf`, which rewrote the resume pointer and dropped its three `[claim: <id>]` annotations. So the leaf's open question (when `6 registered` became 5) is answered: **in the same transaction that published 6**. This is same-transaction invalidation, not slow decay, and no maintenance discipline could have kept the sentence true. **All four withdrawn** from `TOOLBOX.md` in favour of `--report` with the producer field named; **carried only what held at every measurement** — `derived` **11**, `identity_gated` **7**, no `incomplete` outcome, `unresolved` **0**, **39** current surfaces, **5** views. **A fifth number in the same section is also corrected:** `.6` published units `56 -> 57 -> 58 -> 59 -> 60`; the last element is **59**, not 60 — mechanism corrected by `.6b` on re-verification: `5fe81128` removed exactly one census evidence row and added exactly one, so the total held; the "sealed 18 while adding 2" account first published here was wrong — and its stated rule — one more excluded unit per rolling-ledger head — is not a rule: over the 27 transitions from `e6f5012d` the total rises **15** times, **falls twice**, and holds **10** times. **Rig recorded as a fact card** `[[worktree-doctrine-measurement-gitlink]]`: a worktree does not populate the `subs/fsmgen` gitlink, so the census exits before printing at every revision — two earlier attempts recorded nothing for that reason. **Gates:** census `--check` green at 71 units and `--self-test` **27/27**; claim-verification 6 claims / 12 commands; `7/7/6/0/0` control audit and mdBook **89** incomplete both re-derive; knowledge map 269 facts; fact-card catalog 248 cards. **Status snapshot CHANGED:** `CLAIM-VERIFICATION-ADOPTION` `.6a` Done and `.6b` opened for the two surfaces `.6`/`.6a` did not sweep — the mdBook doctrine chapter and the census fact card still publish the withdrawn counts plus two stale mdBook-census constants. `.7` still owns the gate. SpecForge NOT complete.
- ✅ **`SOURCE-IR-REPRODUCIBILITY.2` (`2026-08-28`) — Done: reviewed source regions resolve by content identity, not ordinal position; exact source regions **13/14 → 14/14**.** An anchor pinned to `elem_00219` broke when ingest moved the prose to `elem_00230`. The obvious remedy was measured and rejected — matching the excerpt in the serialized record resolves 8 of 12 reviewed regions and leaves **4 ambiguous**. Shipped rule: a precedence over the region's own natural-language surface (prose text; table caption, then caption+cells; figure caption), failing closed on zero or multiple matches. Attributed by one replay and two projections: every other global metric identical (10/14, 8/12, 45/45, 120/120, 8/16, 0 fabricated, 0 unexplained), exactly one cell changed, no reviewed fact moved. `--self-test` **11/11** with six observed RED perturbations. Eight pinned surfaces moved, including the compiled `exact_source_regions != 13` literal; `.8d`'s record was not re-stamped — `.2` publishes its own replay at revision `5fe81128`. **Gates:** workspace **470/168/1,369/4**, clippy clean, mdBook green, 3,604 files / 1,331,419 KiB scratch removed, residue empty. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.6` (`2026-08-28`) — Done (docs-only correction; reproducers `check_current_claim_census.pl --report`, `check_book_quantitative_claims.pl --report`): three counts this project published about its own claim doctrines had drifted, under a fully green gate.** All **11** counts `TOOLBOX.md` publishes for the three claim doctrines were re-derived from their named producers — **8 confirmed, 3 stale** — rather than patching only the one noticed. **Two of the three were per-commit counters, not constants:** the census unit total and its excluded count rise by one for every slice that prepends a rolling-ledger head — 56 → 57 → 58 → 59 → 60 across `50775894`, `e6f5012d`, `f9e785ca`, `fdda3c53`, and this commit — so re-carrying them is stale on landing (the first attempt wrote 59/35 and this commit made them 60/36); both are **withdrawn** from the prose in favour of `--report`. The one genuinely stale constant is corrected: mdBook incomplete assertion regions 75 → **89**. Confirmed unchanged: 11 derived, 7 identity-gated, 6 registered, 0 incomplete, 86 produced anchors, 51 exact evidence keys, 35 registered annotations, 0 unresolved, and the 7/7/6/0/0 control audit. **Attributed per revision from Git:** census units 56 at `50775894` (correct when written), 57 at `e6f5012d` (`.5`), 58 at `f9e785ca` (`.11`), 59 at `fdda3c53` (`.12`); mdBook incomplete 75 at `50775894`, 89 from `e6f5012d` on — so `.5` started both drifts and `.11`/`.12` widened one. **Mechanism measured, not inferred:** the census closes a `[claim: <id>]`-annotated region on the **presence** of the annotation (TOOLBOX's counts are 3 of the 35 `registered_annotations`, never among the 51 `exact_evidence` keys), and `claim-provenance-gate-active` digest-binds `TOOLBOX.md` only as an unchanged file — neither leg reads a number in a sentence. **Ledger rollover in the same commit (`CHANGES-LEDGER-ROLLOVER.2`):** `.6`'s own record crossed the mandatory 90% line signal (committed root 1,608 lines = 89.3% of the 1,800-line health target; the record took it to 90.9%), and the protocol refuses the append unless the same change performs the declared rollover. Sealed the 18 oldest opening post-migration records into `segment-0013-2026-08-28.md` (259 lines / 21,230 bytes) under a plan pinning boundary commit `fdda3c53` and its exact blob — dry run green before the applied run, no record edited or reordered, no limit moved, retained 75-record migration suffix untouched — leaving a 1,348-line root that is **1,390 lines (77.2%)** after this slice's two records. One census evidence region went with the sealed records and was **retired, not re-anchored**; its line survives byte-exact at `segment-0013-2026-08-28.md:171`. **Status snapshot CHANGED:** `CLAIM-VERIFICATION-ADOPTION` reopens from `done` to `active` (`.6` Done, `.7` opened to bind each published count to its producer's exact report field) and `CHANGES-LEDGER-ROLLOVER` reopens with `.2` Done. SpecForge NOT complete. **Corrected by `.6a` (`2026-08-29`), measured per revision:** four of the eight counts listed above as "confirmed unchanged" — `6 registered`, `86 produced anchors`, `51 exact evidence keys`, `35 registered annotations` — were already **false in this very commit** (5 and 72/50/22), and the unit trajectory ends at **59**, not 60. The remaining four counts and the `7/7/6/0/0` control audit do hold.
- ✅ **`SOURCE-IR-REPRODUCIBILITY.11` (`2026-08-28`) — Done (measurement, read-only, scripts+docs-only; tracked reproducer `scripts/measure_ingest_content_loss.py --oracle`): the mechanism behind `.5`'s largest published number is now measured against docling-core itself, not read out of its source.** `.5` attributes 5,896 dropped items to `iterate_items(traverse_pictures=False)` by **reimplementing** two docling-core predicates. `--oracle` loads each retained converter document through `DoclingDocument.model_validate` in `.venv-docling`, calls `doc.iterate_items()` with no arguments — exactly as the embedded backend helper does — and compares the library's own yield against the model's prediction. **Frame: all 24 persisted artifacts whose converter document was retained, no sampling.** **43,614 converter text items; 22,127 yielded, 22,127 predicted, 0 disagreements**, reported in both directions and never netted, per document and per batch — including the two batched bundles (Arm Debug 7 page ranges, USB 3.2 9) where `self_ref` restarts in every range. **One definition of the traversal:** census and oracle both reach `Batch.traversal_exclusion`, so the oracle cannot confirm a drifted copy. **The account closes rather than merely matching:** every document round-trips through its own `export_to_dict`, so the oracle observes the document ingest traversed; and the residue is 39 empty `formula` items plus exactly the **22,088** content elements the live population holds, `unexplained` **0** everywhere. **Gates:** no Rust code, no canonical mutation, no ingest and no source PDF needed; `--self-test` **27/27** with **six observed RED perturbations** (netting the directions, dropping the batch qualifier, accepting a short probe, confirming without the round-trip proof, dropping the empty-text attribution, confirming while a document was skipped); the run exits non-zero unless every document was measured, agreed, and round-tripped. Report `docs/research/ingest-content-loss-adjudication.md`; KM `[[ingest-drops-figure-interior-text]]`; book `pipeline/sourceir.md`. **Status snapshot CHANGED:** the PDF-to-SourceIR conservation account rests on a re-runnable control rather than a reading of upstream source; `SOURCE-IR-REPRODUCIBILITY` `.11` Done, `.12` opened; frontier → `.12` then `.2`. SpecForge NOT complete.
- ✅ **`SOURCE-IR-REPRODUCIBILITY.5` (`2026-08-28`) — Done (measurement, read-only, scripts+docs-only; tracked reproducer `scripts/measure_ingest_content_loss.py`): no source content is lost at the ingest boundary, and 31% of what the converter emits is dropped as a defect.** `.1` reported three persisted elements — one each in the USB4 Connection Manager guide, USB 3.2, and Wishbone — as content the toolchain emits nowhere. `.5` re-ingested all three, read the `SourceIR` and the converter's own document from the same run, and aligned each flagged text **token by token**: **all three are `retained`**, zero persisted tokens missing, with 6 / 5 / 35 tokens *inserted* between them (`USB4 Host Enhanced SS Host Controller`; `Disabled Stall, Error, or SetFeature`; footnote 14 plus `Sampled · 4.0g`), every covering converter item carrying a `SourceIR` record. **`.1`'s three-paragraph loss finding is withdrawn** — its whole-string containment test cannot see a sentence the converter split or spliced into — so all **31** dropped elements are re-segmentation and re-ingest content loss is **zero**. **The measurement that replaces it is larger:** across the three documents **5,896 of 18,870 converter text items (31%) are dropped as a defect** — figure interior (5,875 `text`, 9 `caption`, 8 `footnote`, 4 `section_header`), reaching no `SourceIR` record and earning no residual when they should have reached one. A further 2,752 are dropped on purpose (2,722 furniture-layer headers/footers, 30 empty formulas), so the raw non-carry rate is 8,648 (46%); `unexplained` is **empty**, so the account is complete. `.12` corrected every surface that led with the 46% instead of the defect. Cause named in both artifacts: `docling_backend.rs` iterates with `traverse_pictures=False`, so `iterate_items` skips every child of a `PictureItem` except its own caption refs, and the skip takes every descendant with it. **Standing, not drift:** `--persisted` mode censuses the artifacts with no ingest — the I2C specification, which `.1` scores as reproducing **byte-for-byte**, discards **1,372** figure-interior items (**39 captions**), while the Arm external-debug guide discards none; a fourth re-ingest of the repository-owned I2S specification (also reproducing exactly) reaches **349 of 464** converter items with no record. **Two further defects measured:** `source_ref` does not identify one converter item under batched ingest (Arm Debug: 6,784 elements, 2,252 distinct refs, 1,883 reused), and preservation has come apart from **faithfulness** — the spliced fragments make sentences the specification never wrote, which no conservation check can see. **Gates:** no Rust code, no canonical mutation — nothing under `generated/` written or removed, producer refuses a modified `crates/` tree; `--self-test` **18/18** with **six observed RED perturbations** (the first being `.1`'s own test, which reproduces `.1`'s answer); `mdbook build` green; knowledge-map derive-and-diff in sync (265→266 facts). Report `docs/research/ingest-content-loss-adjudication.md`; KM `[[ingest-drops-figure-interior-text]]`; book `pipeline/sourceir.md` gains a PDF-to-SourceIR conservation section. **Status snapshot CHANGED:** ingest content loss is measured at zero and the standing conservation gap is published; `SOURCE-IR-REPRODUCIBILITY` `.5` Done, `.8`/`.9`/`.10` opened; frontier → `.2` (content-addressed anchors). SpecForge NOT complete.
