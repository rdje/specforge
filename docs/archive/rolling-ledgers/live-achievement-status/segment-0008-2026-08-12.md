- ✅ **`CORPUS-COVERAGE.2.52` (`2026-08-11`) — Done: the OpenCAPI 25 Gbps PHY mechanical refresh retires two prose acronyms and exposes an inverted grounding filter.** Refresh #52 takes the 34-page / 760-element specification, smallest of the five gated remaining documents. Source structure was already current — the fresh SourceIR differs from the stale one in exactly two path leaves and a whole-tree diff finds no third — so the entire downstream delta is code. That delta is two prose tokens ceasing to be signals: `OD`, the drive-type acronym *open-drain* qualifying the real signal `PWR_BRAKE_N` in "1.8 V level signal only (OD, pull up is on motherboard)", refused because the parenthetical rule requires the noun-phrase head before the abbreviation to be a wire and that head is `only`; and `IS`, the English copula in "the X1 connector size provides full power and is most universal", which had also minted a false `X connector` actor. The order is causal — the direction statement is synthesized only from a `Drives` triple and the relation extractor returns immediately on an empty catalog — so refusing the single parenthetical candidate forecloses both. The adapter's honest block *moves* rather than clears, from `no behavioral content` to `no signals declared in interface`, still emitting no target, so 44/44 emitted ISFs are untouched; `validate` categorises the document `physical-link`, an ISF non-target whose rationale calls a thin result correct rather than a gap. **The refresh also found a real regression it deliberately did not fix: removing false signals *added* ungrounded records.** Semantic/Intent conditional rules move 0 → 2, and replaying the semantic stage from the stale EvidenceIR with the current binary reproduces the stale result exactly — proving the movement is input-driven, because emptying the declared-signal set flips the guard at `semantic.rs:293` that skips the grounding filter whenever a document declares no signals. A census over all 78 SemanticIRs finds 29 documents carrying 1,423 unfiltered conditional rules and 100 unfiltered signal constraints (`NOTICE`, `PDF`, `IMPLEMENTATION`, `MUST`), none of which reaches an emitted target; new tree `SEMANTIC-EMPTY-CATALOG-FILTER` owns the corpus-wide repair. Two guarded ingests and two cascades reproduce all nine artifacts and the 105-file / 35,661,265-byte bundle byte-identically; `CHAIN-CURRENCY` green at 24/78/78/78 with retention exactly the 24 declared bundles, `CORPUS-FRONTIER` re-deriving 57 = 52 + 5. Corpus frontier: 52 done / five remaining at 78/24/78/78 stages; next select and own #53.
- ✅ **`CORPUS-COVERAGE.4.1` (`2026-08-11`) — Done: the corpus frontier census becomes the eighth doctrine, so remaining work can no longer be a number each slice decrements.** `CORPUS-FRONTIER` is registered gate-tier in `scripts/check_doctrines.sh` — adapter `scripts/check_corpus_frontier.sh`, core `scripts/check_corpus_frontier_census.pl` (derive-and-diff archetype), declaration `doctrine/corpus_frontier/census.json`. Cohort and refreshed are derived from each document's own persisted `source.requested_path` and bundle retention; remaining is declared and then attacked four ways: IDENTITY catches a count that drifted in either direction, MEMBERSHIP catches a declared document that has since been refreshed, **OMISSION** scans the whole cohort for an unrefreshed document the declaration forgot — the check the original defect needed, since anything already missing from a list is invisible to every check written over that list — and PROSE requires the root task file to state the same counts. Proven on the real corpus rather than only fixtures: reproducing the historical defect (NVMe removed, denominator shortened to 56/five to match) raises five violations naming the omitted document, and a subtler negative that keeps the cardinality correct while listing a refreshed document (`usb_3_2_revision_1_0_2017_09`) still raises two, so a right count cannot hide a wrong set. It is gate-tier rather than CI-tier like its `CHAIN-CURRENCY` sibling because only each SourceIR's `source` object is needed: a bounded 8 KiB prefix read plus an exact brace scan that tracks string and escape state — a naive scan miscounts a brace inside a path, a regex would guess — costing 52 ms for all 78 documents. The workstation-shaped input, which source root counts as retired, lives in the declaration under a named owner rather than in the executable, and no document, vendor, or protocol name appears in any check (ADR 0006). A refresh must now move the declaration in the same transaction or the gate fails closed. Self-test 10/10 including the absent-corpus skip and the braced-path scan; no Rust changed, so every extraction oracle is orthogonal by construction; the driver reports **all seven executed doctrines PASS (8 registered)**. `CORPUS-COVERAGE.4` is complete; next own `.2.52`.
- ✅ **`CORPUS-COVERAGE.4.0` (`2026-08-11`) — Done: the corpus refresh frontier is derived from evidence, and the document a carried count had lost is restored.** Selecting refresh #52 meant stating the frontier, so the frontier was re-derived rather than read: over 78 persisted `source_ir.json` files plus the retention declaration, the cohort is every document whose SourceIR source is not the tracked in-repo `corpus/` tree (57), refreshed is those with a retained normalized bundle or a `requested_path` already off the retired `/Users/…` boot volume (51), and remaining is neither (6) — the identity `57 = 51 + 6` exact, against a tracked claim of 51 of 56 with five remaining. The sixth is `nvme_base_specification_2_0a_2021_07_26`, the 454-page NVM Express Base Specification 2.0a, source present at 5,154,704 bytes under the authorized `chipdoc` symlink, with no exception recorded anywhere against it. The count was carried, not derived: every refresh from `.2.29` to `.2.51` decremented the previous number by one — 27, 26, 25, … 6, 5 — so the `.2.29` record's adjustment of the 57-document cohort down to 56 for the project's own `ingest README.md` artifact outlived that artifact and propagated untouched through twenty-two slices. `.2.51` shows why candidate re-measurement cannot substitute: it re-derived all six documents it listed from their own SourceIR profiles and still could not see this, because re-deriving a list's rows never validates that list's completeness. Both refreshed witnesses are load-bearing — the path leg covers 50 of 51, but `usb_3_2_revision_1_0_2017_09` was refreshed before the volume migration and is recovered only by its retained bundle. Selection is unaffected: NVMe is the largest of the six at 4,577 elements, so smallest-retained-source still picks `opencapi_25gbps_phy_mechanical_spec_v10` at 760 elements for `.2.52`, with NVMe last at `.2.57`. Read-only over `generated/` with no Rust change, so every extraction oracle is orthogonal by construction; `CHAIN-CURRENCY` exits 0 at 23/78/78/78 with retention exactly the 23 declared bundles, and the active-task-evidence contract, fact-card catalog, Knowledge Map projection, doctrines, and mdBook build all pass. Corpus frontier: **51 done / six remaining** at 78/23/78/78 stages; next own `.2.52`, or `.4.1` to make the census mechanically enforced rather than carried.
- ✅ **`CORPUS-COVERAGE.2.51` (`2026-08-11`) — Done: the Arm SMMU Software Guide refresh isolates its entire delta to one retired caption-named enum.** Refresh #51 takes the 52-page / 600-element guide, smallest of six remaining, re-measured from all six persisted SourceIR profiles. The attribution is exact by construction: source structure was already current, so the fresh SourceIR differs from the stale one in exactly two values — the source path leaving the retired boot volume — isolating the downstream delta to code; and `CORPUS-CHAIN-CURRENCY.3` had already rebuilt the last three stages, isolating it further to the evidence stage. The delta is three `Enum TABLE THE_STREAM_IS_A_…` statements minted when an older binary named `Table 3-1: Stream Security determination` after its caption word and made the description sentences its members; `KG-ISF-COMPLETENESS.5.i`'s independently-evidenced fallback gate retires them, and `.5.ii`'s sentence-spine gate would have emptied the enum independently. Everything else is unchanged — 523 spans, 100 links, 87 anchors, 29 visual records, 13 semantic actors, 76 invariants, 17 contracts, five intent actors, 17 behaviors, 77 constraints — with Semantic and Intent dropping `symbol_definitions` 1 → 0 and the adapter keeping its `no signals declared in interface` block while losing its last rendered type. `validate` calls the document a self-declared `methodology-guide` at high confidence, a category-6 ISF non-target, so the empty inventory and blocked lowering are correct rather than thin. Two guarded ingests and two cascades reproduce all nine artifacts and the 138-file / 39,252,339-byte bundle byte-identically; `CHAIN-CURRENCY` is green at 23/78/78/78 with retention exactly the 23 declared bundles, growing the measurable evidence population by exactly one; 44/44 FSMGen strict, `kg-bench` 156/156, nine provider-free evals at baseline with the known SWD `CSYSPWRUPACK` residual unchanged, persisted paths and locality clean. Corpus frontier: 51 done / five remaining at 78/23/78/78 stages; next select and own #52.
- ✅ **`CORPUS-CHAIN-CURRENCY.2` (`2026-08-10`) — Done: normalized-bundle retention is declared and gated, closing the tree.** Reclaiming a bundle was refresh routine, so which documents could be replayed from the start of the chain was untracked state — only 22 of 78. A refresh now keeps its bundle: the schema-closed `doctrine/chain_currency/retained_bundles.json` declares the exact retained key set, and `check_chain_currency.sh` gained a second leg comparing that declaration with what is on disk. It fails closed both ways — a declared bundle that vanished is an unauthorised reclamation, a bundle no leaf declared is a refresh that never recorded what it kept — so the measurable population grows exactly one document per refresh, and reclamation must name its owning leaf, date, and reason. The full gate reports 22/78/78/78 current stages plus an exact retention match and exits 0; `--self-test` is 16/16 and three live negatives against the real declaration are each named and rejected. The audit also corrected a record: `.2.50`'s "184-file / 52,570,034-byte bundle" measured the document root — the bundle is 183 files / 51,754,156 bytes and the 815,878-byte difference is exactly its `source_ir.json`, with referential integrity complete and nothing lost.
- ✅ **`CORPUS-CHAIN-CURRENCY.1`/`.3` (`2026-08-10`) — Done: persisted-chain currency is now gated, and the corpus was rebuilt to meet it.** `.1` ships `scripts/check_chain_currency.sh`, the doctrine registry's first CI-tier oracle: it replays `evidence`/`semantic`/`intent`/`adapt --target isf` with `--dry-run` from each persisted upstream artifact, compares every emitted `.isf` against the replayed adapter's rendered `source_text`, excludes `validation_reports` exactly as the product's `*_ir_fingerprint` helpers do, prints the unmeasurable count, skips loudly with no corpus, and passes a 10/10 fail-closed self-test; the driver gained a tier column so a deferred doctrine reports as `DEFER` instead of vanishing, and `run_ci.sh` runs `--all`. Its first full run exposed real standing drift — 14/79 semantic, 15/79 intent, 65/79 adapter current — caused by `.2.43a.i`/`.2.43b` retiring the generic phase/gate producers while only later-refreshed documents carried post-retirement artifacts. `.3` rebuilt all 79 downstream chains from their unchanged EvidenceIR in 2m32s with no re-ingest (only the evidence stage reads a normalized bundle), re-validated the same 90 artifacts that carried validation state, and removed the `readme` and `spec` non-corpus scratch chains. **Product-status change: the corpus is 78 documents at 22/78/78/78 measured-current stages with 44/44 emitted ISFs FSMGen-strict clean, down from 57.** The loss is measured, not inferred: exactly 14 documents' SemanticIR interfaces collapsed from a heuristic bulk to zero under current authority and all 14 now block on `no signals declared in interface` — a 14/14 correspondence that exonerates the gate/phase retirements. 56 documents stay unmeasurable at the evidence stage until their bundles are backfilled. Doctrines, book, and locality gates pass. Frontier → `CORPUS-CHAIN-CURRENCY.2` bundle retention, then corpus refresh #51.
- ✅ **`CORPUS-COVERAGE.2.50` (`2026-08-10`) — Done: the OpenCAPI Data Link Layer refresh keeps grounded intent and drops an invented interface.** Two guarded CPU ingests reproduce 57 pages / 64 visuals / 53 tables / 111 sections / 527 elements and a 183-file / 51,754,156-byte bundle at SourceIR `13769977…ca7e`; two cascades reproduce all seven downstream hashes. Current authority removes four stale interfaces/ports/relations, 54 phases, 71 gates, 38 contracts, and the synthetic `endpoint_dlx.isf`; six semantic actors, 87 invariants, four assertions, 24 intent behaviors, and 87 constraints remain. Lowering blocks honestly on `no signals declared in interface` and emits no target. KG 156/156, 57/57 FSMGen strict, nine provider-free evals at baseline, full CI 1,804/five ignored, doctrines, book, path, and locality pass. Corpus frontier: 50 done / six remaining at 80/22/80/79 stages; next select and own #51.
- ✅ **`CORPUS-COVERAGE.2.50a` (`2026-08-10`) — Done: a passive obligation binds only a subject the document names before its lead.** Both deterministic extractors now require a candidate to occur at identifier boundaries before the sentence's first `must`/`shall` (optionally `not`/`never`) `be`/`remain` lead; active `must drive`/`must have … tied` grammar and table-row subject context are untouched by construction. The measured corpus class is 26 of 256 deterministic records across nine documents — later conditions and scopes, protocol and device names, the modal word `MUST` itself, and non-subject fields. An isolated old-versus-new replay over the 21 rebuildable documents changes exactly three, removing eight records and adding none, with the other 18 byte-identical. I2C, USB 3.2, and WISHBONE rebuild and validate twice at `3538fec9…fd49`, and the emitted I2C target loses a false `SCL = 1` rule. Focused tests, KG 156/156, 58/58 FSMGen strict, nine provider-free evals at baseline, full CI 1,804/five ignored, book, doctrines, path, and locality pass. Parent `.2.50` resumes the OpenCAPI Data Link refresh; corpus completion remains 49 done / seven remaining until it closes.
- ✅ **`CORPUS-COVERAGE.2.49` (`2026-08-10`) — Done: the GIC Overview Guide retains interrupt constraints without inventing an 83-interface controller.** Two guarded CPU ingests reproduce 45 pages / 44 visuals / 13 tables / 58 sections / 430 elements and a 139-file / 30,731,394-byte bundle. The GIC-version/CPU-family table is a feature matrix, not three timing records. Current generic authority removes 83 heuristic interfaces, 26 section phases, 54 prose gates, and the stale `controller.isf`; it preserves seven semantic actors / 87 invariants / four contracts / one assertion and three intent actors / four behaviors / 88 constraints / nine assumptions. SourceIR `421166d5…1dd5`, bundle `b1905855…b56`, and all eight downstream artifact/report hashes reproduce. EvidenceIR 350/five ignored, nine provider-free evals, KG 156/156, full CI 1,798/five ignored, and 58/58 FSMGen strict pass. Corpus frontier: 49 done / seven remaining at 80/21/80/79 stages; next create and own #50.
- ✅ **`CORPUS-COVERAGE.2.48` (`2026-08-10`) — Done: the OpenCAPI 32 Gbps PHY Signaling refresh keeps physical timing without inventing digital topology.** Two guarded CPU ingests reproduce 43 pages / 39 visuals / 27 tables / 73 sections / 318 elements; the committed-child run restores 130 normalized files / 34,843,407 bytes in 27.2 seconds. Source 376→318 removes 58 flattened chart/diagram labels; Evidence 613→547 additionally removes four synthetic acronym declarations and four generic-enum statements, while child `.2.48a` closes the ninth false timing row and preserves 60 grounded timings. Current authority removes three stale interfaces, 18 phases, 19 gates, and the four-port/one-enum `channel.isf`; final IntentIR retains seven actors / 21 behaviors / 75 constraints / four assumptions / 60 timings. SourceIR `e2c0b9e5…859f`, bundle `30f70511…8232`, and all eight downstream hashes reproduce. Nine provider-free evals, KG 156/156, and 59/59 FSMGen strict pass. Corpus frontier: 48 done / eight remaining at 80/20/80/79 stages; next select #49.
- ✅ **`CORPUS-COVERAGE.2.48a` (`2026-08-10`) — Done: scalar timing roles require independent source-cell geometry.** The complete 80-document / 105-timing-table census changes 608→585 scalar records by removing 23 spanned note/group rows across SWP, HBM2, eMMC, and OpenCAPI. EvidenceIR now requires an independent parameter cell and independent populated min/typ/max cells while preserving blank/`-` absences, equal values in distinct cells, and optional prose spans. The active OpenCAPI cascade holds both real table 5-11 rows, changes 61→60 timings, and reproduces `b9418108…8951` twice; twelve comparisons keep three non-rebuildable controls byte-exact. Focused tests, nine provider-free evals, KG 156/156, full CI 1,798/five ignored, and 59/59 current emitted ISFs under FSMGen strict pass. Parent `.2.48` resumes committed-release re-ingest and final signoff; corpus completion remains 47 done / nine remaining until it closes.
- ✅ **`CORPUS-COVERAGE.2.47` (`2026-08-10`) — Done: the Cortex-A76 optimization guide keeps engineering constraints without inventing an instruction interface.** Three guarded CPU ingests reproduce 46 pages / 71 visuals / 68 tables / 68 sections / 249 elements. Source 260→249 removes flattened visual labels; Evidence 971→960 and timings 3→0 while grounded prose remains. Current authority removes 228 stale instruction interfaces, 18 phases, 23 gates, 37 behaviors, and the 537-output `consumer.isf`. Final IntentIR retains 15 constraints / two assumptions; lowering blocks only on no declared signals and keeps two canonicalization residuals. The committed release reproduces SourceIR `a396a074…d478`, normalized manifest `872e7dba…1bae`, and downstream `b4b50237…9ade` twice. Nine provider-free evals, KG 156/156, five-stage validation, and 60/60 FSMGen strict pass. Corpus frontier: 47 done / nine remaining; active refresh #48 is OpenCAPI 4.0 32 Gbps PHY Signaling.
- ✅ **`CORPUS-COVERAGE.2.47a` (`2026-08-10`) — Done: timing tables need classified structural authority and scalar values.** The retained 39-document / 284-table surface changes from 2,144 timing records to 608 grounded records; Cortex instruction tables fall 151→0, I2S holds at five, and every survivor has min/typ/max data. Leading all-header rows and identifier-safe tokens now validate an ingest/prior classification without promoting unknown tables. Scalar extraction requires unique min/typ/max columns plus a row value; multi-variant tables remain honest timing residuals. Eight rebuildable cascades reproduce `58411566…6457a` twice with 32 validations; focused tests, nine provider-free evals, KG 156/156, full CI 1,796/five ignored, and 60/60 emitted ISFs under FSMGen strict pass. Parent `.2.47` subsequently closed; corpus completion is 47 done / nine remaining.
