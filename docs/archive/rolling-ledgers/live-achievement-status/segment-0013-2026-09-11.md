- ✅ **`WIRE-BASED-100.9d` (`2026-09-10`) — Done, and with it `WIRE-BASED-100.9` CLOSED (reproducers: `./target/release/specforge eval-extraction crates/specforge/test_data/llm_eval/seed_axi.json --provider skip`, `… seed_axi_temporal.json --provider skip`, `python3 scripts/measure_corpus_canonical_currency.py`): all six AXI numbers hold, both of the leaf's predictions were wrong, and the re-ingest exposed a 115-declaration loss no score could see.** Re-ingesting `ihi0022_l` restored the chain to canonical 3/3/2/2 (320 page artifacts / 333 visual / 0 residuals), taking the corpus to **27 measurable (34.6%) / 51 legacy** and gold-carrying documents to **5 of 7** — the two that remain are exactly the ones `.9a` routed out. `seed_axi` `signal_constraint P=R=F1=1.000` (tp=4 fp=0 fn=0) and `actor_signal_relation P=R=F1=1.000` (tp=6 fp=0 fn=0, source-tolerant + filtered), document-level 4/4 and 6/6; `seed_axi_temporal` `1.000` (tp=3 fp=0 fn=0). **PREDICTION 1 DISPROVEN, WITHDRAWING A `.9b` CLAIM:** AXI temporal was predicted to lose the `ARESETN`/`ARESETn` item and did not, because `eval::temporal_predicate_key` (`crates/specforge/src/eval.rs:458`) uppercases every name — case can never move a score, so `.9b`'s "unreachable by two independent routes" is retired and the sole cause of APB's loss is the deleted resolver. **PREDICTION 2 DISPROVEN:** `KG-ISF-COMPLETENESS.5.iv.a`'s open `AWATOP` prediction is answered NO — `(type AWATOP (bits 4))` is identical in the preserved legacy adapter and the new one. **NEW DEFECT, OWNED AS `WIRE-BASED-100.10`:** AXI's declared inventory fell **289 → 159** signals with all **110 `*CHK`** parity signals gone (`table_signal_declaration_provenance` 411 → 265 records, 115 → 0 `*CHK`) and the actor count rose **21 → 134** with prose words reaching the ISF interface. ~~The SourceIR is structurally identical across the re-ingest (320/333/286/3,652/527) … so the loss is in typed-declaration synthesis, not ingest~~ — **CORRECTED by `.10` (`2026-09-11`): those counts hold and every cell is byte-identical, but `table_kind` differs on 67 of 286 tables, `section_kind` on 56 of 527 sections and `diagram_kind` on 20 of 333 assets; the loss is in the SourceIR classifier.** What stands is that it was invisible to all six scores, which is the point. Across `.9`, **twelve carried numbers were re-derived: eleven held, one was withdrawn.** SpecForge NOT complete.
- ✅ **`WIRE-BASED-100.9c` (`2026-09-10`) — Done (reproducers: `./target/release/specforge eval-extraction crates/specforge/test_data/llm_eval/seed_ahb.json --provider skip`, `… seed_ahb_temporal.json --provider skip`, `python3 scripts/measure_corpus_canonical_currency.py`): the AHB gold survives its re-ingest intact, and `.9b`'s prediction that it would not is CORRECTED.** Re-ingesting `ihi0033_c` restored the chain to canonical SourceIR 3 / EvidenceIR 3 / SemanticIR 2 / IntentIR 2 (104 page artifacts / 70 visual / 0 residuals), moving the corpus to **26 measurable (33.3%) / 52 legacy** and gold-carrying documents to **4 of 7**. All four carried numbers re-derive EXACTLY: `signal_constraint P=R=F1=1.000` (tp=6 fp=0 fn=0), `actor_signal_relation P=R=F1=1.000` (tp=6 fp=0 fn=0, source-tolerant + filtered), document-level recall 6/6 and 6/6, and `temporal_rule P=R=F1=1.000` (tp=4 fp=0 fn=0). **Why AHB is not APB, derived rather than assumed:** the AHB temporal gold's only antecedent is `HREADY`, which the document declares with the spelling the prose uses, and no AHB gold item names the un-indexed `HSEL` — so the resolver `SPEC-TO-INTENT-ALIGNMENT.6d.ii.d.ii` deleted has nothing scored to lose here. `WIRE-BASED-100.4a`'s scope narrows to a gold whose antecedent names an INDEXED-FAMILY signal by its un-indexed prose spelling, while the mechanism still loses every such antecedent silently wherever no gold scores it. The rebuild was structurally inert — the same 1,322 statements / 172 anchors / 1,199 spans / 481 links / 70 visual records as the preserved legacy artifact — so the golds needed no re-anchoring at all (`content-anchored: re-resolved 0/17` and `0/4`). Same two dispositions as `.9b`: the ISF adapter moved `renderable` → `blocked` (`HCLK` demoted out of `(clock HCLK)`) and the declared select became `HSELx`/`HSELxCHK` under identifier opacity, with no ISF signal lost; the normalized bundle is HELD OUT per `RETAINED-BUNDLE-POPULATION-FROZEN`. SpecForge NOT complete.
- ✅ **`WIRE-BASED-100.9b` (`2026-09-10`) — Done (reproducers: `./target/release/specforge eval-extraction crates/specforge/test_data/llm_eval/seed_apb.json --provider skip`, `… seed_apb_temporal.json --provider skip`, `python3 scripts/measure_corpus_canonical_currency.py`): the APB gold can be scored again, and scoring it withdraws a published `1.000`.** Re-ingesting `ihi0024_e` restored the chain to canonical SourceIR 3 / EvidenceIR 3 / SemanticIR 2 / IntentIR 2 (48 page artifacts / 35 visual / 0 residuals), moving the corpus to **25 measurable (32.1%) / 53 legacy** and gold-carrying documents to **3 of 7**. `seed_apb` re-derives EXACTLY — `signal_constraint P=R=F1=1.000` (tp=6 fp=0 fn=0), `actor_signal_relation P=R=F1=1.000` (tp=5 fp=0 fn=0, source-tolerant + filtered), document-level recall 6/6 and 6/6. `seed_apb_temporal` does NOT: **`P=R=F1=0.333` (tp=1 fp=2 fn=2)** against the carried `1.000`, so the `2026-06-06` "APB is 100% on ALL three extraction aspects" headline is **WITHDRAWN**. Cause, attributed from producer history rather than a diff (`git log -S resolve_indexed_signal_family` returns exactly two commits): `WIRE-BASED-100.4` canonicalized the un-indexed prose select `PSEL` to the declared `PSELX` and corrected the gold's antecedent to it; `SPEC-TO-INTENT-ALIGNMENT.6d.ii.d.ii` (`f88d463d`, `2026-08-12`) deleted that resolver when it made document identifiers opaque and now ships the opposite behaviour as a test, so the gold's `PSELX` is unproducible and the `PNSE`/`PBUSER` rules each lose an antecedent. **Not a re-ingest artifact:** the preserved pre-rebuild SemanticIR already carries both defects — the loss had been persisted and unscoreable, hence invisible, for four weeks, which is the missing canonical-currency gate `.9a` routed out, now costing a real number. Recovery is `WIRE-BASED-100.4a`; the gold was not touched. The APB ISF adapter also moved `renderable` → `blocked` (`no source-grounded system clock/reset contract`) — the documented policy for the whole measurable stratum, not a new defect. **Second finding, now owned by `RETAINED-BUNDLE-POPULATION-FROZEN`:** the rebuild restored APB's normalized bundle, and there was no compliant way to record it — declaring it turns `PRODUCTION-GENERICITY` and `RESIDUAL-ACTIONABILITY` red (the retained set is frozen at 24 by a size literal and joined by set equality to a release-blocking behavioral qualification), and recording a deliberate reclamation is refused by the same validators (`reclamations != []`), while ADR 0025 mandates both operations. The bundle is HELD on the repository volume, nothing deleted, the declaration left at 24, and APB's EvidenceIR replay reads unmeasurable until that tree's `.3` restores it. SpecForge NOT complete.
- ✅ **`WIRE-BASED-100.8f` (`2026-09-01`) — Done (reproducer: `python3 scripts/measure_swd_frame_phase_scope.py`, `adversarial control` section): `.8d` survives an attempt to break it, which is a stronger result than `.8e`'s verification.** Replacing the production phase detector with a deliberately permissive proximity rule makes a nearest-preceding rule appear to rescue **5 of 11** frame fields — and every one is a false positive: `statement_1678` (*"a parity check is applied to all packet request and data transfer phases"*) is claimed for SEVEN fields while assigning none, `statement_1798` is about a FAULT response, `statement_0813` is 702 statements away. So the conclusion no longer depends on the production detector being right. **Status CHANGED in precision:** `.8d` said the document "never states the phase in text at all" for five fields; the phase WORDS do occur, what never occurs is an ASSIGNMENT — and that distinction is the whole trap, so it is corrected in the tree, the fact card and the book, and the control now ships in the reproducer. Other audit legs re-checked: all 11 gold statements fall strictly inside their attributed section anchor; every frame gold item carries exactly one fact. Honest residual: `56` is port-derived and NOT load-bearing (the production fact is the manifest at `produced: 0`); `11 of 11` is robust to the port. SpecForge NOT complete.
- ✅ **`WIRE-BASED-100.8e` (`2026-09-01`) — Done (reproducer: `python3 scripts/measure_swd_frame_phase_scope.py`): the director challenged `.8`'s findings; every CONCLUSION survives re-derivation and three of `.8d`'s NUMBERS do not.** Re-derived and holding: the restored oracle's scorecard reproduces exactly at HEAD (0/11, 4/4, 0/13, 1/1); chain currency 24/24; the corpus-wide surface-selective loss; `89d8dee7`'s own "five operations and 40 structurally admitted states" — and the population trap was checked, not assumed, since the 54 legacy artifacts carry 0/0/0 so both frames agree; that commit's exact blast radius (four SWD cards superseded, four book chapters updated, `ROADMAP.md`/the eval chapter/both trees/`swd-derivation-scored-100` untouched); the schema census; the corpus frontier; and `picture_0038`/`picture_0039` with a caption as their only observation. **Corrected:** "61 statements carry a stated phase name" is **56** (the probe dropped the gate's own `parse_count_word` rejection and mis-modelled `trim_matches`); "wrong for at least 7 of 11" was never computed and is **11 of 11, correct on 0**; "at most 4 of 11" section titles is not a valid ceiling — 4 literal, **5** counting `ACK responses`. The fix is a derivation rather than a reword: the new script ports both Rust functions and **checks its count-word list against the source**, with a demonstrated RED case. **Status CHANGED only in precision:** `.8d`'s deferral is supported more strongly by the corrected figures than by the originals, and the three wrong numbers had already spread to six surfaces in under a day — the same rule `.8c` published, now demonstrated on its author. SpecForge NOT complete.
- ✅ **`WIRE-BASED-100.8d` deferred and `WIRE-BASED-100.8` CLOSED (`2026-09-01`) — the proposed fix was disproven by its own read-only measurement before any code was written.** Binding a document-stated phase to nearby statements fires for all 11 SWD gold frame fields and is WRONG for at least 7 (`APnDP`/`RnW`/`Start`/`Parity`/`Stop` would inherit `transfer` and `Park` `data`, all against a gold of `request`; `A`/`ACK`/`DATAIN` sit 278/303/324 statements past the nearest phase-stating sentence, which says `response`). Section-title scope is honest but reaches at most 4 of 11, and no section title contains the word `phase`. **The binding is drawn, not written:** the field-to-phase membership lives in `Figure B4-1`/`B4-2`, captured as `picture_0038`/`picture_0039` with role `ambiguous` and a caption as their only observation — which also explains the retired extractor's 11/11, since it assigned the phase from the FIELD NAME rather than reading the frame. Deferred with its consequence: `serial_frame_field` stays **0/11** and nothing is minted, because a wrong phase is worse than a measured zero; re-open trigger is figure-content extraction, whose typed carrier and assets already exist. **Status CHANGED:** `.8` is `done` — oracle restored, legacy stratum a disposition, retired score corrected, residual measured — but restored over the 24 rebuildable chains ONLY; the APB/AHB/AXI wire golds stay unmeasurable until re-ingest. SpecForge NOT complete.
- ✅ **`WIRE-BASED-100.8b` (`2026-09-01`) — Done (reproducer: `./target/release/specforge eval-extraction crates/specforge/test_data/llm_eval/seed_apb.json --provider skip`): a document the oracle cannot read now yields an explicit UNMEASURABLE disposition instead of aborting the whole run.** `build_predictions` had one representation for a refusal — propagate and die — so any dataset naming a legacy document produced no output at all. Now the extractor returns `TaskOutcome::{Records, Unmeasurable}`, `unmeasurable_disposition` probes the artifact's own `schema_version` via the new `EvidenceIr::persisted_schema_version`, and a below-current artifact is reported with that version plus its re-ingest route while its gold items are withheld from every scorer; a hermetic control pins that WITHOUT the filter those labels would score as false negatives, and a second pins that any non-disposition failure still aborts. **Status CHANGED — a published label was also wrong:** censusing every persisted artifact, the 54 legacy documents are SourceIR schema 1 / EvidenceIR schema **2** / SemanticIR schema 1 / IntentIR schema 1, so **zero schema-1 EvidenceIRs exist** and "the 54 legacy schema-1 chains" was false of the artifact `eval-extraction` actually refuses; corrected across `MEMORY.md`, this ledger, the book, and three fact cards. Rebuildable golds unchanged value for value; `kg-bench` 156/156; specforge lib 472 (+2). SpecForge NOT complete.
- ✅ **`WIRE-BASED-100.8c` (`2026-09-01`) — Done (reproducer: `./target/release/specforge eval-extraction crates/specforge/test_data/llm_eval/seed_swd_derivation.json --provider skip`): the published SWD `29/29 at 1.000` is RETIRED; the honest current number is 5/29.** `protocol_operation` **4/4** and `interface_edge_timing` **1/1** hold; `serial_frame_field` is **0/11** and `protocol_state` **0/13**. Not a regression, separated from one three ways: the currency gate replays 24/24 current so the artifact is not stale; the loss partitions corpus-wide (0 serial_frame_fields in all 24 measurable documents, `machine_name` on 0 of 40 states, 5 protocol_operations) exactly as `89d8dee7`'s own `2026-08-12` entry predicted — *"retires 22 fixed-phase frame and four named-operation records; the generic producer retains five operations and 40 structurally admitted states"*, both numbers re-deriving today; and the producer at `89d8dee7^` gated on `serial wire`/`packet request`/`shift-dr`/`swdio`/`swclk` with a fixed `SerialFramePhase` enum, which ADR 0006 forbids. **So the 29/29 never measured generic capability**, and retiring it was mandatory rather than a trade. **Status CHANGED:** the defect was a PARTIAL retirement — `89d8dee7` superseded four SWD cards and four book chapters but missed every surface publishing the NUMBER (`swd-derivation-scored-100`, two more cards, `ROADMAP.md`, the extraction-eval chapter, both task trees), all now corrected with the retired number kept as dated history. Generic recovery is owned by `.8d`; the gold is untouched and still faithful. SpecForge NOT complete.
- ✅ **`WIRE-BASED-100.8a` (`2026-09-01`) — Done (reproducer: `./target/release/specforge eval-extraction crates/specforge/test_data/llm_eval/seed_i2c_signals.json --provider skip`): the scoring oracle runs again, and what it first reported contradicts a published score.** `eval-extraction` had refused EVERY document because an EvidenceIR's proof is taken over its public fields, `artifact_layout` is one of them, and `extract_on_copy` must relocate the artifact so the corpus is never mutated — so every claim premise's `inputs_sha256` bound the storage path and any copy elsewhere failed with `output or input topology is stale`. Relocation is now a supported operation (`EvidenceIr::load_relocated_to_artifact_base_root`: verify in place, move to `<base>/<document_key>`, re-derive the proof from the same verified SourceIR prefix and sealed context), an unsealed rewrite stays refused, and a hermetic control pins both halves. **First re-derivation since `2026-08-09`, `--provider skip`:** I2C `declared_signal` source-tolerant **1.000** (6/6, complete-gold precision 6/6); SWD `signal_constraint` **1.000** (1/1) and `actor_signal_relation` source-tolerant **1.000** (1/1); SWD derivation gold `protocol_operation` **1.000** (4/4) and `interface_edge_timing` **1.000** (1/1). Corpus provably unmutated; `check_chain_currency.sh --check` 24/24 current; `kg-bench` 156/156. **Status CHANGED, and not only upward:** the same run scores SWD `serial_frame_field` **0/11** and `protocol_state` **0/13** — document-level **5/29** against a published `29/29 at 1.000`. That is the published effect of `89d8dee7` (`2026-08-12`) retiring the fixed-phase frame and named-operation carriers on genericity grounds, not a regression; the defect is that the retired score stood as current for 20 days because the oracle that would have caught it was itself down. Owned as `.8c`. The 54 legacy chains (APB/AHB/AXI golds included) remain refused and unmeasurable. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.9e` (`2026-08-31`) — Done (reproducer: `python3 scripts/validate_residual_actionability_contract.py --check && python3 scripts/validate_residual_actionability_contract.py --self-test`): two reviewed cells could never have been met, and the repair is a gate rather than a corrected literal.** `project_captured_regions` builds a projected captured-region residual as `fact(region_id, cell["family"], f'{region_id}|{cell["family"]}', …)`, so both the `/family` a residual query matches on and the `/fact_key` it keys on come from the reviewed cell — the predicates already determine the key, and **`table_0004|toc_non_contract` and `elem_00017|informational_non_contract` were unsatisfiable by construction**. Normalising them to the region law is metric-neutral, proved by execution: the evaluator on the repaired dataset reproduces `result_snapshot.json` **byte-exactly**, and a leaf-by-leaf comparison gives an identical path set with exactly four differing leaves, all echoed gold strings, none non-string. **The published 8/16 does not move — both cells are unmet before and after — but they are now satisfiable**, which `.9c` needs. Two controls separate the competing hypothesis: an arbitrary third key is equally inert, while moving only a *matched* gold turns the same test RED. `residual_gold_law` now refuses a gold that is neither its own region law nor declared with the carrier that owns it; self-test **38/38**, and at the pre-change revision the gate names both defective cells. **Separately, `.9b`'s published "clean fmt" claim was false** — `cargo fmt --all --check` fails at `0a703cc0` and passes at its parent, so full CI was red for three commits; withdrawn in place and repaired. **Status CHANGED:** `.9e` Done; `.9c` and the population replay remain open in `.9`. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.9d` (`2026-08-30`) — Done (reproducer: `./scripts/validate_residual_actionability_contract.py`): the frozen required-residual contract is repaired AND gated.** `.8a` made the rule executable so it could not drift; **nothing in the repository ran it**, so it drifted anyway — red for **43 commits** after `SOURCE-IR-REPRODUCIBILITY.2` legitimately republished the reviewed result. Attribution is re-derived at each revision from its own producer, not read off a diff: digest MATCH at `893c2fba`, MISMATCH at `245b3b60`, differing in exactly one field. The re-pin re-derives the cell collections from the contract's own decomposition and **moves no published scalar** — 12 cells, 24/8/8/8 observations, corrected 16/8 unchanged — and `--check` re-derives the published **8/16** with a 28/28 self-test. `RESIDUAL-ACTIONABILITY` is now a gate-tier doctrine, proven fail-closed on both a drifted identity and a forged count. **Status CHANGED:** `.9d` Done; 13 registered doctrines, registry and §10 mirror verified in lockstep. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.9b` (`2026-08-30`) — Done (reproducer: `cargo test -p specforge-core --lib -- ir::semantic::tests::captured ir::semantic::tests::a_captured ir::semantic::tests::cited_table`): the captured-region residual carrier now covers captured **table** regions, not only figures.** `.9a`'s rule was measured before it was built, and two of its three legs did not survive. **The naive widening would have residualised regions that already reached a carrier:** `.8c` asks for the region's `evidence_id` or `figure:<asset_id>`, which no table-derived record ever cites, so every captured table region reads as unexplained — 354 of 354 in the AMD IOMMU chain and 210 of 210 in Arm Debug, including the tables that produced their registers and timing. Coverage for a table region is now asked in the `supporting_table_ids` vocabulary its own records use, gathered from all seven declaring SemanticIR surfaces; the current Arm Debug chain resolves to 16 cited / 194 unexplained. Because neither vocabulary can match the other's identifiers, every figure-kind residual `.8c` published is unchanged **by construction**, asserted in both directions. A table residual carries both of its real `EvidenceIR` identities, `visual_NNNN` and `table_NNNN`. **The prose leg is blocked upstream and is NOT claimed:** `EvidenceIR` carries no `SourceIR` content-element identity at all, so a prose residual cannot name the region a reviewed prose cell anchors on; `.9c` owns that. **Found and owned, not caused here:** the frozen residual-actionability contract no longer pins the current result and **nothing executes its checker** — `.9d` owns both halves. **Status CHANGED:** `.9b` Done; the reviewed-population numbers are unchanged because the replay that would move them is a later leaf. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.9a` (`2026-08-30`) — Done (reproducer: the per-cell `residual.*_actionable` fields of `current_result_snapshot.json`): the remaining residual population is re-derived, and `.8`'s description of it does not survive.** The published **8/16** reproduces exactly — eight cells carry an unmet observation but only **four** are required, the rest being `canonical` cells that promote every reviewed key. **`.8` recorded that all four need the `non_contract_region` cause; that is wrong for `packed_page_table_entry`**, whose expected disposition is `residual` and whose oracle describes an intent-bearing packed bit-layout, so it needs `no_canonical_carrier_for_captured_region`. **And no cell is waiting on the cause taxonomy at all:** the reviewed dataset never references `cause`, and the projection carries only identity, provenance, and the three actionability fields. The buildable rule is therefore `.8c`'s structural rule **generalised by region kind** — one residual per captured region no canonical record cites, asked of prose and table regions too — with `project_captured_regions` widened past its `figure`-only gate. Prose keeps the conservative default cause because no structural predicate separates advisory from normative prose without a deontic grammar, so `non_contract_region` stays honestly declared-unbuilt there. **Status CHANGED:** `.9` active, `.9a` Done; `.9b` owns the carrier, the projection, and the population replay. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.7.2.1a` (`2026-08-30`) — Done (reproducers `git rev-list --count e6f5012d..HEAD`, blind-spot probe at `a88b91a3~1`, `check_rolling_ledger_protocol.pl --report`): asked a third time, three of five findings do not hold.** **The eleventh instance of this tree's class is mine:** `.7.2.1` published "39 commits now separating `e6f5012d` from `HEAD`" — **43** four commits later — *in the sentence explaining that a window with no end silently grows*. Withdrawn on three surfaces, with the **command replacing the count** rather than a re-measurement, because a re-measured count is stale on the next commit. "20 and then 24 rows re-pinned" were a run counter, not a per-commit measure (re-derived: 21/22/23/20). "Two thirds of each byte budget" is **66.9%** for `changes` and **59.2%** for the status ledger — one number wearing two denominators. **Two findings re-derive exactly:** the blind spot is 19 dropped / 16 compound / 2 ratio / 1 comma at `a88b91a3~1`, and `governed_globs` was `["TOOLBOX.md"]` at `3c17ae5c~1` while the book chapter carried four unnamed claim tags. A first mechanism was **discarded before publication** — the closed noun list belongs to the *book* census — and the producers were queried instead. **Status CHANGED:** `.7`'s closure is now stated over its actual population, two governed files; new tracking leaf `.13` owns the bound. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.7.2.1` (`2026-08-30`) — Done, and with it `.7` (reproducer `perl scripts/check_published_assertions.pl --check`): the map is closed and the registry is FROZEN.** 30 assertions — 1 `derived`, 2 `gated`, 1 `authored`, **26 `dated`** — over **6** governed regions in **2 governed and 2 exempt** claim-annotated files, **0 unlisted**. A numeral nobody accounted for now fails the build instead of being reported and forgotten, closing the class this repository recorded **ten** times and could not shut with five rounds of prose correction. **Two sentences were repaired rather than recorded**, and both shapes generalise: a window naming its start but not its end cannot be anchored because it silently grows, so "the 27 transitions measured from `e6f5012d`" now names `60a81db7`; and a live count whose producer publishes it only as prose on stderr fits **no** outcome, so the numeral is removed and the reader runs the command. Deleting a value is legitimate — §3 Leg 3 forbids swapping one *unwatched* number for another. A near-contradiction (29 vs 28 revisions) was **adjudicated against project history and left alone**, both being right. **Three probes on REAL shipped prose, restored byte-exact:** an unmapped numeral; a dated value edited 39 → 38, firing three legs at once; and a claim-annotated file on an undeclared surface. **Status CHANGED:** `.7.2.1` and `.7` Done; registry `frozen`. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.7.2.0` (`2026-08-30`) — Done (reproducer `perl scripts/check_published_assertions.pl --self-test`): the gate was about to freeze on a stored list of one.** `governed_globs` named `TOOLBOX.md` and nothing else, so flipping to `frozen` would have called the map complete while the mdBook doctrine chapter — the surface instance 5 actually drifted on — sat outside it. **Membership is now discovered and omission is an error:** every tracked Markdown file carrying a claim tag is found by scanning and resolved through `doctrine/live_document_size/surfaces.jsonl`, and a file whose surface declares no disposition, or that no surface owns, fails the run. Only a surface's **disposition** is authored, never its membership, so a new annotated file joins by itself — the property `.7.0` element 3 wanted, which a glob list cannot give because it fails open. Two exemptions, each stating its reason (dated task evidence; sealed archive segments), and **each proven load-bearing** by flipping the surface to `governed` and requiring the same value to turn fatal — a quiet green run is otherwise equally consistent with the file never having been found. Two candidate rules measured and **rejected with reasons**: `lifecycle` cannot separate `TOOLBOX.md` from the task tree, and the census's own disposition would admit 85 dated verification rows. **Self-test 25/25**; `--check` green at **2 governed and 2 exempt** claim-annotated files over **6** governed regions, unlisted **23 → 27**. **Status CHANGED:** `.7.2.0` Done; `.7.2.1` owns the population, the flip to `frozen`, and two TOOLBOX sentences no closed outcome fits. SpecForge NOT complete.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.7.1a` (`2026-08-30`) — Done (reproducer `perl scripts/check_published_assertions.pl --self-test`): the gate could not see two of the values it exists to watch.** Because `PUBLISHED-ASSERTIONS` derives its own population, its numeral grammar decides which published values *exist* — and a value the scanner misses is a **silent hole**, not an unlisted one: no record can be asked for it and `frozen` phase would call the surface complete anyway. Measured against an **independent tokenizer** rather than by reading the regex (the gate's own lookbehind kept, only the lookahead widened): **19** published values were dropped across the four claim-annotated files — 16 compound-adjective forms, two ratio halves, one comma over-capture. A first probe without the lookbehind was **discarded, not published**, because it flagged `SHA-256` and `H1`, which the gate refuses correctly. One of the 19 is live: `TOOLBOX.md`'s "The 27-case self-test" is a current count of `check_current_claim_census.pl --self-test`, inside a paragraph this gate was written to watch. Both repairs are narrowed to the form identifiers actually take — a comma joins a numeral only before exactly three digits, and only a hyphen followed by a **digit** is excluded — so `1,922` stays one value, `40, noticed` publishes `40`, and `2026-08-28`, `segment-0013`, `SHA-256`, `1.95.0` stay out. **Self-test 19/19**, with all three new cases proven grammar-dependent by **revert-and-re-apply** (14/19 reverted). Real-tree unlisted **21 → 23**. **Status CHANGED:** `.7.1a` Done; `.7.2` now also owns the `governed_globs` stored-surface-list defect and a population that does not fit the declared record bound. SpecForge NOT complete.
