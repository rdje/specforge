- ✅ **`SOURCE-IR-REPRODUCIBILITY.1` (`2026-08-27`) — Done (measurement, read-only, scripts+docs-only; tracked reproducer `scripts/measure_source_ir_reproducibility.py`): the standing SourceIR drift is measured across the whole live corpus — 11/24 reproduce, 13/24 do not, and re-ingesting is a trade rather than a refresh.** Every persisted `generated/source_ir/*` artifact is partitioned into exactly one stratum: **24 live** (SourceIR schema 3), **0 live-but-unmeasurable**, **54 legacy** that no current ingest can reproduce at all — 78 total. The live stratum is exactly the membership of `doctrine/chain_currency/retained_bundles.json`, so the census frame is precisely the population whose chains report current today, and every recorded source resolved on the repository volume, so the whole live stratum was re-ingested with **no sampling inside it**. **11 reproduce exactly; 13 do not**, and the 13 hold **11,379 of the live population's 22,088** persisted content elements. The drift adds **1,804** elements (1,802 `body_text`, one `section_header`, one `caption`) whose text is unambiguously figure interior (`Core`, `External Debugger +`, `Referenced to`, `Ideal Clock`, `requency (GHZ)`), and drops **31** — **28 re-segmentation** whose text still appears in the replayed stream and exactly **3** emitted nowhere. No collection but `content_elements` changes cardinality, and `proof_ledger.ruleset_sha256` is identical for all 24. **The decisive result is the captions:** bindings on tables and visual assets fall **1,191 → 1,152** (seven documents lose, one gains two) — `structured_tables[1].caption_text` is `Figure 5-1: External debugger and core handshake sequence` persisted and `null` replayed. **What is lost is the binding, not the text:** all eight bound captions in that artifact also exist as standalone `content_elements`, so a re-ingest drops the association while the words stay. **Re-ingesting the corpus would therefore cost 39 caption bindings to gain diagram furniture** — the three-paragraph loss in this entry was **withdrawn by `.5` (`2026-08-28`)**, which found all three present word for word. **Not noise:** the largest proportional drift was ingested twice more into separate roots and reproduced identically both times; comparing two same-input replays isolates the only remaining difference to each claim's `scope`/`conclusion_sha256` with the ruleset digest and all 448 addresses equal, so excluding the proof surface is structurally necessary. **Gates:** no Rust code, no canonical mutation — nothing under `generated/` written or removed, and the producer refuses to run with a modified `crates/` tree; producer `--self-test` 14/14 with **two observed RED perturbations**; `scripts/check_doctrines.sh` green; `mdbook build` green; knowledge-map derive-and-diff in sync (263→264 facts). Report `docs/research/source-ir-reproducibility-census.md`; KM `[[source-ir-reingest-trades-captions-for-figure-text]]`; book `pipeline/sourceir.md` reproducibility contract corrected. **Status snapshot CHANGED:** the stale-artifact population is no longer unknown — it is 13 of 24 live chains; `SOURCE-IR-REPRODUCIBILITY` `.1` Done, `.4`/`.5` opened; frontier → `.2` (content-addressed anchors). SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.8d` (`2026-08-27`) — Done: the reviewed population is replayed and the residual closure is published — 4/16 → 8/16, and a whole category becomes supported.** All 12 reviewed sources and all 48 isolated stages replayed from clean production at `483e525d` under unchanged reviewed authority (dataset `c743bcda`, prior memory `a416cc8b`, orchestrator `55c81520`, every source digest-equal to the reviewed lock). Residual actionability moves **4/16 → 8/16**, source-region disposition 8/14 → 10/14, required-modality accounting 6/12 → 8/12, and canonical provenance closure 43/43 → **45/45**, while stage conservation stays 120/120, IntentIR stays 40/0/0 TP/FP/FN, and fabricated facts and unexplained drops stay zero. **`platform-system-ip` becomes the third supported reviewed category**, which is exactly the whole-category closure `.8c`'s bounded family was selected to deliver. The published result is the replay's own evaluator output byte for byte — nothing re-summarized. The delta is attributed, not assumed: a control leg re-projected the *same* replayed artifacts with the frozen pre-change builder (`edf0a871`) and reproduced the published 4/16 baseline exactly, so `.8c`'s production carrier moved no reviewed metric and every difference is the fixture projection's; cell-level comparison shows exactly two cells changed, both `static_component_topology`, each 0/1 → 1/1 exact, provenanced, and actionable at both promoted stages. The frozen `.8a` contract gained a closed `state` on its selected family so a shipped family is validated against the actionable bucket rather than the gap bucket, with four new RED cases (28/28). **One unrelated regression surfaced and is reported, not absorbed:** reviewed cells anchor on ordinal SourceIR element ids, and the Cortex-A76 reviewed prose has moved `elem_00219` → `elem_00230` (249 → 260 content elements, identical table/visual/page counts), so exact source regions are **13/14**. Input digest, production revision, Docling and model versions, batching, and run-to-run noise are each excluded by direct measurement, and `check_chain_currency.sh` starts from the persisted `source_ir.json` so the ingest boundary is outside its oracle — both facts now owned by the new `SOURCE-IR-REPRODUCIBILITY` tree. **Gates:** workspace suite 470/168/1,369/4, Clippy `-D warnings` clean, contract self-test 28/28, all nine gate-tier doctrines pass; 3,095 files / 1,158,476 KiB of population scratch and 4,192 files / 1,903,200 KiB of diagnostic scratch removed with an empty residue census. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.8c` (`2026-08-27`) — Done: production now explains the captured region, not only the refused record.** `residual_decisions` could explain a record the pipeline refused but never a source region it never turned into a record — the more dangerous silence, because nothing was left in the artifact to notice. `captured_region_residuals` closes that on `SemanticIR` and `IntentIR`: every captured visual region no canonical record cites earns exactly one typed `CapturedRegionResidualRecord` with its region id, typed kind, `EvidenceIR` provenance, closed cause `no_canonical_carrier_for_captured_region`, boundary `evidence_to_semantic_ir`, and an operator replay route. Coverage is a provenance membership test over concrete record collections, never a text scan. Reading the producers corrected the frozen design once: `mine_verified_figure_contracts` cites a region as `figure:<asset_id>`, so testing only the evidence id would have residualized figures that demonstrably reached a carrier; both forms now count through one shared constructor. Statement-mediated coverage is refused under either reading — admitting it explains 110 of the corpus's 1,089 captured figure-kind regions when applied to the collections coverage reads and 466 when applied to every collection carrying `supporting_statement_ids`, and the reviewed `picture_0001` appears only in the second, so the reading a naive implementation would pick is the one that reports success on a cell the review requires to fail. The registry expanded 168→170 rules over 50 public fields per stage, the frozen `.8a` contract now names the shipped carrier and its checker resolves any named carrier to a real production declaration (24/24 RED), and two chains snapshotted before the rebuild differ only in the new collection at both stages (I2S 20, I2C 103). `.8d` is the remaining child and its external-source input is already digest-verified.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.8` (`2026-08-27`) — Done: residual actionability counts only what the review requires, and the first bounded carrier closed a category.** The former 4/24 ratio was a *declared* denominator that charged two observations to every cell merely declaring residual queries, including eight belonging to four canonical cells already exact at all three promoted stages — observations no correct pipeline could satisfy, since emitting one would assert that the same fact both reached and did not reach `IntentIR`. `.8a` froze the rule as an executable contract deriving 17 required and five met observations over 18 closed cases and 19 control classes, bound four typed causes and three actionability fields, selected the bounded `static_component_topology` family, rejected 21 mutations, and repaired a silent green whose published reproduction named the facade package and ran no test. `.8b` made the evaluator agree: the published ratio is 4/16 over an affected population of twelve, the frozen first result re-derives as 0/82, and no per-cell score or other global dimension moved. A missing canonical key still adds a required observation and is met only by an exact, provenanced, actionable residual for that key. `.8c` shipped the first bounded production carrier and `.8d` replayed the population and published 8/16 with `platform-system-ip` supported; four required-and-absent cells remain, all needing carriers for the still-unbuilt `non_contract_region` cause.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.5` (`2026-08-15`) — Done: three-leg claim verification is fully adopted as SpecForge's fifth portable architecture.** Authors and reviewers now classify current actionable assertions, re-derive them from canonical source, challenge a named competitor through exact known-bad RED evidence, and bind every producer/dependency to tracked stale detection; missing and stochastic evidence stays explicit. The bounded registry, current/manual censuses, producer audit, Knowledge Map relationship, public workflow examples, all-tier doctrines, selected full CI, mdBook, and repository-local residue checks close the tree. (`claim-verification-contract-published`, `claim-provenance-gate-active`, `current-claim-census-frozen`, `mdbook-quantitative-census-frozen`)
- ✅ **`CLAIM-VERIFICATION-ADOPTION.2` (`2026-08-15`) — Done: published-claim provenance is mechanically gated.** ADR 0044's self-bounded registry joins unique honest claim status to direct argv re-derivation/RED controls, exact tracked artifact SHA-256, complete stale-check membership, retained evidence, and refresh ownership. Three verified claims execute eight commands; 22/22 positive/RED cases cover honest statuses plus missing, unknown, duplicate, stale, untracked, unsafe, false-RED, supersession, and bounds. `CLAIM-VERIFICATION` is the tenth doctrine through the existing driver; record validity is not misrepresented as semantic truth. (`claim-provenance-gate-active`)
- ✅ **`CLAIM-VERIFICATION-ADOPTION.1b` (`2026-08-15`) — Done: the consumed workflow-capacity authority is retired.** The exact 16→21 increase row is gone after the measured profile became committed baseline state. The registry again contains only its bounded control; the profile, standards, paths, limits, ADR, producer, catalogs, Knowledge Map, and mdBook are unchanged. `.2` subsequently activated provenance enforcement.
- ✅ **`CLAIM-VERIFICATION-ADOPTION.1a` (`2026-08-15`) — Done: workflow-standard capacity is measured and restored.** Git history reproduces 14 explicit members and a four-member peak day; 21 is the smallest profile keeping 14 below warning and 18 strictly below rollover. Stable paths and per-file bounds remain fixed, reachable aggregates move with count, and five controls plus full-catalog arithmetic keep the result falsifiable. `.1b` subsequently retired the exact one-use authority. (`workflow-standard-capacity-profile`)
- ✅ **`CLAIM-VERIFICATION-ADOPTION.1` (`2026-08-15`) — Done: the three-leg author/reviewer contract is published.** `CLAIM_VERIFICATION.md` and ADR 0042 govern current actionable assertions through source re-derivation, a dimensionally different oracle with an observed known-bad RED case, and tracked stale-detecting durability. Bootstrap, README, commit, toolbox, pull-request review, workflow catalog, and mdBook routes require one exact claim/no-claim declaration. Mechanical registry enforcement is not claimed; `.2` owns it. The required two files made workflow standards 14/16; `.1a` subsequently restored measured headroom. (`claim-verification-contract-published`)
- ✅ **`DECISION-RECORD-CAPACITY-HEADROOM.2a` (`2026-08-15`) — Done: the decision-capacity prerequisite is cleanly closed.** The exact `.2` increase row is gone after the 58-record / 393-fact / 3,584-question profile became committed baseline state. The authority registry contains only its self-bounding control; no capacity, decision, route, projection, book, or product byte changed. Claim-verification adoption is unblocked.
- ✅ **`DECISION-RECORD-CAPACITY-HEADROOM.2` (`2026-08-15`) — Done: the coupled capacity profile is applied and fault-tested.** The decision collection is now 43/58 files with unchanged per-file bounds and stable paths; 393 fact slots derive from 336 cards plus 57 possible decision facts, and 3,584 question keys derive from the eight-per-fact ratio and 512-key quantum. Sixty focused cases reject isolated record/fact/question drift; Knowledge Map stays 250 facts / 1,914 questions / 13 shards. `.2a` subsequently removed the consumed one-use authority and closed the tree.
- ✅ **`DECISION-RECORD-CAPACITY-HEADROOM.1` (`2026-08-15`) — Done: the next decision/fact profile was empirically derived before implementation.** ADR 0041 includes its own file in a 43-record population and preserves the nine-file peak active day. Fifty-eight is the smallest bound placing current use below 80% and one peak below 90%; it implies 393 fact slots and 3,584 question keys without changing stable decision paths, Knowledge Map shard topology, or portable hard caps. `.2` subsequently applied and fault-tested that exact transaction.
- 🧭 **`CLAIM-VERIFICATION-ADOPTION.0` (`2026-08-15`) — Mapped before adoption: the requested three-leg standard gained executable ownership.** The six-leaf program governs actionable current claims through source re-derivation, a dimensionally different falsification oracle, and tracked stale-state durability. Its initial census found strong existing ingredients but no local standard, claim registry, or review template. The capacity prerequisite subsequently closed and `.1` published the contract; registry enforcement remains pending.
- ✅ **`SPEC-CLARIFICATION-LOOP.2` (`2026-08-15`) — Done: governed unresolved state now plans minimal blocking/advisory clarification packets.** The core planner consumes normalized residual, contradiction, completeness/validation, adapter-block, and external-choice needs; exact equivalence keys deduplicate compatible questions, structural reach computes information gain, dependency components topologically order and reject cycles, and blocking status is retained conservatively. Executable autonomous actions and their dependents are withheld until replan. Fifteen focused clarification tests, warning-denied Clippy/Rustdoc, all five production-genericity components, mdBook, and mandatory doctrines pass; `.3` owns CLI/list/inspect/export/answer workflows, so no interactive-runtime claim is made yet.
- ✅ **`SPEC-CLARIFICATION-LOOP.1` (`2026-08-15`) — Done: clarification and answer authority now have a fail-closed typed IR.** Schema-1 packets bind immutable question definitions, exact evidence/current artifact/proof context, alternatives, downstream impact, dependency order, answer shape, and lifecycle. Schema-1 answer envelopes bind typed values or explicit non-answers to exact questions, responder/policy provenance, and supersession, but remain untrusted: source locators must resolve to native captured proof, supplements must become governed source, and only external design choices may later use a narrow dedicated premise. Ten focused tests, workspace Clippy, production genericity, Rustdoc, and mdBook pass. Runtime planning starts at `.2`; no interactive resume claim is made yet.
- ✅ **`SPEC-CLARIFICATION-LOOP.0` (`2026-08-15`) — Done: autonomous-first, user-assisted completion is task-owned and publicly aligned.** The eight-leaf program freezes the missing product loop: when governed PDF evidence suffices, continue autonomously; otherwise emit a typed, source/proof-linked clarification packet explaining the unresolved proposition, alternatives, downstream impact, acceptable answer schema, validation rules, and exact resume plan. User answers are evidence—not automatic truth—and must pass authority/currentness/consistency checks before minimal affected replay. Runtime implementation starts at `.1`.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6d.ii.f.v` (`2026-08-15`) — Done: production-genericity signoff is complete within the governed boundary.** The unconditional doctrine composes the proof-carrying 168-rule production surface, 27 adversarial structural controls, and conformance-owned identity, alpha, reviewed paraphrase/layout, semantic-negative, held-out, and population evidence. The final held-out matrix is 35 pass / zero fail / 16 honestly unmeasurable / zero invalid; the clean population is 39/0/1 IntentIR TP/FP/FN with 42/42 provenance and zero fabrication. The selected milestone full CI passes. This proves one specification-instance-neutral production core, not perfect recovery or product completion: APB still misses one canonical fact, coverage/accounting remain incomplete, and trajectory history is insufficient. Parents `.f`, `.6d.ii`, `.6d`, and `.6` close; `.7` is next.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.7` (`2026-08-16`) — Done: canonical recovery closes every reviewed stage without widening authority.** `.7b` recovers APB's independently explicit `PSEL` state and `.7c.i` carries only that exact source/support/subject/kind record through SemanticIR without creating an interface, declaration, alias, or subject-wide exemption. The independent 12-source/48-stage replay publishes 40/0/0 TP/FP/FN at EvidenceIR, SemanticIR, and IntentIR, 43/43 provenance, 120/120 conservation, zero fabrication and zero unexplained drops. Wire-protocol joins physical-link as supported; all controller hard gates pass and `.8` ranks first. Strict authorities are byte-current, 6,189 files / 2,294,248 KiB of exact scratch are absent, and all 24 retained chains remain current.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6d.ii.f.iv.b` (`2026-08-15`) — Done: the repaired reviewed population is current and fabrication-free.** All 12 hash-locked PDFs and 48 isolated stages replay from clean `e125aac7` under an evidence-recorded CPU/16-page policy. IntentIR improves from 24/2/16 to 39/0/1 TP/FP/FN; provenance is 42/42 and conservation 117/118. GIC-400's 15 facts are exact, AMD's false register is absent, and the sole canonical miss is APB `PSEL=HIGH`. All controller hard gates pass; insufficient history keeps state honestly unmeasurable and ranks `.7` first. `.6e` is superseded. The four-value replay command is executable-guarded, 5,616 files / 2,038,580 KiB across three roots plus the runtime map are removed, and residue is absent. `.f.v` subsequently closes final behavioral signoff.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6d.ii.f.iv.a` (`2026-08-15`) — Done: the clean-population register carriers are structurally restored.** A closed role may carry exactly one parenthesized qualifier, and an already-classified register map may preserve access from an explicit header or one unambiguous closed-literal column; arbitrary suffixes and access-like register names remain non-authoritative. Real-PDF replay recovers Arm Debug's 12 exact register/access facts and GIC-400's 15 source-named register/access/offset facts while the AMD packed-layout fabrication stays absent. The audited SourceIR migrator now replays neutral captured classifications and current validation. Exact ADR 0025 reconciliation changes only Arm SourceIR and two downstream register surfaces; all four replayed stages are 24/24 current / zero stale, with 24 blocked adapters and zero emitted files. `.f.iv.b` owns the replay-driver command-shape guard and clean publication.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6d.ii.f.iii.a` (`2026-08-15`) — Done: the valid I2C alpha failure is production-remediated and closed against clean-revision evidence.** Four universal mechanisms caused the six-signal/352-occurrence failure: underscore token splitting, lexical relation ordinals, declared-alias leakage into section-topic grammar, and spelling-sorted semantic collections. Structural production repairs preserve every record and all 11,170 claims; conformance now normalizes only embedded source-derived ids, unordered reference/responsibility sets, and top-level ISF declarations while non-bijective structure still rejects. Exact I2C replay passes 5/5 stages over 266,194 leaves. The refreshed 17-document matrix is 35 pass / zero fail / 16 unmeasurable / zero invalid: all 34 PDF relations plus I2C alpha are fresh under `2cdcd131`; retained evidence supplies only alpha eligibility. Exact ADR 0025 reconciliation remains 24/24 current and zero stale through the adapter.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6d.ii.f.ii` (`2026-08-14`) — Done: the complete behavioral transform/comparison harness is calibrated and sensitivity-qualified.** Conformance now runs deterministic PDF identity, normalized-text alpha, reviewed paraphrase/layout, and semantic-negative pairs through all five proof-bearing stages without exposing recipes or expected outcomes to core. The real “at least” negative removes exactly one SemanticIR assertion and its cumulative claim at SemanticIR, IntentIR, and adapter; ordinary invariance rejects and the exact declared complement passes. Nine synthetic omission/contradiction/reversal/value/timing/symbol/name/proof/stage faults reject. Missing provider/source and vacuity are unmeasurable; stale/ambiguous/partial runs are invalid. SpecForge is NOT genericity signoff-ready; held-out and population evidence remain open.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6d.ii.f` (`2026-08-15`) — Done: the governed behavioral program is complete.** The exact 24 proof-current rows and 23 non-vacuous text projections remain unchanged; seven reviewed overlaps are calibration, while 17 current-only rows include four vendor-novel and 13 family-novel strata. `.f.i` froze the oracle, `.f.ii` closed transform/comparison sensitivity, `.f.iii` and `.f.iii.a` closed the held-out production failure, `.f.iv` published the fabrication-free population, and `.f.v` composes final signoff without converting 16 unavailable alpha strata into passes.
- ✅ **`SPEC-TO-INTENT-TASK-EVIDENCE-CONTAINMENT.4` (`2026-08-14`) — Done: the bounded alignment program is independently audited and `.6d.ii.f` is eligible.** A clean same-volume clone of migration commit `10ee4a49`, created without local hardlinks, passes strict fsck and exact source/capsule identity. An independent 21-region streaming reconstruction, 58-owner/58-route closure, and 42-marker census reproduce SHA-256 `e70892a5…a26c`. A clone-only future trial changes exactly root, index, behavioral part, manifest, and contract; all seven legacy parts and the capsule stay byte-identical, the route becomes `post_migration`, and all 55 live-size surfaces pass. The proven transaction is installed: root 127 lines / 5,707 bytes, index 86 / 5,937, eight parts 2,162 / 282,891, behavioral part 22 / 974, and `.f` pending/eligible. All nine doctrines/genericity components, 1,954/5/0 Rust tests, five doctests, mdBook, and locality pass; the 1,358-file clone and 73-file generated book are removed with residue absent. SpecForge is NOT genericity signoff-ready; `.f` now owns behavioral qualification.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6d.ii.e.vii` (`2026-08-14`) — Done: the complete structural boundary and exact migration are qualified.** The clean `.e` range is 15 commits / 146 tracked files / 26,093 insertions / 3,756 deletions. The same 24 retained chains are current across all five proof stages; 54 legacy chains remain explicitly unmeasurable. Final adapter ledgers share one ruleset, cover all 168 rules, and contain 148,708 cumulative claims. All 120 non-proof/non-validation migration comparisons—including residual decisions—are exact; current residual surfaces are 0/3/8/62 Source/Semantic/Intent/adapter objects with zero migration delta. All 24 current adapters are blocked/no-file. All nine doctrines/components, 1,953/6/0 Rust tests, five doctests, mdBook, containment, locality, and exact 217-file cleanup pass. Structural `.e` is complete; `.f` alone owns population behavioral qualification before the genericity parent can close.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6d.ii.e.vi` (`2026-08-13`) — Done: structural genericity is adversarially and alpha-qualified.** The CI form of the unconditional production-genericity wrapper runs 27 controlled dependency, inventory/schema, rule/alpha/bypass, and flow/authority faults. Distinct identity, raw literal, substring, regex, unregistered inference, proofless promotion, laundering, schema-specialization, and missing-alpha cases fail; clean display, provenance, and excluded test-only spelling uses pass. A test-only oracle executes each premise/compatibility-shaped structural alpha contract, and an independent inventory join proves all 168 exact ids/stages/surfaces/premises/capabilities/obligations/compatibilities/owners without changing production proof identity. The live flow remains 2,169 functions / 11,295 helper edges / 10,419 decisions / 1,371 sensitive macros. All nine doctrines, 1,953/6/0 Rust tests, five compile-fail doctests, warning-denied Rustdoc, mdBook, cleanup, and final locality pass. SpecForge is NOT genericity signoff-ready: `.e.vii` owns final structural/migration-delta qualification and `.f` owns population behavior.
