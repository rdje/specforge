- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6c.i` (`2026-08-12`) — Done: timing constraints now preserve explicit caption units and direct table provenance.** I2S `table_0004` already retained five exact scalar rows and `all values in ns`; the loss was the EvidenceIR carrier, which read only a unit column and had no direct table-support field. A closed generic caption grammar now supplies only `all value(s) in <timing unit>`, explicit row units take precedence, and every table-derived timing record carries its source in default-empty `supporting_table_ids`, separate from statement provenance. The five reviewed I2S facts retain exact `ns` plus `table_0004` through IntentIR. ADR 0025 reconciled the five affected retained cascades: 231 timing rows gain one existing table id, only those five units change, all 15 carrier-neutralized stages and all adapters equal their backups, and whole-chain currency is 24/24 plus 78/78/78. The exact 89-file reconciliation root, 64-file live probe, and standalone log are absent. Full CI passes all eight doctrines, 1,866 tests / five ignored / zero failed, rustdoc, mdBook, and final locality. `.6c.ii` owns clean 12-source result/controller publication. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6b.iii` (`2026-08-12`) — Done: default bounded activation now follows measured resource risk.** The retired flat 512-page assumption is replaced by a deterministic total-RAM budget: 40% of physical memory at a conservative measured 75 MB/page, capped at 399 pages. The 24-GiB host therefore selects threshold 131 and retains the adaptive 64-page batch; explicit nonnegative overrides remain exact. Unknown page count fails closed, and Unix signal termination is now a typed, staging-safe diagnostic distinct from both ordinary exit failure and the measured RAM guard—without claiming signal equals OOM. An override-free 400-page Arm Debug replay completed all four stages with exact path-normalized SourceIR and validation-neutral downstream fidelity; its 795-file / 184,164-KiB root is removed and absent. Full CI passes all eight doctrines, 1,866 tests / five ignored / zero failed, rustdoc, mdBook, and final locality. Frontier: `.6c`. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6b.ii.b` (`2026-08-12`) — Done: the register-access carrier is now qualified across the complete current population.** All 12 unchanged source identities and 48 isolated stages replay at committed revision `bb152dfb`. Arm Debug closes exactly: 12 false positives + 12 false negatives + 12 unprovenanced records become 12 exact register/access facts with direct `table_0044` support. AMD IOMMU and GIC-400 each gain provenance without hiding their retained false key; no other cell changes. Aggregate TP/FP/FN move 7/22/33→19/10/21, provenance 3/29→17/29, conservation 21/54→57/78, source disposition 0/14→1/14, and required-modality accounting 0/12→1/12; residual actionability stays 0/24 and all categories remain incomplete. Controller v4 is still `diverging` on ten fabricated and twelve unprovenanced records and selects `.6c`, the five-record I2S missing-`ns` family. The exact 3,913-file / 1,090,884-KiB replay root and runtime map are removed and absent. A twice-reproduced default single-pass SIGKILL on the 400-page Arm source opens `.6b.iii`; forced bounded ingestion completed with equivalent SourceIR structure, proving the old above-500-page assumption unsafe. Full CI passes all eight doctrines, 1,862 tests / five ignored / zero failed, rustdoc, mdBook, and final locality. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6b.ii.a` (`2026-08-12`) — Done: the canonical register carrier now preserves register access and table provenance.** Root cause was SourceIR → EvidenceIR representation, not table recognition: Arm Debug `table_0044` retained all 12 Access cells and the register-map reader parsed them, but `RegisterRecord` had no register-level access field and only a statement-provenance list. The typed carrier now separates optional register access from field access and direct `supporting_table_ids` from `supporting_statement_ids`. Generic register-map, field-table, bit-layout-chain, and safe fragment-merge paths retain the appropriate source values/ids; absent access stays absent and legacy artifacts default cleanly. A bounded 1,195-file probe confirmed 12/12 exact access strings plus `table_0044` at EvidenceIR, SemanticIR, and IntentIR, then was removed with residue absent. ADR 0025 then identified and reconciled exactly four retained cascades: register counts remain 45/1/6/1, all non-carrier content and every adapter are unchanged, and currency is green at EvidenceIR 24/24 plus all downstream stages 78/78; the 31-file rollback root is absent. Full CI passes all eight doctrines, warning-deny Clippy, 1,862 tests / five ignored / zero failed, rustdoc, mdBook, and final locality. The subsequent `.6b.ii.b` clean population replay is the current product-quality authority. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6b.i` (`2026-08-12`) — Done: current-binary qualification now covers all 12 reviewed documents.** A deterministic population runner verified the unchanged 12 source digests, copied the eight external authorities only through an untracked same-volume runtime map, and produced 48 hash-pinned isolated stage artifacts without touching canonical `generated/`, reviewed gold, or the frozen `.4c` result. The 14-cell current result preserves 7 true positives / 33 false negatives, records 22 false positives and provenance closure 3/29, and proves frozen 41→22 false facts / 45→26 provenance failures are exactly the already-retired 19-record AIA TOC delta. All other defect families reproduce. Controller v3 meets replay currency 12/12, remains `diverging` on 22 fabrication and 26 provenance hard failures, and recommends `.6b.ii`; Arm Debug's 12 missing-access register keys are the largest current family. The exact 4,313-file / 1,194,976-KiB workspace and runtime path map are removed and absent. Full CI passes all eight doctrines, warning-deny Clippy, 1,861 tests / five ignored / zero failed, warning-deny rustdoc, mdBook, and final locality. SpecForge NOT complete.
- ✅ **`FSMGEN-REFRESH-INTEGRATE-6.1` (`2026-08-12`) — Done: FSMGen is pinned to `a51dcdad0` and SpecForge no longer fabricates rule-over-transaction priority.** The 1,139-commit fast-forward was audited against the current ISF/downstream contract. The new strict gate exposed a Cartesian priority product created only inside `isf_ir.rs`; SemanticIR/IntentIR carry no precedence authority. Priority storage/rendering is removed. A generic recursive named-drive analysis residualizes an overlapping rule only when exactly one local transaction owns the drive, retaining the transaction and recording `isf_rule_transaction_conflict_*`. The rebuilt 78-document adapter chain is current; all 44 emitted ISFs contain zero priority lines and pass new-tip FSMGen strict with zero diagnostics; one AHB adapter gains five honest residuals. The pin's bounded static actor-network metadata/handoffs also opens tracked reassessment `DOC-INTENT-TAXONOMY.4c.ii`; it does not override the measured sparse/rootless topology carrier. Full CI passes all eight doctrines, 1,859 Rust tests / five ignored / zero failed, rustdoc, mdBook, and final locality. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.6a` (`2026-08-12`) — Done: current replay truth is now separated from the frozen reviewed baseline.** A generic repository-contained four-stage replay harness ran the hash-equal 827,669-byte RISC-V AIA source below isolated `.project-data/tmp` storage without changing canonical artifacts. The complete reviewed 20×2 TOC `table_0004` changes kind `timing_parameter`→`unknown`; EvidenceIR, SemanticIR, and IntentIR timing records each change 19→0, false/unprovenanced IntentIR facts change 19→0, and expected true positives hold 0→0. All source/stage hashes, production/timing-authority revisions, command, and cleanup proof are strict tracked evidence. The 132 MB replay output, empty failed first root, and source copy were removed exactly with no residue. Controller v2 no longer presents frozen 41/45/33 counts as whole-population current gates: operational current-binary replay is a hard 1/12 metric, leaving 11 documents unqualified. State remains `diverging`, history `insufficient_history`, and `.6` remains first—but now means replay current truth before repairing only defects that survive. Full CI passes all eight doctrines, formatting, warning-deny Clippy, 1,857 tests / five ignored / zero failed, Rust docs, mdBook, and final locality. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.5b` (`2026-08-12`) — Done: the first evidence-composed trajectory snapshot is reproducible and selects owned work.** A separate `ir::trajectory_snapshot` adapter derives the nine-dimension input from the frozen `.4c` result and the exact `.2` provider-free 17-row ledger; a live registry equality test prevents capability-observation drift, while the generic controller remains untuned. The tracked input/report are byte-current and a generic root-relative runner reproduces the report. State is `diverging` because current hard gates expose 41 fabricated canonical facts, 45/48 records without provenance closure, and 33 unexplained stage drops; history is independently `insufficient_history`, so no trend or stall is invented. Visible supporting measures include 14/14 source/modality capture, 7/48 precision, 7/40 recall, 21/54 conservation, 3/48 provenance, 17/17 capability accounting, 12/17 non-omitted capabilities, 5/10 provider-free integrated execution, and 0/12 required-modality document accounting. The full owned ranking recommends `.6` hard honesty/provenance ahead of `.7` source loss, `.8` residual actionability, and `.9` capability breadth. Full CI passes all eight doctrines, warning-deny Clippy, 1,854 Rust tests / five ignored / zero failed, Rust docs, mdBook, and final locality. Frontier: `.6`. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.5a` (`2026-08-12`) — Done: reviewable trajectory state and task ranking now have a strict multi-metric engine.** A version-1 public schema and `ir::trajectory` require all nine ADR-0034 dimensions with bounded rational denominators, targets, improvement/materiality rules, hard/required flags, oracles, populations, uncertainty, and root-relative evidence—never one weighted score. Current hard failures classify divergence; otherwise exact paired deltas distinguish convergence/divergence/mixed, stall requires its configured comparable-history window, and missing or statistically inadequate authority remains unmeasurable. Estimated points cannot use point-only history to claim direction. Gap ranking is lexicographic—hard invariant, source loss, semantic regression, missing oracle, residual, breadth—then population, causal evidence, reversibility, uncertainty, and stable id. Every proposed task must already exist in the named task tree. Report-only authority rejects semantic mutation and requires review. Sixteen focused tests kill state, omission, fabrication, path, owner, unknown-field, uncertainty, and authority mutants. `.5a` contains no `.4c` outcome values. Full CI passes all eight doctrines, warning-deny Clippy, 1,850 Rust tests / five ignored / zero failed, Rust docs, mdBook, and final locality. Frontier: `.5b`, compose frozen evidence and persist the first owned proposal. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.4c` (`2026-08-11`) — Done: the first frozen source-to-IntentIR result is published and localizes the measured blocker upstream.** The byte-pinned report covers 12 review-complete documents / 14 cells and leaves all six categories `incomplete`—none `supported`, none `unmeasurable`. All 14 source-region and modality-capture queries are exact, but 0/14 cells have a fully accounted disposition and required-modality accounting is 0/12 documents. Canonical IntentIR precision is 7/48 (14.5833%), recall 7/40 (17.5%), provenance closure 3/48 (6.25%), conservation-or-residual 21/54 (38.8889%), residual actionability 0/24, with 41 fabricated facts and 33 unexplained drops. Ten cells first fail at SourceIR → EvidenceIR, four at EvidenceIR → SemanticIR, and none at SemanticIR → IntentIR; the seven canonical true positives present in EvidenceIR remain 7/7 across both later boundaries. The next constraint is source-to-evidence fact formation and explicit residualization, not demonstrated ISF/FSMGen expressiveness. A generic repository-relative runner and full byte-currentness test reproduce the 104,669-byte snapshot without changing `.4b` selection, gold, extraction, or source/stage artifacts. Full CI passes all eight doctrines, warning-deny Clippy, 1,834 Rust tests / five ignored / zero failed, Rust docs, mdBook, and final locality. Frontier: `.5`, reviewable multidimensional trajectory control. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.4b` (`2026-08-11`) — Done: the first balanced source-to-IntentIR review population is frozen without extractor tuning.** The tracked fixture holds 12 documents / exactly two per category and 14 complete bounded cells across 2 figure, 3 prose, and 7 table documents. Four sources are repository-relative; eight necessary read-only sources retain only portable id/hash/necessity. Every source and original SourceIR/EvidenceIR/SemanticIR/IntentIR is pinned, the deterministic builder refuses byte or reviewed-region drift, and a Rust contract test proves complete review authority, balanced coverage, measurable evaluation, and byte-stable repeated reports without publishing `.4c`'s product result. The selection is honestly retrospective: frozen before gold construction and subsequent extractor change, not claimed historically unseen. The same slice repaired every obsolete livework reference to the verified `/Volumes/SSD/Documents/livework/chipdoc` source library on the repository's SSD; all 104 authorized external absolute fields resolve and no boot-volume livework reference remains. That repair exposed and closed a frontier-authority bug: source path no longer counts as refresh evidence, and `CORPUS-FRONTIER` now checks an explicit exact 52-refreshed / five-remaining partition over the derived 57-document cohort with 13/13 mutation cases. Full CI passes all eight doctrines, warning-deny Clippy, 1,833 Rust tests / five ignored / zero failed, Rust docs, mdBook, and final locality. No category support conclusion is claimed; frontier: `.4c`, publish exact stage losses and the evidence-ranked blocker.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.4a` (`2026-08-11`) — Done: exact source-to-IntentIR stage-loss evaluation now has a strict, mutation-tested engine.** A versioned public schema and `ir::source_to_intent_eval` evaluate review-locked, hash-pinned four-stage JSON snapshots with data-defined queries and no document/vendor/protocol/layout branches. The report preserves exact multiset TP/FP/FN denominators, first failing stage, source disposition, modality accounting, provenance closure, conservation-or-residual, residual actionability, fabrication, unexplained drops, and explicit category status. Repository sources must be root-relative; necessary external read-only sources store only portable id/hash/necessity. Incomplete gold remains `unmeasurable`, and controlled omission, fabrication, provenance loss, silent drop, missing modality, and inactionable residuals are all detected. This is the oracle foundation only: no category support result is claimed until `.4b/.4c` lock and score two held-out documents per category. SpecForge NOT complete.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.3` (`2026-08-11`) — Done: the existing VLM region-crop seam now reaches typed, grounded, verified temporal intent.** Rebuilding EvidenceIR projects a usable timing note into optional `VisualEvidenceItem.figure_region`; samples need explicit integer/string/`T<n>` tick addresses, conflicting duplicates become unknown, and untyped annotation text never becomes invented bounds. SemanticIR grounds lanes against the same document's signal catalog, converts through `PartialTrace`, and requires verifier `Pass` before lowerability, then applies the ordinary fusion/fidelity path; IntentIR carries the contracts unchanged. Model-only lanes cannot cross grounding, unsupported/verifier-disagreeing candidates stay residual, and a stable span after tick zero stays residual because the current contract has no anchor that can represent its origin. Validation reports both `typed_figure_regions` and `typed_figure_regions_unavailable` with per-item warning ids. A reviewed fixture pins the retained NXP UM11732 PDF and page-4 timing crop identities and proves persisted PDF → SourceIR → EvidenceIR typed region → verified SemanticIR/IntentIR `WS` contract while rejecting an injected `INVENTED_BY_MODEL` lane. It is explicitly `reviewed_fixture_no_live_vlm`: no local endpoint was ready and the corpus still contains zero persisted VLM notes, so wiring is proven but live recall is not claimed. The required root-last Rust-analysis rollover seals 14 exact prior records and keeps this slice live at 51 records / 1,018 lines, with the manifest chain validated. Focused region/waveform/vertical/validation tests, warning-deny Clippy, mdBook, Knowledge Map, doctrines, and full CI pass. Frontier: `.4`, held-out source-to-IntentIR evaluation and exact stage-loss accounting.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.2` (`2026-08-11`) — Done: default convergence now exposes its complete production-capability boundary on every successful run.** `converge` prints 17 machine-readable JSON rows covering all 16 production subcommands. Each row names the capability, exact command/entrypoint, stable participation (`integrated`, `scheduled`, or `omitted`), per-run state, and reason, so an integrated provider-backed stage disabled by configuration stays visible while a standalone capability can no longer masquerade as default delivery. Constrained contract extraction, Tier-3 actor/signal resolution, register-bit recovery, and IntentIR NLI demotion remain explicit omissions; condition repair is covered only when LLM-primary promotion actually runs; validation and cross-document prior learning are scheduled outside the single-document loop. A Clap-derived test partitions all 28 subcommands and requires every production command in the ledger, making future capability islands fail closed. A provider-free CLI smoke emitted all 17 rows; its five exact reproducible README artifact directories were removed and the corpus frontier re-proved at 57 = 52 + five. The ledger changes stdout/report state only—no IR schema/content, adapter output, provider default, or corpus artifact changes. Focused warning-deny Clippy, 28 tests, mdBook, Knowledge Map/catalog, roadmap, rolling-ledger, and current-truth checks pass. Frontier: `.3`, prove real PDF visual/layout evidence reaches typed multimodal consumers.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.1` (`2026-08-11`) — Done: source-to-IntentIR completeness is now a strict category-aware contract, not an artifact-count proxy.** A versioned machine-readable contract covers all six document categories across SourceIR, EvidenceIR, SemanticIR, and IntentIR, naming required modalities, artifact families, typed destinations, provenance and conflict outcomes, and evidence-based non-applicability. Support requires at least two held-out documents per category and exhaustive reviewed gold per applicable document/family/modality cell; precision, recall, source-region disposition, required-modality accounting, canonical provenance closure, stage conservation, residual actionability, and high-confidence category precision are exact, while fabricated facts, unexplained drops, silently removed conflicts, and forced classifications are zero. Residuals preserve evidence of loss but never count as recovered typed content, and an empty/blocked ISF cannot establish upstream completeness. The contract explicitly leaves message-field transport and platform-topology/clock-domain cells incomplete; no current category is retroactively claimed supported before `.4` measures it. The mdBook and Knowledge Map expose the same contract. JSON semantics, 66 focused completeness tests, book test/build, projections, doctrines, and full CI pass; no product code or generated corpus artifact changed. Frontier: `.2`, make the canonical workflow account for every production extractor.
- ✅ **`SPEC-TO-INTENT-ALIGNMENT.0` (`2026-08-11`) — Done: SpecForge's endpoint and its present constraint are now separated, measured, and durable.** The endpoint remains specification-to-executable-intent; the present bottleneck is faithful PDF content population through the typed IR chain, not ISF/FSMGen expressiveness. The codebase is architecturally convergent but not product-complete: 78 persisted artifacts exist at each of SourceIR, EvidenceIR, SemanticIR, and IntentIR, while 44 emit `.isf`; those population counts prove stage execution and honest blocking, not semantic recall. A direct code-path audit found that the default `converge` orchestration runs core enrich/semantic/intent/adapter stages but does not directly run every production capability (`extract-contracts`, `signal-resolve`, `recover-register-bits`, and the standalone NLI-demotion path), so capability-island coverage must not be reported as default end-to-end coverage. The typed `FigureRegion`/waveform consumers are real, but source code explicitly records that `FigureRegion` has no producer wiring and downstream waveform coverage is synthetic; real multimodal production remains open. ADR 0033 fixes the upstream-first sequence. ADR 0034 and the trajectory-control research define a machine-readable objective vector, exact hard gates, held-out semantic outcomes, stage-loss accounting, drift/stall classification, evaluator-strength tests, and review-gated task proposals; **the controller is a design, not yet shipped runtime behavior**. Roadmap, task-tree, Knowledge Map, architecture analysis, and mdBook now agree. No product code or generated IR changed; the full gate passes all eight doctrines, formatting, clippy, 1,810 tests with five ignored, Rust docs, mdBook, and project-data locality. Frontier: `.1`, define the per-document-category EvidenceIR-to-IntentIR content contract.
- ✅ **`SEMANTIC-EMPTY-CATALOG-FILTER` (`2026-08-11`) — Done: the SemanticIR grounding filter no longer switches itself off on the documents that need it most.** Both grounding filters — signal constraints and conditional rules — were wrapped in one guard: *if the document declares no signals, promote everything unchecked.* The effect was an inversion, not a generosity. The filter was strongest on documents that **had** signal authority and absent on documents that had none, which is exactly where an ungrounded record is least likely to be real; a document declaring one real signal filtered every prose-derived record against that one-element catalog while a document declaring zero filtered nothing. It surfaced in the most counter-intuitive way available — a refresh that retired two *false* signals emptied a catalog and its promoted conditional rules rose from 0 to 2. The repair deletes the `declared_signal_names.is_empty()` special case so **one predicate governs every document**: keep a record when the signal it names is declared, or when a rule names no signal at all (a genuine system-level behavioral rule). Rejected records are **demoted, not dropped** — they stay in EvidenceIR with full provenance and surface as one proportionate `semantic_ungrounded_records_not_promoted` packet stating both counts, the declared-catalog size, and a sorted, capped sample of the undeclared names; a fully grounded document gets no packet at all. Measuring the *populated* branch before writing code is what made the slice honest: 41 of 45 populated-catalog documents were **already** dropping records silently (1,230 rules, 47 constraints), so uniform demotion could not leave them byte-identical, and the acceptance bar was revised from byte-identity to “added residuals only” rather than adopting an asymmetric rule that would have reintroduced the very discontinuity being removed. A read-only replay of all 78 documents gives **11 identical / 41 changed only in `residual_decisions` / 26 content-moved**, and every one of the 26 has an empty declared catalog, so the revised bar is met exactly; empty-catalog promotion falls 1,423 → 780 conditional rules (the 780 naming no signal are kept) and 100 → 0 signal constraints. All 78 downstream chains were rebuilt from unchanged EvidenceIR and the same 179-artifact validation population re-validated, so the reported population neither grew nor shrank. **Product status is unchanged at the boundary:** all 44 emitted `.isf` are byte-identical and pass FSMGen `--strict --check` with zero diagnostics, `kg-bench` holds 156/156, the nine provider-free evals sit at baseline, `CHAIN-CURRENCY` is green at 24/78/78/78, `CORPUS-FRONTIER` re-derives 57 = 52 + 5, and `run_ci.sh` is green with all eight doctrines passing. Two questions were settled by measurement: the low-confidence exclusion is **not** what empties a catalog here (all 33 empty-catalog documents carry zero interface signal records of any confidence), and the demoted names are not uniformly noise — Wishbone `CLK`/`CYC`/`STB`, DTI `TDATA`/`TKEEP`/`TLAST`, and USB `ACK`/`ERDY`/`NRDY` are real wires whose catalogs were never captured, an extraction-breadth gap the packet now makes findable per document and which is recorded as a tracked open question rather than claimed here. Corpus frontier unchanged at 52 done / five remaining; next own `CORPUS-COVERAGE.2.53`.
