- ✅ **`MDBOOK-DOCTEST-HYGIENE.1` (`2026-08-08`) — Done: every affected live-book example is now truthfully classified.** Enumerated all 34 fence openings across the ISF adapter, historical R16 temporal-intent, validation, and troubleshooting chapters. The 26 failing openings now identify non-Rust material as text and incomplete illustrative Rust as ignored doctests; three self-contained Rust examples remain executable, and no example body changed. `mdbook test docs/book` moves from 26 failures to success and the 36-page HTML build remains green; frontier → `.2` canonical enforcement and closure.
- ✅ **`ARTIFACT-PATH-PORTABILITY.2` (`2026-08-08`) — Done: SourceIR and EvidenceIR now survive repository moves without weakening provenance.** Both stages keep runtime-absolute paths for existing Rust consumers but serialize repository-owned source/layout/upstream/prior/section/span/visual paths relative to the discovered root. Source text carries stable origin metadata; explicit external inputs remain exact; unlabeled legacy repository paths use the bounded rebase contract. Validation resolves artifact/prior inputs at I/O, and convergence uses repository-derived or injected roots without process-CWD mutation. Full CI passes all six doctrines, formatting/Clippy, 1,753 tests / five ignored, rustdoc, mdBook, and locality. All 335 current affected generated artifacts remain unchanged for `.4`; frontier → `.3` downstream stages and consumers.
- ✅ **`ARTIFACT-PATH-PORTABILITY.1` (`2026-08-08`) — Done: one move-safe persisted-path contract now precedes every stage migration.** Repository-owned runtime paths encode as root-relative strings; existing relative/current-absolute values resolve at the discovered current root; legacy absolute values can rebase only from recognized project-data roots to exactly one present, contained target. Typed `repository_owned` / `external_input` origin keeps external inputs out of compatibility rebasing, while traversal, ambiguity, missing targets, and symlink escape fail closed. Thirteen focused cases, 1,748 full-suite passes / five ignored, formatting, and warning-deny Clippy pass. Schema instances and all 335 affected generated artifacts remain unchanged; frontier → `.2` SourceIR/EvidenceIR activation.
- 🧭 **`ARTIFACT-PATH-PORTABILITY.0` (`2026-08-08`) — Done: the post-move generated-lineage defect is measured and owned before repair.** The deleted boot-volume root remains in 335/506 generated JSON/Markdown artifacts and 262,996 scalar path values across SourceIR, EvidenceIR, SemanticIR, IntentIR, adapters, and prior memory; no artifact names the current SSD root. SourceIR stores a canonical input and four downstream builders canonicalize before persisting upstream pointers; validation/learning consumers can silently skip missing lineage and recovery can fail. A five-leaf task tree now owns the common relative/legacy/external path contract, every producer/consumer, verified 700 MiB migration, enforcement, and closure. No generated artifact or Rust behavior changed; frontier → `.1` design/tests before mutation.
- ✅ **`SWD-SERIAL-EXTRACTION.4e` (`2026-08-08`) — Done: Chapter B4 interface-edge timing is now one typed, fully scored fact.** `EvidenceIR` records actor/data/clock/edge plus both sample and drive-state-change semantics through a registered, declared-signal-gated universal prose reader; the eval key requires the complete tuple. A fresh repository-local CPU re-ingest produces exactly one target/SWDIO/SWCLK rising-edge record from `statement_1948`, and the 29-fact SWD derivation gold scores source-tolerant P/R/F1 1.000 across frame fields (11), operations (4), states (13), and edge timing (1). Seven neighboring WIRE suites retain their expected gates, `kg-bench` is 156/156, and full CI passes 1,735 tests / 5 ignored. The canonical projection gap remains explicit under `.7`; the live proof also surfaced absolute cross-stage artifact pointers for a dedicated clean-tree follow-up.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.7` (`2026-08-08`) — Done: the complete containment/locality adoption is closed and independently reverified.** Reopened all four rolling capsules, roadmap and FSMGen-feedback history, 121 task routes, five catalogs / 238 members, 146 fact cards, 147 facts / 1,023 questions, eight currentness contracts, and all 604 Markdown paths / 37 surfaces; every identity/route passes and debt count is zero. The cold read corrected stale migration/debt prose, retired two `.5` route tokens, and replaced one active old-volume Claude-hook default while preserving sealed historical command evidence. README is warning-safe at 113 lines / 4,257 bytes / 94 max characters. Two non-blocking early warnings remain explicitly owned below rollover: fact cards/index at 81.0% lines / 81.9% bytes and reviewed validation at 85.0% lines. Six doctrines, warning-deny Clippy, 1,730 tests / 5 ignored, rustdoc, the 36-file book, submodule/locality/residue checks, and clean diff all pass. No adoption frontier remains.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.6a` (`2026-08-08`) — Done: every SpecForge-owned temp/cache/runtime seam now follows the repository SSD by default.** Added forced Cargo and canonical-shell roots, a runtime Rust locality boundary for all six production tempdirs and child processes, exact rollback-safe Docling/eval rebuilds, and the sixth doctrine with three Rust plus five shell cases. Root-caused copied venv launchers crossing into the stale 2.9 GiB boot-volume repository; reduced 53 stale references to zero, checksum-copied both required models, proved revision-aware offline lookup and a network-disabled one-page ingest, then deleted only the exact ancestor-only old tree after full comparison. The ambiguous shared Hugging Face cache and approved `~/.cargo`/`~/.rustup` stores remain untouched. Full CI with every locality variable initially unset passes 6/6 doctrines, warning-deny Clippy, 1,730 Rust tests / 5 ignored, rustdoc, and the 36-part book. The final producer census also exposed FSMGen `File::Temp` residue; invocation-scoped child tempdirs now leave zero `.fsm`/log files. Frontier → `.7` final closure audit.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.5i` (`2026-08-08`) — Done: collection/projection containment is closed with zero debt and no corpus-KB warning.** The resulting tree has 601 Markdown files / 37 governed surfaces, five canonical indexes / 237 members, 121 task routes, 145 fact-card routes, 146 generated facts / 1,017 questions, 12 direct indexed surfaces, and eight enforced currency contracts. Root-caused the reported corpus-KB pressure to a six-lines-per-pass aggregate and one joined-evidence row—not aggregate capacity—then changed the producer to one fixture row, failure-only detail, evidence counts, and one fixture per bullet. Replayed 156/156 fixtures, preserved human synthesis plus paired JSON, and reduced the 24-page plane to 1,172 lines / 124,679 bytes / 201 max lines / 305 max-line bytes under unchanged ceilings. Full doctrine/CI and the 35-part book pass. Frontier → `.6` / `.6a` canonical same-volume temp/cache roots and residue census.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.5h` (`2026-08-08`) — Done: the mdBook is now the sole maintained public product-contract plane.** Audited all four root user/architecture references against the 33-part entry book and Git history; the defect was semantic time-layer mixing, not copied paragraphs. Preserved unique six-modality extraction and normative `IntentIR` boundary/guardrail material in two directly indexed book contracts, while richer topical chapters remain authoritative for current behavior. Reduced the four stable roots from 1,341 lines / 68,472 bytes to 50 lines / 2,325 bytes of bounded direct pointers, retired their inapplicable canonical-collection catalog, and extended executable currentness across root routes, book claims, `IntentIr`, and the sole `Isf` target. Final book authority is exact at 35 files / 12,490 lines / 748,629 bytes; product behavior is unchanged. Frontier → `.5i` final collection/projection closure and corpus-KB warning disposition.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.5g.iii` (`2026-08-08`) — Done: every managed corpus-KB projection now has executable tracked-input currentness.** Reconciled the validation page to ADR 0012's reviewed `71/74/65/67` boundary through explicit `--validation-snapshot` production, and refreshed the complete KG plane from 150 to 156/156 passing fixtures. A data-only contract and read-only 15-case oracle now bind 312 fixture files, eleven Markdown managed regions, paired JSON, seven producer regions, and both human-authored sides of every marker; canonical IR and typed prior memory remain forbidden outputs. `corpus_knowledge_base.currency` is enforced. The truthful aggregate is now at an 81.4% line warning without a ceiling increase; `.5i` owns disposition before rollover. Frontier → `.5h` maintained-reference truth-plane audit.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.5g.ii` (`2026-08-08`) — Done: the durable source-PDF registry now has executable exact membership and identity currentness.** Proved the existing 22 rows exactly cover all 22 Git-indexed PDFs below `corpus/`, with zero missing/extra paths, bad keys/directories, or bad signatures. ADR 0013 makes that Git set—not the table itself, host-local libraries, or generated artifacts—the membership denominator. A data-only contract pins the 63-line registry, real `stable_stem`/`document_key` regions, and three `SourceIR::build` seams; a read-only checker passes 13 fail-closed repository-local cases and leaves no residue. `LIVE-DOC-SIZE` now enforces `source_pdf_registry.currency`; no row, PDF, Rust source, extraction behavior, or roadmap direction changed. Frontier → `.5g.iii` managed corpus-KB currency.
- ✅ **`LIVE-DOCUMENT-SIZE-CONTAINMENT-ADOPTION.5g.i` (`2026-08-08`) — Done: the tracked validation snapshot now has executable, non-mutating currentness at its reviewed boundary.** Root-caused `project-validation` as an unsuitable doctrine oracle because it validates and writes three outputs, while ambient git-ignored reports are neither clone-stable nor owner-approved; the local AHB sidecar already differs from the reviewed score. A tracked contract now pins four report identities, 29 recommendations, zero executions, exact snapshot/live-block identities, review evidence, and four Rust producer regions. The read-only checker passes ten fail-closed repository-local cases, rejects fixture residue, runs unconditionally under `LIVE-DOC-SIZE`, and moves `validation_snapshot.currency` from debt to enforced. Producer and mdBook wording now says “last reviewed”; validation data and canonical artifacts did not change. Frontier → `.5g.ii` tracked source-PDF registry currency.
