- ✅ **`CORPUS-COVERAGE.2.46` (`2026-08-10`) — Done: OpenCAPI Discovery refresh keeps facts and removes invented topology.** Three guarded ingests reproduce 40 pages / 54 visuals / 49 tables / 50 sections / 172 elements with 47–53% memory free and 188/188 portable normalized paths. Source 181→172 and Evidence 831→754 remove only flattened labels, synthetic table/front-matter enums, and false `BDF`/`DL`/`VPD` declarations. Final IntentIR retains ten behaviors / 55 constraints / six timings; lowering blocks honestly on no grounded signals. Validation exposes the remaining table/visual/normative/temporal capture frontier. Two cascades reproduce eight downstream hashes; final committed-binary validation pins all 12. The required change record triggers a lossless 14-record rollover into `changes-0003`. Corpus frontier: 46 done / ten remaining / 61 strict-clean emitted ISFs; next select #47.
- ✅ **`CORPUS-COVERAGE.2.46a` (`2026-08-10`) — Done: bare `data (ACRONYM)` no longer fabricates one-bit ports.** The complete 80-EvidenceIR census finds four real qualified serial-data declarations and two false property/example declarations. The sparse fallback now requires `serial` or `high-speed` only for the ambiguous `data` head; all other declaration grammars stay generic. Four real-document replays remove OpenCAPI `VPD` and Wishbone `DO` while preserving I2C `SDA`/`USDA`/`SDAH` and I2S `SD`. Two cascades reproduce 33 hashes; 351 EvidenceIR tests, nine WIRE/I2C/SWD gates, KG 156/156, full CI, and 61/61 FSMGen strict pass. Parent `.2.46` resumes final Discovery refresh signoff.
- ✅ **`CORPUS-COVERAGE.2.43b` (`2026-08-10`) — Done: generic whole-sentence gates are compatibility data, not semantic authority.** Exact audit finds 27,168 records producing 21,206 behaviors / 642,401 actor assignments and 5,974 constraints / 366,087 interface assignments with zero rendered-ISF or executable value. New SemanticIR emits `gates: []`; old records round-trip but current Intent ignores them, and 79/79 populated/empty variants converge. Typed conditional/temporal surfaces remain. Focused/module tests, WIRE/I2C/SWD, KG 156/156, full CI 1,788/five ignored, 66/66 FSMGen strict, book/doctrines/path/locality pass. A lossless root-last rollover restored engineering-note headroom. Frontier → parent `.2.43` final replay.
- ✅ **`CORPUS-COVERAGE.2.43a.i` (`2026-08-10`) — Done: generic section phases are compatibility data, not semantic authority.** The exact 3,180-record title audit finds 720 `Reset value` headings and only one clean literal-phase section, already covered by typed recognition. A 79-document replay removes 2,238 synthetic behaviors / 48 pure-inferred actors / 2,257 responsibilities with zero adapter renderability, status, or executable-count loss. New SemanticIR emits `phases: []`; old populated artifacts load and round-trip but current IntentIR ignores them. All 79 populated/empty variants converge byte-identically at IntentIR and identically at the adapter. The 404 SemanticIR / 50 IntentIR tests, nine WIRE/I2C/SWD datasets, KG 156/156, full CI 1,789/five ignored, 66/66 FSMGen strict, book/doctrines/path/locality pass. Pending `.2.43a.ii` owns the separately surfaced typed-qualifier precision audit.
- ✅ **`CORPUS-COVERAGE.2.43a` (`2026-08-10`) — Done: sentence cues cannot promote whole sections into semantic phases.** Exact production-boundary replication classifies 11,286 retained generic phases as 3,180 heading-authorized and 8,106 sentence-fallback-only across 77 documents. `build_phases` now requires a non-empty phase-like heading while typed `<qualifier> phase` recognition remains independent. Unchanged 173-statement #43 evidence rebuilds phases 3→0 / behaviors 22→19 with all other semantic counts stable; two cascades reproduce six hashes. Three controls, 408 semantic tests, WIRE/I2C/SWD, KG 156/156, full CI 1,796/five ignored, 66/66 FSMGen strict, book/doctrines/path/locality pass. Pending `.2.43a.i` owns the separately surfaced broad title-grammar risk before parent #43 resumes final classification.
- ✅ **`CORPUS-COVERAGE.2.43` (`2026-08-10`) — Done: the OpenCAPI Ready Definition is portable and honestly non-emitting.** One guarded CPU ingest produces 15 pages / six visuals / three tables / 30 sections / 169 elements at 62% peak sampled memory; all 42 project paths are relative and present. Source 171→169 removes two flattened diagram labels; Evidence 176→173 removes those plus synthetic `Signal DL is width 1.`. Completed child audits retire generic phase/gate projections while preserving three grounded actors, 19 compliance behaviors, 25 constraints, and two assumptions. Lowering blocks only on no signals and leaves `adapter.json` plus its report. Twelve hashes, two cascades, WIRE/I2C/SWD, KG 156/156, 66/66 strict, book/doctrines/locality, exact nine-file cleanup, and zero residue pass. Corpus is 43 done / 13 remaining; frontier → `.2.43a.ii` typed transaction-phase precision.
- ✅ **`CORPUS-COVERAGE.2.42` (`2026-08-10`) — Done: the OpenCAPI Certified Definition is portable and honestly non-emitting.** Two guarded CPU ingests reproduce 15 pages / eight visuals / five tables / 31 sections / 151 elements at 18% and 33% peak memory; all 46 project-owned paths are relative and present. Source 155→151 removes four flattened diagram labels; Evidence 172→167 removes those plus synthetic `Signal DL is width 1.`. Current authority removes the stale interface/output, while `.2.42a` removes administrative workflow intent and preserves four real compliance behaviors/eight constraints. Lowering blocks on no signals and leaves only `adapter.json` plus its report. Twelve hashes, two cascades, WIRE/I2C/SWD, KG 156/156, 66/66 strict, book/doctrines/locality, exact cleanup, and zero residue pass. Corpus is 42 done / 14 remaining; frontier → select and own #43.
- ✅ **`CORPUS-COVERAGE.2.42a` (`2026-08-10`) — Done: certification administration remains evidence, not engineering intent.** The first OpenCAPI Certified Definition cascade turned submission/listing, email review, conflict escalation, mark use, and test-lab process into five phases, six gates, and 15 behaviors despite no typed hardware surface. A 23-document projection finds 238 administrative/reference statements. The shared semantic authority boundary now requires compound organizational context and preserves protocol requests, electrical contacts, technical conflicts, measured tests, and actual compliance requirements. Unchanged 167-statement evidence rebuilds to two technical actors, zero phases/gates, four genuine compliance behaviors, and eight constraints. Two cascades reproduce six hashes; 402 semantic tests, nine WIRE/I2C/SWD datasets, KG 156/156, full CI 1,793/five ignored, and 66/66 FSMGen strict pass. Parent `.2.42` subsequently completed its guarded replay and final signoff.
- ✅ **`CORPUS-COVERAGE.2.41` (`2026-08-09`) — Done: the OpenCAPI AFU address note is portable and honestly non-emitting.** Two guarded CPU ingests reproduce 14 pages / nine visuals / two tables / 23 sections / 118 elements at 20% and 19% peak memory; all 14 image/layout pairs and nine visual paths are relative and present. Zero-declaration EvidenceIR stays at 106 statements. Current authority removes five acronym-group interfaces/five actors/eight outputs; `.2.41a` removes the separate false legal phase/gate and two legal behaviors. Final semantic/intent hardware and behavior surfaces are empty, so lowering blocks and leaves only `adapter.json` plus its report. Twelve hashes, WIRE/I2C/SWD, KG 156/156, full CI, 66/66 strict, book/doctrines/locality, exact cleanup, and zero residue pass. Corpus is 41 done / 15 remaining; frontier → select and own #42.
- ✅ **`CORPUS-COVERAGE.2.41a` (`2026-08-09`) — Done: legal conditions remain evidence but cannot become semantic intent.** An OpenCAPI permissions paragraph under `Approved` used `while` to become a false gate and Intent behavior. The retained before projection finds 32 legal/administrative gates across 21 documents. A compound, word-bounded predicate now runs at the shared SemanticContext boundary without vendor/document/single-token policy. Real #41 preserves 106 evidence statements while phases/gates fall 1→0, invariants 17→15, and behaviors 2→0. Two cascades reproduce six hashes; nine WIRE/I2C/SWD gates, KG 156/156, full CI 1,790/five ignored, and 66/66 FSMGen strict pass. Parent `.2.41` resumes final artifact signoff and cleanup.
- ✅ **`CORPUS-COVERAGE.2.40` (`2026-08-09`) — Done: the OpenCAPI Certified note independently confirms glossary authority.** Two guarded CPU ingests reproduce 13 pages / three visuals / two tables / 25 sections / 173 elements at 21% and 22% peak system memory used; 13/13 image and 13/13 layout paths are final and repository-relative. Evidence 173→172 removes only synthetic `Signal DL is width 1.` from the sibling terms table. Current authority removes the stale one-interface/one-output surface while preserving actors, phases, invariants, behaviors, and constraints. Lowering blocks on no signals plus no behavior and leaves only `adapter.json` plus its report. Two cascades reproduce 12 hashes; WIRE/I2C/SWD, KG 156/156, 66/66 strict, path, and locality gates pass. Corpus is 40 done / 16 remaining; frontier → select and own #41.
- ✅ **`CORPUS-COVERAGE.2.39` (`2026-08-09`) — Done: the OpenCAPI Ready note is portable and its glossary acronym is not a signal.** Two guarded CPU ingests reproduce 10 pages / six visuals / five tables / 23 sections / 105 elements at 18% peak system memory used; 10/10 page image/layout paths are final and repository-relative. Evidence 113→112 removes only synthetic `Signal DL is width 1.` from a terms table that defines data link layer, not a wire. Current authority removes the stale one-interface/one-output surface while preserving the guide's actors, phases, invariants, behaviors, and constraints. Lowering blocks on no signals plus no behavior and leaves only `adapter.json` plus its report. Two cascades reproduce 12 hashes; WIRE/I2C/SWD, KG 156/156, 66/66 strict, path, and locality gates pass. Corpus is 39 done / 17 remaining; frontier → select and own #40.
- ✅ **`CORPUS-COVERAGE.2.38` (`2026-08-09`) — Done: Introducing CoreSight is portable and honestly non-emitting.** Two guarded CPU ingests produce 32 pages / 25 visuals / four tables / 42 sections / 250 elements at 44% peak system memory used; all 32 page paths are final and repository-relative. Source 333→250 is a net removal of 83 flattened diagram labels with five contaminated prose/header records cleaned. Evidence becomes 228 statements / six normative facts / one signal constraint / zero relations or declarations. The nine stale prose relations, six interfaces, eight ports, four connectivity edges, and old four-signal/two-enum surface lack hardware authority. The high-confidence methodology guide blocks with one temporal residual and leaves only `adapter.json`. Two cascades reproduce 12 hashes; WIRE/I2C/SWD, KG 156/156, 66/66 strict, path, and locality gates pass. Corpus is 38 done / 18 remaining; frontier → select and own #39.
- ✅ **`CORPUS-COVERAGE.2.38a` (`2026-08-09`) — Done: validation backannotation is contained to the explicit artifact.** A copied #38 rollback exposed that all five persistence helpers wrote IR through the embedded canonical layout while the sidecar/reporting path followed the copy; SourceIR also materialized canonical normalized summaries. `validate <artifact>` now writes persisted JSON only to the resolved CLI path and its report beside it. One all-stage regression and a real five-stage release replay require copied artifacts to gain reports while the complete canonical tree stays byte-exact. Existing canonical-path tests, Clippy, full CI (1,788 pass / five ignored), mdBook, doctrines, and locality are green. The original hashes/counts and side-effect state remain explicit; parent #38 resumes from authenticated-source regeneration.
- ✅ **`CORPUS-COVERAGE.2.37` (`2026-08-09`) — Done: AArch64 External Debug is portable and honestly non-emitting.** Guarded CPU ingest produces 25 pages / 11 visuals / two tables / 36 sections / 266 elements at 19% peak system memory used; 25/25 page paths are final and repository-relative. Exactly 81 visual-label body records disappear while structural classes hold. Evidence retains 243 statements / five conditionals / two normative facts and zero relations/signals. Current authority removes 64 heuristic interfaces, a visual-label `host` actor, and the stale 73-signal `agent.isf`; the high-confidence methodology guide blocks and leaves only `adapter.json`. Two cascades reproduce nine hashes; WIRE/I2C/SWD, KG 156/156, all 66 emitted ISFs strict, persisted-path, and locality gates pass. Corpus is 37 done / 19 remaining; frontier → select and own #38.
- ✅ **`CORPUS-COVERAGE.2.36` (`2026-08-09`) — Done: CoreSight Base System is portable, authority-safe, and honestly under-extracted.** Guarded CPU ingest produces 29 pages / 19 visuals / 12 tables / 404 elements at 17% peak system memory used; 29/29 page paths are final and repository-relative. Source structure and EvidenceIR counts hold exactly, including zero relations/signals. Current generic authority removes 88 stale interfaces and the 100-signal/two-rule `agent.isf`; lowering blocks and leaves only `adapter.json`. Two cascades reproduce nine hashes; WIRE/I2C/SWD, KG 156/156, 67/67 strict, mdBook/doctrines/path/locality, and exact cleanup pass. The durable residual is upstream cat-3 capture recall: 19 unenriched visuals, 87 partially structured normative statements, and seven untyped temporal-source rules. Corpus is 36 done / 20 remaining; frontier → select and own #37.
- ✅ **`CORPUS-COVERAGE.2.35` (`2026-08-09`) — Done: USB4 Connection Manager is portable and honestly non-emitting.** Guarded CPU ingest produces 96 pages / 46 visuals / 23 tables / 1,313 elements at 28% peak system memory used; 96/96 page paths are final and repository-relative. Current generic authority removes 13 stale relations, four interfaces, 11 ports, and the `SB`/`USB`/`USB4` adapter while retaining structured source evidence. The methodology guide blocks on no declared interface signals and leaves only `adapter.json`. Two cascades reproduce nine hashes; WIRE/I2C/SWD, KG 156/156, 68/68 strict, mdBook/doctrines/locality, and exact cleanup pass. Corpus is 35 done / 21 remaining; frontier → select and own #36.
- ✅ **`CORPUS-COVERAGE.2.34b.ii.b` (`2026-08-09`) — Done: USB4 Inter-Domain refresh is portable and authority-safe.** Two guarded 51-page ingests reproduce every final hash at 18% peak system memory used; 51/51 page paths are final-rooted and repository-relative, with all non-path content byte-equal to the preserved defective run. The stale one-signal/two-rule/eight-enum model now blocks honestly at zero interfaces/signals/rules and leaves only `adapter.json`. Nine WIRE/I2C/SWD gates are 1.000, KG is 156/156, all 69 current emits are FSMGen-strict clean, and full gates precede exact evidence deletion. `.2.34` is closed; corpus is 34 done / 22 remaining; frontier → select and own #35.
- ✅ **`CORPUS-COVERAGE.2.34b.ii.a` (`2026-08-09`) — Done: Docling page sidecars are path-portable before promotion.** Fresh USB4 exposed 51 absolute `normalized.staging` image paths in page JSON. The producer now cross-checks sidecar/summary identity, rejects traversal and symlink escape, persists the final repository-relative path, retains intentional `null`, and preserves the last-good bundle on any malformed record. Four page tests, existing metadata/stub-ingest tests, formatting, and warning-deny Clippy pass. Frontier → `.ii.b` real USB4 rerun/signoff.
- ✅ **`CORPUS-COVERAGE.2.34b.i` (`2026-08-09`) — Done: canonical SWD recovered coherently after a signoff-path side effect.** A temporary oracle command resolved to the canonical artifact root; the untouched downstream chain was preserved, then rebuilt from validated EvidenceIR. Current generic authority removes synthetic `LEVEL`, three phrase-shaped actors, and four relations while retaining all 29 SWD protocol facts. Two cascades are byte-identical; SWD derivation/base relation, FSMGen strict, focused/WIRE, and KG 156/156 pass. No code changed. The closing locality gate separately found 51 USB4 page sidecars with absolute staging paths; `.2.34b.ii.a` owns that producer fix before `.ii.b` signoff.
- ✅ **`CORPUS-COVERAGE.2.34a` (`2026-08-09`) — Probe done; its stale source-route blocker is resolved.** The selected Inter-Domain Service retained chain emitted one `USB4` signal/two rules, a high-value transfer check for #33. The probe failed closed because its host-library symlink still named the boot-volume checkout. The director subsequently supplied the SSD checkout; `.2.34b` repointed the ignored repository-relative route and completed guarded ingest without a boot-volume project dependency.
